name: flow policy attacks
on: [pull_request_target]
permissions: {contents: write}
jobs:
  test:
    permissions: write-all
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps: [{uses: "actions/checkout@v7", with: {persist-credentials: true}}]
