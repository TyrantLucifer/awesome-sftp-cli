# Stage 6 Verification Record

- **Status**: In Progress — M6.1 complete; M6.2 workspace migration and REL-002 saved-workspace shell completion are Verified while independent database migration/package/native/preview-channel slices retain exact evidence and open final boundaries; M6.3 doctor/support-bundle, compatibility, real-authentication, exact OpenSSH/Kerberos, REL-007 review coverage, and the REL-011/012 daemon fail-closed/concurrent-client rollback slice have evidence; M6.4 REL-009 has complete implementation-batch evidence, REL-008 exact-candidate gate-record contracts are active, and REL-010 final-only zero-gap audit is active; PLAT-009 and OBS-010 are Verified while the remaining release rows retain their explicit boundaries; no RC is frozen and protected/final release gates remain open
- **Updated**: 2026-07-19
- **Repository root**: `/data00/home/tianchao.thatcher/projects/awsome-sftp-cli`
- **Branch**: `codex/stage6-hardening-release`
- **Delivery PR**: Draft [#6](https://github.com/TyrantLucifer/awsome-sftp-cli/pull/6), title `feat: ship AMSFTP 1.0.0`, base `main`; remains Draft until final gates
- **Sole exact-main baseline**: commit `312bcccbcbd54246bbe5ff9babf4f14560449176`, tree `e0316c286ce11512cb0b92c917fa29b80f9e3305`
- **Baseline Hosted run**: [29579514879](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29579514879) — exact main commit, completed `success`, 24/24 jobs successful
- **Production Helper distribution / production Level 2**: **CLOSED**
- **Final release/tag/Homebrew state**: not created

This ledger records only observed evidence. Planned commands and acceptance ownership live in the [Stage 6 execution plan](../stages/06-hardening-release-plan.md); a planned gate is not evidence.

## Initial safety checkpoint

| Check | Result |
|---|---|
| exact baseline identity | PASS: local `main`, `origin/main`, and remote `refs/heads/main` all resolved to `312bcccbcbd54246bbe5ff9babf4f14560449176`; tree `e0316c286ce11512cb0b92c917fa29b80f9e3305` |
| fixed branch/tag audit | PASS: remote Stage 6 branch and `v1.0.0` tag were absent before branch creation; `codex/stage6-hardening-release` was created once from the sole baseline |
| exact-main Hosted run | PASS: run `29579514879` completed successfully at the exact baseline SHA; 24 of 24 jobs succeeded |
| baseline local CI | PASS: CI-equivalent `make ci` completed unit/contract/integration, docs, tidy/verify, lint, full race, four fuzz smokes, `govulncheck`, `actionlint`, and darwin/linux × arm64/amd64 builds |
| baseline worktree | PASS: index/worktree clean before branch creation and no product files changed before the baseline gate |
| production credentials | ABSENT by design: no production Helper offline key/custody ceremony or final Developer ID/notary evidence is available; no substitute material was generated |

The local baseline command used the installed SDK at `/data00/home/tianchao.thatcher/sdk/go1.25.7/bin`, `umask 0022`, root-owned persistent fixture root `/var/lib/amsftp-tests/1001`, and external build/coverage directories. It establishes that the frozen baseline is green in this environment; it is not the final required Go 1.26.5 or exact Go 1.25.12 release evidence.

## Canonical baseline command

```sh
env PATH=/data00/home/tianchao.thatcher/sdk/go1.25.7/bin:$PATH AMSFTP_TEST_PERSISTENT_ROOT=/var/lib/amsftp-tests/1001 BUILD_DIR=/tmp/amsftp-stage6-baseline/build COVERAGE_DIR=/tmp/amsftp-stage6-baseline/coverage sh -c 'umask 0022; make ci'
```

Result: **PASS** with exit status 0 on the untouched exact-main tree.

## CI failure classification contract

Every failed job is recorded with exact SHA/tree, workflow/run/job, platform image, failing command/test, retained logs/artifacts, same-SHA companion result, rerun result, and reproducibility. Classification rules:

- **Code**: reproducible locally or on another same-SHA leg; changed Stage 6 area is implicated; assertion/security/compatibility contract fails; or available evidence is ambiguous.
- **Environment/known fixture**: the same SHA passes a comparable companion, logs identify a runner/service/timing condition outside changed behavior, and a targeted no-code/no-assertion/no-timeout-change rerun passes.
- **Infrastructure**: setup, service, quota, network, or runner failure occurs before the product/test contract executes and is confirmed by same-SHA evidence.

Only a subsequent complete exact-candidate matrix can become final release evidence. Reruns do not erase the original failure or its classification.

### Initial Draft-PR CI classification

Plan-only commit `59c0d2003e41a6ec798fc696bcffcd4d72526622` produced failed push run [29581551106](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29581551106) and PR run [29581560324](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29581560324) on attempt 1. The failures were outside the documentation-only change:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| push auth-integration | Stage 1 replacement-daemon wait timed out after the OpenSSH auth cases passed | PR auth-integration passed | push attempt 2 passed |
| push oldstable Ubuntu 24.04 | existing remote-command cancel fixture reached start deadline before command bytes | PR oldstable Ubuntu 24.04 passed | push attempt 2 passed |
| push native Ubuntu 22.04 | existing Level 2 in-flight cancel result timing fixture | PR native Ubuntu 22.04 passed | push attempt 2 passed |
| PR oldstable macOS 15 Intel | existing Helper exact-stderr-cap reader observed zero bytes | push oldstable macOS 15 Intel passed | PR attempt 2 passed |

Both failed-only attempt 2 reruns completed `success` without code, assertion, timeout, or workflow changes. Classification: **environment/known timing fixtures, not introduced code**. Attempts 1 remain preserved and neither attempt is final release evidence.

### M6.1 public-interface CI classification

Public-interface commit `51b7cfc2b5c4c3ce9c6989bb482564d1b096f603` produced successful push run [29582457142](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29582457142) and failed PR run [29582459680](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29582459680) on attempt 1. The PR failures were confined to existing macOS timing fixtures:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| PR oldstable macOS 15 | existing Helper exact-stderr-cap reader observed zero bytes; the existing Stage 2 PTY fixture then observed a transient empty selection before delete confirmation | push oldstable macOS 15 job `87891201150` passed | attempt 2 superseded/cancelled by newer PR SHA before execution |
| PR native macOS 15 | existing Helper heartbeat-termination test observed process termination before the client failure became visible | push native macOS 15 job `87891201232` passed | attempt 2 superseded/cancelled by newer PR SHA before execution |

Classification: **environment/known timing fixtures, not introduced code**. The same SHA passed both directly comparable push jobs, Linux native/oldstable legs, quality, integration, reproducibility, and all changed app/config/docs packages; the failing tests do not exercise the new config/help/man/completion behavior. The first failed-only rerun request was cancelled by repository PR concurrency after `01a7b0b` was pushed, so that cancellation was retained as attempt 2 and did not count as evidence. A later no-change attempt 3 reran native macOS 15 job `87897122811` and oldstable macOS 15 job `87897122849`; both passed, followed by successful compare job `87897962800`, without code, assertion, timeout, or workflow changes. Attempts 1–3 remain preserved and none is final release evidence.

Keymap commit `01a7b0b17bf9fc4fe906ed94a82447a7918eb977` then produced successful push run [29582955855](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29582955855) and failed PR run [29582958715](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29582958715) on attempt 1. The sole failure was oldstable macOS 15 Intel job `87892873657`: existing `TestLocalTailDetectsTruncateAndRotateAsHints` completed its 140 ms polling window with no notice or chunk. Directly comparable same-SHA push job `87892849850` passed, as did every other job and all changed packages. Classification before rerun: **environment/known Helper tail-polling timing fixture, not introduced code**. Failed-only same-SHA attempt 2 reran job `87895838476` and completed `success` without code, assertion, timeout, or workflow changes, confirming that classification. Attempt 1 remains preserved; neither attempt is final release evidence.

Cache/transfer commit `55e2d521fb3d90a877e586719d3c123e9ce8374b` produced failed push run [29584976904](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29584976904) on attempt 1 while the same-SHA PR run [29584980364](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29584980364) passed each directly comparable job:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| push quality | existing Stage 3 foreground-PTY fixture observed an empty remote shell cwd marker | PR quality job `87899619189` passed | attempt 2 passed check/lint, then `FuzzNormalizePath` reported `context deadline exceeded` at the 1-second fuzz deadline before reaching the PTY step; attempt 3 job `87902781998` passed |
| push oldstable macOS 15 | existing Helper stderr-hard-cap process fixture received the expected process failure as a closed handshake/read pipe before its overflow classification | PR oldstable macOS 15 job `87899619230` passed | attempt 2 instead hit the existing Helper heartbeat-termination visibility fixture; attempt 3 job `87902782317` passed |
| push native Ubuntu 24.04 | existing weighted-round-robin timing fixture reached its 3-second observation point before the second grant | PR native Ubuntu 24.04 job `87899619214` passed | attempt 2 job `87901591208` passed |
| PR native macOS 15 Intel | existing Helper heartbeat-termination fixture observed process termination before the client failure became visible | push native macOS 15 Intel job `87899607669` passed | attempt 2 job `87901596887` and compare job `87902963047` passed |

The exact same tree also passed local current-toolchain `make ci` and exact-oldstable `make check`. Final classification: **environment/known PTY, process-reader, Helper heartbeat, scheduler, and fuzz-deadline timing fixtures, not introduced code**. No code, assertion, timeout, or workflow change was made in response. Failed-only same-SHA push attempt 2 confirmed the original Ubuntu scheduler failure as timing-only but encountered the two different known timing fixtures recorded above; push attempt 3 then completed `success`. PR attempt 2 also completed `success`, confirming its heartbeat classification. Attempts 1–3 remain preserved and none is final release evidence.

Preview-config commit `f931f76cbd0925acabc108f1b70a416fc82943a6`, tree `fdaa0e29a678023a68be8833aed5accf9a318792`, produced failed push run [29586241010](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29586241010) on attempt 1 while the same-SHA PR run [29586244438](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29586244438) completed `success`. Push macOS 15 Intel job `87903826466` alone failed in the existing `TestLevel0FilenameSearchCancellationRetainsResultsAndReportsCanceled` race fixture after its two-second cancellation observation did not reach the provider. The directly comparable PR macOS Intel job `87903837630` passed, as did push quality, Ubuntu native, oldstable, all preview/config/app coverage and every other job. Failed-only attempt 2 job `87905078050` passed ordinary search but instead hit the existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` timing fixture, observing process termination before client failure visibility. No-change attempt 3 macOS Intel job `87905751697` then passed ordinary tests, race, build and native smoke, and the entire attempt completed `success`. Final classification: **environment/known search-cancellation and Helper-heartbeat timing fixtures, not introduced code**. Preview configuration does not touch either failing path, and no code, assertion, timeout, or workflow change was made in response. Attempts 1–3 remain preserved and none is final release evidence.

Search-config commit `43c336102e8b32e5d2a153bb4f8912d71c887d3b`, tree `4d4361b6d66a5ea4409f445ac15027d9f6253a86`, produced failed push run [29587283734](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29587283734) on attempt 1 while the same-SHA PR run [29587287634](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29587287634) completed `success`. Push Ubuntu 22.04 native job `87907297497` alone failed in the existing `TestTransferSchedulerWeightedRoundRobinNeverStarvesBulk` three-second observation window after recording three rather than four granted bytes and three rather than two waiters. The commit did not change transfer or scheduler code; its search/config/app packages passed in the failed job, and directly comparable PR Ubuntu 22.04 job `87907311590` passed. Failed-only no-change attempt 2 job `87909139504` then passed the scheduler fixture, full race, build and native smoke, and the entire attempt completed `success`. Final classification: **environment/known weighted-round-robin timing fixture, not introduced code**. No code, assertion, timeout, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Retry-config commit `6bc45302c11313f6302160b4574d40d8d664f8a5`, tree `8fba34c6e1cf6f4bcab8911d97961a6e94974d35`, produced successful push run [29588219723](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29588219723) and failed PR run [29588221737](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29588221737) on attempt 1. PR Ubuntu 22.04 native job `87910423007` alone failed when existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow` observed the short-lived helper probe succeed before the stderr hard-cap reader surfaced overflow; directly comparable same-SHA push job `87910415665` passed. No-change attempt 2 job `87912261052` passed that original Helper fixture and ordinary tests, then failed under full race when existing `TestSameExecutableIdentityRejectsSameInodeRewrite` observed a same-inode overwrite retain the prior executable identity. The retry commit did not touch Helper or platform identity code, all changed retrypolicy/config/app/transfer packages passed, and the directly comparable push job had already passed both phases. No-change attempt 3 Ubuntu job `87913207665` passed ordinary tests, full race, builds, native filesystem/security/PTY smokes and provenance; compare job `87914056910` and the complete run then finished `success`. Final classification: **environment/known Helper process-reader and filesystem identity timing fixtures, not introduced code**. No code, assertion, timeout, or workflow change was made in response. Attempts 1–3 remain preserved and none is final release evidence.

Integrity/direct-closure commit `e74fa5966573cbf21d7e5b5795591a0c83c8859b`, tree `46335f00568ce65b2aa3af491e032ca916b04acb`, produced successful push run [29589409774](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29589409774) and failed PR run [29589410924](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29589410924) on attempt 1:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| PR oldstable macOS 15 Intel job `87914446885` | existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process termination before client-failure visibility | push oldstable macOS 15 Intel job `87914442988` passed | attempt 2 job `87916471504` passed |
| PR native macOS 15 job `87914446915` | existing Stage 2 PTY delete fixture sent delete while the listing was still loading, then observed `delete requires at least one target` instead of the confirmation | push native macOS 15 job `87914442991` passed | attempt 2 job `87916471509` passed ordinary tests, full race, build and native smokes |

All changed transfer/config/app packages passed in the original failed jobs. Failed-only attempt 2 used the same SHA with zero code, assertion, timeout, or workflow changes; both original fixtures passed, compare job `87916473397` passed, and the complete rerun finished `success`. Final classification: **environment/known Helper-heartbeat and PTY listing/selection timing fixtures, not introduced code**. Both attempts remain preserved and neither is final release evidence.

Diagnostic-config commit `cfff95390209616d25764d059b09d3610f91e2a7`, tree `0fd0b698b059d6a5bf2aadd3485831d78b533c2e`, produced successful push run [29590345136](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29590345136) and failed PR run [29590349058](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29590349058) on attempt 1. PR native macOS 15 Intel job `87917578113` alone failed in existing `TestLevel2InFlightCancelPropagatesAndNeverCommitsOrDeletesSource`: cancellation was reported and the part remained, but the expected terminal result combination was not visible at the fixture's observation point. The directly comparable same-SHA push native macOS 15 Intel job `87917565910` passed its complete ordinary/race/native sequence. The diagnostic/config/app changes did not touch Level 2 cancellation, and their tests passed in the failed job. Failed-only attempt 2 reran native macOS 15 Intel job `87919577526`; the original test, complete race, build and native smokes passed, compare job `87921027698` passed, and the run completed `success`. Final classification: **environment/known Level 2 cancellation-result timing fixture, not introduced code**. No code, assertion, timeout, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Effective-keymap/reset commit `4150814a3e19a81f2254095b426da19051c762a9`, tree `bb3ce60e5c02fc9d55aa0b9dd014167c21b25a7c`, produced successful PR run [29591483906](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29591483906) and failed push run [29591479558](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29591479558) on attempt 1:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| push native macOS 15 Intel job `87921375855` | existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process termination before client-failure visibility under race | PR native macOS 15 Intel job `87921391118` passed ordinary tests, full race, build and native smokes | attempt 2 passed the original fixture but job `87922997291` instead observed zero of the expected exact 65536 stderr bytes in existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap`; attempt 3 job `87924709260` passed |
| push native Ubuntu 24.04 job `87921375990` | existing Stage 3 foreground-PTY fixture observed an empty shell cwd marker | PR native Ubuntu 24.04 job `87921391063` passed | attempt 2 job `87922997471` passed |

All changed keymap/config/app packages passed in the failed jobs. Attempt 2 proved both original failures non-reproducible but encountered the separate known Helper process-reader visibility failure above; attempt 3 then passed complete ordinary/race/native execution and compare job `87925752301`, and the run completed `success`. Final classification: **environment/known Helper heartbeat, Helper stderr-reader, and foreground-PTY timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Attempts 1–3 remain preserved and none is final release evidence.

Configuration-policy/Helper-closure commit `a2a0503f319ec078f415753a3f2e6a2a631d8ca7`, tree `ccbeaabb0d17c3fa59ee08920ddbbfe1833d6edd`, produced successful PR run [29592906123](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29592906123) and failed push run [29592902733](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29592902733) on attempt 1. Push oldstable macOS 15 Intel job `87926142341` alone failed when existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap` observed zero of the expected 65,536 diagnostic bytes. The directly comparable same-SHA PR job `87926155119` passed, as did every other push job and all changed helper/config/app packages. Failed-only no-code attempt 2 job `87927818502` passed the original fixture and the run completed `success`. Final classification: **environment/known Helper stderr-reader visibility timing fixture, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Fixed-quit/keymap-to-reducer commit `0beb36836855ced9dd89f2ebbcfa2587aec48d75`, tree `3215602fd5a58a5ffc7532635d740749415d979b`, produced successful PR run [29615547368](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29615547368) and failed push run [29615545218](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29615545218) on attempt 1. Push native macOS 15 job `87999733032` alone failed during full race: existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process exit before client-failure visibility, and the existing Stage 2 PTY fixture sent delete while the listing/selection was not ready and observed `delete requires at least one target`. Directly comparable same-SHA PR native macOS 15 job `87999739248` passed both ordinary and race phases; the commit changes only fixed Normal-mode `q` keymap validation and an integrated keymap-to-reducer regression, not Helper heartbeat or Stage 2 listing/delete behavior. Failed-only no-change attempt 2 job `88000691806` passed, and both the PR run and rerun completed with all 24 jobs successful. Final classification: **environment/known Helper-heartbeat and PTY listing/selection timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Durable Job CLI commit `bf60e8c02e335063cb1d7f5c272ba8adca131300`, tree `08e0d169cd2eef8cb9bd954e4c046537c0540f44`, produced successful PR run [29616649509](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29616649509) and failed push run [29616646032](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29616646032) on attempt 1:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| push native Ubuntu 22.04 job `88003082785` | existing `TestStage3LocalShellForegroundPTY` read an empty shell cwd marker during full race | PR native Ubuntu 22.04 job `88003093080` passed ordinary tests, full race, build, and native smokes | attempt 2 job `88004337455` passed |
| push quality job `88003082786` | existing one-second `FuzzNormalizePath` smoke ended with `context deadline exceeded` after the other three fuzz targets passed | PR quality job `88003093079` passed complete check/lint/fuzz/supply-chain/integration | attempt 2 job `88004337387` passed |

The changed app package passed in the failed native race leg, and the quality leg passed check/lint before reaching its unrelated fuzz deadline. Both same-SHA PR companions passed; failed-only attempt 2 then completed all 24 jobs successfully without code, assertion, timeout, fixture, or workflow changes. Final classification: **environment/known foreground-PTY marker and fuzz-deadline timing fixtures, not introduced code**. Attempts 1–2 remain preserved and neither is final release evidence.

Daemon lifecycle CLI commit `60ab5528b66c1b90599f524576ab2d7919a5f536`, tree `c00ff7aa721d278b422f4a6e809662840dd9a339`, produced successful push run [29617804339](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29617804339) and failed PR run [29617806472](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29617806472) on attempt 1. PR native macOS 15 Intel job `88006553742` alone failed during `make test-race` when existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process termination before the client failure became visible. The directly comparable same-tree push native macOS 15 Intel job `88006547086` passed its complete ordinary/race/build/native-smoke sequence, and every changed daemon/app test passed in the failed PR job. Failed-only no-change attempt 2 job `88007628988` passed the original fixture, complete race, build, and native smokes, and the PR run completed `success`. Final classification: **environment/known Helper-heartbeat visibility timing fixture, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Final configuration-mapping commit `3d3dc307816005e1d969f2bd004cd7826ae07826`, tree `cd08a0f2d49dbda99f475ba9ed041c8b11948fe9`, produced successful push run [29618446376](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29618446376) and failed PR run [29618448078](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29618448078) on attempt 1. PR native macOS 15 job `88008434657` alone failed during `make test-race` when the same existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process termination before client-failure visibility. The directly comparable same-SHA push native macOS 15 job `88008429465` passed, every changed config/app/daemon package passed in the failed PR job, and the change does not modify Helper process-session code. Failed-only no-change attempt 2 job `88009601950` passed the original fixture, complete race, build, and native smokes, and the PR run completed `success`. Final classification: **environment/known Helper-heartbeat visibility timing fixture, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Helper-status commit `fbb6f4bdf5806f526b99d5f2dfae9df668842941`, tree `db0c66d28f17e7101e76b441b7b2324fd65eb708`, produced failed push run [29619225477](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29619225477) and PR run [29619227237](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29619227237) on attempt 1. Push Ubuntu 22.04 job `88010724766` alone failed when existing `TestLevel0FilenameSearchCancellationRetainsResultsAndReportsCanceled` did not observe provider cancellation within two seconds; same-SHA PR job `88010730094` passed. PR macOS 15 Intel job `88010730081` failed in the existing Helper heartbeat-termination visibility fixture; same-SHA push job `88010724831` passed. PR macOS 15 job `88010730123` failed when the existing Stage 2 PTY fixture sent delete before its selection was visible; same-SHA push job `88010724808` passed. Failed-only no-change attempt 2 jobs `88011655125`, `88011668360`, and `88011668347` passed their full native sequences, both compare jobs `88012218917` and `88012249701` passed, and both workflows completed `success`. Final classification: **environment/known search-cancellation, Helper-heartbeat, and foreground-PTY timing fixtures, not introduced code**. The changed app tests passed in the failed legs; no code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Historical-source inventory commit `9e2ed6df9fc736d0d86fa5e6f70d3a0ef0146224`, tree `73a8b94974ffc98862f0d28243834acba5d999bc`, produced failed push run [29619828356](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29619828356) and PR run [29619830232](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29619830232) on attempt 1. Push quality job `88012483752` failed when existing `TestRunLocalCommandReportsNonzeroAndCancellation` reached its short context deadline before process startup; same-SHA PR quality completed successfully. PR oldstable Ubuntu 22.04 job `88012489821` failed when existing `TestLevel2InFlightCancelPropagatesAndNeverCommitsOrDeletesSource` returned the correct cancellation and retained part before its result enumeration became visible; same-SHA push oldstable Ubuntu 22.04 job `88012483773` passed. Changed compatibility tests passed in both failed jobs. Failed-only no-change attempt 2 push quality job `88013259504` and PR oldstable job `88013357273` passed, compare jobs `88013787138` and `88013531210` passed, and both workflows completed `success`. Final classification: **environment/known command-start and Level 2 cancel-result timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved; they are the exact M6.1 implementation Hosted evidence, not final release evidence.

M6.1 documentation-closeout commit `137899f047905e3eeac0f064bee1eaa057d21957`, tree `638323d97fe5b88eb165267fbeea8c0f6e9121d0`, produced successful push run [29620380123](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29620380123) and PR run [29620382502](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29620382502) after four preserved attempts. PR attempt 1 native macOS Intel job `88014098824` alone failed when existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap` observed zero rather than 65,536 diagnostic bytes; the same-SHA push counterpart passed. Failed-only attempt 2 job `88014887257` passed that fixture but reached the observation deadline in existing `TestLocalTailDetectsTruncateAndRotateAsHints` after already observing the truncate notice and new chunk but before the later rotate event. Attempt 3 job `88015178920` passed ordinary tests, full race, build, and native smokes, then artifact upload failed with GitHub infrastructure DNS error `ENOTFOUND`. Failed-only no-change attempt 4 native job `88016419565` and compare job `88017004406` passed, completing the workflow `success`. Final classification: **environment/known Helper reader and tail-polling timing fixtures followed by GitHub artifact-upload infrastructure failure, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. A subsequent required history audit found the malformed SQLite v3 provenance value, so this commit is superseded as M6.1 closeout evidence and is not final release evidence.

Provenance-correction commit `d4d524331b796ab408f292d0412e2d6728afacd0`, tree `2643255fe3cfc07bcd046af42b553c308face3ef`, produced failed push run [29621518452](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29621518452) and PR run [29621520034](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29621520034) on attempt 1. Push oldstable macOS Intel job `88017354931` observed the existing tail fixture's truncate notice and new chunk before its rotate observation deadline; same-SHA PR counterpart `88017359436` passed. Push native macOS Intel job `88017354962` observed process termination before existing Helper heartbeat client-failure visibility; same-SHA PR counterpart `88017359451` passed. PR oldstable Ubuntu 22.04 job `88017359412` observed the short-lived binding probe succeed before the existing stderr-overflow reader surfaced overflow; same-SHA push counterpart `88017355016` passed. The changed compatibility package passed with 100% coverage in all failed legs. Failed-only no-change attempt 2 push jobs `88018137111` and `88018137137`, PR job `88018137206`, and compare jobs `88018592289` and `88018263168` passed; both workflows completed `success`. Final classification: **environment/known Helper tail, heartbeat, and stderr-reader timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved; they are exact M6.1 correction evidence, not final release evidence.

M6.1 closeout commit `cb267a3b4adabf6f43fae51f7ed0a8f432d0aa53`, tree `c8db67def8f711535229416d6f575b47284056ab`, produced failed push run [29622036145](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29622036145) and PR run [29622037354](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29622037354) on attempt 1. Push oldstable macOS Intel job `88018837330` observed the existing Level 2 cancellation result before its terminal combination became visible; push native Ubuntu 22.04 job `88018837400` reached the existing daemon-restart probe before the control socket appeared; push and PR native macOS Intel jobs `88018837420` and `88018840749` observed process termination before existing Helper heartbeat failure visibility. Same-SHA PR companions `88018840757` and `88018840725` passed the Level 2 and daemon-restart fixtures, while the unchanged parent code had already passed both native macOS legs on the provenance-correction rerun. The commit changed documentation only. Failed-only no-change attempt 2 jobs `88019590581`, `88019590587`, `88019590651`, and `88019590691` all passed; both workflows and their compare jobs completed `success`. Final classification: **environment/known Level 2 cancellation, daemon-startup, and Helper-heartbeat timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved; they close M6.1 but are not final release evidence.

## Milestone status

| Milestone | Status | Evidence |
|---|---|---|
| M6.1 configuration/keymap/public interfaces | Complete | provenance-corrected commit `d4d524331b796ab408f292d0412e2d6728afacd0`, tree `2643255fe3cfc07bcd046af42b553c308face3ef`; complete application schema/runtime mapping, versioned config/keymap/Job/daemon/Helper-status interfaces, shared help/man/completion, compatibility boundaries, and canonical 11-source historical inventory; current Go 1.26.5 `make ci`, exact Go 1.25.12 `make check`, push `29621518452`, and PR `29621520034` pass |
| M6.2 migration/package/clean machine | In Progress | the 13-source historical closure, verified restore, SQLite head 4 retention, Helper state lifecycle, deterministic public package/dependency/NOTICE closure, installed man/completion, frozen Stage 5 upgrade/rollback/uninstall, four native platforms, preview-bundle picker, and pinned-cache recovery have exact evidence while production trust remains closed. Homebrew preview SHA `ed7dcd46dab669b929a483767a97890776caf4b9` passes local dual-Go gates and Hosted macOS ARM/Intel local-tap install 0.9→upgrade 1.0→version→formula test→forced all-version uninstall→untap. This is CI-only loopback pre-publication equivalence with a harness-only license placeholder. Real public channel, production distribution, protected final bytes, the owner-selected project LICENSE, and final clean-machine work remain pending |
| M6.3 security/compatibility/diagnostics | In Progress | Foundation/doctor/support-bundle plus hardening SHA `ba334b8d8968f5b09c91c0185996994b9a307ff4` deliver shared policy, read-only probes, preview/create/private publication, semantic diagnostic/export registries, safe-shaped persistent/archive scans, and byte-preserving corrupt-state summaries. Compatibility/security evidence SHA `6653a617045b1ed0c03c5b297dbcbd38db27116d` passed PR 24/24. Real-auth correction SHA `fd5ebe499331f5697cdc5eb1e238d12e939ee53a` has dual-workflow scans after complete OpenSSH/Askpass and externally renewed Kerberos/GSSAPI matrices. OpenSSH-floor/current SHAs `e7733ec23ec1903dbdc41a926db20fe6defcccc7`/`a7f3ddd8a99df973518140de7eae45e295ae6ea2` record exact `OpenSSH_8.9p1 Ubuntu-3ubuntu0.16`/`OpenSSH_9.6p1 Ubuntu-3ubuntu13.18`; Kerberos-binding SHA `14e5ec4e63c70fb93ed93d15adbe7fdf7cce5d5e` records exact `Kerberos 5 version 1.20.1` in both workflows and rechecks it before mutation. OBS-010 now has fixed/multi-failure, complete code-map, real endpoint, and four-native-platform installed-daemon evidence and is `Verified`; PLAT-009 is `Verified`; REL-006 is `In Progress` with explicit untested boundaries. Production Helper artifact scanning and final review remain pending |
| M6.4 RC/1.0 | In Progress | REL-009 has complete implementation-batch evidence for its linked user/operations lifecycle. REL-008 adds five canonical contracts plus a strict 17-gate candidate/final record validator. REL-010 adds a final-only audit for the frozen 23 feature rows, ADR-backed final decisions, and exact checked 12 exit criteria; current truthful output remains 15 nonterminal rows and 12 unchecked exits after the cold-start, doctor, workspace-migration, and saved-workspace-completion closures. No exact RC record, independent new-user exercise, final release artifacts, tag, release, or channel evidence exists |

## Feature status

WORK-006, JOB-010, PLAT-009, VIM-013, VIM-014, OBS-010, REL-001, and REL-002 are `Verified`; the other 15 Stage 6 rows are `In Progress`, with no `Planned` row remaining. The final M6.1 cold-start audit closes default/remapped keymap, deliberate macro/named-register exclusion, and complete versioned configuration without creating an absent user file. WORK-006 now has immutable historical input, exact no-replace pre-migration backup, interruption/retry/source-race/conflict preservation, and non-destructive export/repair evidence; REL-003 separately retains SQLite restore/recovery/final-channel boundaries. JOB-010 has complete retention evidence; HELP-013 remains production-closed. REL-002 now dynamically completes only saved private workspace files immediately after `--workspace`, without creating an absent state root or SQLite database, and has archive/native installed-binary evidence on all four native platforms. PLAT-003/REL-005 have four native archive-style and local-tap Homebrew preview lifecycles but not final release bytes or a real public channel. SEC-012/OBS-009 have support-bundle preview/create/private publication, semantic diagnostic/export registries, safe-shaped persistent-log/expanded-archive corpus scans, corrupt-state byte preservation, and dual-workflow exact-SHA real OpenSSH/Askpass/Kerberos scans, but still need protected final evidence. OBS-010 now has complete fixed-result, all-check multi-failure, troubleshooting-map, real OpenSSH/endpoint, and four-native-platform installed-daemon evidence. PLAT-009 has dual-workflow exact 8.9p1/current version-bound doctor/auth/support-scan evidence and exact public support records. REL-006 records exact MIT Kerberos `5 version 1.20.1` with explicit untested boundaries. REL-007 has an eight-domain review table with executable negative evidence and explicit partial boundaries. REL-008 has a complete gate catalog and strict external record validator but no exact-RC record or final evidence. REL-009 has four documentation contracts, the linked user/operations lifecycle, complete dual-Go local batch gates, and classified dual-workflow Hosted evidence, but awaits an independent exact-RC new-user exercise. REL-010 now has the final-only 23-row/12-exit audit and truthfully fails while those final blockers remain. Production Helper distribution and production Level 2 remain **CLOSED**.

## Stage 6 feature evidence

| Feature ID | Result | Evidence |
|---|---|---|
| WORK-006 | PASS | Strict v1 decoding and the pinned historical workspace fixture feed deterministic v2 migration. Before v2 publication, the store writes exact v1 bytes to an independent private temporary inode, fsyncs it, publishes `<name>.schema-v1.backup` with a no-replace link, validates byte identity, and fsyncs the directory. Interruption/retry reuses only the exact backup; conflicts, source changes, and invalid inputs preserve all evidence. The operations runbook gives explicit stopped-daemon macOS/Linux export and new-name repair steps without in-place edits. |
| JOB-010 | PASS | SQLite head 4 freezes the retention claim and fixed 1.0 policy; focused crash/reference/startup-owner tests, complete current and oldstable local gates, and corrected exact-SHA push `29626927513` plus PR `29626928885` Hosted gates pass. The first candidate remains explicitly rejected. |
| PLAT-009 | PASS | Exact system OpenSSH `8.9p1 Ubuntu-3ubuntu0.16` and current `9.6p1 Ubuntu-3ubuntu13.18` each pass dual-workflow native/version-bound doctor, complete OpenSSH/Askpass authentication, support-bundle scan, and the applicable Kerberos/GSSAPI matrix. Production runtime has no version-string rejection; the public environment matrix records the exact tested floor/current versions and stable doctor failure behavior. |
| PLAT-006 | PASS | Domain, IPC, workspace, LocalFS, search, sorting/filtering, and renderer contracts already preserve raw path bytes while safely displaying invalid UTF-8. The extension-free separate-process SFTP v3 fixture now proves exact invalid filename bytes and payload across the actual wire through list, stat, and bounded read, closing the remaining remote end-to-end evidence gap. |
| OBS-010 | PASS | The fixed doctor registry covers config/runtime/socket/daemon/OpenSSH/known-host/database/cache/Helper/disk plus optional endpoint with exact human/JSON/remediation contracts. All 11 probes may fail together without exporting raw cause, endpoint or path. The native clean-install lifecycle now runs the installed doctor against a live daemon on macOS 15 ARM/Intel and Ubuntu 22.04/24.04, requiring exact ordered result objects and zero `probe_failed`; the troubleshooting registry remains byte-identical to its committed code map. |
| VIM-013 | PASS | Final cold-start contract uses an absent configuration, exports the complete versioned 74-binding Normal/Visual keymap, verifies unchanged Vim-first `hjkl`, Visual and repeat bindings with zero overrides, and proves the read-only command creates no configuration file. Existing conflict/reserved/context/count/tcell/reducer/reset contracts remain green under current race and exact oldstable. |
| VIM-014 | PASS | Final cold-start contract rejects `macro_record` through the real config command with exit 3 and preserves the owner-private source bytes, then verifies generated help/man and bash/zsh/fish completion do not advertise macros or named registers. The action registry, schema, repeat, and single durable clipboard boundary remain unchanged. |
| REL-001 | PASS | Final cold-start contract runs validate plus versioned/redacted effective generation against an absent user file, verifies the explicit resolution policy and exact 14-field schema surface, and proves neither command creates the file. Complete defaults/schema/runtime mappings and CLI failure contracts pass current race and exact oldstable. |
| REL-002 | PASS | Bash, zsh, and fish scripts retain their shared ordered command/flag facts and now invoke one hidden read-only workspace-name query only immediately after `--workspace`. The query resolves the platform state path without preparing runtime directories, scans at most 1,024 entries, accepts only private regular `.json` files, returns at most 256 lexical names, does not read workspace contents, and creates neither an absent root nor SQLite state. Focused current/race and exact oldstable tests, an isolated installed-binary bash/zsh smoke, complete dual-Go local gates, quality archive installation, and all four native installed lifecycles pass with the fixed historical name `release-smoke`; help/man omit the private query while documenting the public behavior. |
| REL-006 | IN PROGRESS | Implementation SHA `14e5ec4e63c70fb93ed93d15adbe7fdf7cce5d5e` captures `/usr/bin/klist -V` as exact `Kerberos 5 version 1.20.1`, binds the retained value into the real auth harness, and rejects drift before user/KDC mutation. Push/PR auth jobs `88105308661`/`88105369623` both pass the complete GSSAPI expiry/recovery matrix. The SFTP compatibility batch adds fixture-tested, separate-process, extension-free SFTP v3 Level 0 and raw-name wire evidence. The required `ubuntu-24.04-arm` lifecycle and exact ARM64/linux/arm64 provenance passed in later push/PR runs `29683308347`/`29683309618`. The current real ProFTPD `mod_sftp` batch has local RED/GREEN evidence for exact pre-mutation version binding, production Provider browse/durable transfer and exact preview-TUI execution; exact-head Hosted remains its acceptance gate. The public matrix still keeps macOS Kerberos, broader vendor coverage, final protected artifacts/channel, and production Helper open. |
| REL-007 | IN PROGRESS | SHA `cc6559e41572aa21a7fc249fe0086fe4df50fe27`, tree `6d1ead4f23de7a1ac344d959f0b8cb75c004ab7f`, makes the security finding ledger name all eight required review domains with stable IDs and bind each to executable negative evidence. Three review-contract tests, exact current `make ci`, exact oldstable `make check`, and both Hosted quality/auth paths pass. Current reviewed scope has no unresolved high-severity finding. Credentials/authentication, Helper, direct transfer, log redaction, and recovery remain partial where hardware/platform/protected artifact/production boundaries are unavailable; final independent security review remains open. |
| REL-008 | IN PROGRESS | SHA `bad687f67789960bac426bd9414cfbcc5b859b49`, tree `bfa026b52e2a55738f023623093347c5ee78d21e`: five canonical contracts first failed because no exact-candidate gate catalog, identity/result validator, anti-substitution metrics, Hosted phase binding, or strict record CLI existed. GREEN adds `amsftp-release-gate-record-v1` with 17 required gates, exact evidence kinds, candidate SHA/tree binding, completed/success-only results, physical 100 GiB and process/network Level 2 metrics, declared soak acceptance, two independent exact-candidate reviews, zero-skipped candidate push/PR, and zero-skipped post-merge main. Exact current/oldstable local gates pass; PR `29657551397` passed 24/24, and push `29657550405` passed quality/auth with two known Helper reader timing residuals covered by same-SHA PR Intel jobs. No exact RC or real record exists, so this is executable gate preparation rather than final evidence. |
| REL-009 | IN PROGRESS | SHA `19e8ab73d5f90a51815634b9b36112f86711f372`, tree `ceb8e749ac11c873b703d3bee3f3168cb9f5fce2`: four canonical contracts first failed on missing user/operations navigation, first-run SSH/Kerberos and exact CLI path, bounded diagnostic/Job/Helper/direct fallback, and upgrade/rollback/withdrawal lifecycle. GREEN adds and links those documents, corrects stale troubleshooting command spellings, and passes exact current `make ci`, exact oldstable `make check`, focused docscheck, public help/man/completion parity, and both Hosted quality/auth paths. Residual Hosted failures are classified existing timing fixtures with same-SHA companion coverage and no rerun. An independent new-user exercise on the exact RC remains open. |
| REL-010 | IN PROGRESS | SHA `181f8a8fbf083c5dadf450373981693e82ecdf74`, tree `a05b87fd21a90b84970f835331edfb5148b5b6b5`: four canonical contracts first failed because no final-only release audit existed. GREEN adds explicit `docscheck --release`, freezes all 23 Stage 6 feature IDs, permits final states only as `Verified` or ADR-backed `Deferred`/`Removed`, and rejects changed/reordered/extra/missing or unchecked exit criteria. Exact current/oldstable local gates and push/PR at 24/24 pass. Ordinary docscheck remains green. After the cold-start, doctor, workspace-migration, and saved-workspace-completion closures, current release-mode output intentionally reports 15 nonterminal feature rows and all 12 unchecked exits; those blockers are not final evidence. |
| HELP-013 | IN PROGRESS | Helper state v2 atomically migrates v1 and retains exact parallel artifacts plus one active high-water selection; failed candidate handshake preserves the old active artifact; new Plans switch while frozen old Jobs resolve the old backend; exact reference-leased removal survives lost responses/restart; cleanup is bounded and preserves staged candidates. Public status/install/upgrade/disable/remove now have exact consent, input rejection, human/JSON error, and help/man/completion contracts before runtime; restricted serve remains a separate private process role. With production trust/composition closed mutation commands return stable configuration exit 3 without RPC, probe, state change, write, or delete. State lifecycle exact-SHA Hosted and public-surface focused dual-Go/race/lint/docs gates pass; production distribution and real Level 0 composition remain open. |

## Exit criteria

All 12 Stage 6 exit criteria remain open. Their milestone ownership and required proof are mapped in the execution plan. No checkbox may be closed from design intent, cross-builds, synthetic substitutes, or credentials that do not exist.

## Protected release boundary

Production Helper distribution and production Level 2 stay **CLOSED**. Opening them requires final release bytes, real protected Developer ID/notary success where applicable, byte-identical accepted notarization input and final tar binary, a production Helper manifest bound only to those final bytes, real offline signature/custody evidence, four-platform manifest-to-tar identity, clean quarantine/Gatekeeper evidence, and all security/compatibility/release gates. Fixture keys, public CI, tabletop ceremonies, or locally generated credentials cannot satisfy this boundary.

## Command ledger

| Candidate | Command/evidence | Result |
|---|---|---|
| exact main | local/origin/remote ref and tree audit | PASS; all identities matched the frozen baseline |
| exact main | Hosted run `29579514879` | PASS; completed success, 24/24 jobs |
| exact main | CI-equivalent baseline `make ci` | PASS; exit 0, no code change required |
| M6.1 config defaults RED | `go test ./internal/config -run='^TestDecode(AppliesDocumentedDefaultsToOmittedFields\|RequiresExplicitSchemaVersion)$' -count=1 -v` | Intended FAIL: minimal explicit-version document produced `ipc.max_frame_bytes must be greater than zero`; missing version remained rejected |
| M6.1 config defaults GREEN | `go test ./internal/config -count=1` | PASS: omitted fields inherit the single documented `Default()` source; explicit schema version remains required; unknown/trailing/invalid explicit values remain rejected |
| M6.1 config integration gate | `go test ./internal/app ./internal/config -count=1`; CI-equivalent `make check` | PASS: application loading regression and complete unit/provider/docs/tidy/verify gate green; config coverage 88.6% |
| M6.1 config CLI/machine-output RED | `go test ./internal/app ./internal/config -run='(TestRunReturnsStableTypedExitCode\|TestWriteRedactedEffectiveConfig)' -count=1` | Intended compile FAIL: no `config` role, typed exit contract, redacted effective writer, or output-version contract existed |
| M6.1 config CLI/machine-output GREEN | `go test ./internal/app ./internal/config -count=1`; focused `-race` on both packages | PASS: `config validate`/`print-effective`, explicit private-file validation, output v1, argv redaction/non-mutation, exit 0–8 snapshot, dispatch and error-channel contracts green |
| M6.1 config CLI complete local gate | CI-equivalent `make check`; `make lint`; `make docs-check`; `git diff --check` | PASS after adding precise `#nosec G302` rationale for the two test-only owner-private 0700 directories; no product permission or lint rule was weakened |
| M6.1 help/man/completion RED | `go test ./internal/app -run='(TestPublicHelpManAndCompletionsShareCommandFacts\|TestRunCompletion\|TestCommittedManPage)' -count=1` | Intended compile FAIL: no shared public CLI facts, man renderer, completion renderer, or completion command existed |
| M6.1 help/man/completion GREEN | focused `go test` and `go test -race ./internal/app -count=1`; `make lint`; `make docs-check` | PASS: ordered facts drive help/man and bash/zsh/fish static completions; committed man parity and forbidden remote/auth operation scans green; lint 0 issues |
| M6.1 keymap registry RED | `go test ./internal/keymap ./internal/tui -run='(TestDefaultSnapshot\|TestNewSupportsContext\|TestNewRejects\|TestTranslateTCellEventWithKeymap)' -count=1` | Intended compile FAIL: no keymap registry, default snapshot, override validation, context lookup, or keymap-aware tcell translation existed |
| M6.1 config keymap RED | `go test ./internal/config -run='TestDecode(AcceptsContextKeymapRemap\|RejectsConflictingOrReservedKeymap)' -count=1` | Intended compile FAIL: schema had no keymap section |
| M6.1 keymap GREEN | `go test ./internal/config ./internal/keymap ./internal/tui ./internal/app -count=1`; `go test -race ./internal/keymap ./internal/tui -count=1`; `make lint`; `make docs-check` | PASS: exact Vim default snapshot, Normal/Visual remap isolation, conflict/unreachable/unknown/count/control/dangerous rejection, schema round-trip, app wiring, default tcell regressions, race, lint 0, and docs green |
| M6.1 keymap complete local gate | CI-equivalent `make check`; `make lint`; `make docs-check`; `git diff --check` | PASS: full unit/provider/integration/docs/tidy/verify gate green; keymap coverage 90.3%, config 87.0%, TUI 69.7%; lint 0 and clean diff check |
| M6.1 fixed-quit/keymap-to-reducer RED | current Go 1.26.5 `go test ./internal/keymap ./internal/tui` on parent `a2a0503f319ec078f415753a3f2e6a2a631d8ca7` plus the new contracts | Intended FAIL: `q` was accepted as a Normal remap target even though the real application event loop consumes it as fixed quit before keymap dispatch; the new tcell-to-reducer count/Visual/repeat/delete-confirmation regression already passed |
| M6.1 fixed-quit/keymap-to-reducer GREEN | current Go 1.26.5 ordinary and race tests for keymap/config/TUI/app | PASS: `q` is rejected as an unreachable fixed Normal-mode quit input; real tcell input preserves count-before-lookup, context-local Visual dispatch, reserved repeat, and directory-delete confirmation without a second mutation path |
| M6.1 keymap-to-reducer complete local gate | current Go 1.26.5 and exact oldstable Go 1.25.12 CI-equivalent `make check`; current `make lint`; `make docs-check`; `git diff --check` | PASS under `GOTOOLCHAIN=local`, `umask 0022`, trusted fixture root, and separate external build/coverage directories; both complete unit/provider/integration/docs/tidy/verify gates green, lint 0 |
| M6.1 compatibility inventory RED | `go test ./internal/compatibility -count=1` | Intended compile FAIL: no registry/snapshot/Markdown renderer and no explicit owning constants for SQLite head, cache manifest, Helper manifest, or Helper envelope existed |
| M6.1 compatibility inventory GREEN | `umask 0022; go test ./internal/compatibility ./internal/app ./internal/cachefs ./internal/helper ./internal/ipc ./internal/state/migration ./internal/workspace -count=1` | PASS: exact nine-boundary snapshot and committed reference parity; config/workspace/SQLite/cache/IPC/Helper values resolve from owning package constants; outbound Helper envelope and cache manifest behavior is unchanged |
| local permission-fixture classification | the same focused command first ran under inherited `umask 0077` | ENVIRONMENT, not code: deliberate `0755` cache fixtures were masked to `0700`, so two wrong-mode negatives could not create their unsafe precondition; both targeted tests and the full focused set passed under the required CI-equivalent `umask 0022` with no code/assertion/timeout change |
| M6.1 compatibility inventory complete local gate | focused race on compatibility/cachefs/helper/IPC; CI-equivalent `make check`; `make lint`; `make docs-check`; `git diff --check` | PASS: race green; full unit/provider/integration/docs/tidy/verify green; compatibility registry 100% statement coverage; lint 0 and clean diff check |
| M6.1 cache/transfer config RED | `go test ./internal/config ./internal/app -run='Test(DefaultCacheAndTransfer\|DecodeAppliesPartialCacheAndTransfer\|CacheAndTransferSettings\|RuntimeCacheLimits\|RuntimeTransferLimits)' -count=1` | Intended compile FAIL: config had no cache/transfer schema and app had no validated runtime mapping |
| M6.1 cache/transfer config GREEN | focused config/app tests | PASS: omitted values freeze existing cache and manager defaults; partial documents preserve defaults; cache/concurrency/queue can only tighten ceilings; bandwidth is bounded; daemon maps validated values into cache manager and transfer scheduler without hot-reloading Job semantics |
| M6.1 config-source diagnostic RED | `go test ./internal/app -run='TestLoadApplicationConfigNamesSourcePath' -count=1` | Intended FAIL: the validation error named the invalid transfer field but omitted the configuration source path |
| M6.1 config-source diagnostic GREEN | focused app tests | PASS: inspect, validate, open, decode, and validation errors retain the exact config path while preserving field-level diagnostics |
| M6.1 cache/transfer complete local gate | CI-equivalent `go test -race ./internal/config ./internal/app -count=1`; `make check`; `make lint`; `make docs-check`; `git diff --check` | PASS under `umask 0022`: focused race green; full unit/provider/integration/docs/tidy/verify green; config coverage 87.5%; lint 0 and clean diff check |
| M6.1 current toolchain gate | exact tree `a9424373d4c0000473fdfedce06f9fd0c50f3dcf`; official Go 1.26.5 linux-amd64 archive SHA-256 `5c2c3b16caefa1d968a94c1daca04a7ca301a496d9b086e17ad77bb81393f053`; `GOTOOLCHAIN=local make ci` | PASS: exact local toolchain identity, complete check/lint/race/fuzz/supply-chain/actionlint and four-target builds green |
| M6.1 exact oldstable toolchain gate | exact tree `a9424373d4c0000473fdfedce06f9fd0c50f3dcf`; official Go 1.25.12 linux-amd64 archive SHA-256 `234828b7a89e0e303d2556310ee549fbcf253d28de937bac3da13d6294262ac1`; `GOTOOLCHAIN=local go version`; `GOTOOLCHAIN=local make check` | PASS: reported exact `go1.25.12 linux/amd64`; full unit/provider/integration/docs/tidy/verify gate green |
| M6.1 preview config RED | `go test ./internal/config ./internal/app -run='Test(DefaultPreviewSettings\|DecodeAppliesPartialPreviewSettings\|PreviewSettings\|RuntimePreviewLimits\|PreviewLocationUsesConfiguredRenderLimits)' -count=1` | Intended compile FAIL: config had no preview schema/runtime mapping and `previewLocation` accepted only hard-coded renderer/image defaults |
| M6.1 preview config GREEN | focused config/app tests; full config/app tests and focused race | PASS: config defaults derive from the owning preview package; every field and dependent combination can only tighten existing ceilings; the client freezes validated renderer/image limits and metadata rendering proves the configured output bound. The first full-package run correctly caught the exact `Default()` test snapshot missing the new public section; the snapshot was completed and the unchanged product implementation then passed full and race gates |
| M6.1 preview complete local gate | official Go 1.26.5 with `GOTOOLCHAIN=local`; CI-equivalent `make check`; `make lint`; `make docs-check`; `git diff --check`; exact Go 1.25.12 `make check`, all under `umask 0022` | PASS on current and exact oldstable: full unit/provider/integration/docs/tidy/verify green; config coverage 88.8%; lint 0 and clean diff check |
| M6.1 search config RED | `go test ./internal/search ./internal/config ./internal/app -run='Test(DefaultSearchBudgets\|DefaultSearchSettings\|DecodeAppliesPartialSearchSettings\|SearchSettings\|RuntimeSearchBudgets\|PendingSearchIdentities)' -count=1` | Intended compile FAIL: search owned no public current-budget defaults, config had no search section/runtime mapping, and filename/content identities accepted only hard-coded budgets |
| M6.1 search config GREEN | focused search/config/app tests; full package tests and focused race | PASS: owning search defaults preserve the exact prior filename/content envelopes; omitted/partial config and every field can only tighten those ceilings; startup maps validated millisecond durations and freezes each configured budget into the corresponding request identity. The first full lint gate correctly rejected unchecked unsigned/signed duration conversions; duration milliseconds were changed to validated signed fields, no suppression was added, and focused tests plus scoped lint returned green |
| M6.1 search complete local gate | official Go 1.26.5 `make check`; `make lint`; `make docs-check`; `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: full unit/provider/integration/docs/tidy/verify green; config coverage 90.2%; lint 0 and clean diff check; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 retry config RED | `go test ./internal/retrypolicy ./internal/config ./internal/app ./internal/transfer -run='Test(DefaultRetrySettings\|DefaultsFreezeCurrentRuntimeBehavior\|DecodeAppliesPartialRetrySettings\|RetrySettingsRemainBoundedAndNonAggressive\|RuntimeRetrySettings\|ManagerRetryAt)' -count=1` | Intended compile FAIL: no shared retry-policy defaults, config section/runtime mapping, or manager-frozen retry delay existed |
| M6.1 retry config GREEN | focused retrypolicy/config/app/transfer tests; full four-package tests and focused race; scoped lint | PASS: shared defaults preserve three existing reconnect delays and one-minute Job retry; explicit empty reconnect array disables attempts; validation cannot add attempts, shorten corresponding defaults, decrease delays, exceed 30 seconds, or set Job retry outside 60 seconds..10 minutes; client and manager freeze runtime values, persisted `retry_at` uses the manager delay, and scoped lint reports 0 issues |
| M6.1 retry complete local gate | official Go 1.26.5 `make check`; `make lint`; `make docs-check`; `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: full unit/provider/integration/docs/tidy/verify green; retrypolicy coverage 100%, config 91.4%, transfer 74.6%; lint 0 and clean diff check; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 integrity/direct-closure config RED | `go test ./internal/transfer ./internal/config ./internal/app -run='Test(ProductionPolicyDefaults\|DefaultIntegrityAndDirectTransfer\|DecodeAppliesPartialIntegrityPolicy\|IntegrityAndDirectTransferSettings\|RuntimeDirectPolicy)' -count=1` | Intended compile FAIL: transfer exposed no owning production policy defaults, config/runtime mapping, or configured copy-intent freeze boundary |
| M6.1 production-closure evidence regression RED | `go test ./internal/transfer -run '^TestLevel2PolicyAndProductionClosureNeverInvokeDirectFixture/ordinary_runtime_is_production_closed$' -count=1` after freezing explicit `IntegrityStrong` | Intended FAIL: the frozen integrity value made the old whole-struct `DirectPolicy.zero()` check report `level2_policy_disabled` instead of the required `production_distribution_closed`, despite all three enablement gates remaining false |
| M6.1 integrity/direct-closure config GREEN | focused and complete transfer/config/app tests; focused race | PASS: owning transfer defaults freeze `strong` and production distribution closed; only `strong`/`require_strong` validate, `baseline`/unknown policies fail closed, and `direct_transfer.enabled=true` is rejected before startup. The client maps the validated policy once and every copy Intent carries it through the existing immutable Planner/route-evidence chain. Zero direct policy now means all three enablement gates are false, independent of the integrity floor, so ordinary runtime retains `production_distribution_closed`; it cannot inject the fixture-only Level 2 backend or satisfy its workspace/data gates |
| M6.1 integrity/direct-closure complete local gate | official Go 1.26.5 `go test -race ./internal/transfer ./internal/config ./internal/app`; `make check`; `make lint`; `make docs-check`; `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: focused race and full unit/provider/integration/docs/tidy/verify green; config coverage 91.6%, transfer 74.6%; lint 0, docs and diff clean; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 diagnostic config RED | official Go 1.26.5 `go test ./internal/diagnostic ./internal/config ./internal/app` with `GOTOOLCHAIN=local` | Intended compile FAIL: diagnostic owner exposed no frozen default config or configurable ring capacity, the versioned schema had no diagnostic section, and app had no validated runtime mapping |
| M6.1 diagnostic config GREEN | official Go 1.26.5 `go test ./internal/diagnostic ./internal/config ./internal/app` with `GOTOOLCHAIN=local` | PASS: defaults derive from the diagnostic owner; partial config overlays correctly; log bytes/backups/ring records can only tighten current ceilings; OpenDaemon honors the ring cap; daemon startup maps the values into persistent and read-only-degraded logging paths |
| M6.1 diagnostic config complete local gate | official Go 1.26.5 focused three-package race, `make check`, `make lint`, `make docs-check`, `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: focused race, full unit/provider/integration/docs/tidy/verify, lint 0, docs and diff green; config coverage 92.0%, diagnostic 75.4%; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 effective-keymap/reset RED | official Go 1.26.5 focused keymap/config/app test command | Intended FAIL: keymap had no versioned effective binding export, config had no validated non-redacted encoder for persistence, app rejected both new subcommands, and shared completion facts omitted them |
| M6.1 effective-keymap/reset GREEN | official Go 1.26.5 focused and complete keymap/config/app tests | PASS: deterministic output v1 enumerates exact Normal then Visual effective bindings with current/default input and override/remap state; reset requires exact `--yes`, preserves rejected-source bytes and every unrelated value, publishes validated 0600 config through synced atomic replacement, leaves no temporary file, and shares help/man/completion facts |
| M6.1 effective-keymap/reset complete local gate | official Go 1.26.5 focused three-package race, `make check`, `make lint`, `make docs-check`, `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: full unit/provider/integration/docs/tidy/verify and race green; app coverage 43.6%, keymap 88.9%, config 91.4%; the first scoped lint reported only a new if/else style eligible for tagged switch, the equivalent switch was applied, and lint then reported 0 issues; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 config policy/Helper closure RED | official Go 1.26.5 focused config tests with `GOTOOLCHAIN=local` | Intended compile FAIL: config exposed no typed resolution policy, effective output carried no precedence/reload/freeze facts, the schema had no Helper section, and the Helper owner exposed no production-distribution gate |
| M6.1 config policy/Helper closure GREEN | official Go 1.26.5 focused helper/config/app tests | PASS: exact policy freezes CLI/workspace/user/default order, unsupported system/environment config layers, bounded OpenSSH/external-command environment role, restart-only reload, and plan-time Job semantics; `helper.enabled` defaults to explicit opt-out and is gated by the production-closed owner policy |
| M6.1 config policy local environment classification | current/exact-oldstable `make check` launched from the interactive shell without the CI umask | Both toolchains failed the same cachefs public-mode and instance-lock public-mode negative fixtures because shell `umask 077` converted requested 0755/0644 setup into private modes; changed helper/config/app packages passed. Classification: local environment mismatch, not code. No code, assertion, timeout, or fixture change; the exact commands below were rerun with CI `umask 0022` and trusted persistent root. |
| M6.1 config policy/Helper closure complete local gate | official Go 1.26.5 and exact Go 1.25.12 `make check` with `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, and external build/coverage roots; focused race, `make lint`, `make docs-check`, `git diff --check` | PASS on current and exact oldstable: complete unit/provider/integration/docs/tidy/verify green, config coverage 91.5%, helper 74.4–74.5%, app 43.8%, lint 0, docs/diff green |
| M6.1 durable Job CLI RED | official Go 1.26.5 focused `go test ./internal/app` after adding dispatch, RPC, output, bound, confirmation, and exit-class contracts | Intended compile FAIL after correcting one test-only route constant name: `runJobCommand` and `JobCLIOutputVersion` did not exist; no public Job role, handler, shared help/man/completion fact, bounded parser, or versioned output existed |
| M6.1 Job pre-connect/error-envelope RED | focused app tests for connector ordering and `Run` stderr | Intended compile FAIL: `runJobWithConnector` and `jobCommandError` did not exist; production would have resolved paths and connected/started the daemon before rejecting invalid or unconfirmed input, and JSON-mode errors would have received the human `amsftp: job handler` prefix |
| M6.1 Job explicit DTO RED | focused list/control/events JSON tests | Intended FAIL: internal `Snapshot`, `Location`, and `EventRecord` Go field names leaked into the public JSON, and event payload remained a quoted storage string rather than an embedded JSON value |
| M6.1 Job connection classification RED | focused app exit-class test | Intended compile FAIL: no connection-specific classifier existed; an ordinary control-socket dial/startup failure would otherwise have fallen through the RPC classifier to internal exit 1 instead of network exit 5, while cancellation still required exit 8 |
| M6.1 durable Job CLI GREEN | complete and focused app ordinary/race tests | PASS: canonical Job IDs, 1..100 page bounds, list/events/pause/resume/cancel high-level RPC routing, pre-connect validation, exact same-ID cancel confirmation, human tables, explicit snake_case v1 success/error DTOs, empty-array stability, embedded event JSON, safe daemon diagnostics, stdout/stderr separation, and stable exit-class mapping are covered; no raw provider mutation route was added |
| M6.1 Job CLI complete local gate | official Go 1.26.5 and exact Go 1.25.12 `make check` with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; current focused race, `make lint`, `make docs-check`, `git diff --check`; built-binary help/completion/invalid-JSON smoke | PASS on current and exact oldstable after the final connection-classification change: full unit/provider/integration/docs/tidy/verify green, app coverage 45.6%, lint 0, docs/diff green, and the binary returned exit 2 with empty stdout and one v1 JSON stderr object. The first lint caught four new ST1005 capitalization findings; only those strings were lowercased and final lint reported 0 issues. The first smoke harness stopped before product assertions because zsh reserves `status`; renaming that shell-local variable produced the passing no-product-change rerun |
| M6.1 daemon lifecycle CLI RED | official Go 1.26.5 `go test ./internal/daemon ./internal/app` after adding handshake-info, shutdown ordering, validation, lifecycle, output, and help/completion contracts | Intended compile FAIL: `Client.Info`, shutdown request/response/server callback, daemon command runtime/parser/output, and public lifecycle facts did not exist |
| M6.1 daemon lifecycle CLI GREEN | official Go 1.26.5 complete daemon/app ordinary and race tests; real isolated control-socket lifecycle in `TestDaemonRoleServesLocalProviderAndStopsCleanly` | PASS: status is probe-only and reports absent/running distinctly; start is idempotent; stop validates exact `--confirm stop` before runtime activity, uses only the verified owner-private IPC connection, receives an acknowledgement before one-shot server cancellation, drains daemon resources, and removes the socket; bare daemon/explicit migration-resume compatibility remains intact; daemon and Job commands share the v1 machine-error renderer |
| M6.1 daemon binary smoke environment classification | built binary with isolated XDG config/state/cache/runtime roots, then `amsftp daemon status --format json` | ENVIRONMENT/SAFE DIAGNOSTIC: runtime validation selected the existing user fallback `/tmp/amsftp-1001`, whose pre-existing socket refused connection; status correctly returned one network-class v1 JSON error instead of deleting, replacing, or misreporting the unhealthy socket. No user runtime artifact was changed. The real lifecycle gate uses explicit owner-private test paths and passed without weakening validation |
| M6.1 daemon runtime-invariant RED | official Go 1.26.5 `go test ./internal/app -run '^TestDaemonCommandRejectsRuntimeWithoutClient$' -count=1` | Intended FAIL with a nil-pointer panic: a malformed injected probe could report `found=true` without a client, and a malformed starter could return `(nil, nil)` |
| M6.1 daemon runtime-invariant GREEN | the same focused test after adding explicit invariant checks | PASS: both malformed runtime results return stable internal exit 1 instead of panicking; production probe/starter behavior is unchanged |
| M6.1 daemon lifecycle complete local gate | official Go 1.26.5 and exact Go 1.25.12 `make check` with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; current focused race, `make lint`, `make docs-check`, `git diff --check`, rerun after the final invariant guard | PASS on current and exact oldstable: complete unit/provider/integration/docs/tidy/verify and focused daemon/app race green; app coverage 46.8%, daemon 68.4%; lint 0 issues; docs and diff clean; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 final configuration mapping RED | official Go 1.26.5 focused app/daemon tests with `GOTOOLCHAIN=local` | Intended compile FAIL after one test-only pane constant correction: public `listing.default_page_size` had no list-call input, and `ServerConfig` had no configured frame maximum; graph/code search confirmed both public fields were otherwise validation/output-only |
| M6.1 final configuration mapping GREEN | the same focused tests; complete config/app/daemon ordinary tests and focused race | PASS: every client directory page uses the startup-frozen validated listing size; daemon reader and writer use the startup-frozen validated frame maximum while retaining the 8 MiB protocol ceiling and legacy constructor default; oversized input is rejected at the framed connection boundary |
| M6.1 final configuration mapping complete local gate | official Go 1.26.5 and exact Go 1.25.12 `make check` with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; current focused config/app/daemon race, `make lint`, `make docs-check`, `git diff --check` | PASS on current and exact oldstable: complete unit/provider/integration/docs/tidy/verify and focused race green; app coverage 47.2%, daemon 68.4–68.5%, config 91.5%; lint 0 issues; docs and diff clean; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 Helper status CLI RED | official Go 1.26.5 focused app tests after adding public dispatch, pre-connect validation, RPC lifecycle, L0/L1 parsing, versioned output, cleanup, and help/man/completion contracts | Intended compile FAIL after one test-only pointer-assignment correction: `runHelperCommand`, `runHelperWithConnector`, and the Helper RPC abstraction did not exist; the only production Helper role accepted restricted `serve`, and public command facts exposed no Helper management surface |
| M6.1 Helper status CLI GREEN | complete app ordinary tests and focused app race | PASS: `helper status <SSH-host>` validates command/host/format before runtime activity, connects through the authenticated local daemon, reads exactly one SSH Provider capability snapshot, strictly validates Endpoint/state/`helper_status v1`/bounded unique constraints/L0-L1 combinations, emits human or explicit snake_case v1 JSON, displays the production-distribution gate, and releases the temporary Endpoint on success, malformed status, or snapshot failure; it performs no install/enable/upgrade/disable/remove mutation and restricted `helper serve` remains absent from completions |
| M6.1 Helper success-publication ordering RED/GREEN | focused release-failure and client-close-failure tests | Intended RED showed the first implementation had already written a JSON success object before reporting cleanup failure; GREEN decodes and retains the result in memory, completes exact Endpoint release and RPC client close first, and only then publishes success, so a cleanup failure leaves success stdout empty and returns one classified error |
| M6.1 Helper status local gate | official Go 1.26.5 full `make ci` and exact Go 1.25.12 CI-equivalent `make check` with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; current focused app race, `make lint`, `make docs-check`, `git diff --check` | PASS on current and exact oldstable after the cleanup-before-success regression: complete unit/provider/integration/docs/tidy/verify, full race, four fuzz smokes, supply-chain/actionlint and four-target builds green; app coverage 48.8%, helper 74.6%, compatibility registry 100%; lint 0 issues, docs/diff clean; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 historical-source inventory RED | official Go 1.26.5 focused compatibility tests after adding the frozen-source contract | Intended compile FAIL: `HistoricalSourceSnapshotText`, `HistoricalSources`, and the capture-status model did not exist, so no complete pre-mutation source set or immutable fixture evidence could be asserted |
| M6.1 historical-source inventory GREEN | `go test ./internal/compatibility -count=1` | PASS: exact 11-source config/workspace/SQLite/cache/Helper inventory with provenance and planned immutable paths; config v1 plus workspace v1/v2 are captured with exact SHA-256 and accepted by current owner readers; the other eight entries are truthfully marked `capture-required-before-M6.2-mutation`, so a planned path cannot be mistaken for fixture evidence |
| M6.1 historical-source inventory local gate | official Go 1.26.5 `make ci`; exact Go 1.25.12 `go version` and `make check`, with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; `git diff --check` | PASS on current and exact oldstable after replacing the first lint-rejected variable `os.ReadFile` test call with repository-root-confined `fs.ReadFile`: complete unit/provider/integration/docs/tidy/verify, full race, four fuzz smokes, supply-chain/actionlint, four-target builds, lint 0, and diff check green; compatibility coverage 100%; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 exact implementation Hosted gate | commit `9e2ed6df9fc736d0d86fa5e6f70d3a0ef0146224`, tree `73a8b94974ffc98862f0d28243834acba5d999bc`; push `29619828356`; PR `29619830232` | PASS on failed-only attempt 2 after the two attempt-1 timing failures were independently contradicted by same-SHA companions; all changed compatibility/fixture/docs contracts, native/oldstable, auth, quality, builds, reproducibility, compare and provenance jobs green; no code/test/timeout/workflow change |
| M6.1 provenance canonicality RED | `go test ./internal/compatibility -run '^TestHistoricalSourceProvenanceCommitsAreCanonicalObjectIDs$' -count=1 -v` | Intended FAIL: SQLite v3 provenance length was 41 rather than a canonical 40-character object ID |
| M6.1 provenance canonicality GREEN | focused complete compatibility tests; explicit `git cat-file -t` for every distinct provenance value | PASS: Git history identifies `939ba9c5d978b8ea5bf1ae060ff62a0769d0d6c0` as the commit that first added `version3.go`; all inventory provenance values are lowercase 40-hex IDs and all eight distinct values resolve to Git commit objects |
| M6.1 provenance correction local gate | official Go 1.26.5 full `make ci`; exact Go 1.25.12 `go version` and `make check`; `make docs-check`; `git diff --check`, with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots | PASS on the correction worktree: complete unit/provider/integration/docs/tidy/verify, lint, full race, four fuzz smokes, supply-chain/actionlint, four-target builds and exact oldstable check green; oldstable reported `go1.25.12 linux/amd64`; docs and diff clean |
| M6.1 exact provenance-correction Hosted gate | commit `d4d524331b796ab408f292d0412e2d6728afacd0`, tree `2643255fe3cfc07bcd046af42b553c308face3ef`; push `29621518452`; PR `29621520034` | PASS on failed-only attempt 2 after all three attempt-1 Helper timing failures passed directly comparable same-SHA companions; changed compatibility/object-ID contracts, native/oldstable, auth, quality, builds, reproducibility, compare, and provenance jobs green; no code/test/timeout/workflow change |
| M6.1 exact closeout Hosted gate | commit `cb267a3b4adabf6f43fae51f7ed0a8f432d0aa53`, tree `c8db67def8f711535229416d6f575b47284056ab`; push `29622036145`; PR `29622037354` | PASS on failed-only attempt 2 after known Level 2 cancellation, daemon-startup, and Helper-heartbeat timing fixtures passed same-SHA or directly comparable companions; documentation-only delta, all native/oldstable/quality/build/reproducibility/compare/provenance jobs green; no code/test/timeout/workflow change |
| M6.2 complete historical capture RED | official Go 1.26.5 focused `TestEveryHistoricalMigrationInputIsCapturedBeforeM62Mutation` | Intended FAIL for exactly the eight uncaptured SQLite v1/v2/v3, cache entry/materialization v1, and Helper release/state/metadata v1 sources; config v1 and workspace v1/v2 remained captured |
| M6.2 historical writer capture | temporary capture harnesses executed at exact provenance commits `486a63f90be51c0d79a454bef52e9e3302df5250`, `4eb1961b7b3b5495620fb1f6fcb3b88c52a4fba9`, `939ba9c5d978b8ea5bf1ae060ff62a0769d0d6c0`, `8a4ada06836b9ed71c72b40949d6b87d8e1f849a`, and `145b50ae871aa91f8acc0505d2b6b9bd19bae742` | PASS: real historical SQLite migration/store APIs generated v1/v2/v3 databases; real cache publication APIs generated entry/materialization manifests; real Helper manifest/state APIs generated release/state/metadata bytes. All temporary capture harnesses and worktrees were removed after copying the exact outputs |
| M6.2 initial historical file capture GREEN | official Go 1.26.5 focused compatibility/cachefs/helper tests with trusted persistent fixture root | PASS for the then-enumerated 11 source files: exact SHA-256 matches repository bytes, current migration owner validates SQLite heads 1–3 read-only, current cache owner reads historical entry/materialization manifests with exact blob binding, and current Helper owner parses the release manifest and loads enabled state/metadata. This did not yet assert that each SQLite directory contained every backup referenced by its catalog |
| M6.2 capture safety-lint correction | first current `make ci`, then focused tests and `make lint` after correction | Initial full gate reached lint after every ordinary test passed and correctly rejected variable-path test helpers plus a `uint64`→`int` slice conversion. Classification: introduced test-code safety defects. GREEN uses repository-confined `DirFS`, destination-confined `os.Root`, and an explicit v1/v2/v3 migration mapping; focused owner tests pass and lint reports 0 issues. No suppression or product relaxation was added |
| M6.2 complete historical capture local gate | official Go 1.26.5 full `make ci`; exact Go 1.25.12 `go version` and `make check`; `make docs-check`; `git diff --check`, with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots | PASS on current and exact oldstable after the safety-lint correction: complete unit/provider/integration/docs/tidy/verify, full race, four fuzz smokes, supply-chain/actionlint, four-target builds, lint 0, hashes and diff check green; oldstable reported `go1.25.12 linux/amd64` |
| M6.2 initial historical capture Hosted gate | commit `6664273e78f1294371b1d403d12f48ed79926683`, tree `dfbab379cbcfb347727ffce88fc892127d75d1a9`; push [29623281641](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29623281641); PR [29623282730](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29623282730) | PASS first attempt, 24/24 jobs in each workflow at the exact SHA. No CI failure or rerun occurred. This proves the original 11-file contracts only and is superseded for complete migration-source evidence by the directory-closure correction below |
| M6.2 SQLite directory-closure RED | official Go 1.26.5 focused `TestInitializeUpgradesEveryPinnedHistoricalSQLiteState` using each pinned main database in a fresh owner-private root | Intended FAIL for v2 and v3: `ReconcileBackupRetentionAfterSchemaValidation` reported verified `.amsftp-backup-v1-22222222222222222222222222222222.sqlite3` / `.amsftp-backup-v1-33333333333333333333333333333333.sqlite3` missing. Classification: capture-model defect, not a product retention defect; the catalogs correctly failed closed and were not weakened |
| M6.2 SQLite bundle correction | temporary harnesses at exact provenance commits `4eb1961b7b3b5495620fb1f6fcb3b88c52a4fba9` and `939ba9c5d978b8ea5bf1ae060ff62a0769d0d6c0` | PASS: the historical coordinator created the held v1 and v2 backups and catalog rows with their real backup identity; the v3 fixture's representative recovery row was written through historical `editstore.Create`. Corrected main databases plus both held backups are pinned by exact SHA-256; temporary harnesses, worktrees, and outputs were removed after capture |
| M6.2 SQLite directory-closure GREEN | official Go 1.26.5 focused compatibility and `internal/statefs` upgrade tests | PASS: all 13 source files match pinned hashes and current-owner readers; v1/v2/v3 main databases upgrade to the compiled head, preserve representative historical Job rows and the v3 recovery-detail row, and complete verified-backup retention reconciliation with the catalog-referenced companions present |
| M6.2 SQLite directory-closure local gate | official Go 1.26.5 `make docs-check` and full `make ci`; exact Go 1.25.12 `go version` and `make check`; `git diff --check`, with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots | PASS first attempt on both toolchains: current ordinary/provider/integration/docs/tidy/verify, lint 0, full race, four fuzz smokes, supply-chain/actionlint and four-target builds green; exact oldstable reported `go1.25.12 linux/amd64` and completed `make check`; no failure classification was needed |
| M6.2 SQLite directory-closure exact-SHA Hosted gate | commit `9c2638ff12296008a72ee81d65955d2af03a2ba3`, tree `a05b5cba5a3647cf8b790f9ce4ca598ab2f0dbf6`; push [29624193160](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29624193160); PR [29624194445](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29624194445) | PASS first attempt, 24/24 jobs in each workflow at the exact SHA: native/current/oldstable/auth/quality/build/reproducibility/compare/provenance all green. No CI failure, rerun, or classification was needed |
| M6.2 SQLite directory-closure evidence closeout Hosted gate | documentation-only commit `2dc0eda99be86b280cb1793ba9b4bf5afeac84d8`, tree `99eb824af0689e749e2c97e08ea121c275a81a05`; push [29624523022](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29624523022); PR [29624524733](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29624524733) | Push passed first attempt 24/24. PR attempt 1 oldstable macOS Intel job `88026135538` alone failed when existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap` observed zero rather than 65,536 diagnostic bytes; the same-SHA push and other oldstable/native legs passed, and both parent commit workflows had passed first attempt. Failed-only no-change attempt 2 completed 24/24. Classification: known Helper process-reader timing instability, not introduced code. No code, fixture, assertion, timeout, or workflow was changed |
| M6.2 read-only migration diagnosis RED/GREEN | focused current tests for historical v1/v3, restored v1 backup, synthetic newer head, and exact sidecar presence | RED did not compile before the inspection API existed. GREEN opens only a sidecar-free `immutable=1&mode=ro` view, validates supported heads exactly, reports `pristine`, `current`, `upgrade_available`, `upgrade_ready`, `explicit_resume_required`, `restored_backup_hold`, `newer_schema`, or `sidecar_recovery_required`, and preserves database/directory names, bytes, modes, sizes, and mtimes. It never treats a main-only view as valid when recovery sidecars exist |
| M6.2 verified restore publication RED/GREEN | focused current `internal/statefs` and `internal/state/migration` tests over pinned v2/v3 catalogs and held v1/v2 backup companions | RED lacked any restore or hold-release API. GREEN selects one exact verified catalog row, validates source catalog/hash/attrs/no-sidecars/original whole-schema and exact restore hold, copies only the closed immutable backup through a no-follow handle, hashes during a stable snapshot, full-syncs a 0600 deterministic temp, immutable-validates it, publishes no-replace, syncs the root, and revalidates the final bytes. Existing destinations are never overwritten or removed; only an exact already-published final is idempotently reused. Wrong digest/hold, corrupt source, collision, and wrong-mode temp fail without destination mutation. Ordinary startup returns no writable DB and leaves a restored backup byte-exact and held |
| M6.2 restore process-death and explicit continuation matrix | real current test subprocess exits at `temp_reserved`, `copy_complete`, `temp_synced`, `temp_verified`, `final_published`, and `directory_synced`; atomic migration-attempt tests | PASS: every pre-publication death leaves at most the one exact deterministic temp and retry rebuilds it; every post-publication death reuses only the exact hash/schema/hold-matching final; catalog DB and held backup bytes/attrs remain unchanged and no temp survives successful retry. A separate transaction changes the exact restored hold directly to one new unbacked `preparing` attempt; mismatch rolls back both changes, and successful approval still requires explicit resume, which creates a fresh verified backup before forward migration |
| M6.2 restore slice local gate | official Go 1.26.5 focused suites, `make lint`, and full `make ci`; exact Go 1.25.12 `go version` and `make check`; `git diff --check`, with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures, and separate external build/coverage roots | PASS: focused and package suites green; lint 0; current full ordinary/provider/integration/docs/tidy/verify, race, four fuzz smokes, vulnerability/actionlint, and four builds passed first completed attempt. Exact oldstable reported `go1.25.12 linux/amd64` and passed `make check`. An earlier evidence command printed `go version` without `GOTOOLCHAIN=local`, so that standalone probe auto-selected 1.26.5 from the module declaration; its accompanying check was manually interrupted and excluded. The clean rerun applied `GOTOOLCHAIN=local` to both the version probe and gate and is the only oldstable evidence counted here |
| M6.2 restore slice exact-SHA Hosted gate | commit `16cfd914b17239203259283e6324b3e7a50ba422`, tree `bd837f5149d26257ed989c6a1e2bb538b43ce19c`; push [29625386334](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29625386334); PR [29625387450](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29625387450) | Push passed first attempt 24/24. PR attempt 1 oldstable macOS Intel job `88028626645` alone failed in two already-recorded timing fixtures: exact-stderr-cap visibility returned 0 rather than 65,536 bytes, then the Stage 2 PTY delete fixture observed selection before listing readiness. Same-SHA push oldstable macOS Intel job `88028623167` and every other PR job passed. Failed-only attempt 2 job `88030021622` passed without code, assertion, timeout, fixture, or workflow changes and the run completed `success`. Classification: **environment/known Helper reader and PTY listing timing fixtures, not introduced code** |
| M6.2 Job-history retention RED/GREEN | official Go 1.26.5 focused `internal/state/migration`, `jobstore`, `cachestore`, `editstore`, `statefs`, `compatibility`, and `app` tests | RED failed to compile without Version 4, a compiled migration registry, retention reconciliation, or cache-reference claim exclusion. GREEN freezes Version 4 checksum `d1db8d80521f879053904e109f3d5e3fcefbcb9866a2689c1258bb4cdf916700` and whole-schema digest `74d8a1991fd420cec7d3e37d25ce2f081c29577ddf21ba97815e3d54aec0b693`; the fixed 1.0 policy keeps the newest 1,000 terminal Jobs and all terminal Jobs younger than 30 days, deletes only bounded row chunks in at most 64 transactions per call, resumes one durable exact claim after restart, and never removes nonterminal, edit-bound, upload-referenced, or active/uncertain-leased Jobs. Cache upload references cannot attach after a retention claim, and daemon restart completes a pre-existing claim before accepting work. All focused packages pass |
| M6.2 Job-history retention batch local gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 `go version` and `make check`; `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, and separate external build/coverage roots | PASS first completed attempt on both toolchains. Current completed fmt/vet, all ordinary/provider/integration tests, docscheck, tidy/verify, lint with 0 issues, full race, four fuzz smokes, govulncheck with zero called/imported vulnerabilities, actionlint, and all four cross-build targets. Oldstable reported `go1.25.12 linux/amd64` and completed fmt/vet, all tests, docscheck, tidy, and verify. No failure classification or rerun was needed |
| M6.2 first Job-history retention exact-SHA Hosted candidate | commit `2ec8a7d895f1303d8d0c5df821b74d7edd6d90b3`, tree `21d428cc3e29be0e23edffe2dc363b95bd9c56fb`; push [29626251042](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29626251042); PR [29626252640](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29626252640) | **Rejected candidate.** Attempt 1 push/PR macOS Intel native jobs both hit the existing exact-stderr-cap 0/65,536 Helper visibility fixture; PR quality hit the existing one-second `FuzzNormalizePath` deadline. Same-SHA oldstable Intel, macOS ARM, push quality, and all changed state packages passed. Failed-only attempt 2 moved to different known Helper-heartbeat, Stage 3 foreground-PTY, and fuzz/integration timing outcomes, but PR macOS Intel full race also timed out waiting for the daemon socket in the changed `TestDaemonRestartRecoversEveryNonterminalJobStateDeterministically`. Because that failure implicated this batch's added retention setup inside a wall-clock daemon test, it was classified as an **introduced test-design defect**, not waived as flaky. The correction removes retention ownership proof from the socket/timing fixture, extracts the production startup sequence into a directly testable function, and proves claim completion before return without changing any timeout or weakening an assertion. Both failed attempts remain preserved and cannot serve as batch evidence |
| M6.2 retention startup-owner correction focused gate | exact current Go 1.26.5 ordinary once plus race `-count=5` for `TestDaemonRestartRecoversEveryNonterminalJobStateDeterministically` and `TestInitializeDurableJobStoreResumesRetentionClaimBeforeReturn` | PASS: the original daemon restart fixture again tests only daemon/recovery behavior; the new direct owner test exercises the exact production `RecoverInterrupted → ReconcileHistory → CheckpointIdle` sequence and verifies the claimed terminal Job, claim, children, and Plan are gone before return. Five focused race repetitions pass without socket polling for the retention assertion |
| M6.2 retention startup-owner correction local gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 `go version` and `make check`; same trusted fixture root, `GOTOOLCHAIN=local`, `umask 0022`, and new external build/coverage roots | PASS first completed attempt after the production/test restructuring. Current ordinary, docs/tidy/verify, lint 0, complete race including app/statefs, four fuzz smokes, vulnerability/actionlint, and four builds passed. Exact oldstable reported `go1.25.12 linux/amd64` and passed complete check. This supersedes the rejected candidate's local result for the corrected tree |
| M6.2 corrected Job-history retention exact-SHA Hosted gate | commit `b3bd42eae7660c4dbfaa65ba1ee5ef6dce83aafe`, tree `088e19a546860cac38ecc911935c1dc21120b761`; push [29626927513](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29626927513); PR [29626928885](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29626928885) | PASS after evidence-based failed-only reruns, with no code, assertion, timeout, fixture, or workflow changes. Attempt 1 push oldstable Ubuntu 22.04 job `88033068334` hit the existing Helper binding-probe stderr-overflow visibility race; PR oldstable macOS Intel job `88033072415` hit that race plus the existing exact-stderr-cap 0/65,536 visibility race. Same-SHA platform companions, exact local oldstable, and every changed app/state package passed. Attempt 2 oldstable jobs `88033824383` and `88033826815` passed; PR native macOS Intel job `88033826809` also passed complete race. Push native macOS Intel job `88033824412` then independently hit existing `TestOpenSSHProcessSessionRejectsStderrBeyondHardCap` pipe-close timing (`file already closed`) after all changed app/state packages passed. Failed-only attempt 3 job `88034383549` passed, and both workflows completed `success`. Classification: **environment/known Helper process-reader timing instability, not introduced code**. The rejected `2ec8a7d` candidate remains rejected; only this corrected exact SHA closes the batch and verifies JOB-010 |
| M6.2 Job-history retention evidence closeout Hosted gate | documentation-only commit `35220c3deab9c626a3b8bc54ab95bd5205b4b946`; push [29627661937](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29627661937); PR [29627663274](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29627663274) | PR passed first attempt 24/24. Push attempt 1 native macOS ARM alone observed process termination before the existing Helper heartbeat failure became visible; failed-only attempt 2 moved to the existing exact-stderr-cap visibility race and observed 0 rather than 65,536 bytes. Same-SHA PR and all push companion jobs passed both changed docs and unchanged code. Failed-only no-change attempt 3 completed 24/24. Classification: **environment/known Helper reader timing instability, not introduced code**; no code, assertion, timeout, fixture, or workflow changed |
| M6.2 Helper lifecycle RED | official Go 1.26.5 focused helper/transfer/jobstore/compatibility command after adding the five related contracts | Intended compile FAIL: state v2 exact load/activation/removal/reconciliation APIs, `HelperStateSchemaVersion`, and durable `SameHostCopyResolver` did not exist |
| M6.2 Helper lifecycle focused GREEN | official Go 1.26.5 complete `internal/helper`, `internal/transfer`, and `internal/compatibility` tests; focused `-race` across helper/transfer/jobstore/compatibility with `-count=3` | PASS: v1→v2 migration and parallel exact records, failed-handshake old-active preservation, non-lowering high-water, frozen old-Job exact resolution after active switch, one exact removal claim with lost-response restart completion, and 1..64 cleanup that preserves installed and staged candidate metadata all pass. Production Helper distribution and Level 2 remain CLOSED |
| M6.2 Helper lifecycle batch local gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 `go version` and `make check`; `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, separate external build/coverage roots; `git diff --check` | PASS first attempt on both toolchains. Current completed fmt/vet, all ordinary/provider/integration tests, docs/tidy/verify, lint 0, full race, four fuzz smokes, govulncheck/actionlint, and four-target builds. Oldstable reported `go1.25.12 linux/amd64` and completed the full check. No failure classification or rerun was needed |
| M6.2 Helper lifecycle exact-SHA Hosted gate | implementation commit `129a62d2aec9a3b9a04e54e30aebcecd8eb08b4c`, tree `ae3e4277afd2669723b1a238e412be97de355284`; push [29628470454](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29628470454); PR [29628471505](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29628471505) | PASS first attempt, 24/24 jobs in each workflow: native macOS 15 Intel/ARM and Ubuntu 22.04/24.04, exact oldstable, auth, quality, four builds, independent reproducibility pairs, compare, and provenance all green. No failure, rerun, or instability classification occurred |
| M6.2 Helper lifecycle evidence closeout Hosted classification | documentation-only commit `bff24282cfd2c7a166dd5aef67104aba59b28c2e`; push [29628766183](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29628766183); PR [29628767452](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29628767452), both attempt 1 | Both workflows completed `failure`, retained without rerun. Push quality job `88038340718` and oldstable macOS Intel `88038340757` hit existing command-cancellation/process-start timing fixtures; the corresponding PR package/job passed. PR quality `88038343961` hit existing daemon-close visibility, native macOS `88038344007` hit the Stage 2 PTY selection-before-listing fixture, and native macOS Intel `88038344012` hit the known Helper heartbeat visibility fixture; corresponding push companions passed. All five failures are contradicted by same-SHA companions, the delta is docs-only, and no changed code path exists. Classification: **environment/known cancellation, daemon, PTY, and Helper timing fixtures, not introduced code**. No code, assertion, timeout, fixture, workflow, or rerun changed the evidence |
| M6.2 public packaging RED | official Go 1.26.5 focused `go test ./internal/releasepack -count=1` after the five related contracts were added | Intended compile FAIL: `BuildPublicBundle`, exact archive/version types, checksum/SPDX/provenance models, and production Helper byte admission did not exist. A later writer contract separately failed to compile without `WriteBundle`, and the command contract failed without the manifest loader/runner |
| M6.2 public packaging focused GREEN | official Go 1.26.5 `go test ./internal/releasepack ./internal/tools/releasepack -count=1`; focused vet/lint; `make make-contract-scan docs-check`; `git diff --check` | PASS: exact deterministic darwin/linux × amd64/arm64 tar.gz names and canonical six-file contents; fixed gzip/tar metadata and `VERSION.json`; filename-ordered checksums; SPDX 2.3 and provenance binding the same four bytes; mandatory LF-terminated LICENSE/NOTICE/install/uninstall; canonical release identity; no fixture-trust leakage; one new no-overwrite output directory with exactly seven 0644 artifacts; strict manifest schema, confined non-symlink inputs, distinct binaries, and dependency license declarations. Linux production admission accepts only final unsigned bytes; Darwin accepts only Developer ID/hardened-runtime/timestamp/strict/notary `Accepted` evidence whose Accepted-ZIP SHA-256 binds the exact admitted bytes. Public packaging does not open production Helper/Level 2 |
| M6.2 public packaging batch local gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 `go version` and `make check`; `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, and separate external build/coverage roots | PASS first attempt on both toolchains. Current completed make-contract, fmt/vet, all ordinary/provider/integration tests including releasepack/tool coverage, docs/tidy/verify, lint 0, full race, four fuzz smokes, govulncheck with zero called/imported vulnerabilities, actionlint, and all four cross-build targets. Oldstable reported `go1.25.12 linux/amd64` and completed the full check. No local failure classification or rerun was needed |
| M6.2 binary identity/service-ID RED | official Go 1.26.5 focused releasepack/tool tests after the next five related contracts were added | Intended compile FAIL: `VersionMetadata` lacked the ADR-0009 application/launchd/systemd/Homebrew fields; `GoBuildEvidence`, binary inspection, and platform evidence binding did not exist, so manifest target labels could not be proven against the executable itself |
| M6.2 binary identity/service-ID focused GREEN | official Go 1.26.5 `go test ./internal/docscheck ./internal/releasepack ./internal/tools/releasepack -count=1`; focused lint; actionlint; docs-check/diff-check | PASS: Go build-info inspection rejects non-Go bytes, wrong main package, wrong target, CGO, missing trimpath, dirty VCS, and commit drift; exact archive metadata carries all four frozen ADR-0009 identifiers. Maintained install/uninstall instructions preserve data by default, use exact daemon/service/package identifiers, separate remote Helper removal, and forbid broad path deletion. The Hosted quality path now builds clean four-target preview binaries, packages twice, compares all bytes, verifies checksums, extracts native Linux, version-smokes exact commit, exercises clean-home daemon start/status/stop, and removes only its isolated install root. CI-only license/notice material is explicitly not release evidence |
| M6.2 first public-packaging exact-SHA Hosted classification | implementation commit `3566b457de18d6c1946f821109d6ccba4956f587`, tree `0f6a9acaaa0be2ababeb12d6e39e6c90ca723bca`; push [29629276220](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29629276220); PR [29629277537](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29629277537), both attempt 1 | Retained without rerun. Push native macOS Intel job `88039693242` hit the known exact-stderr-cap 0/65,536 visibility fixture and oldstable macOS Intel `88039693254` hit the known Helper heartbeat visibility fixture; PR native macOS Intel `88039696498` independently hit the same heartbeat fixture under race. Same-SHA companion platforms/phases passed: PR oldstable Intel `88039696556`, both macOS ARM native jobs, and the ordinary phase of PR native Intel. Both quality jobs and every releasepack/tool ordinary/race execution in the affected jobs passed. Classification: **environment/known Helper stderr/heartbeat reader timing fixtures, not introduced code**. No code, assertion, timeout, fixture, workflow, or rerun changed the evidence; skipped compare jobs remain non-evidence |
| M6.2 binary identity/clean-machine batch local gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 `go version` and `make check`; `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, and separate external build/coverage roots | PASS first attempt on both toolchains. Current completed make-contract, fmt/vet, all ordinary/provider/integration tests, docs/tidy/verify, lint 0, full race, four fuzz smokes, govulncheck with zero called/imported vulnerabilities, actionlint including the new Hosted shell, and all four cross-builds. Exact oldstable reported `go1.25.12 linux/amd64` and completed full check. No local failure classification or rerun was needed; exact clean-commit packaging/lifecycle execution follows after commit because dirty VCS is intentionally rejected |
| M6.2 binary identity/clean-machine exact-SHA Hosted gate | commit `958dec07addbe12c0c02404af4baf90558286909`, tree `d567739f32e89ca17ca4d2c3c6502e0579dc4c0d`; push [29629620491](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29629620491); PR [29629622018](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29629622018) | PASS first attempt, 24/24 jobs in both workflows. Quality jobs `88040608096` and `88040611851` both passed the new deterministic double-package/byte-compare/checksum and clean-home version/daemon/uninstall lifecycle step, plus check, lint, fuzz, supply-chain and real integration. Native, oldstable, four build targets, reproducibility pairs, compare, auth, and provenance all passed; no failure classification or rerun was needed |
| M6.2 runtime dependency closure RED/GREEN | four current-Go `CGO_ENABLED=0` release-target builds, `go list -deps`, `go version -m`, exact clean-commit package smoke, and focused releasepack/tool tests | RED first failed to compile without embedded dependency/replacement evidence, then exposed that a single platform-agnostic inventory cannot describe target-specific modules; later RED proved the prior lexical SPDX check accepted `MIT OR` and `MIT Apache-2.0`. The first exact clean-commit smoke correctly rejected a preliminary Linux declaration that retained Darwin-only `github.com/ncruces/go-strftime`; independent target `go list -deps` and binary build-info established that both it and `github.com/mattn/go-isatty` are Darwin-only. GREEN records a 20-module union (Darwin 20, Linux 18), makes every module explicitly target-bound, requires each binary's exact filtered path/version/sum/replacement set, requires canonical h1 sums and parsed SPDX expressions, and emits resolved replacement plus target coverage into deterministic SPDX package records. Missing/extra target, module, version, sum, or replacement evidence fails closed |
| M6.2 runtime license evidence audit | local module-cache license sources plus `go mod verify`; no network or repository mutation by the read-only audit | PASS for the 20 linked modules: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, and the `modernc.org/libc` BSD-3-Clause plus MIT third-party composition are recorded with exact selected module sums. The replaced `github.com/pkg/sftp v1.13.11` has no selected original sum and resolves to the exact TyrantLucifer fork/sum. `modernc.org/libc/LICENSE-3RD-PARTY.md`, `modernc.org/memory` redistribution texts, the repository's absent project LICENSE, and final assembled NOTICE remain explicit release gates; this inventory does not claim to discharge them |
| M6.2 dependency-closure batch local gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, separate external build/coverage roots | PASS first attempt on both toolchains. Current completed make-contract, fmt/vet, all ordinary/provider/integration tests, docs/tidy/verify, lint 0, full race, four fuzz smokes, govulncheck with zero called/imported vulnerabilities, actionlint, and four-target builds. Exact oldstable completed the full check and both module graphs verified. No failure classification or rerun was needed |
| M6.2 dependency-closure exact-SHA Hosted gate | commit `99db0e822b80d01519191e9bd13a894794c018c2`, tree `05beb8459b9f2c158c090f91747f833a21d06f17`; push [29630238252](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29630238252); PR [29630239460](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29630239460) | PASS first attempt, 24/24 jobs in each workflow. Push quality `88042305231` and PR quality `88042308046` both passed target-aware runtime inventory, replacement/SPDX validation, deterministic double packaging and clean-home lifecycle; native, oldstable, four builds, reproducibility pairs, compare, auth and provenance all passed. No failure, rerun, or instability classification was needed |
| M6.2 installed man/completion and upgrade/rollback RED | official Go 1.26.5 focused releasepack/tool/docscheck tests plus actionlint after adding five related lifecycle contracts | Intended compile FAIL: release materials and manifest lacked `Man`, so no archive could contain the generated man page. The first workflow-policy run also intentionally failed because the historical upgrade gate required full checkout history while exact checkout policy allowed only the default shallow form |
| M6.2 installed man/completion and upgrade/rollback focused GREEN | official Go 1.26.5 `go test ./internal/releasepack ./internal/tools/releasepack ./internal/docscheck ./internal/app -count=1`; scoped lint; actionlint; docs-check/diff-check; trusted-root manual lifecycle under `/var/lib/amsftp-tests/1001` | PASS: every archive adds canonical 0755 `share/`, `share/man/`, `share/man/man1/` and 0644 `amsftp.1`; the quality harness installs binary/man and binary-generated bash/zsh/fish completions into exact prefix paths. Only the quality checkout may request exact `fetch-depth: 0`; any other checkout mapping or nonzero depth remains rejected. Frozen Stage 5 `312bccc` creates schema v3, current upgrades to v4 and lists Jobs, old daemon on v4 runs degraded while current Job CLI reports stable unavailability, DB SHA-256 remains unchanged, current then restarts successfully, and exact-file uninstall preserves DB plus extracted archive. The old daemon is terminated by signal because Stage 5 does not implement the current shutdown RPC |
| M6.2 installed lifecycle batch local gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, separate build/cache roots | PASS first attempt on both toolchains. Current completed make-contract, fmt/vet, ordinary/provider/integration tests, docs/tidy/verify, lint 0, full race, four fuzz smokes, govulncheck, actionlint and four-target builds. Oldstable completed full check and module verification. No local failure classification or rerun was needed; exact clean-commit packaging/lifecycle and Hosted execution remain pending until this batch is committed |
| M6.2 installed lifecycle exact clean-commit smoke | implementation commit `0fdcb9cbcc734e9674d2455ec8123eacca722498`, tree `5f7bf0b208f6fce1e01252dc730d5c5014884f68`; current Go 1.26.5, `GOTOOLCHAIN=local`, `umask 0022`, trusted root `/var/lib/amsftp-tests/1001` | PASS first attempt. Four clean-VCS target binaries packaged twice byte-identically; seven outer artifacts and every checksum passed. Extracted Linux amd64 installed exact binary/man/bash/zsh/fish paths and completion signatures. Frozen Stage 5 created v3, current upgraded to v4, old rollback exposed stable degraded Job error without changing DB SHA-256, current recovered, and exact uninstall retained DB plus extracted archive. Hosted execution remains pending |
| M6.2 installed lifecycle first Hosted classification | evidence commit `bcd230bd35ef93e10dd6d4a31192619b231913c5`, tree `7f02a4eb46d81f8c0ff267d7664398387b7b8ec2`; push [29630759384](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29630759384); PR [29630760485](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29630760485), attempt 1 | Both completed failure with 21 success / 2 failure / 1 skipped. Both quality jobs deterministically failed actionlint/ShellCheck SC2034 because the new lifecycle polling loops declared unused `attempt`; the lifecycle step was skipped, so this SHA is not passing evidence. Classification: **introduced CI contract regression**, fixed without changing assertions, deadlines, or security behavior by making the bounded counter part of a C-style loop. The other push failure was the known Helper heartbeat-termination visibility fixture; the other PR failure was the known exact-stderr 0/65,536 visibility fixture. Both had same-SHA ordinary/platform/oldstable companions and are classified **known Helper reader timing instability, not introduced code**. No rerun was requested |
| M6.2 lifecycle shell-lint regression focused GREEN | official Go 1.26.5 actionlint plus actionlint with pinned ShellCheck v0.11.0 container; docs-check and diff-check | PASS: both bounded 200-iteration polls use `for ((attempt = 0; attempt < 200; attempt++))`; ShellCheck now observes the counter, SC2034 is absent, and the polling bound/50 ms sleep/error handling are unchanged |
| M6.2 lifecycle shell-lint correction exact workflow smoke | correction commit `e7e4fc2e83d3d349ac7d75f19ebda737027b8d11`, tree `fe6946253ca2252b3dd03a3d5f34d22295882e81`; exact workflow step extracted directly from `.github/workflows/ci.yml`, current Go 1.26.5, trusted root `/var/lib/amsftp-tests/1001` | PASS first attempt. Both deterministic package outputs matched, all four checksums passed, binary/man/three completions installed, frozen Stage 5 v3 upgraded to v4, old daemon degraded with byte-identical DB, current recovered, and exact uninstall retained state plus extracted bytes. This proves the C-style bounded poll preserves runtime behavior; replacement Hosted evidence remains pending |
| M6.2 lifecycle replacement Hosted classification | evidence commit `f9dd5fe09e857a4d31ad91f308b2de82692a23bf`, tree `82e03ec6093a4c99dacbb5865f08cc50866f952d`; push [29631033394](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631033394); PR [29631034359](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631034359), attempt 1 | Push finished 22 success / 1 failure / 1 skipped; PR finished 21 success / 2 failure / 1 skipped. Both quality jobs passed actionlint/ShellCheck and reached the lifecycle step, proving the loop correction, deterministic package/checksums, installed man/completions, identity and frozen Stage 5 startup. Both then started current in intentionally degraded mode because the new harness placed persistent HOME under `RUNNER_TEMP`, whose writable ancestor is rejected by the production trust boundary; `job list` consequently returned the stable unavailable error. A matching local unsafe-root reproduction exposed `ancestor "/tmp" is writable`, while five consecutive runs under `/var/lib/amsftp-tests/1001` upgraded v3→v4 and listed Jobs. Classification: **introduced CI fixture regression, not a product migration regression and not a flake**. The PR's additional macOS Intel heartbeat-visibility failure had same-SHA push/macOS/oldstable companions and remains known timing instability. No rerun was requested |
| M6.2 lifecycle trusted-root fixture RED/GREEN and exact smoke | fix commit `b7a740010c7e7188f9514e6676a0324aa34433a2`, tree `4c07deeeee85303cd9150c2a91bf64e7c3eba0e6`; official Go 1.26.5 and exact Go 1.25.12 focused docscheck; actionlint; exact workflow step from a clean clone under `/var/lib/amsftp-tests/1001` | RED: the new `workflow.ci_quality_lifecycle_root` rule rejected `clean_home="${RUNNER_TEMP}/public-package-home"`. GREEN: the workflow now derives clean HOME from the already prepared `/var/lib/amsftp-tests/$(id -u)` root, and policy rejects both direct runner-temp use and a declared-but-unused trusted root. Current/oldstable docscheck and actionlint pass. Exact clean-commit workflow smoke passes the complete double package/checksum/install, Stage 5 v3→current v4, old degraded byte-identical rollback, current recovery and exact uninstall sequence |
| M6.2 lifecycle trusted-root replacement Hosted classification | evidence commit `d625a677f59e6e21d55516d2533db34c43d516fa`, tree `fe6be539e70853444df97b116fb2462ff0f665d1`; push [29631477902](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631477902); PR [29631479273](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631479273), attempt 1 | Push passed 24/24 first attempt; quality [88045809334](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631477902/job/88045809334) passed actionlint/ShellCheck and the complete deterministic package/install, Stage 5 v3→current v4, old degraded byte-identical rollback, current recovery and exact uninstall lifecycle. PR finished 22 success / 1 failure / 1 skipped: only quality [88045813455](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631479273/job/88045813455) failed because the initial Stage 5 daemon did not become status-visible within the bounded 200×50 ms poll; actionlint, packaging, install and identity had already passed. Classification: **runner/process readiness timing instability, not introduced code**, supported by the same-SHA push's complete lifecycle and every other PR job passing. No rerun or file change was made |
| M6.2 public Helper install/upgrade RED | official Go 1.26.5 focused `go test ./internal/app -count=1` after adding four related public-surface slices | Intended FAIL: `install` and `upgrade` were unknown public Helper commands and bash/zsh/fish completion lacked both lifecycle verbs and the explicit shared-session/stable-home consent flag |
| M6.2 public Helper install/upgrade focused GREEN | official Go 1.26.5 `go test ./internal/app -count=1`; scoped vet/lint; docs-check/diff-check | PASS: public management parser accepts only status/install/upgrade; install/upgrade require literal `--accept-shared-session-stable-home` and reject duplicates, option-shaped hosts, arbitrary manifest/artifact/path inputs and other management arguments before runtime. Restricted serve remains the separate private stdio role and is absent from completion. While protected distribution trust is closed, human and JSON lifecycle requests return stable configuration exit 3 before connector/RPC/SSH, with no remote probe or write. Help, man and bash/zsh/fish completion share the same facts |
| M6.2 public Helper install/upgrade batch local gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, separate external build/coverage/cache roots | PASS first completed attempt on both toolchains. Current completed make-contract, fmt/vet, all tests, docs/tidy/verify, lint 0, full race, four fuzz smokes, govulncheck/actionlint and four cross-builds. Oldstable reported `go1.25.12 linux/amd64` and completed full check plus module verification. Exact clean CLI and Hosted evidence follow after commit |
| M6.2 public Helper install/upgrade exact clean CLI smoke | implementation commit `49f39401c1693a92917a77efae1ab0a41175237b`, tree `3a0e068088252a77d526960ddd7bb2a0360f982f`; official Go 1.26.5; fresh trusted HOME under `/var/lib/amsftp-tests/1001` | PASS first attempt. Real built binary help and bash/zsh/fish completion contain install, upgrade and exact consent. Install/upgrade in human and JSON modes each return exit 3 with closed-distribution text; JSON is `output_version:1`/`configuration`. Arbitrary `--manifest` returns usage exit 2. The fresh HOME remains byte-empty: no config, state, cache, runtime, daemon socket, RPC/SSH probe or metadata write occurred. Hosted classification follows below |
| M6.2 public Helper install/upgrade Hosted classification | evidence commit `2f39983ebb693b96fb9c97f81580e1d0b6521bb0`, tree `b1db00bd167615980c5abaaf18583fd7f5652821`; push [29631757615](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631757615), PR [29631758514](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631758514), attempt 1 | Push quality [88046698166](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29631757615/job/88046698166) passed all app/docs/lint/fuzz/supply-chain checks, Helper help/man/bash/zsh/fish contracts, and the complete deterministic package, v3→v4, old degraded byte-identical rollback, current recovery and exact uninstall lifecycle. PR quality passed the same public contracts and initial upgrade, then only the second Stage 5 rollback readiness poll exceeded 200×50 ms; same-SHA push completed it. Push auth readiness, Ubuntu PTY contiguous-render observation, and macOS heartbeat client-failure visibility also missed existing bounds; their exact PR companions all passed. Classification: **four known runner/process readiness, PTY refresh, or heartbeat visibility timing instabilities; no introduced regression and no infrastructure failure**. No rerun, assertion, timeout, fixture, or file change was made |
| M6.2 immutable Homebrew formula RED | official Go 1.26.5 focused `TestHomebrewFormula` contract batch | Intended compile FAIL: `HomebrewFormulaRequest` and `BuildHomebrewFormula` did not exist, so no code bound the four platform/architecture release URLs and hashes, install layout, completion generation, version smoke, input rejection, or Helper-trust exclusion |
| M6.2 immutable Homebrew formula focused GREEN | implementation commit `da59438421655160ae463799b9a6dc763f2268a6`, tree `c8091efe9fdfd51c4af50007da504f8930a034cd`; official Go 1.26.5 and exact Go 1.25.12 | PASS: deterministic `Amsftp` formula maps darwin/linux arm64/amd64 to one canonical immutable GitHub Release URL and exact archive SHA-256 each, installs binary/man and generates bash/zsh/fish completions through the public CLI, and freezes `--version` smoke. It rejects prerelease, missing/compound license, missing/duplicate target, wrong name, and empty bytes; the generated formula also passes the available Ruby 2.5.5 syntax checker. No service or Helper trust path is rendered; publication and protected final-byte admission remain absent |
| M6.2 immutable Homebrew formula local batch gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; focused race/vet; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent root and separate external build/coverage/cache roots | PASS first attempt. Current completed full test/docs/tidy/verify/lint 0/race/fuzz/govulncheck/actionlint/four cross-builds; oldstable completed full check and module verification. No `brew` executable is present locally, so this is renderer/contract evidence, not public Homebrew clean-install evidence; the latter remains a protected final-release gate |
| M6.2 immutable Homebrew formula Hosted classification | evidence commit `4830bbecea709a356aa66044e3f77e12dc215395`, tree `cd3a6ac136bb34ed1970d41c0cdabe9084100328`; push [29632304606](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29632304606), PR [29632305697](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29632305697), attempt 1 | PR passed 24/24 including quality [88048292490](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29632305697/job/88048292490), full package/lifecycle/integration/provenance, and compare. Push passed Homebrew/releasepack tests at 83.2% coverage, lint, all fuzz smokes, supply-chain, four native, four oldstable, auth, four builds, eight reproducibility producers and reproducibility-compare; its only failure was quality [88048289365](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29632304606/job/88048289365) after those passes because the first Stage 5 daemon did not become status-visible within 200×50 ms. The same-SHA PR completed that exact lifecycle. Classification: **known runner/process readiness timing instability, not introduced code or contract regression**. No rerun or file change was made |
| M6.2 public Helper disable/remove RED | official Go 1.26.5 focused app/public-contract tests | Intended FAIL: both verbs were unknown usage errors and bash/zsh/fish completion did not expose them, so neither consent nor pre-runtime closure could be proven |
| M6.2 public Helper disable/remove focused GREEN | implementation commit `38356f0ee613206cab79931a9abd577241e34245`, tree `f96d0e68a581b95ade8373a421eefa1623638da6`; official Go 1.26.5 and exact Go 1.25.12 focused app; focused race/vet/lint/docs | PASS: disable is host-scoped and rejects the unrelated stable-home consent; exact remove requires literal `--accept-shared-session-stable-home`; both reject arbitrary artifact/path input and duplicate/wrong consent before runtime. With lifecycle composition absent, human/JSON return stable configuration exit 3 before runtime paths, connector, RPC, SSH, probe, state mutation, write, or delete. Help, committed man, bash, zsh, fish, and user CLI docs share the same facts. This exposes a fail-closed management contract only; no disable/removal execution or Level 0 lifecycle completion is claimed |
| M6.2 public Helper disable/remove local batch gate and environment classification | current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent root, external build/coverage/cache roots | The first current run passed all ordinary tests/docs/lint/full race, then a reused GOCACHE presented 163 accumulated `FuzzEnvelopeDecode` baseline cases and exceeded the unchanged one-second smoke deadline. `internal/ipc` was untouched. A fresh cache began with the canonical six cases, passed the isolated fuzz command, and then passed complete current `make ci`; no timeout, assertion, corpus, or code changed. Classification: **local accumulated-cache/environment instability, not an introduced regression**. Exact oldstable full check and module verification passed first attempt |
| M6.2 public Helper disable/remove exact-SHA Hosted classification | evidence commit `9c2c19185ede624d5d7905614bb0bc6112b90a19`, tree `9df2d461844a7a60b7c78af429322a3624f0c25a`; push [29632738902](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29632738902); PR [29632740292](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29632740292), attempt 1 | Both quality jobs passed `make check`, app/docs public command contracts, lint 0, fuzz, supply-chain and the complete deterministic package/install, Stage 5 v3→v4, old degraded byte-identical rollback, current recovery and exact uninstall lifecycle. Push macOS ARM failed the existing heartbeat-failure visibility and asynchronous PTY selection fixtures; exact same-SHA PR ARM passed. PR macOS Intel failed exact-stderr/heartbeat visibility; exact same-SHA push Intel passed. Classification: **known process-reader/PTY timing instability, no introduced code or contract regression**. Each workflow finished 22 success / 1 failure / 1 dependency-skipped compare; no rerun, timeout/assertion change, or file change was made |
| M6.2 trusted Helper release source RED | official Go 1.26.5 focused `TestProductionReleaseAssets` / `TestReleaseSource` batch | Intended compile FAIL: `ReleaseAssets`, `ProductionReleaseAssets`, and the release source did not exist, so no code froze archive/manifest/signature identities, admitted metadata before archive bytes, safely extracted the canonical binary, or composed an Installer request |
| M6.2 trusted Helper release source review BLOCK and correction | read-only supply-chain review of the working implementation, followed by focused RED/GREEN | Initial review correctly BLOCKED three release-contract violations: archive bytes were read before preliminary consent/probe/high-water; the binary used `make([]byte, manifest.Size)` instead of streaming; and the expanded limit was post-hoc after `tar.Next`, allowing hidden extension records to consume unbounded decompressed input. Correction made `Resolve` metadata-only, changed `ArtifactOpener` to carry Installer context, deferred every archive read until the frozen install order reaches the artifact step, streamed into an exact immediately unlinked 0600 file, and placed a hard `io.LimitedReader` before `tar.Reader`. A fourth negative requires USTAR and rejects GNU encoding. No timeout, assertion, or production trust was weakened |
| M6.2 trusted Helper release source focused GREEN and re-review | implementation commit `53fceb0bf672607ba52d47550a65b095f3e0c76e`, tree `e340b11f90aaf630fe981252665561b09683280d`; current Go 1.26.5 full Helper package/vet and focused `-count=20`; exact Go 1.25.12 Helper package; current focused race `-count=5`; read-only re-review | PASS. Exact four version/target URLs and sidecar names reject noncanonical versions/targets. Manifest/signature length, signature, revoke/deny/floor/client and requested identity all pass before archive access; invalid metadata and declined preliminary consent record zero archive reads. Official GitHub redirects are HTTPS/CDN/chain bounded. Each of the two drift-bound validations and the final upload independently streams an exact USTAR archive through a 0600 unlinked file, with compressed/expanded limits, strict entry order/path/type/mode/owner/format, no symlink/duplicate/extra/trailing member/data, and expected+1/size/SHA-256 binding. Bind and Installer repeat current policy. Re-review result: `APPROVE`; production verifier/policy remain empty and public lifecycle remains runtime-closed |
| M6.2 trusted Helper release source first full-gate lint classification | exact current Go 1.26.5 `make ci`, fresh external cache/build/coverage roots | Ordinary/provider/integration tests, docs/tidy/module verification and Helper coverage passed, then lint deterministically reported nine new issues: two gzip-copy G110 findings lacked adjacent hard-limit justification; three redirect errors were capitalized; deprecated `Header.Xattrs` was read despite USTAR+empty PAX already excluding it; and test code had one simplified selector plus three constant parameters. Classification: **introduced lint/contract regression, not instability**. The hard compressed/expanded limits remained unchanged. Corrections added precise gosec justification, lowercase errors, removed the redundant deprecated field, simplified the selector and removed constant parameters; no timeout or security assertion changed |
| M6.2 trusted Helper release source final local batch gate | correction/evidence commit `d64ee1a5477c42ee4d9a8112e0172fa846a0d4ef`, tree `82ccbf9993a0a98329e06a7cebb182320fc49e46`; exact current Go 1.26.5 full `make ci`; exact Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent root, separate fresh build/coverage/cache roots | PASS. A first correction rerun found one final constant test parameter and was still classified as the same introduced lint regression; after removing it, full lint reported 0 issues. Final current completed all ordinary/provider/integration tests, docs/tidy/verify, full race, four canonical fresh-cache fuzz smokes, govulncheck with zero called/imported vulnerabilities, actionlint and all four cross-builds. Final oldstable completed the full check and both module verifications. A separate preflight without the required `umask 0022` made only existing cachefs wrong-mode/public-mode negative fixtures private under interactive `077`; it is classified known local environment mismatch and did not affect either canonical gate. No production code or assertion was changed for that preflight |
| M6.2 trusted Helper release source exact-SHA Hosted classification | evidence SHA `6f6a1a5ca869bf59fa513965d4b19ea8ab099e85`, tree `a70437ff8e00eda4703e2e83a33251f09df33f29`; push [29633884400](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29633884400); PR [29633885229](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29633885229), attempt 1 | Push completed 24/24 success, including quality, current/oldstable ARM+Intel macOS, both Linux versions, exact installed lifecycle, four builds and reproducibility/compare. PR completed 21 success / 2 failure / 1 dependency-skipped compare: current ARM passed the new Helper package and every other package, then the existing Stage 2 PTY harness sent delete before the asynchronously refreshed selection became available; oldstable ARM printed the Stage 3 foreground-PTY proof as passed, then runner cleanup `killpg(SIGTERM)` returned `EPERM`. Exact same-SHA push current and oldstable ARM jobs both passed, and PR quality/Intel/Linux/build/reproducibility all passed. Classification: **known runner PTY/process-refresh/cleanup timing instability, no introduced release-source or contract regression**. No rerun, timeout/assertion weakening, or file change was made |
| M6.2 trusted Helper release source final-docs Hosted classification | final evidence SHA `ee079c132c1f399c6143816833e6f9a8264eccd4`, tree `2e5f7236b59a1a879351b34893fb36c7a5ba3c5d`; push [29634169086](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29634169086); PR [29634171013](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29634171013), attempt 1 | Push finished 21 success / 2 failure / 1 dependency-skipped compare; PR finished 22 success / 1 failure / 1 dependency-skipped compare. Both quality jobs, Linux current/oldstable, four builds and reproducibility producers passed. Push macOS ARM/Intel failures were only the existing Helper heartbeat client-failure visibility race; exact same-SHA PR ARM/Intel companions passed. PR oldstable macOS Intel alone observed zero bytes in the existing exact-stderr visibility fixture; exact same-SHA push companion passed. Classification: **known Helper process-reader visibility timing instability, no introduced code, documentation, release-source, or contract regression**. No rerun, timeout/assertion weakening, or file change was made |
| M6.2 Helper owner composition RED | official Go 1.26.5 focused helper/daemon/OpenSSH/app test slices added before implementation | Intended compile failures proved the owner surfaces did not exist: no persistent alias registry, no lifecycle manager or recovery entry point, no restricted daemon route, no exact SFTP MKDIR primitive, and no production composition. The RED tests fixed the desired zero-side-effect closed-trust ordering, fresh-process arguments, raw SFTP packet, strict RPC schema/consent, durable removal recovery and remote/auth lifetime before implementation |
| M6.2 Helper owner composition focused GREEN and negative review | official Go 1.26.5 helper/daemon/OpenSSH/app package tests, focused race, vet, scoped/full lint, and `git diff --check` | PASS. A private 0600 registry atomically maps exact Host aliases to random opaque EndpointIDs and reverses them after restart; public/symlink/corrupt/oversize/duplicate state fails closed. The daemon accepts only command, host alias and exact consent, rejecting artifact/manifest/command injection before service dispatch. Lifecycle execution freezes release target/version, keeps one auth attempt across independent fresh SFTP/probe/MKDIR/formal-handshake processes, makes disable local-only, and resumes one exact JobStore removal lease after restart. The first full lint deterministically found eight introduced findings (one intentional permission fixture, one bounded conversion, six capitalized errors); precise test annotations/bounds and lowercase errors corrected them with no security or timeout weakening, after which lint reported 0 issues |
| M6.2 Helper owner composition local batch gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent root `/var/lib/amsftp-tests/1001`, separate external build/coverage/cache roots | PASS. Current completed make-contract, fmt/vet, all ordinary/provider/integration tests, docs/tidy/module verification, lint 0, full race, four fuzz smokes, govulncheck with zero called/imported vulnerabilities, actionlint and all four cross-builds. Exact oldstable completed all package/provider tests, docscheck, tidy diff and both module verifications with exit 0. Helper coverage is 74.2% current/74.1% oldstable and OpenSSH coverage is 48.6%. Production verifier/policy remain empty, public mutation remains closed before connector/RPC/SSH, and daemon consent is deliberately unavailable; these gates prove fail-closed composition, not production Helper distribution. Exact-SHA Hosted classification follows after commit |
| M6.2 Helper owner composition exact-SHA Hosted classification | implementation/evidence SHA `9ee5afaa3290ccd2d6c00b0aee58563caae73844`, tree `68494a03d2cf3abe1ac33149908d6fa9ff0ab8ed`; push [29635275750](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29635275750); PR [29635277754](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29635277754), attempt 1 | Push passed 24/24 first attempt, including quality, auth, current/oldstable Linux and macOS ARM/Intel, four builds, all reproducibility producers and both compares. PR finished 19 success / 4 failure / 1 dependency-skipped compare. Quality passed `make check`, all new owner tests, lint/fuzz/supply-chain and deterministic packaging, then only the initial frozen Stage 5 daemon failed to become status-visible within the unchanged 200×50 ms poll; same-SHA push quality completed the entire upgrade/rollback/uninstall lifecycle. PR oldstable Ubuntu 24.04 saw the existing binding-probe stderr-overflow reader report a short-lived success; native macOS Intel saw 0 rather than 65,536 exact-cap diagnostic bytes; native macOS ARM received a closed handshake pipe before the expected overflow classification. Same-SHA push companions for all three platforms passed, as did every build/reproducibility job. Classification: **known runner/process readiness and Helper process-reader timing instability, no introduced owner-composition or contract regression**. No rerun, timeout/assertion weakening, workflow change, or file change was made |
| M6.2 Helper owner composition final-docs Hosted classification | evidence SHA `0d95560f37154059eb147907a9e0ac65b79d63bb`, tree `1d38c91c384a556092949f09f17c140d61194f29`; push [29635519945](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29635519945); PR [29635521038](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29635521038), attempt 1 | Push passed 24/24. PR finished 21 success / 2 failure / 1 dependency-skipped compare: native macOS Intel and ARM each hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose`, where the process terminated before client-failure visibility; both exact same-SHA push native companions passed their full ordinary/race/native sequence. Classification: **known Helper heartbeat visibility timing instability, no introduced code or documentation regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.2 four-platform native lifecycle RED | official Go 1.26.5 focused docscheck policy contract | Intended FAIL: the native matrix had no clean archive-style install/first-run daemon/Job/exact uninstall step. Additional negative slices proved the initial fragment-only policy still credited conditional execution, reused or non-private HOME, non-isolated state, missing version/status checks, and incomplete uninstall |
| M6.2 four-platform native lifecycle focused GREEN and real Linux smoke | official Go 1.26.5 focused/full docscheck, policy runner, actionlint, and a real built binary under owner-private `/var/lib/amsftp-tests/1001` | PASS. The policy requires the lifecycle immediately after the trusted native build/help/version prefix, with no condition/custom context, and freezes ordered fresh HOME, exact install modes/layout, bash/zsh/fish completion, source↔installed version identity, isolated daemon/Job start/status/stop, Linux/Darwin database paths, exact owned-file removal, and database preservation. The real Linux smoke started/listed/stopped the daemon, removed binary/man/completions, and retained its SQLite database |
| M6.2 four-platform native lifecycle local batch gate | exact current Go 1.26.5 full `make ci`; exact Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent root, and fresh external build/coverage/cache roots under `/tmp/amsftp-m62-native.J8AYeP` | PASS first attempt on both toolchains. Current completed docs/tidy/module verification, lint 0, full race, four fresh-cache fuzz smokes, govulncheck, actionlint and four cross-builds; oldstable reported `go1.25.12 linux/amd64` and completed the full check. A preflight without `GOTOOLCHAIN=local` made the old binary delegate to Go 1.26.5; it was rejected as oldstable evidence and the exact local-toolchain rerun passed |
| M6.2 four-platform native lifecycle exact-SHA Hosted classification | implementation SHA `99905179e1eac23819a8b7f25edffc07c29cf653`, tree `4fc9c7903016f0f061bebd5a0e32d21a4548b6ab`; push [29636325414](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29636325414); PR [29636326555](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29636326555), attempt 1 | PR executed the new lifecycle successfully on Ubuntu 22.04/24.04 and macOS ARM/Intel; push also passed it on Ubuntu 22.04 and macOS ARM. Push finished 21 success / 2 failure / 1 dependency-skipped compare: Ubuntu 24.04 hit existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow` and macOS Intel hit existing heartbeat client-failure visibility during `make test-race`, before lifecycle; their exact PR companions passed full native execution. PR finished 22 success / 1 failure / 1 dependency-skipped compare: oldstable macOS Intel observed 0 rather than 65,536 bytes in the existing exact-stderr-cap fixture; exact push companion passed. Classification: **known Helper process-reader/heartbeat timing instability, no introduced lifecycle or policy regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.2 four-platform lifecycle final-docs Hosted classification | evidence SHA `c0b3936e98547548762f7f7d0f95dd5350d53cc3`, tree `deaf69f69c8e96c96e5914a28b6bb654a5d90f7c`; push [29636659920](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29636659920); PR [29636660987](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29636660987), attempt 1 | PR passed 24/24. Push finished 21 success / 2 failure / 1 dependency-skipped compare: quality's existing `TestDaemonRoleServesLocalProviderAndStopsCleanly` observed `use of closed network connection` during daemon-stop visibility, and native macOS ARM printed the Stage 3 PTY proof before cleanup `os.killpg(SIGTERM)` returned `EPERM`. Both exact same-SHA PR companions passed. Classification: **known daemon shutdown visibility and PTY cleanup timing instability, no code or documentation regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.2 preview-bundle SSH/TUI handoff RED | official Go 1.26.5 focused provenance-policy tests after freezing the new producer/consumer profile | Intended FAIL: quality had no SHA-bound preview-bundle upload, auth had no `needs: quality`, no pinned download/checksum/extraction step, and both real authentication matrices still consumed a fresh loose `go build` binary. Negative slices also reject action-SHA drift, artifact-name drift, removed checksum/commit identity, and fallback to the loose binary |
| M6.2 preview-bundle SSH/TUI handoff focused GREEN | official Go 1.26.5 focused/full docscheck, docscheck runner, actionlint, and `git diff --check` | PASS. Quality uploads only its already checksum-verified seven-file public-preview bundle under `public-preview-bundle-${{ github.sha }}`. The dependent auth job downloads that exact same-run artifact through the pinned action, requires exactly seven top-level files, verifies all archive checksums, extracts only the canonical Linux AMD64 archive, binds binary and `VERSION.json` to `GITHUB_SHA`, and feeds that extracted binary to both the OpenSSH/TUI recovery and MIT Kerberos/GSSAPI matrices. This is a public-preview artifact handoff rehearsal, not a GitHub Release or Homebrew publication |
| M6.2 preview-bundle SSH/TUI handoff local batch gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent root, and fresh external roots under `/tmp/amsftp-m62-channel.zdBRCc` | After the first full current attempt deterministically stopped at `fmt-check` on the newly edited policy file, gofmt corrected the introduced formatting regression; the focused suite passed and the full gate was restarted from the beginning. The final current run passed ordinary/provider/integration tests, docs/tidy/verify, docscheck 85.3%, lint 0, full race, four fresh-cache fuzz smokes, govulncheck, actionlint and four builds. Exact oldstable reported `go1.25.12 linux/amd64` and passed the complete check and module verification. No test, timeout, or policy assertion was weakened |
| M6.2 preview-bundle SSH/TUI handoff exact-SHA Hosted classification | implementation SHA `e61b28441996bc50846d45c2833774748f323e71`, tree `5d200a0ddb360b6667fc688a765514a332000e12`; push [29637272062](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29637272062); PR [29637273319](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29637273319), attempt 1 | Both quality jobs built, verified, lifecycle-tested and uploaded the bundle; both dependent auth jobs downloaded it, passed the exact seven-file/checksum/binary/VERSION identity step, and completed real OpenSSH authentication, fresh-user Stage 1 SSH/TUI recovery, and MIT Kerberos/GSSAPI. PR passed 24/24. Push finished 22 success / 1 failure / 1 dependency-skipped compare: native macOS Intel alone observed 0 instead of 65,536 bytes in existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap`; exact same-SHA PR macOS Intel passed. Classification: **known Helper process-reader visibility timing instability, no introduced artifact-handoff/auth regression**. No rerun, assertion, timeout, or file change was made |
| M6.2 preview-handoff final-docs Hosted classification | evidence SHA `d55b50f5593c744b4dcd1ede1eb662fcc0d5601b`, tree `226aec7a1627d7d141d376633f9fd793bb48cffc`; push [29637684172](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29637684172); PR [29637685174](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29637685174), attempt 1 | Push finished 21 success / 1 failure / 2 dependency-skipped jobs: quality alone timed out waiting for the existing Stage 3 temporary-sshd PTY `shell exited; pane refreshed` observation, while exact-SHA PR quality passed. PR finished 22 success / 1 failure / 1 dependency-skipped compare: native macOS Intel hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose`, while exact-SHA push native macOS Intel passed. The PR archive-auth job passed download/identity/OpenSSH/TUI/Kerberos. Classification: **known PTY refresh and Helper heartbeat visibility timing instability, no documentation, archive-handoff, or authentication regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.2 no-argument picker RED | exact current Go 1.26.5 focused `TestHostedStage1RecoveryExercisesNoArgumentPicker` | Intended FAIL: the frozen recovery harness contained no `fresh_picker = PtyApp([])` path, so it could not prove a no-configuration picker screen, SSH host selection, archive-binary connection marker, or saved-workspace reopen |
| M6.2 no-argument picker focused GREEN | exact current Go 1.26.5 focused/full integration tests; Python compile; shell syntax; `git diff --check` | PASS. The root harness creates a third public-key-only sshd with a root-owned chroot marker and verifies that fixture first through direct batch SFTP. A fresh user launches the archive binary with no arguments, observes all SSH aliases, selects `recovery-picker`, and sees the remote marker. It then saves the existing two-remote workspace, launches with no arguments again, filters the picker, selects the actual workspace row rather than the manual-host row, and sees both remote markers. The original explicit-location control remains first in the Hosted sequence |
| M6.2 no-argument picker local batch gate | exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, fresh external build/coverage/cache roots under `/tmp/amsftp-m62-picker.U90col` | PASS. Current completed make-contract, fmt/vet, ordinary/provider/integration/docs/tidy/verify, lint 0, full race, four fuzz smokes, govulncheck, actionlint, and four builds. Oldstable reported `go1.25.12 linux/amd64` and completed the full check and tools module verification. A local root/OpenSSH attempt was not counted as product evidence because this host failed the unchanged explicit-location control with `transport_interrupted` after direct SFTP preflight; the Hosted auth matrix remained the real composition gate. All fixed users, install paths, state, and chroot fixtures were cleaned |
| M6.2 no-argument picker exact-SHA Hosted classification | implementation SHA `fbddcaa0208d2fca5c285162c5c9ffbf7fa8d318`, tree `ad8df693973f93f5604cc56996c2f9786019eef2`; push [29638717696](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29638717696); PR [29638718437](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29638718437), attempt 1 | Both quality jobs built, verified, lifecycle-tested and uploaded their same-run bundles. Both auth jobs downloaded and verified the exact artifact, passed the unchanged explicit-location control, the new no-argument SSH-host and saved-workspace picker paths, and MIT Kerberos/GSSAPI, then uploaded provenance. PR passed 24/24. Push finished 22 success / 1 failure / 1 dependency-skipped compare: oldstable macOS Intel alone hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose`; the integration package passed there and the exact same-SHA PR oldstable macOS Intel companion passed. Classification: **known Helper heartbeat visibility timing instability, no introduced picker, archive-handoff, OpenSSH, or Kerberos regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.2 no-argument picker final-docs Hosted classification | evidence SHA `85eb5fdcfdbe807aa6f6c32f65db058841746b46`, tree `643f8c0947a996870a805791cd66c4efb2bd90c4`; push [29639156825](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29639156825); PR [29639157994](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29639157994), attempt 1 | PR passed 24/24. Push finished 22 success / 1 failure / 1 dependency-skipped compare: native macOS ARM alone hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose`; the exact same-SHA PR native ARM companion passed. Push quality/auth and every new picker/archive path passed. Classification: **known Helper heartbeat/process-reader timing instability, no documentation or picker regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.2 pinned-cache recovery RED | exact current Go 1.26.5 focused tests in `internal/integration/pinned-cache-lifecycle`, `internal/integration/pinned-cache-failure`, and `internal/docscheck` | Four intended compile failures froze the missing contracts: production-owner seed/verify, tampered evidence rejection, implicit failed-attempt refusal plus explicit resume/verified-backup reuse, and ordered CI composition. A preliminary shell probe where `go` was absent from `PATH` was environment discovery and is not counted as RED |
| M6.2 pinned-cache recovery focused GREEN | exact current Go 1.26.5 and exact oldstable Go 1.25.12 focused package tests; current scoped lint/docscheck; frozen Stage 5 source build | PASS. The lifecycle helper publishes through `cachemanager.PublishComplete`, records a no-replace 0600 exact entry/blob/content identity, and verifies through `ResolvePinnedOffline` plus production blob reads. The failure helper validates exact v3→v4 heads/digest, creates a production verified backup, and persists a fixed failed attempt. Implicit initialization returns explicit-resume-required; explicit resume reaches v4 and reuses the same backup. CI policy rejects missing/reordered seed, failure, resume, and post-rollback verification. The same helper source builds against frozen Stage 5, proving the seed is written by historical production code |
| M6.2 pinned-cache trusted-root lifecycle | frozen baseline `312bcccbcbd54246bbe5ff9babf4f14560449176`, current implementation `925b22eb6a743816abcd11d1e552a7af1ce2992a`, trusted root `/var/lib/amsftp-tests/1001` | PASS. Frozen Stage 5 creates schema v3, seeds and verifies the exact pinned entry; current verifies it before migration; a controlled persisted failed attempt and verified backup are installed; ordinary current daemon serves degraded and rejects Job API with `durable transfer service is unavailable` without changing database SHA-256; `--resume-migration` reaches v4; current verifies again; frozen Stage 5 rollback remains degraded/read-only with byte-identical database; pinned entry/blob identity and bytes survive rollback and the final current restart. This is deliberately a controlled persisted-failure boundary rehearsal, not a claim that a v4 SQL statement naturally failed; statement-level rollback remains covered by the migration runner tests |
| M6.2 pinned-cache local batch gate | implementation/evidence SHA `925b22eb6a743816abcd11d1e552a7af1ce2992a`, tree `540681e14deec18b6d86a3d74f738b129305db18`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent root, fresh external roots | PASS. Current completed make-contract, fmt/vet, all tests/docs/tidy/module verification, lint 0, full race including both new integration packages, four fuzz smokes, govulncheck with no called vulnerabilities, actionlint, and four builds. Oldstable completed all package/provider/docs/tidy/module checks. The first canonical current attempt reached lint and found introduced error-wrapping plus signed-conversion findings; exact `%w`/`errors.Join` and validated frozen heads corrected them without weakening tests. A preflight under interactive `umask 077` failed only existing permission-negative fixtures and is excluded as environment mismatch |
| M6.2 pinned-cache exact-SHA Hosted classification | implementation SHA `925b22eb6a743816abcd11d1e552a7af1ce2992a`, tree `540681e14deec18b6d86a3d74f738b129305db18`; push [29639963220](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29639963220); PR [29639964534](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29639964534), attempt 1 | Both quality jobs passed the new frozen/current lifecycle, controlled failure boundary, explicit resume, read-only rollback, and exact pinned-cache revalidation. Both auth jobs, all oldstable jobs, four builds, reproducibility producers/compare, and provenance passed. Push finished 21 success / 2 failure / 1 dependency-skipped: native macOS ARM hit existing Helper heartbeat client-failure visibility in race; native macOS Intel hit existing remote-command cancellation timing in ordinary tests. PR finished 22 success / 1 failure / 1 dependency-skipped: native macOS ARM hit the same Helper heartbeat fixture, while Intel passed and is the exact-SHA companion for the push-only cancellation failure. The new integration packages passed on the failing runners. Classification: **known Helper/command process timing instability, no introduced cache, migration, rollback, or workflow-policy regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.2 pinned-cache evidence-sync Hosted classification | documentation SHA `eb67f5553aaf39e3952969083254fdd1d57cb376`, tree `73bcea2faf97f93c072aec93ecc94aa0bcd40217`; push [29640392949](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29640392949); PR [29640394144](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29640394144), attempt 1 | Push finished 19 success / 3 failure / 2 dependency-skipped: quality stopped only because the frozen Stage 5 daemon did not become status-visible within the existing readiness poll; native macOS ARM and Intel hit the existing Helper heartbeat client-failure visibility race. Exact-SHA PR quality completed the full pinned-cache lifecycle. PR finished 22 success / 1 failure / 1 dependency-skipped: native macOS Intel hit existing exact-stderr-cap visibility and observed 0 instead of 65,536 bytes, while the push Intel companion passed ordinary tests before its separate heartbeat race. Both workflows passed auth, oldstable, builds, reproducibility, and the new integration packages. Classification: **known Stage 5 readiness and Helper process-reader timing instability, no documentation or pinned-cache regression**. No rerun or gate change was made |
| M6.2 reviewed third-party NOTICE RED | exact current Go 1.26.5 focused `internal/releasepack`, `internal/tools/releasenotice`, `internal/docscheck`, and Make-contract tests | Intended compile failures first froze the absent deterministic notice model/builder and strict materials CLI; `--check` initially failed at argument parsing; docscheck initially lacked a quality-workflow real-NOTICE policy. After the Make target was added, `make make-contract` correctly rejected its unregistered Go probe shape. These failures preceded the minimum production implementations and contract registration |
| M6.2 reviewed third-party NOTICE focused GREEN | exact current Go 1.26.5 focused tests/vet/lint; `make make-contract docs-check notice-check`; actionlint and `git diff --check` | PASS. The material inventory covers all 20 runtime modules exactly once, preserves declared and resolved replacement identity plus reviewed SPDX, and SHA-256-binds canonical non-symlink sources with hard file/count/output bounds. Deterministic rendering includes every root license plus `modernc.org/libc/LICENSE-3RD-PARTY.md` and `modernc.org/memory`'s `LICENSE-GO`/`LICENSE-MMAP-GO`. Tests reject missing/extra/duplicate modules, identity/version/license/hash/byte drift, unsafe names, symlinks, source tampering, and committed NOTICE drift. The CI policy requires regeneration and packages only the committed byte-identical NOTICE; it does not select the project license |
| M6.2 reviewed third-party NOTICE local batch gate | implementation SHA `197ff6f12135be670df40bd5f0dfe7e8c72fe483`, tree `e1d203edcb820c332a98650cd6256bf6f5872a43`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent root and fresh external roots | PASS first completed attempt on both toolchains. Current completed make-contract, fmt/vet, all ordinary/provider/integration tests, docscheck and notice byte check, both module verifications, lint 0, full race, four fresh-cache fuzz smokes, govulncheck with no called vulnerabilities, actionlint, and four builds. Oldstable reported `go1.25.12 linux/amd64` and completed the full check including the live-module NOTICE verification. A preliminary non-login shell omitted Go from `PATH`; it was environment discovery, not product evidence, and the canonical fixed-toolchain gates then passed without code or test change |
| M6.2 reviewed third-party NOTICE exact-SHA Hosted classification | implementation SHA `197ff6f12135be670df40bd5f0dfe7e8c72fe483`, tree `e1d203edcb820c332a98650cd6256bf6f5872a43`; push [29641186074](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29641186074); PR [29641186949](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29641186949), attempt 1 | Push finished 22 success / 1 failure / 1 dependency-skipped: quality regenerated, byte-compared, packaged and lifecycle-tested the real NOTICE; auth, all oldstable, builds and reproducibility passed. Native macOS Intel alone hit existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap` visibility and observed 0 rather than 65,536 bytes; the exact-SHA PR Intel companion passed. PR finished 21 success / 1 failure / 2 dependency-skipped: quality completed NOTICE generation/comparison, deterministic package creation and archive verification, then the frozen Stage 5 daemon missed the existing readiness poll; exact-SHA push quality completed that lifecycle. Every changed releasepack/releasenotice/docscheck package passed. Classification: **known Stage 5 readiness and Helper reader timing instability, no introduced notice, module-resolution, packaging, or workflow-policy regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.2 NOTICE evidence-sync exact-SHA Hosted classification | documentation SHA `c76498d709b1f1d3d03790f188e93b9da34a0cde`, tree `8163f3e81393d25e47635f01800fce5c7e490360`; push [29641613717](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29641613717); PR [29641614623](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29641614623), attempt 1 | Push finished 19 success / 3 failure / 2 dependency-skipped: oldstable Ubuntu retained temporary part state in the existing Level 2 in-flight-cancel observation race, native macOS ARM missed the existing Stage 2 PTY delete-selection refresh, and quality missed the frozen Stage 5 readiness poll after packaging the real NOTICE. PR finished 22 success / 1 failure / 1 dependency-skipped: oldstable macOS Intel hit the same existing Stage 2 PTY selection-refresh fixture. Same-SHA independent paths passed the changed documentation/NOTICE contracts. Classification: **known Level 2 cancel-observation, PTY refresh, and Stage 5 readiness timing instability, no documentation or NOTICE regression**. No rerun, timeout, assertion, workflow, or file change was made |
| M6.2 local-tap Homebrew preview RED/GREEN | exact current Go 1.26.5 focused `internal/releasepack` and `internal/docscheck` tests | RED froze the absent CI-only local asset server, tap creation, prior-version install, same-SHA upgrade, version, formula test, uninstall and untap ordering. GREEN renders two exact preview formulae, serves only bounded canonical assets from a loopback Go server, creates a temporary local tap, and exercises clean 0.9.0 install followed by 1.0.0 upgrade. The harness-only BSD-3-Clause placeholder is explicitly not the project LICENSE; no public formula, GitHub Release, final protected bytes, production trust, tag, or release is created |
| M6.2 first Homebrew preview Hosted RED | implementation SHA `9ffcb18b2076569b7e8aa0dfc0f66228538e5175`, tree `ac6e061055668c3f556613d2f809cb23d6a6588a`; push [29642145835](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29642145835); PR [29642147182](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29642147182), attempt 1 | Both Hosted macOS ARM/Intel preview jobs deterministically failed because the initial inline Python loopback server never became ready. PR Ubuntu also hit an unrelated known native race. Classification: **introduced preview-server composition defect**. No rerun or gate weakening occurred; the server was replaced by the repository's compiled Go preview server in the next commit |
| M6.2 Go preview-server path RED | implementation SHA `7556a87a1b29765a1d60356a313ce8ffb3a109c6`, tree `cb93eafa02d2b2be46a6c1ccc2bb333538b9401e`; push [29642572830](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29642572830); PR [29642573891](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29642573891), attempt 1 | The Go server became ready, but both macOS architectures deterministically rejected ordinary temporary roots because `/var` resolves through `/private/var`: `homebrew preview asset root must not traverse symlinks`. Classification: **cross-platform root-identity defect**. The subsequent RED requires the caller's final root component to remain a real directory while allowing a resolved parent symlink; request and per-entry identity protections remain fail closed |
| M6.2 resolved-parent correction Hosted RED | implementation SHA `128e8aa12aee3e30cc45b8b0161c5199960a54d5`, tree `8a03622bd10b3513bf898dfb9bee0835cb96e1eb`; push [29642986362](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29642986362); PR [29642987810](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29642987810), attempt 1 | Hosted macOS ARM reached real Homebrew install, 0.9 version, 1.0 upgrade, version and formula test. Ordinary uninstall removed only 1.0.0, leaving 0.9.0 in the Cellar, so untap failed deterministically. Classification: **introduced multi-version uninstall-policy defect**. No rerun occurred; the next RED requires forced all-version uninstall and an empty Cellar assertion before untap |
| M6.2 forced-uninstall correction Hosted RED | implementation SHA `d19dd1757194eda77672740a75e448ff5651dcc8`, tree `ab9dea6b1441ac7d744649ed758622fd07d794f7`; push [29643384880](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29643384880); PR [29643385899](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29643385899), attempt 1 | Hosted macOS ARM installed/upgraded/tested, forcibly removed both versions, proved the Cellar absent and untapped successfully. The step then failed because expected `SIGTERM` made the server `wait` return 143 under `set -e`. Classification: **introduced expected-shutdown handling defect**. No rerun occurred; the final RED accepts exactly 143 after the explicit kill rather than swallowing arbitrary server failures |
| M6.2 Homebrew preview local batch gate | implementation SHA `ed7dcd46dab669b929a483767a97890776caf4b9`, tree `be1ccc50a0986add61890f799bb6fbb2c4d0a76d`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and fresh external build/coverage caches | PASS. Current completed make-contract, fmt/vet, all ordinary/provider/integration tests, docs/NOTICE/tidy/module verification, lint 0, full race, four fuzz smokes, govulncheck, actionlint, and four builds. Oldstable reported `go1.25.12 linux/amd64` and completed the full check. One earlier oldstable full attempt hit existing same-inode rewrite ctime visibility; the focused test passed 30/30 and the unchanged complete oldstable rerun passed, classified local filesystem timing instability. No assertion, timeout, or product policy was weakened |
| M6.2 Homebrew preview exact-SHA Hosted classification | implementation SHA `ed7dcd46dab669b929a483767a97890776caf4b9`, tree `be1ccc50a0986add61890f799bb6fbb2c4d0a76d`; push [29643786050](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29643786050); PR [29643787252](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29643787252), attempt 1 | Push macOS ARM job `88078472989` and Intel job `88078472963` both installed 0.9.0, verified exact commit/arch identity, upgraded to same-SHA 1.0.0, passed formula test, forcibly removed both versions, proved Cellar cleanup, untapped, and accepted exact server exit 143. PR ARM job `88078476236` completed the same lifecycle. Push finished 22 success / 1 failure / 1 skipped: oldstable Intel alone observed 0 instead of 65,536 bytes in existing exact-stderr-cap visibility. PR finished 20 success / 2 failure / 2 skipped: Intel hit existing heartbeat-failure visibility before Homebrew, while quality missed the frozen Stage 5 daemon readiness poll after full packaging; exact same-SHA push Intel and quality passed. Classification: **Homebrew preview GREEN; residual failures are known Helper reader/heartbeat and Stage 5 readiness timing instability, no introduced path/install/upgrade/uninstall/server-shutdown regression**. No rerun, timeout, assertion, workflow, or file change was made. This evidence is not a public Homebrew channel, project LICENSE selection, final protected bytes, tag, or release |
| M6.2 Homebrew preview evidence-sync exact-SHA Hosted classification | documentation SHA `774a1380afe56dd80c510f49dc8f8c97aa918ee7`, tree `84db52387e2bdbcc29e0a8bcba03561e823b4e9c`; push [29644239170](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29644239170); PR [29644240643](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29644240643), attempt 1 | Push passed 24/24, including both macOS local-tap Homebrew lifecycles, quality, auth and compare. PR finished 22 success / 1 failure / 1 dependency-skipped: native macOS Intel ordinary tests alone hit existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow` and incorrectly observed the unsafe probe succeed; exact same-SHA push Intel passed ordinary/race tests and the full Homebrew lifecycle. Classification: **known Helper stderr-reader visibility timing instability, no documentation or Homebrew regression**. No rerun, timeout, assertion, workflow, or file change was made |
| M6.3 redaction/doctor/support-bundle RED | exact current Go 1.26.5 focused `internal/redaction`, `internal/doctor`, and `internal/supportbundle` tests | Intended compile failures froze the absent sensitivity/export policy, ten required doctor codes plus optional endpoint and safe report model, and support-bundle reviewed layout/preview/consent/archive APIs. After correcting one test-only composite-literal syntax error, the canonical RED failed only on missing symbols/behavior. No implementation claim was made from the malformed preliminary invocation |
| M6.3 foundational focused GREEN | exact current Go 1.26.5 focused redaction/config/diagnostic/doctor/supportbundle tests, race, and vet | PASS. `public/system_metadata/pseudonymous/secret/content` drive one export policy now reused by effective config args and persistent diagnostic tokens. Doctor emits v1 fixed-order stable code/status/severity/detail/remediation, maps absent probes to safe skipped results, maps invalid/error probes to `probe_failed`, and never retains raw causes. Support-bundle preview accepts only the reviewed JSON file/sensitivity pairs, hashes every file, binds explicit consent to one copied snapshot, rejects caller order/duplicate/unknown/wrong-label/invalid JSON/size/post-preview drift, and emits deterministic 0600 USTAR/gzip within hard file/expanded/compressed bounds |
| M6.3 initial batch-gate lint correction | first exact current Go 1.26.5 full `make ci`, then focused lint/test and full restart | All ordinary/provider/integration tests, docs/NOTICE, tidy and module verification passed before lint correctly reported two QF1008 selectors on embedded `gzip.Writer.Header` fields. Classification: **introduced staticcheck defect**. Direct `ModTime`/`OS` field selection fixed it without behavior or contract changes; focused test/lint passed and the complete current gate was restarted from the beginning |
| M6.3 seeded-secret security-review RED/GREEN | exact current Go 1.26.5 focused redaction/supportbundle tests after initial SHA `b501f586c5ce001723f0b18418fc0bc1b05432ff` | Review found that a caller could falsely label JSON as reviewed while embedding an obvious seeded email/path/token string. RED showed both system-metadata and pseudonymous sources were accepted. GREEN adds a shared reviewed-string policy and iterative JSON traversal bounded to depth 32 and 8,192 nodes; keys and values must be stable safe metadata/tokens/remediation or the redaction placeholder, while secret/content/unsafe values fail without echoing raw bytes. The initial SHA's push/PR runs `29644999607`/`29645001911` were automatically cancelled by the final correction under workflow concurrency and are not evidence |
| M6.3 foundation final local batch gate | final implementation SHA `ae172d9bb419bc6b0b05cf3ef3467258496d463f`, tree `fc865f75271cbc50162d867c43f15f9d270a70f9`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and fresh external build/coverage caches | PASS after the security correction. Current completed make-contract, fmt/vet, all ordinary/provider/integration tests, docs/NOTICE/tidy/module verification, lint 0, full race including all three new packages, four fuzz smokes, govulncheck, actionlint and four builds. Oldstable reported `go1.25.12 linux/amd64` and completed the full check. Because the correction tightened a security boundary, both complete gates were rerun despite the batch policy; no assertion, timeout, sensitivity label, size/depth/node bound or existing product behavior was weakened |
| M6.3 foundation exact-SHA Hosted classification | final implementation SHA `ae172d9bb419bc6b0b05cf3ef3467258496d463f`, tree `fc865f75271cbc50162d867c43f15f9d270a70f9`; push [29645261188](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29645261188); PR [29645262834](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29645262834), attempt 1 | Push passed 24/24: quality, auth, all current/oldstable/native platforms, builds, reproducibility/compare and both Homebrew preview lifecycles green. PR finished 22 success / 1 failure / 1 dependency-skipped compare. Native macOS Intel race alone failed existing `TestStage2LocalPTYCopyAndDurableJobsReattachMVP`: delete ran before the asynchronous listing selection became active and reported `delete requires at least one target`; exact same-SHA push Intel passed ordinary/race tests, Homebrew and the full job. `internal/redaction`, `internal/doctor`, and `internal/supportbundle` all passed in the failed PR job. Classification: **known Stage 2 PTY selection-refresh timing instability, no introduced redaction, doctor, support-bundle, config, diagnostic, packaging, or platform regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.3 foundation docs-sync exact-SHA Hosted classification | documentation SHA `20bc338a8a3d550037ad1e37762fd14104680cd0`; push [29645672691](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29645672691); PR [29645673946](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29645673946), attempt 1 | Push passed 24/24. PR finished 21 success / 2 failure / 1 dependency-skipped compare. Oldstable Ubuntu 24.04 job `88083397013` ordinary `make check` alone failed existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow` after the unsafe binding probe unexpectedly succeeded; exact same-SHA push oldstable Ubuntu 24.04 passed. Native macOS ARM job `88083397028` ordinary `make test` alone failed existing `TestStage2LocalPTYCopyAndDurableJobsReattachMVP` because delete ran before asynchronous selection activation and reported `delete requires at least one target`; exact same-SHA push ARM passed. All new redaction/doctor/support-bundle packages passed in both failed jobs. Classification: **known Helper stderr-reader visibility and Stage 2 PTY selection-refresh timing instability, no documentation or diagnostic-foundation regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.3 doctor/immutable inspection RED | exact current Go 1.26.5 focused `internal/statefs`, `internal/transport/openssh`, and `internal/app` tests | Intended compile failure froze four related absent slices: immutable stopped-database/full compiled-head inspection plus read-only filesystem bytes; bounded `ssh -G -T` inspection and direct endpoint reachability; composed real local probes; and public doctor role/help/man/completion. The canonical RED failed only on missing `InspectDatabase`, `ErrDatabaseActive`, `AvailableFilesystemBytes`, `InspectConfig`, `runDoctorWithRuntime`, `doctorRuntime`, `RoleDoctor`, and related contracts |
| M6.3 doctor security-review RED/GREEN | exact current Go 1.26.5 focused OpenSSH inspection test | Review found the first parser accepted duplicate singleton fields and an unknown `StrictHostKeyChecking` value. The RED failed both `duplicate` and `unknown` because `InspectConfig` returned nil error. GREEN tracks each security-relevant singleton exactly once and accepts only the explicit OpenSSH yes/ask/accept-new/no plus boolean aliases; malformed/ambiguous expansion fails with a generic error that never contains subprocess bytes |
| M6.3 doctor focused GREEN and real smoke | implementation SHA `7aa6f20d1cada28064cfd4f169442ef219a9b1a8`, tree `df6450899ae9316600d38f72f426f3a4c308ce91`; current focused normal/race/vet; isolated private HOME/XDG CLI smoke | PASS. `statefs` race 79.497s, OpenSSH race 2.057s, app race 25.271s; focused vet silent green. An actual built `amsftp doctor --format json --endpoint localhost` under isolated HOME/XDG roots returned the fixed eleven-result v1 report and a byte-for-byte before/after filesystem inventory proved no config/state/cache/runtime creation. Doctor never prepares paths, repairs/migrates a database, starts a daemon, changes Helper state, authenticates, emits SSH payload, invokes configured proxy paths, or uploads. Sidecar-free stopped SQLite uses immutable compiled-head validation; live WAL/SHM reports `database_active`. `ssh -G -T` stdout/stderr are independently capped at 64 KiB; direct endpoint reachability is a five-second no-payload TCP connect; proxy configuration reports `endpoint_proxy_not_probed` |
| M6.3 doctor initial full-gate lint correction | initial implementation SHA `d57b558`; exact current Go 1.26.5 `make ci` | Make contract, fmt, vet, every ordinary/provider/integration test, docs/NOTICE, tidy and module verification passed before lint reported one G101 on the deliberately named test redaction canary. Classification: **introduced test annotation defect, no production behavior failure**. A line-scoped `#nosec G101` explains that the fixed value is a subprocess-output redaction canary rather than a credential; the focused test passed and the full gate restarted from the beginning |
| M6.3 doctor final local batch gate | final implementation SHA `7aa6f20d1cada28064cfd4f169442ef219a9b1a8`, tree `df6450899ae9316600d38f72f426f3a4c308ce91`; exact current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and fresh external build/coverage/cache/tmp | PASS. The first post-fix restart reached ordinary tests with only 126–130 MiB free because this batch's multiple fresh GOCACHE roots consumed `/tmp`; unchanged migration-space tests correctly required 134,332,416 bytes and failed closed. This is classified **local gate-environment exhaustion, not test instability or product regression**. Only the explicitly created disposable batch caches were deleted; no threshold/assertion/timeout/code changed. A fresh complete current gate then passed make-contract, fmt/vet, all ordinary/provider/integration tests, docs/NOTICE/tidy/verify, lint 0, full race, four fuzz smokes, govulncheck with no reachable vulnerability, actionlint, and four builds. After deleting that completed current gate root, exact oldstable completed the full check |
| M6.3 doctor docs-sync exact-SHA Hosted classification | documentation SHA `d345c362ad8b6ea8c67b09a4e351ad27e929965a`, tree `e4a724c7470882a714f4c356c49d1d67477a650d`; push [29646928333](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29646928333); PR [29646929356](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29646929356), attempt 1 | Push finished 22 success / 1 failure / 1 dependency-skipped compare. Native macOS Intel ordinary tests alone hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose`; the process terminated before client-failure visibility. PR finished 21 success / 2 failure / 1 dependency-skipped compare: native Ubuntu 22.04 race hit existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow`, while the exact same-SHA push Ubuntu 22.04 job passed; native macOS Intel race hit the same heartbeat fixture, while its PR ordinary step and the changed doctor/statefs/OpenSSH-inspection/app packages passed. All auth integrations, builds, reproducibility producers, quality checks, and other platforms passed. Classification: **known Helper heartbeat/stderr reader visibility timing instability, no doctor, immutable-state, OpenSSH-inspection, CLI, documentation, packaging, or authentication regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.3 support-bundle CLI/publication RED | exact current Go 1.26.5 focused `internal/supportbundle` and `internal/app` tests | Intended compile failure froze the absent private publisher, command runtime, eight-source composer, public role/dispatch, help/man, and static completion contracts. The canonical RED failed only on missing `Publish`, `supportBundleRuntime`, `supportBundleSnapshot`, `runSupportBundleWithRuntime`, and `composeSupportBundleSources`; the role/dispatch assertions were part of the same frozen test set and compiled after those primary symbols existed |
| M6.3 support-bundle focused GREEN and real smoke | implementation SHA `bfc59c8c0478a7043b8287f52d31dd3477b30554`, tree `d519c6ae90d486733906c8125e5f80ecdb1e9c9e`; exact current Go 1.26.5 focused normal/race/vet/lint; isolated private HOME/XDG real CLI smoke | PASS. Preview/create recompose the same eight reviewed sources and Build refuses post-preview drift. The composer omits raw diagnostic messages, IDs, paths, command argv, Job errors and locations; a seeded email/path/token value was absent from every source and all eight passed the bounded reviewed-JSON gate. Daemon reads are local, read-only, limited to 256 diagnostics and 100 Jobs, tolerate absence/failure with stable empty summaries, and never start the daemon or connect to an endpoint. Publication requires a canonical absolute target under an existing platform-validated owner-private directory, uses `O_EXCL|O_NOFOLLOW`, exact 0600, chunked cancellation checks, file/parent sync, and exact-target cleanup on cancellation or injected sync failure. Existing files and symlinks remain unchanged. Supportbundle race passed in 1.044s, app race in 24.875s, focused vet was silent, and lint reported 0 issues after line-scoped explanations for deliberate permission-negative fixtures. An actual `support-bundle preview --format json` under isolated HOME/XDG returned manifest plus eight files and the before/after filesystem inventory stayed empty. Complete current/oldstable batch gates and exact-SHA Hosted evidence follow after documentation sync |
| M6.3 support-bundle local batch gate | evidence SHA `93dc88efaefeccd78d7ed278c76815b5efbc9679`, tree `77754c8f940d6d58c90a209cea5b43ad9d557dc5`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022` | PASS first attempt on both toolchains. Current completed make-contract, fmt/vet, all ordinary/provider/integration tests, docs/NOTICE/tidy/module verification, lint 0, full race including supportbundle/app, four fuzz smokes, govulncheck with no called vulnerability, actionlint, and all four cross-builds. Oldstable reported `go1.25.12 linux/amd64` and completed make-contract, fmt/vet, all ordinary/provider/integration and provider-contract tests, docs/NOTICE, tidy diff, and both module verifications. Supportbundle coverage was 76.8% on both runs. No assertion, timeout, permission policy, redaction rule, consent rule, or size bound was weakened. Exact-SHA Hosted evidence follows after this evidence sync |
| M6.3 support-bundle exact-SHA Hosted classification | evidence SHA `496356908990d4bb5d9473acad754fd8ae7217ca`, tree `f36264b79f5b49d6bb257cec1438160a948ff33f`; push [29647975703](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29647975703); PR [29647977377](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29647977377), attempt 1 | Both runs completed 22 success / 1 failure / 1 dependency-skipped. Push oldstable Ubuntu 22.04 alone hit existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow` after the unsafe probe unexpectedly succeeded; exact same-SHA PR oldstable Ubuntu 22.04 passed the complete job. PR native macOS ARM alone hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` after process termination preceded client-failure visibility; exact same-SHA push macOS ARM passed. New supportbundle/app tests passed inside both failed jobs. Classification: **known Helper stderr-reader/heartbeat visibility timing instability, no support-bundle, redaction, publication, CLI, docs, packaging, or auth regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.3 semantic-secret/corrupt-state RED | exact current Go 1.26.5 focused diagnostic/app tests | Canonical RED first failed because `supportBundleGatherRuntime` and `gatherSupportBundleSourcesWithRuntime` were absent and because the existing persistent handler emitted the lexically valid `askpass_answer_canary_7f3a` as both component and event. The preliminary non-login shell returned `go: command not found`; it was environment discovery and is not counted as RED evidence. No product code was changed before the fixed-toolchain RED |
| M6.3 semantic-secret/corrupt-state focused GREEN | implementation SHA `ba334b8d8968f5b09c91c0185996994b9a307ff4`, tree `1725549ade259b172f41af66d52dc2319a583fa8`; exact current focused normal/race/vet | PASS. Persistent diagnostics accept only reviewed components/events/error codes. Build/platform/doctor/diagnostic/Job/database/daemon exported strings use domain-specific version patterns, enums or registries instead of lexical safety alone. Eight safe-shaped canaries cover usernames, hostnames, private keys, Askpass answers, Kerberos material, environment values, command arguments and file content; none appears in persistent JSON, any reviewed source, or the fully expanded tar archive. Injected corrupt config/database/cache bytes plus an unhealthy daemon yield only `invalid`, `fail`, `probe_failed` and `unhealthy`; every fixture remains byte-identical. Affected ordinary packages passed, full diagnostic/daemon/app/supportbundle race passed in 1.250s/12.878s/24.865s/1.043s, and focused vet was silent |
| M6.3 semantic-secret/corrupt-state local batch gate | implementation SHA `ba334b8d8968f5b09c91c0185996994b9a307ff4`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures | PASS. Current passed first attempt: make-contract, fmt/vet, all ordinary/provider/integration and provider-contract tests, docs/NOTICE/tidy/module verification, lint 0, full race, four fuzz smokes, govulncheck with zero called/imported vulnerabilities, actionlint and four cross-builds. The first oldstable attempt reached vet then `/tmp` exhausted because the completed current gate's disposable cache used 2.6 GiB; only that exact batch root was deleted. A second attempt put test TMPDIR on `/data00`, whose uid=1000 ancestor was correctly rejected by platform validation and whose directory order differed from the expected fixture. With Go cache kept external but test TMPDIR restored to `/tmp`, unchanged exact Go 1.25.12 reported its version and passed full check. These are **local gate environment/root-selection failures**, not product or test instability; no code, assertion, timeout, permission rule, ordering contract, or security bound changed |
| M6.3 semantic-secret docs-sync exact-SHA Hosted classification | documentation SHA `b5293d7dd12587c1e6c326a31a60ec55b21051ad`, tree `607452b25395f92a559fe7499119b7b2331f1cbf`; push [29648932835](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29648932835); PR [29648934061](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29648934061), attempt 1 | PASS 24/24 on both runs. Quality, real authentication, current/oldstable/native platforms, four builds, reproducibility producers/compare, provenance, and both Homebrew preview lifecycles all completed successfully. This is exact evidence for the semantic diagnostic/export registries, safe-shaped persistent-log and expanded-archive corpus scan, corrupt-state byte preservation, and their documentation; it is not production Helper artifact evidence or final release evidence |
| M6.3 compatibility/troubleshooting/security exact-SHA Hosted classification | implementation SHA `6476bfc8b80e1aeefb10bc02cbc30eb6740e35a6`, tree `eb0a5df41cc7445b9a96fb14e055ff623dc8c4d3`; evidence SHA `6653a617045b1ed0c03c5b297dbcbd38db27116d`, tree `070079d62ed710266a48d7883adf7fc2fd12df32`; push [29649710398](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29649710398); PR [29649711563](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29649711563), attempt 1 | PR passed 24/24. Push finished 21 success / 2 failure / 1 dependency-skipped compare: Ubuntu 22.04 native race job `88093822588` hit existing `TestTransferSchedulerAccumulatesLowRateTokensUpToOneBoundedQuantum` after its one-second grant window elapsed; macOS Intel native race job `88093822606` hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` after process termination preceded client-failure visibility. Exact same-SHA PR companions on both platforms passed, and the new compatibility/troubleshooting/docscheck packages passed inside both failed push jobs. Classification: **known scheduler and Helper-heartbeat timing instability, no compatibility matrix, troubleshooting map, threat-model, finding-ledger, documentation-contract, or permission regression**. No rerun, assertion, timeout, workflow, or file change was made |
| M6.3 real-auth support-bundle artifact-scan RED/GREEN and initial local batch gate | implementation SHA `403a8fb123942225fa2e0e7f29a8a74b68a0d7bd`, tree `d6dda93169fd6cf6e0135c8a3587c9485089b5cd`; exact current Go 1.26.5 focused checks and full `make ci`; exact oldstable Go 1.25.12 full `make check`; `GOTOOLCHAIN=local`, `umask 0022` | Canonical RED failed only because the shared scanner did not exist. GREEN runs public preview/create after the complete real OpenSSH password/MFA/key matrix and externally renewed Kerberos case, requires a user-owned 0600 archive, expands it, and scans command output, compressed bytes, and every extracted file for literal credentials/private paths plus exact SHA-256/base64 and credential-file copies; cleanup runs on every exit. Bash syntax, focused ordinary/race/vet/docscheck, complete current `make ci`, and exact-oldstable `make check` pass. The first current gate stopped at introduced G204/G304 findings; line-scoped fixed-path explanations cleared focused lint before a complete green restart. Exact Hosted execution follows in the next two rows; production Helper artifact scanning remains pending |
| M6.3 real-auth artifact-scan first exact-SHA Hosted RED | evidence SHA `00ca5d8ecf5f4ab47d7150f3dbaf17163678fb80`, tree `d6c70be9c9be17683a342d5072e555eb3e198c08`; push [29650644772](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29650644772); PR [29650645963](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29650645963), attempt 1 | Push finished 22 success / 1 failure / 1 dependency-skipped: auth job `88096778532` completed every OpenSSH/Askpass case, then public support-bundle publication deterministically rejected the login-home scan root before Kerberos ran. Classification: **introduced harness path defect**; the security boundary was not weakened. PR finished 21 success / 1 failure / 2 dependency-skipped: quality completed tests/package/version but hit the existing Stage 5 daemon-readiness timing, while exact same-SHA push quality passed; dependent auth was skipped. No rerun occurred. The correction moves only test artifacts to the existing validated state root |
| M6.3 real-auth artifact-scan correction local and exact-SHA Hosted gate | correction SHA `fd5ebe499331f5697cdc5eb1e238d12e939ee53a`, tree `6415e2d189727652617f6c931a74500a88df2067`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; push [29651140328](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29651140328); PR [29651141681](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29651141681), attempt 1 | Focused syntax/ordinary/race/vet/docscheck/lint and both complete local gates pass. PR passed 24/24. Push finished 21 success / 2 failure / 1 dependency-skipped: oldstable macOS ARM job `88097572764` hit existing exact-stderr-cap visibility and observed 0 rather than 65,536 bytes; native macOS ARM job `88097573332` hit existing heartbeat client-failure visibility. Exact same-SHA PR companions `88097576240`/`88097576270` passed. Push auth job `88098085447` and PR auth job `88098124518` both passed; each retained log has exactly two `real authentication support-bundle secret scan passed` markers, after complete OpenSSH/Askpass and externally renewed Kerberos/GSSAPI matrices. Classification: **real-auth support-bundle artifact scan GREEN; residual push failures are known Helper reader/heartbeat timing instability**. No rerun, assertion, timeout, production policy, or workflow change was made |
| M6.3 OpenSSH 8.9p1 floor RED/GREEN, local, and exact-SHA Hosted classification | implementation SHA `e7733ec23ec1903dbdc41a926db20fe6defcccc7`, tree `eb18a732c3cc82a5224b4f5c0decf135c40cc9d4`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; push [29652064203](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29652064203); PR [29652066243](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29652066243), attempt 1 | Three canonical RED slices froze an exact Ubuntu 22.04 `/usr/bin/ssh -V` 8.9p1 capture plus native full-auth execution, public doctor OpenSSH/endpoint pass assertions before authentication, and absence of an `OpenSSH_8.9p1` string rejection in production app/transport runtime. RED failed only on the missing workflow floor step and doctor call; runtime hardcode absence was already green. Focused syntax/docscheck ordinary/race/vet/lint/actionlint and both complete local gates pass. Push native Ubuntu 22.04 job `88099968231` and PR job `88099973802` both logged `OpenSSH_8.9p1 Ubuntu-3ubuntu0.16`, `authentication doctor OpenSSH and endpoint checks passed`, `real authentication support-bundle secret scan passed`, and `hosted authentication matrix passed`. Both auth jobs `88100524879`/`88100552618` also passed. Push finished 21 success / 2 failure / 1 dependency-skipped: oldstable macOS Intel/ARM jobs `88099968188`/`88099968196` hit the existing Stage 2 PTY selection-refresh timing (`delete requires at least one target`); exact same-SHA PR companions `88099973769`/`88099973755` passed. PR finished 22 success / 1 failure / 1 dependency-skipped: native macOS ARM job `88099973823` hit existing Helper heartbeat client-failure visibility; push companion `88099968225` passed. Classification: **OpenSSH 8.9p1 floor, doctor, real authentication, and support scan GREEN; residual failures are known PTY/Helper timing instability**. No rerun, assertion, timeout, support claim, or production policy changed |
| M6.3 OpenSSH-floor evidence docs-sync exact-SHA Hosted classification | documentation SHA `2b6d7458fd59b0214bc46f3ceecf02fa4d88d87f`, tree `1ea0bc9bf61f7fc2575ab55160aa0b0980056cb4`; push [29652577238](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29652577238); PR [29652579165](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29652579165), attempt 1 | PR passed 24/24. Push finished 22 success / 1 failure / 1 dependency-skipped: Ubuntu 22.04 native ordinary test job `88101296817` alone saw `TestInspectConfigRejectsDisabledKnownHostsAndRecognizesProxy` return stable `expansion failed`; exact same-SHA PR companion `88101301661` passed the complete native job, and push auth `88101836234` passed. Classification: **existing system OpenSSH config-expansion fixture instability, no compatibility-floor, doctor/auth/support-scan, registry, security-ledger, or documentation regression**. No rerun, assertion, timeout, or file change was made |
| PLAT-009 current OpenSSH RED/GREEN, local, and exact-SHA Hosted classification | implementation SHA `a7f3ddd8a99df973518140de7eae45e295ae6ea2`, tree `43a1ca89c7de6b2cbc1a66894c0917375f57e340`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; push [29652955122](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29652955122); PR [29652956741](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29652956741), attempt 1 | Three canonical RED slices failed only on the missing Ubuntu 24.04 exact current-version capture, binding into the real auth harness, and pre-mutation drift rejection. GREEN captures `/usr/bin/ssh -V`, accepts only an OpenSSH-shaped value, passes the exact retained value through `sudo env`, and requires a byte-identical live recheck before creating users or sshd state; the 8.9p1 floor passes the same binding. Workflow provenance freezes the new step and dataflow. Focused syntax/docscheck/actionlint and complete dual-Go local gates pass. Push auth job `88102746004` and PR auth job `88102770741` both logged `OpenSSH_9.6p1 Ubuntu-3ubuntu13.18`, `current OpenSSH version binding passed`, doctor OpenSSH/endpoint pass, complete auth, support-bundle scan, and Kerberos/GSSAPI scan; PR passed 24/24. Push finished 22 success / 1 failure / 1 dependency-skipped: native Ubuntu 24.04 ordinary job `88102292509` hit the same existing `InspectConfig` expansion fixture; exact same-SHA PR companion `88102296973` passed. Classification: **PLAT-009 exact floor/current compatibility GREEN and Verified; residual push failure is existing OpenSSH config-expansion fixture instability**. No rerun, assertion, timeout, support boundary, or production policy changed |
| PLAT-009 evidence-sync exact-SHA Hosted classification | documentation SHA `8a53718dacb532393addec0051d5b55a5f896cf7`, tree `a9eefe1a94d5883917d87757513a5614b3d44eba`; push [29653561994](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29653561994); PR [29653563045](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29653563045), attempt 1 | Push finished 19 success / 3 failure / 2 dependency-skipped: quality stopped in the existing daemon-readiness wait after package/version checks while PR quality passed; oldstable Ubuntu 22.04 hit existing `TestLevel2InFlightCancelPropagatesAndNeverCommitsOrDeletesSource` while the PR companion passed; native macOS Intel ordinary tests hit the existing terminal foreground-process-group race. PR finished 22 success / 1 failure / 1 dependency-skipped: macOS Intel race hit existing Helper heartbeat client-failure visibility. Both auth jobs passed. Classification: **documentation-only PLAT-009 sync with known daemon/transfer/PTY/Helper timing failures, no compatibility or support-claim regression**. No rerun, assertion, timeout, workflow, or file change was made |
| REL-006 MIT Kerberos exact-version RED/GREEN, local, and exact-SHA Hosted classification | implementation SHA `14e5ec4e63c70fb93ed93d15adbe7fdf7cce5d5e`, tree `e105ae8f567575fbaefc4736042716acb1958581`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; push [29653921546](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29653921546); PR [29653923169](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29653923169), attempt 1 | Three canonical RED slices failed only on absent exact MIT Kerberos capture, missing retained-version dataflow into the real harness, and missing pre-mutation drift rejection. GREEN captures `/usr/bin/klist -V`, accepts only `Kerberos 5 version *`, passes the byte-identical value through `sudo env`, and rejects drift before creating users or KDC state; workflow provenance freezes the new step and dataflow. Focused docscheck/syntax/actionlint and complete dual-Go local gates pass. Push/PR auth jobs `88105308661`/`88105369623` both logged exact `Kerberos 5 version 1.20.1`, `current MIT Kerberos version binding passed`, and passed the complete expiry/recovery GSSAPI matrix. Push finished 21 success / 2 failure / 1 dependency-skipped: macOS Intel race job `88104825253` hit existing Stage 2 PTY selection refresh and macOS ARM ordinary job `88104825258` hit existing Helper exact-stderr-cap visibility; exact same-SHA PR platform steps passed. PR finished 22 success / 1 failure / 1 dependency-skipped: macOS ARM job `88104828923` completed ordinary/race/native lifecycle then external `formulae.brew.sh` cask API curl timed out during `brew update`; same-SHA macOS Intel completed the full Homebrew lifecycle. Classification: **REL-006 exact MIT Kerberos 1.20.1 binding GREEN; residual failures are known PTY/Helper timing and external Homebrew API availability, not Kerberos/workflow/provenance regression**. No rerun, assertion, timeout, support boundary, or production policy changed |
| REL-007 eight-domain review coverage RED/GREEN, local, and exact-SHA Hosted classification | implementation/evidence SHA `cc6559e41572aa21a7fc249fe0086fe4df50fe27`, tree `6d1ead4f23de7a1ac344d959f0b8cb75c004ab7f`; exact current Go 1.26.5 full `make ci`; exact oldstable Go 1.25.12 full `make check`; push [29655143700](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29655143700); PR [29655145234](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29655145234), attempt 1 | Three canonical RED slices failed only on absent eight-domain coverage rows, executable negative-test anchors, and explicit open production/final-review boundaries. GREEN records stable scoped `reviewed`/`partial` dispositions and keeps REL-007 `In Progress`. Both quality jobs `88108003301`/`88108007055` and both auth jobs `88108438224`/`88108554124` passed. Push finished 22 success / 1 failure / 1 dependency-skipped: native macOS Intel job `88108003345` hit existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow`; exact same-SHA PR Intel job `88108007127` passed ordinary, race, native, and Homebrew lifecycle. PR finished 22 success / 1 failure / 1 dependency-skipped: native macOS ARM job `88108007107` installed preview 0.9 and then external `formulae.brew.sh/api/internal/packages.arm64_sequoia.jws.json` download failed during `brew update`; exact same-SHA push ARM job `88108003355` completed the full Homebrew lifecycle. Classification: **REL-007 review coverage GREEN; residual failures are known Helper process-reader timing and external Homebrew API availability, not a security-ledger/docscheck regression**. No rerun, assertion, timeout, workflow, support boundary, or production policy changed |
| REL-007 evidence-sync exact-SHA Hosted classification | documentation SHA `1fff139a32a30e1f47edf34650051446903d4d10`; push [29655548628](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29655548628); PR [29655549777](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29655549777), attempt 1 | Each workflow finished 20 success / 2 failure / 2 dependency-skipped. Push Ubuntu 24.04 race job `88109086364` hit existing `TestLocalTailDetectsTruncateAndRotateAsHints`; same-SHA PR companion `88109090192` passed the complete native job. PR macOS Intel race job `88109090199` hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose`; same-SHA push companion `88109086349` passed ordinary, race, native, and Homebrew lifecycle. Both quality jobs completed packaging/checksums/completion/version, then the existing Stage 5 initial-daemon readiness poll expired; both immediate-parent `cc6559e` quality jobs passed with identical product code. Classification: **documentation-only REL-007 evidence sync with known tail polling, Helper heartbeat visibility, and daemon-readiness timing residuals, not a review-coverage regression**. No rerun, assertion, timeout, workflow, support boundary, or production policy changed |
| REL-009 user/operations documentation RED/GREEN, local batch gates, and exact-SHA Hosted classification | implementation SHA `19e8ab73d5f90a51815634b9b36112f86711f372`, tree `ceb8e749ac11c873b703d3bee3f3168cb9f5fce2`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; push [29656202301](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29656202301); PR [29656203801](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29656203801), attempt 1 | Four canonical tests first failed only on the absent documentation index coverage, first-run guide, operations runbook, and upgrade/rollback guide. GREEN adds and links the missing paths, corrects stale troubleshooting CLI spellings at the generated registry source, and passes focused REL-009 tests on both toolchains plus full docscheck and help/man/completion parity. The first local `make ci` attempt used an unsafe `/var/tmp` descendant and exposed generated-reference drift; the second exposed introduced helper G304 lint. Both causes were corrected directly; final exact current `make ci` and exact oldstable `make check` pass completely. Push finished 20 success / 3 failure / 1 dependency-skipped; PR finished 22 success / 1 failure / 1 dependency-skipped. Both quality jobs `88110927179`/`88110930798` and auth jobs `88111426037`/`88111481417` passed. Push Ubuntu 22.04 job `88110927222` hit existing `TestInspectConfigUsesBoundedNonConnectingOpenSSHExpansion`; same-SHA PR Ubuntu 22.04 job `88110930847` passed completely. Push Intel ordinary job `88110927255` hit existing exact-stderr-cap visibility after PR Intel ordinary tests passed; push ARM race job `88110927257` and PR Intel race job `88110930820` hit existing heartbeat client-failure visibility, while same-SHA PR ARM job `88110930812` passed the complete native/Homebrew path. Classification: **REL-009 docs and public CLI parity GREEN; residual failures are existing OpenSSH expansion and Helper process-reader visibility timing, not a documentation/registry regression**. No rerun, assertion, timeout, workflow, support boundary, or production policy changed. REL-009 remains `In Progress` only pending an independent exact-RC new-user exercise |
| REL-009 evidence-sync exact-SHA Hosted classification | documentation SHA `bf89ba401c5981b362d9711beab417cfc0749751`, tree `26ce0334cbcb59fa255d349f3ddc8ccb191b4b37`; push [29656656113](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29656656113); PR [29656657281](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29656657281), attempt 1 | Each workflow finished 22 success / 1 failure / 1 dependency-skipped; both quality jobs `88112124780`/`88112128086` and auth jobs `88112629935`/`88112649329` passed. Push macOS ARM native job `88112124793` hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose`; same-SHA PR ARM job `88112128173` passed ordinary, race, native, and Homebrew lifecycle. PR oldstable macOS Intel job `88112128163` hit existing `TestLocalTailDetectsTruncateAndRotateAsHints` after observing the truncate notice and replacement chunk in separate poll results; same-SHA push oldstable Intel job `88112124805` passed. Classification: **documentation-only REL-009 evidence sync with known Helper heartbeat and tail polling timing, not a documentation or product regression**. No rerun, assertion, timeout, workflow, support boundary, or production policy changed |
| REL-008 exact-candidate gate-record RED/GREEN and local batch gates | working tree based on `bf89ba401c5981b362d9711beab417cfc0749751`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted runtime temp roots and separate external build/coverage/cache roots | Five canonical tests first failed at compile time on the absent gate IDs/types, record validators, and CLI. GREEN adds an ordered 17-gate catalog, exact candidate commit/tree and completed/success binding, wrong-kind/missing/duplicate rejection, physical 100 GiB byte floor, isolated Level 2 process/network/zero-daemon-content metrics, declared soak duration plus race/deadlock/leak acceptance, two independent exact-candidate reviews, and exact candidate push/PR plus post-merge main all-job success. The bounded strict JSON CLI rejects unknown/trailing input and validates candidate/final phases. Focused ordinary/race/vet/scoped-lint and both toolchains pass; exact current `make ci` completed first attempt. The first oldstable full attempt stopped during compilation when two fresh caches filled the root filesystem; only the two exact disposable REL-008 temp/cache roots were deleted, and no product/test input changed. A second preflight inherited interactive `umask 0077`, which converted deliberate `0755/0644` unsafe fixtures to safe `0700/0600`; the three affected negative tests all passed under canonical `0022`, followed by a complete exact Go 1.25.12 `make check` pass. Classification: **two local gate-environment mismatches (ENOSPC and noncanonical umask), not product failures or flaky reruns**. No timeout, assertion, fixture, workflow, or product code was weakened. No RC or evidence record is claimed |
| REL-008 exact-SHA Hosted classification | implementation SHA `bad687f67789960bac426bd9414cfbcc5b859b49`, tree `bfa026b52e2a55738f023623093347c5ee78d21e`; push [29657550405](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29657550405); PR [29657551397](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29657551397), attempt 1 | PR passed 24/24. Push finished 21 success / 2 failure / 1 dependency-skipped; both quality jobs `88114495581`/`88114498094` and both auth jobs `88114991942`/`88115022375` passed. Push native macOS Intel job `88114495527` hit existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose`; same-SHA PR native Intel job `88114498095` passed. Push oldstable macOS Intel job `88114495528` hit existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap` with 0 rather than 65,536 observed bytes; same-SHA PR oldstable Intel job `88114498100` passed. Both failing jobs independently passed `internal/releasegate` and `internal/tools/releasegate`. Classification: **REL-008 gate-record implementation GREEN; residual failures are known Helper process-reader visibility timing, not a releasegate/docs regression**. No rerun, assertion, timeout, workflow, support boundary, or production policy changed. These implementation runs do not constitute the required exact-RC record |
| REL-008 evidence-sync Hosted classification | documentation SHA `6290dc0f896499a43f8547168f04eac36cc60c11`, tree `0e0b10df10d0caeecb77aa1d12bca1e7078e1a5f`; push [29657922809](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29657922809); PR [29657925493](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29657925493), attempt 1 | Push finished 22 success / 1 failure / 1 dependency-skipped; PR finished 20 success / 2 failure / 2 dependency-skipped. Push quality `88115465464` and auth `88115923097` passed. Push native macOS ARM `88115465552` completed install then failed when `brew update` could not download Homebrew's external `formulae.brew.sh` package metadata; same-tree PR native ARM `88115471907` passed. PR oldstable macOS Intel `88115471879` hit existing Helper exact-stderr-cap visibility (0 rather than 65,536 bytes) and Stage 2 PTY selection timing; same-tree push oldstable Intel `88115465551` passed. PR quality `88115471885` timed out waiting for the frozen Stage 5 daemon in the clean-home lifecycle; same-tree push quality passed, and the dependent PR auth job was therefore skipped while push auth passed. Classification: **documentation-only evidence sync with one external Homebrew network failure and known process/PTY/daemon timing residuals, not a releasegate or documentation regression**. No rerun, assertion, timeout, workflow, support boundary, or production policy changed |
| REL-010 final-only zero-gap audit RED/GREEN and local batch gates | implementation SHA `181f8a8fbf083c5dadf450373981693e82ecdf74`, tree `a05b87fd21a90b84970f835331edfb5148b5b6b5`, based on REL-008 evidence-sync SHA `6290dc0f896499a43f8547168f04eac36cc60c11`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted runtime temp roots, and separate external build/coverage/cache roots | Four canonical tests first failed at compile time because the Stage 6 feature audit, exit-criteria audit, and explicit CLI selector did not exist. GREEN freezes the exact 23 Stage 6 IDs, final `Verified`/ADR-backed `Deferred`/`Removed` states, and the exact ordered 12 checked criteria; it rejects missing/unexpected/stage-drifted rows, nonterminal statuses, unlinked decisions, and changed/reordered/extra/missing/unchecked exits. Ordinary docscheck passes unchanged. `docscheck --release .` exits nonzero and reports exactly 21 `release.stage6_status` plus 12 `release.exit_criteria` findings on the current truthful tree. Focused current race/vet, oldstable tests, scoped lint, docscheck, release-audit count, and diff check pass. Exact current `make ci` and exact oldstable `make check` both completed on their first product invocation. The two discarded shell attempts were harness-only: one used zsh's read-only `status` variable and the next lacked Go on `PATH`; neither reached or changed a product assertion. This is audit readiness, not release success |
| REL-010 exact-SHA Hosted evidence | implementation SHA `181f8a8fbf083c5dadf450373981693e82ecdf74`, tree `a05b87fd21a90b84970f835331edfb5148b5b6b5`; push [29658450113](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29658450113); PR [29658451365](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29658451365), attempt 1 | Both workflows passed 24/24 without rerun. Push/PR quality jobs `88116820540`/`88116824005` and auth jobs `88117265957`/`88117343975` passed; all native, oldstable, reproducibility, comparison, build, and release-audit-containing ordinary test paths passed. This is exact implementation evidence for the REL-010 audit, not an exact-RC gate record or release success |
| REL-010 evidence-sync exact-SHA Hosted classification | documentation SHA `c81887a8fcfacd488ff88c23ead46f34f2f972e5`, tree `a6bf81d1ff77fb183a981db29af0c6a513ca62a1`; push [29658806407](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29658806407); PR [29658807676](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29658807676), attempt 1 | Each workflow finished 21 success / 2 failure / 1 dependency-skipped; push/PR quality jobs `88117745389`/`88117749194` and auth jobs `88118243683`/`88118336113` passed. Push native macOS Intel `88117745415`, PR native macOS Intel `88117749207`, and PR oldstable macOS Intel `88117749263` all hit the existing Helper heartbeat client-failure terminal-visibility fixture. Push native Ubuntu 22.04 `88117745402` hit existing `TestRunRemoteCommandRejectsSameInodeExecutableRewrite`; its same-SHA PR companion `88117749213` passed. Classification: **documentation-only REL-010 sync with known Helper terminal visibility and executable-rewrite fixture timing, not an audit or product regression**. No rerun, assertion, timeout, workflow, support boundary, or production policy changed |
| M6.4 Hosted timing-stability RED/GREEN and local batch gates | implementation SHA `60255dc8dc5e4732ae0fe55ac11ca860390bc81c`, tree `27fe962f3c712eae16e061a06d8460542eaaedf5`, based on `c81887a8fcfacd488ff88c23ead46f34f2f972e5`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted runtime temp roots, and separate external build/coverage/cache roots | Four canonical RED slices failed on missing `ProcessSession.Wait`/tail poll controls and the Stage 2 contract's absent ready-selection barrier. GREEN joins the Helper client reader, OpenSSH process, and stderr reader before terminal assertions; drives truncate and rotate with two injected polls while retaining the original 80 ms duration; makes `read_until` recognize an already-rendered screen; and initially required filename plus `cache:lru | normal` before sending delete. The first real PTY GREEN run exposed an incorrect test marker (`| normal |` rather than the actual screen text) and was corrected without changing a timeout or product assertion. The first Helper stress run exposed that child EOF may cancel `exec.CommandContext`, so `Wait` is used as a barrier while process error remains independently observable; the terminal diagnostic/client-failure assertions were retained. Final current race repetition passed 200 iterations for the three Helper fixtures, the unchanged-80-ms tail fixture passed another 200 iterations, and real Stage 2 PTY passed 10 iterations. Focused oldstable, vet, scoped lint, docscheck, exact current `make ci`, and exact oldstable `make check` pass. No timeout, assertion, workflow, support boundary, production Helper, or Level 2 policy was weakened |
| M6.4 timing-stability exact-SHA Hosted classification | implementation SHA `60255dc8dc5e4732ae0fe55ac11ca860390bc81c`, tree `27fe962f3c712eae16e061a06d8460542eaaedf5`; push [29659577751](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29659577751); PR [29659579470](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29659579470), attempt 1 | Push finished 21 success / 1 failure / 2 dependency-skipped; PR finished 22 success / 1 failure / 1 dependency-skipped. Push quality `88119774199` hit unchanged `TestRunLocalCommandReportsNonzeroAndCancellation`: its 20 ms context expired before `exec` start under runner load; same-SHA PR quality `88119778614` passed. PR oldstable macOS Intel `88119778700` exposed an introduced exact-stderr fixture race: the new child exited immediately after handshake, allowing `os/exec` to close stdout while the client still read the hello payload; same-SHA push oldstable Intel `88119774210` passed. All four native jobs in both workflows passed, including the corrected Helper heartbeat and Stage 2 PTY paths; PR auth `88120357972` passed while push auth was dependency-skipped. Classification: **timing barriers GREEN, one unrelated existing local-command start-timing residual, and one introduced exact-stderr child-lifetime regression requiring correction**. No rerun was used |
| M6.4 exact-stderr child-lifetime correction and final local gates | correction SHA `573aeefcaa488d73a7b5186a46ef03f87c1d8416`, tree `93552e809524e0638e9fa443e9ca13734737603e`, based on `60255dc8dc5e4732ae0fe55ac11ca860390bc81c`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; canonical local environment | The Hosted RED above showed that immediate child exit was not a valid way to expose terminal stderr. The correction removes `stderr-exact-exit`, restores the resident `stderr-exact` child, calls `session.Close()` (which now joins client/process/stderr through `Wait`) before reading `Diagnostic`, and retains the exact 65,536-byte assertion. Corrected current race and oldstable each passed 200 iterations; exact current `make ci`, exact oldstable `make check`, docscheck, and diff check pass. No timeout or diagnostic assertion was weakened |
| M6.4 child-lifetime correction exact-SHA Hosted classification | correction SHA `573aeefcaa488d73a7b5186a46ef03f87c1d8416`, tree `93552e809524e0638e9fa443e9ca13734737603e`; push [29660039347](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29660039347); PR [29660040623](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29660040623), attempt 1 | PR passed 24/24. Push finished 22 success / 1 failure / 1 dependency-skipped; push/PR quality `88121018081`/`88121021230`, auth `88121536992`/`88121597866`, and oldstable Intel `88121018139`/`88121021248` passed. Push native macOS Intel `88121018155` alone observed exactly 32,768 diagnostic bytes instead of 65,536; same-SHA PR native Intel `88121021243` passed. The terminal barrier did complete, so the evidence isolates the remaining cause to the fixture's ignored single-call `os.Stderr.Write` short result, not asynchronous post-terminal observation. Classification: **child-lifetime race fixed; exact-stderr writer still non-deterministic on a macOS pipe and requires exact-write correction**. No rerun, timeout, or assertion change was used |
| M6.4 exact-stderr exact-write correction and final local gates | correction SHA `60ff9080812e0e47f720d102671116a6557b9921`, tree `d83c801a6413910bc08b33734621e0fb4dcf3acc`, based on `573aeefcaa488d73a7b5186a46ef03f87c1d8416`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; canonical local environment | The Hosted RED above showed the fixture's single `os.Stderr.Write` can legally return a short count on macOS. The correction uses `io.CopyN` to write the exact 65,536-byte accepted boundary and 65,537-byte overflow boundary, treating source-side copy failure as child failure rather than ignoring it. Both tests passed current race and oldstable at 200 iterations each; exact current `make ci`, exact oldstable `make check`, docscheck, and diff check passed. The original byte boundaries, timeouts, and diagnostic assertions remain unchanged |
| M6.4 exact-write correction exact-SHA Hosted classification | correction SHA `60ff9080812e0e47f720d102671116a6557b9921`, tree `d83c801a6413910bc08b33734621e0fb4dcf3acc`; push [29660519282](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29660519282); PR [29660520860](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29660520860), attempt 1 | Both workflows finished 22 success / 1 failure / 1 dependency-skipped. Push native macOS Intel `88122281214` installed and ran preview 0.9, updated the local tap to 1.0, then failed because GitHub DNS resolution failed during Homebrew's external update. PR Helper exact-boundary coverage passed, but oldstable macOS `88122285547` failed Stage 2 delete because `cache:lru | normal` is visible even while the active pane reports `loading`; delete input arrived before the selected file row and produced `delete requires at least one target`. The exact-write correction itself is covered by same-SHA successful Helper tests. Classification: **exact stderr writer deterministic; one external Homebrew DNS failure and one introduced insufficient PTY readiness marker requiring correction**. No rerun, timeout, or assertion change was used |
| M6.4 fixed-prefix loaded-selection correction focused and complete local gates | correction SHA `2f4ed536ca2553e1bd266ed66fd275b664a00690`, tree `7961527cf2273dc55a88f8409b6af989c80c97b8`, based on `60ff9080812e0e47f720d102671116a6557b9921`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; canonical local environment | The prior Hosted RED proved cache policy and mode do not imply listing completion. This correction routed copy, move, rename, and delete through one filename-plus-`READ-ONLY | sort:name` barrier and froze all four action orderings. The complete local PTY passed current race 20 iterations in 156.608 seconds and oldstable 10 in 76.794 seconds. An earlier delete-only form exposed copy's same premature action in the complete race gate; a docs table pipe error and root-filesystem ENOSPC were corrected/classified before accepted gates. With independent caches on `/data00`, exact current `make ci` and exact oldstable `make check` passed. No timeout, user action, behavior, or assertion was weakened |
| M6.4 fixed-prefix correction exact-SHA Hosted classification | correction SHA `2f4ed536ca2553e1bd266ed66fd275b664a00690`, tree `7961527cf2273dc55a88f8409b6af989c80c97b8`; push [29661476223](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29661476223); PR [29661477838](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29661477838), attempt 1 | Push finished 21 success / 1 failure / 2 dependency-skipped; PR finished 19 success / 3 failure / 2 dependency-skipped. Push/PR quality `88124789074`/`88124793242` both reached the real temporary-sshd download row, which was loaded and selected but included `caps:3@1 | helper:L0 not_available` before `sort:name`; the fixed prefix never appeared. PR native Ubuntu 22.04 `88124793253` independently timed out waiting for an unchanged Stage 1 replacement daemon. PR native macOS Intel `88124793256` hit the residual overflow child-lifetime race (`file already closed` during handshake); same-SHA push Intel passed, and all oldstable jobs passed. Classification: **four-action barrier wiring GREEN; fixed loaded prefix is not remote-capability-safe and requires final-screen state matching, with two unrelated known residuals**. No rerun, timeout, or assertion change was used |
| M6.4 final-screen selection observer correction focused and complete local gates | correction SHA `98a3315070f12b97fe43fa1a79dcdfc4e7428701`, tree `6c2e1dda9cb1768d98498458dcbe25adfe684aea`, based on `2f4ed536ca2553e1bd266ed66fd275b664a00690`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; canonical local environment | The correction adds `vt-observer -final -absent` so one replay requires the selected filename on the final visible screen and rejects final `READ-ONLY | loading`; ordinary historical `read_until` semantics remain unchanged. Unit coverage freezes loading rejection plus local and remote capability-loaded rows. An initial two-process checkpoint form was discarded after 10-run pressure amplified replay cost and timed out despite loaded final screens. The single-pass form passed the complete local PTY plus observer under current race 20 iterations in 156.018 seconds and exact oldstable 10 in 76.980 seconds. Final review tightened the required target from a visible filename to the cursor marker `> filename`; the complete local PTY, static contract, and observer unit then passed one focused current-race and one oldstable run. A retained current/race 10-run temporary-sshd attempt failed only after the isolated sshd stopped accepting sessions and the UI reported `transport_interrupted`; no selected filename existed, so it is classified as transport environment failure and was not rerun. Before the accepted final-tree gate, two current invocations put `GOTMPDIR` on `/data00` and one omitted canonical `umask 0022`; the resulting private-root/unsafe-mode fixture failures were isolated with focused diagnostics and not accepted as product gates. With the canonical environment restored, exact current `make ci` passed ordinary/race, lint 0, four fuzz targets, vulnerability/actionlint and four builds; exact oldstable `make check` passed ordinary/Provider/integration/docs/tidy/verify. No timeout, action, behavior, or assertion was weakened |
| M6.4 final-screen correction exact-SHA Hosted evidence | correction SHA `98a3315070f12b97fe43fa1a79dcdfc4e7428701`, tree `6c2e1dda9cb1768d98498458dcbe25adfe684aea`; push [29662965862](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29662965862); PR [29662967034](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29662967034), attempt 1 | Both workflows passed 24/24 with zero skipped jobs and no rerun. Push/PR quality `88128602684`/`88128606205`, native macOS Intel `88128602712`/`88128606222`, native macOS ARM `88128602694`/`88128606237`, and auth `88129085277`/`88129154512` all passed. This closes the fixed-prefix correction's exact-SHA Hosted evidence gap; it does not create an RC record or open production Helper/Level 2 |
| M6.4 physical 100 GiB release-lab RED/GREEN | implementation SHA `c57ba4f78e34ce3c30d0185fa9dfa11729ad682f`, tree `6402169170a002cb6dcb5a264b43686c4bb31473`, based on physical-lab commits `3f5621873cdd81ce6172752ad5c5abf5fefc3f8e` and `c8982c8b38c46c648026e939e94a86780bccb406`; exact current Go 1.26.5 and exact oldstable Go 1.25.12 | Five related RED/GREEN slices freeze strict lab/control/evidence/candidate/toolchain environment binding, reject sparse files, bind reports to the exact clean candidate SHA/tree, exercise production Worker cancel/journal-reopen/resume LocalFS→real sshd/SFTP plus complete SFTP→Local, and isolate an owner-private explicit OpenSSH config without changing the ADR-0001 default argument vector. Focused current ordinary/race/vet/lint and oldstable tests pass; `make lint` reports zero issues. Two preflight failures were fail-closed and produced no evidence: `/data00` is not a trusted control-root ancestor for uid 1001, and system OpenSSH 7.9p1 rejects the frozen `StdinNull` option. The corrected run used separate trusted control state and a locally built OpenSSH 10.4p1 client whose upstream digest and release signature were verified; system SSH and production defaults were not changed |
| M6.4 physical-lab local batch-gate classification | candidate SHA `c57ba4f78e34ce3c30d0185fa9dfa11729ad682f`, tree `6402169170a002cb6dcb5a264b43686c4bb31473`; exact current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; canonical `umask 0022`, trusted runtime temp and external cache roots | Oldstable full check passed. Current `make ci` passed contracts, fmt/vet, ordinary/provider/integration tests, docs/NOTICE/tidy/module verification, lint 0, and the changed OpenSSH/integration race paths, then stopped in existing `internal/helper` `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow` after an unsafe probe unexpectedly succeeded. The same unchanged test failed five times under `-race -count=100` on an unmodified archive of the prior accepted HEAD: the fixture writes only one byte beyond the 65,536-byte cap while `command.Wait` may close the pipe before the stderr collector observes that last byte. Classification: **pre-existing Helper fixture/pipe-close timing, not a physical-lab or OpenSSH-config regression**. No timeout, assertion, security option, fixture boundary, workflow, or product policy was weakened, and the full gate was not rerun to manufacture green |
| M6.4 physical 100 GiB pre-RC harness proof | candidate SHA `c57ba4f78e34ce3c30d0185fa9dfa11729ad682f`, tree `6402169170a002cb6dcb5a264b43686c4bb31473`; report `/data00/home/tianchao.thatcher/amsftp-release-evidence/physical-100gib-c57ba4f78e34ce3c30d0185fa9dfa11729ad682f.json`; current Go 1.26.5; ext4; OpenSSH 10.4p1; 2026-07-18T23:14:28Z–2026-07-19T01:34:57Z | PASS in 8439.07 seconds. Production Worker uploaded a physically allocated 100 GiB file to a real isolated sshd/SFTP endpoint, canceled at the persisted 1 GiB checkpoint, closed/reopened SQLite state, resumed the retained partial file, committed it, removed the verified source, and downloaded the full object back. Each direction transferred exactly 107,374,182,400 bytes (214,748,364,800 total); source, remote, and download allocations meet or exceed logical size; upload/download SHA-256 both equal `f0b14a8da7f1c48a0846647a078b97956edd8df451a62fc4b466879aa24d4fd7`. The O_EXCL/fsynced report is mode 0600 and binds the exact candidate. The temporary roughly 200 GiB lab and control roots were removed after success and are not recoverable; the report remains. Its declared purpose is `pre-rc-harness-proof-not-final-rc-evidence`: it proves the harness and bidirectional path work, but cannot close the final exact-RC physical gate or open Production Helper/Level 2 |
| M6.4 physical-lab evidence-sync exact-SHA Hosted classification | documentation SHA `7eb8adeee00e7c43552d21d4c2cc982f52117a07`, tree `850ab19f6e65ebc8cd64d74f8763da4fb8c7d40d`; push [29668874858](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29668874858), attempt 1; PR [29668876151](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29668876151), attempts 1–2 | Push passed 24/24 on the first attempt. PR attempt 1 finished 22 success / 1 failure / 1 dependency-skipped: macOS Intel ordinary tests hit existing `TestStage2LocalPTYCopyAndDurableJobsReattachMVP` selection-refresh timing, sending delete while the final screen still showed `READ-ONLY | loading`; the same captured tail later showed `> payload.txt`. Exact same-SHA push macOS Intel, PR macOS ARM, both macOS oldstable jobs, and PR quality passed. With no code, assertion, timeout, workflow, or product-policy change, only the failed job was rerun; attempt 2 passed ordinary tests, race, Homebrew lifecycle, native security fixtures, and the dependent compare, yielding 24/24 success. Classification: **existing Stage 2 PTY refresh timing, not a physical-lab, explicit OpenSSH-config, documentation, or transfer regression**. These runs validate this batch and its evidence sync; they are not the final exact-RC record and do not open Production Helper/Level 2 |
| M6.1 final cold-start closure RED/GREEN | exact current Go 1.26.5 and exact oldstable Go 1.25.12; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent test/temp/cache roots | RED: final-only audit reported exactly VIM-013, VIM-014, and REL-001 as `release.stage6_status` blockers for this batch. Three focused contracts then freeze: absent-config validate/effective generation with exact resolution policy and 14-field schema plus zero mutation; absent-config 74-binding Normal/Visual default keymap with Vim-first bindings, zero overrides, and zero mutation; macro action fail-closed with byte preservation plus help/man/bash/zsh/fish non-advertisement. GREEN: current race and exact oldstable focused app/keymap/config/tui suites pass. Updating only those three evidence-complete rows to `Verified` removes their three final-audit findings while leaving the other 18 Stage 6 rows and all 12 exit criteria truthfully open |
| M6.1 final cold-start closure local batch gates | exact current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent temp root and owner-owned caches | Final oldstable and current gates pass from their beginnings. The first oldstable attempt passed all tests/docs then found two locked NOTICE modules absent from the newly selected cache; `go mod download all` populated them but also added four non-tidy checksum lines, so that run correctly stopped at tidy-diff. The four command-created lines were removed and the zero-code-change full oldstable rerun passed NOTICE, tidy/verify and tool-module verification. The first current attempt passed all tests/docs/NOTICE/tidy then hit root-filesystem ENOSPC while compiling lint tools. Only exact owner-owned, rebuildable Go caches and the failed build temp were cleared, releasing about 7 GB; the zero-code-change full current rerun passed lint with zero issues, full race, four fuzz targets, govulncheck, actionlint and four builds. Classification: **local module-cache preparation and disk-capacity failures, not cold-start/product regressions**; no assertion, timeout, security policy, dependency inventory, or product behavior was weakened |
| M6.1 final cold-start closure exact-SHA Hosted classification | implementation SHA `8eb8190d872f4fe3800e8dc54df683661199c265`, tree `0a0f3071d6353b14f862580c783646b964e453da`; push [29670479782](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29670479782), attempts 1–2; PR [29670480608](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29670480608), attempts 1–2 | Each attempt 1 completed 23 success / 1 failure. Push oldstable macOS Intel [88148415205](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29670479782/job/88148415205) failed the existing Helper heartbeat-failure fixture while its exact-SHA PR companion [88148418196](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29670480608/job/88148418196) passed. PR oldstable Ubuntu 24.04 [88148418153](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29670480608/job/88148418153) failed the existing OpenSSH config-expansion fixture while its exact-SHA push companion [88148415189](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29670479782/job/88148415189) passed. All new cold-start contracts passed in both workflows. With no code, assertion, timeout, fixture, workflow, or product-policy change, only each failed chain was rerun once; both attempt 2 runs finished 24/24 success. Classification: **known Helper process-reader and OpenSSH runner-environment instability, not a cold-start, keymap, config, or documentation regression**. These runs validate this batch; they do not create the final exact-RC record or open Production Helper/Level 2 |
| M6.3 OBS-010 final doctor RED/GREEN and focused gates | exact current Go 1.26.5 and exact oldstable Go 1.25.12; `GOTOOLCHAIN=local`, canonical trusted cache/temp roots | RED: final-only release audit reported OBS-010 once among exactly 18 Stage 6 nonterminal rows and 12 unchecked exits; the native lifecycle policy then failed because it did not execute the installed doctor. GREEN adds three related slices: all 11 endpoint-inclusive probes can fail simultaneously while ordered human/JSON expose only stable `probe_failed` and remediation tokens; the existing complete troubleshooting registry remains byte-identical to the committed code map; and the four-platform native clean-install lifecycle runs the installed doctor against its live daemon, requiring the exact 10-code order plus complete status/severity/detail/remediation objects and zero `probe_failed`. A local real installed-daemon probe produced that exact healthy/degraded result and was stopped and removed. Current focused/race, exact oldstable focused, ordinary docscheck, and native workflow policy pass. Marking only OBS-010 `Verified` leaves 17 Stage 6 rows and all 12 exit criteria truthfully open; Production Helper/Level 2 remain CLOSED |
| M6.3 OBS-010 final doctor complete local batch gates | exact current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent temp/cache/build/coverage roots | Final oldstable and current gates pass from their beginnings. The first oldstable run reached all packages and exposed an introduced docscheck regression: the production native lifecycle satisfied the new doctor policy, but the canonical `testdata/valid` workflow still represented the old lifecycle, so every derived negative fixture gained the same unexpected finding. Synchronizing only that canonical fixture made focused current/oldstable docscheck pass, and the restarted full oldstable check passed all packages, Provider, docs/NOTICE, tidy and both module verifications. The first current run passed ordinary/docs/module checks then lint reported introduced gosec G101 on a test-only failure marker named `secret`; renaming it to a raw marker preserved the same non-export assertion, scoped race/lint passed with zero issues, and the restarted full current CI passed lint 0, full race, four fuzz targets, govulncheck, actionlint and four builds. Classification: **two introduced test/policy maintenance regressions corrected without weakening behavior, assertions, timeouts, workflow safety, or product boundaries** |
| M6.3 OBS-010 final doctor exact-SHA Hosted classification | implementation SHA `7f920f224c898581011a52f62a670216bd4771cf`, tree `bcbc8703a671fef28897e0dbb69d7fb6ada17bbe`; push [29671780164](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164), attempts 1–2; PR [29671781592](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671781592), attempts 1–2 | Attempt 1 push finished 22 success / 1 failure / 1 dependency-skipped: native macOS Intel [88152050269](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164/job/88152050269) hit the existing 20 ms local-command cancellation/process-start race before reaching the doctor lifecycle, while the same-SHA PR job passed that package before a separate failure. PR finished 20 success / 3 failure / 1 dependency-skipped: native macOS Intel [88152054511](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671781592/job/88152054511) hit the existing Helper hello/heartbeat pipe-close race while the push job passed that package; oldstable macOS Intel [88152054512](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671781592/job/88152054512) hit the existing exact-stderr-cap 0/65,536 visibility race while push companion [88152050275](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164/job/88152050275) passed; native macOS ARM [88152054506](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671781592/job/88152054506) hit the existing Stage 2 PTY delete-before-refresh race while push companion [88152050195](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164/job/88152050195) passed. With no code, assertion, timeout, fixture, workflow, or product-policy change, only each failed chain was rerun once; push native Intel [88152781161](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164/job/88152781161), PR native ARM [88152781175](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671781592/job/88152781175), PR native Intel [88152781185](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671781592/job/88152781185), and PR oldstable Intel [88152781187](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671781592/job/88152781187) passed, and both attempt 2 runs finished 24/24. The installed-doctor success marker appears exactly once in successful native evidence for macOS ARM [88152050195](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164/job/88152050195), macOS Intel [88152781161](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164/job/88152781161), Ubuntu 22.04 [88152050222](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164/job/88152050222), and Ubuntu 24.04 [88152050206](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29671780164/job/88152050206). Classification: **known command-start, Helper reader, exact-stderr, and PTY refresh timing instability, not a doctor, workflow-policy, or OBS-010 regression**. These runs verify OBS-010; they do not create the final exact-RC record or open Production Helper/Level 2 |
| M6.2 WORK-006 final workspace-migration RED/GREEN and focused gates | exact current Go 1.26.5 and exact oldstable Go 1.25.12; `GOTOOLCHAIN=local`, canonical trusted cache/temp roots | RED first failed to compile because no migration-backup path or publication existed. Four related GREEN slices freeze source-schema recognition, exact private v1 backup before v2 publication, interruption/retry and collision idempotence, and stopped-daemon export/repair guidance. A review RED then proved a direct source hard-link follows in-place source mutation; the corrected design writes an independent 0600 temporary inode, fsyncs it, publishes with a no-replace link, validates exact bytes, removes the temporary, and fsyncs the directory. It revalidates the source before both backup and final replace. Current full workspace/docscheck race, exact oldstable focused tests, invalid-source preservation, historical-fixture bytes, conflicting backup, interrupted retry, source-race refusal, v2 no-backup, and documentation contracts pass. Marking only WORK-006 `Verified` leaves 16 Stage 6 rows and all 12 exit criteria truthfully open; SQLite/public-channel rollback remains REL-003 and Production Helper/Level 2 remain CLOSED |
| M6.2 WORK-006 final workspace-migration complete local gates | exact current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent temp/cache roots and separate external build/coverage directories | Exact oldstable passed from the beginning: all ordinary/Provider tests, docs/NOTICE, tidy, and both module verifications. Current passed make-contract, fmt/vet, every ordinary/Provider/integration test, docs/NOTICE, tidy/verify, lint with zero issues, and the complete race suite including workspace/statefs/Helper/PTY. The first current invocation then reached the existing one-second `FuzzWireBytes` smoke and returned `context deadline exceeded`; this batch changes only workspace/docscheck and the same invocation had already passed all changed code under ordinary and race. The failed fuzz target was rerun once and passed, then the not-yet-run `FuzzNormalizePath`, govulncheck with zero called/imported vulnerabilities, actionlint, and all four cross-builds passed. Classification: **known fuzz deadline instability, not a workspace migration, backup, or documentation regression**. No product code, assertion, timeout, fuzz budget, workflow, or security boundary was changed in response |
| M6.2 WORK-006 final workspace-migration exact-SHA Hosted classification | implementation SHA `5482e3af91115f8763d7fe12bb0ea36370225667`, tree `7d08f573de5253a1442e6bbbd2b6474dda51874b`; push [29672994886](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29672994886), attempts 1–2; PR [29672995964](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29672995964), attempt 1 | PR passed 24/24 on its first attempt. Push attempt 1 finished 22 success / 1 failure / 1 dependency-skipped: native macOS Intel [88155251088](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29672994886/job/88155251088) hit the existing Helper hello/heartbeat pipe-close race, while exact-SHA PR companion [88155253977](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29672995964/job/88155253977) passed the Helper package, full race suite, native lifecycle, and complete job. Only that failed chain was rerun once. Attempt 2 native macOS Intel [88155986499](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29672994886/job/88155986499) passed the original Helper failure point, full race suite, and installed native lifecycle; it installed and verified the loopback Homebrew 0.9.0 bundle, then `brew update` failed because the runner could not resolve `github.com` for Homebrew and unrelated taps. The run therefore finished 22 success / 1 failure / 1 dependency-skipped, and was not rerun again. All changed workspace and docscheck contracts passed in both workflows. Classification: **existing Helper reader timing followed by external macOS-runner DNS failure, not a workspace migration, backup, documentation, native-package, or Homebrew-formula regression**. No code, assertion, timeout, fixture, workflow, or product-policy change was made in response. These runs verify WORK-006; they do not create the final exact-RC record or open Production Helper/Level 2 |
| M6.2 REL-002 saved-workspace completion RED/GREEN and focused gates | implementation SHA `935aa3162eaefebaf8f34426b861b2f5cbb6ca5f`, tree `4beeaa6bb409ace2cd705489f8e1bc061f97a9cd`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent temp/cache roots | RED failed to compile because neither bounded workspace-name enumeration nor the hidden completion query existed. GREEN adds a non-mutating platform-path query; missing roots remain absent, more than 1,024 entries fail closed, only private regular `.json` files are accepted, lexical output is capped at 256, and corrupt contents remain name-completable without being read. Bash/zsh/fish invoke it only immediately after `--workspace` with stderr suppressed; public help/man omit the hidden query. Focused current ordinary/race and exact oldstable app/workspace/docscheck suites pass. A real isolated installed binary returned empty without creating state, then returned exact `release-smoke`; bash and zsh functions produced the same name and no database appeared. The first auxiliary actionlint/shell-smoke command used a nonexistent Go path and installed its zsh stub too late; the corrected harness used the exact toolchain, strict shell mode, and preinstalled stub and passed. No product assertion was reached or changed by the discarded harness attempt |
| M6.2 REL-002 saved-workspace completion complete local gates | exact current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent temp/cache roots and separate external build/coverage directories | Exact oldstable passed from the beginning: all ordinary/Provider tests, docs/NOTICE, tidy, and both module verifications. The first current run passed ordinary/Provider/integration tests, docs/NOTICE, tidy and module verification, then lint correctly identified G306/G302 on two intentionally unsafe permission fixtures. Narrow `nolint:gosec` comments were added only at those negative-test modes; scoped lint returned zero issues and focused race stayed green. Restarted current `make ci` passed from the beginning through lint 0, complete race, four fuzz targets, zero called/imported vulnerabilities, actionlint, and all four cross-builds. Ordinary docscheck passes; final-only release audit intentionally exits nonzero with exactly 15 nonterminal Stage 6 rows and 12 unchecked exits. No behavior, permission check, assertion, timeout, workflow, or production boundary was weakened |
| M6.2 REL-002 saved-workspace completion exact-SHA Hosted classification | implementation SHA `935aa3162eaefebaf8f34426b861b2f5cbb6ca5f`, tree `4beeaa6bb409ace2cd705489f8e1bc061f97a9cd`; push [29674313327](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674313327), attempts 1–2; PR [29674314208](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674314208), attempts 1–2 | Each attempt 1 finished 22 success / 1 failure / 1 dependency-skipped. Push oldstable Ubuntu 22.04 [88158683968](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674313327/job/88158683968) hit existing `TestSameExecutableIdentityRejectsSameInodeRewrite`: the equal-size rewrite plus restored mtime did not expose a changed ctime on that runner, while exact-SHA PR companion [88158686632](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674314208/job/88158686632) passed. PR native macOS Intel [88158686627](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674314208/job/88158686627) hit existing `TestRunLocalCommandReportsNonzeroAndCancellation` when its 100 ms context expired before process start; exact-SHA push companion [88158683936](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674313327/job/88158683936) passed full race, installed lifecycle, Homebrew, native security, PTY, and completion smoke. With no code, assertion, timeout, fixture, workflow, or product-policy change, only each failed chain was requested for one rerun; push oldstable [88159455125](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674313327/job/88159455125) and PR native Intel [88159456740](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674314208/job/88159456740) passed, and both attempt 2 workflows finished 24/24. Successful push quality [88158683957](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29674313327/job/88158683957) and all four native jobs log the installed `completion __workspaces` query, absent-root no-create check, exact `release-smoke` fixture result, and generated bash/zsh/fish wiring before daemon/database creation. Classification: **existing filesystem identity timestamp and local-command start-timing instability, not a completion, workspace, archive, native-install, man, or workflow regression**. These runs verify REL-002; they do not create an exact-RC record, final public channel, or open Production Helper/Level 2 |
| M6.3 REL-011/012 daemon compatibility RED/GREEN and focused gates | implementation SHA `c9bef69c0f2e11af2b3c4b65ac7df20f8b1d7e4f`, tree `bee3b39761450f62122985d4530b6bdcb490b381`; exact current Go 1.26.5 and exact oldstable Go 1.25.12; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent temp/cache roots | RED proved both ordering defects: daemon `start` reported success and invoked the starter after incompatible and unhealthy probe failures, while an incompatible future-major hello allocated one runtime session before rejection. A separate docscheck RED found all seven fail-closed/rollback contract phrases absent. GREEN centralizes probe-error rejection before all daemon subcommands, closes any partial client, preserves stable network machine output including remote `protocol_incompatible`/`never`/`none`, and permits startup only after a successful absent result. `ServeConn` now reads and accepts hello, clears the handshake deadline, and only then allocates session/request state. A real owner-private Unix Socket kept a `0.9.0-stage5` compatible client connected across a raw future-major rejection; that client called `ProviderEndpoints` again and `os.SameFile` proved the control Socket was not replaced. Upgrade and compatibility docs now prohibit forced Socket deletion/replacement and require verified shutdown before rollback. Focused current ordinary/race and exact oldstable app/daemon/docscheck suites pass. The first three-way focused gate exposed only a test-fixture typed-nil interface panic in the new absence-uncertain case; returning a true nil interface corrected the fixture, and all three unchanged product gates passed |
| M6.3 REL-011/012 daemon compatibility complete local gates | exact current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent temp/cache roots | The first oldstable full gate passed fmt/vet and every changed app/daemon/docscheck contract, then correctly rejected the manually extended committed compatibility Markdown because its owning `Markdown()` renderer still emitted the prior text. Adding the identical daemon paragraph to that sole renderer restored generated-reference parity; focused current/oldstable compatibility/docscheck passed and the restarted full oldstable gate passed all ordinary/Provider/integration tests, docs/NOTICE, tidy and both module verifications. Current `make ci` then passed from the beginning: all ordinary/Provider/integration tests, docs/NOTICE/tidy/verify, lint with zero issues, complete race, four fuzz targets, zero called/imported vulnerabilities, actionlint and four cross-builds. Ordinary docscheck passes; final-only release audit intentionally remains exactly 15 nonterminal Stage 6 rows and 12 unchecked exits. No behavior, assertion, timeout, workflow, or compatibility boundary was weakened |
| M6.3 REL-011/012 daemon compatibility exact-SHA Hosted classification | implementation SHA `c9bef69c0f2e11af2b3c4b65ac7df20f8b1d7e4f`, tree `bee3b39761450f62122985d4530b6bdcb490b381`; push [29675968632](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675968632), attempt 1; PR [29675970097](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675970097), attempts 1–2 | Push passed 24/24 on its first attempt. PR attempt 1 finished 20 success / 2 failure / 2 dependency-skipped. Quality [88163381596](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675970097/job/88163381596) completed check/lint/fuzz/supply-chain/package generation, then its existing Stage 5 rollback lifecycle kept the old process alive without observing `old_running` inside the 200×50 ms window; exact-SHA push quality [88163377167](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675968632/job/88163377167) passed the same lifecycle. PR native Ubuntu 22.04 [88163381621](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675970097/job/88163381621) passed ordinary/race and reached the existing hosted Stage 1 recovery harness, which timed out waiting 30 seconds for a replacement daemon; exact-SHA push companion [88163377191](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675968632/job/88163377191) passed. With no code, assertion, timeout, fixture, workflow, or product-policy change, only those two failed chains were rerun once. Attempt 2 quality [88164281643](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675970097/job/88164281643), native Ubuntu 22.04 [88164281730](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675970097/job/88164281730), dependent auth [88164682223](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675970097/job/88164682223), and compare [88164282200](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29675970097/job/88164282200) passed, yielding 24/24. Classification: **existing daemon readiness/replacement observation timing, not a fail-closed start, handshake/session, concurrent-client, Socket-identity, rollback-documentation, or REL-011/012 regression**. These runs validate this implementation slice; they do not complete the final native Helper matrix, published rollback rehearsal, residual-Socket recovery, exact-RC record, public channel, or Production Helper/Level 2 gates |
| M6.3 extension-free SFTP v3 compatibility RED/GREEN and complete local gates | exact current Go 1.26.5 and exact oldstable Go 1.25.12; canonical trusted cache/temp/build/coverage roots | Initial behavior RED showed that the package's default in-memory server advertised `hardlink@openssh.com`, so it could not prove a no-extension boundary; unsafe publication consequently succeeded. GREEN isolates the virtual server in a child test process, disables every OpenSSH extension there without contaminating global test state, and proves three related slices: Level 0 list/stat/read/download with read-only capability exposure; unsupported/EffectNone no-replace publication with exact source preservation and absent destination; invalid UTF-8 filename bytes preserved across actual SFTP wire list→stat→read and payload. The docs RED then failed only on absent generated-matrix and feature evidence. Both batch-end invocations passed every ordinary/Provider/integration test before docs-check correctly rejected `PLAT-006` without a separate `PLAT-006 | PASS` evidence row; adding that exact row made current/oldstable docs/NOTICE/tidy/verify pass. Current lint then found only introduced G204 on `os.Executable`; a line-scoped exact-test-binary explanation cleared lint with zero issues. The not-yet-run full race, four fuzz smokes, govulncheck, actionlint, and four builds all passed. No product behavior, assertion, timeout, global extension state, or support boundary was weakened. PLAT-006 is `Verified`; PLAT-005/REL-006 remain open for native vendor evidence; Production Helper/Level 2 remain CLOSED. |
| M6.3 native Linux arm64 CI contract RED/GREEN and complete local gates | exact current Go 1.26.5 `make ci` and exact oldstable Go 1.25.12 `make check`; `GOTOOLCHAIN=local`, canonical `umask 0022`, trusted persistent cache/temp/build/coverage roots | RED increased the canonical CI producer contract from 23 to 24 legs and 46 to 48 downloaded provenance files, then failed because the native matrix and manifest lacked `ubuntu-24.04-arm`. Separate negative cases remove the arm leg or forge its recorded runner/Go identity. GREEN adds only the real GitHub-hosted ARM runner to the existing native matrix, so it inherits the unchanged ordinary/contract/race, source/install identity, daemon/Job/doctor, uninstall/state-retention, ext4/XFS, kernel-security, and PTY lifecycle. The aggregator now requires `native-ubuntu-24.04-arm`, exact `runner_os=Linux`, `runner_arch=ARM64`, `go_env_goos=linux`, and `go_env_goarch=arm64`; all other canonical leg order, producer isolation, dirty-tree rejection, and artifact comparison contracts remain unchanged. The first oldstable full gate correctly rejected a manual generated-environment Markdown edit; moving the identical Linux arm64 evidence into the owning compatibility registry restored generated parity, and the restarted oldstable gate passed every ordinary/Provider/integration test, docs/NOTICE, tidy, and module verification. The first current gate passed all changed docscheck/compatibility contracts but hit the previously recorded same-inode executable rewrite fixture when equal-size bytes plus restored mtime exposed no changed ctime. An unchanged focused rerun passed; the restarted current gate then passed ordinary/Provider/integration tests, docs/NOTICE/tidy/verify, lint with zero issues, complete race, four fuzz targets, zero called/imported vulnerabilities, actionlint, and all four cross-builds. No code, assertion, timeout, workflow safety, or product boundary was weakened. Exact-SHA Hosted push/PR are required before accepting this batch. PLAT-003/REL-006 remain `In Progress`; final protected bytes/channel, native vendor SFTP, macOS Kerberos, and Production Helper/Level 2 remain open. |
| M6.3 native installed daemon upgrade/rollback RED/GREEN and focused gates | exact current Go 1.26.5 and exact oldstable Go 1.25.12 app/docscheck suites; exact native workflow script extracted from `.github/workflows/ci.yml`; canonical `umask 0022` and trusted persistent roots | Product RED failed to compile because regular client autostart had no independent Socket-path absence proof. GREEN permits the starter only after `Lstat` returns `ErrNotExist`; an existing or uninspectable path returns the preserved connection/inspection failure without deletion or startup. Workflow-policy RED failed because native CI had no composed installed upgrade/rollback/residual-Socket proof. GREEN adds the same lifecycle to all five native runners: build exact frozen Stage 5, atomically install it, observe it with current read-only clients, replace binary while the old daemon remains live, stop the verified old PID, start current, SIGKILL current, prove the residual path remains a Socket and status fails, recover current on the same path, roll back to Stage 5 on v4 state, prove explicit Job unavailability plus unchanged database SHA-256, then restore current. The exact YAML block passed locally on Linux. Four earlier harness iterations exposed historical CLI/RPC differences, Linux inode reuse, and Stage 5's missing Job CLI; each correction changed only the harness to use the current read-only client or behavior-based proof. Current and oldstable focused suites pass. No timeout, product assertion, Socket deletion, database mutation, support boundary, or Production Helper/Level 2 policy was weakened; exact-SHA Hosted gates remain pending. |
| M6.3 native installed daemon upgrade/rollback complete local gates | exact current Go 1.26.5 check/lint/race/fuzz/supply-chain/actionlint/four builds; exact oldstable Go 1.25.12 `make check`; canonical `umask 0022`, trusted persistent temp roots, and external cache/build/coverage roots | The first oldstable full gate exposed a real interaction with the new fail-closed rule: the PTY-spawned daemon inherited the client session and died at terminal exit, leaving the Socket that earlier launches silently replaced. A dedicated RED failed to compile without daemon spawn isolation. GREEN applies `Setsid` to autostart, and the PTY harness now uses authenticated shutdown; the formerly failing oldstable integration and restarted full oldstable gate pass. Current attempt 1 used an untrusted `/data00` Go temp ancestor and therefore failed the existing owner-chain guard before its config assertion; after moving only `GOTMPDIR` to the trusted root, ordinary check passed. The same discarded invocation also hit nondeterministic local filename-search traversal order; an unchanged focused rerun passed. Current check then reached lint, whose sole G304 finding was the new test reading its exact test-owned path; a line-scoped explanation produced lint 0. The already-passed check was not repeated after that comment: complete race, four fuzz smokes, zero called/imported vulnerabilities, actionlint, and all four builds then passed. No production behavior, assertion, timeout, ordering contract, workflow, or safety boundary was weakened. |
| M6.3 native installed daemon upgrade/rollback exact-SHA Hosted classification | implementation SHA `5b72101b0d02df7530566079f39e34fdf74f17d3`, tree `c409a84d69dcc1067697cd43e1012cd7caf9389d`; push [29681590469](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29681590469), attempts 1–2; PR [29681591589](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29681591589), attempt 1 | PR passed 25/25 on its first attempt. Push attempt 1 finished 21 success / 2 failure / 2 dependency-skipped. ARM native [88178588355](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29681590469/job/88178588355) hit the existing Helper stderr-overflow reader race before the new lifecycle, while same-SHA PR ARM [88178591559](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29681591589/job/88178591559) passed the same test and complete job. Push quality [88178588381](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29681590469/job/88178588381) kept the frozen Stage 5 process alive without observing `old_running` in the existing 200×50 ms readiness loop; PR quality [88178591535](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29681591589/job/88178591535) passed the same preview lifecycle. With no code, assertion, timeout, fixture, workflow, or product-policy change, only those failed chains were rerun once. Attempt 2 quality [88179311365](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29681590469/job/88179311365) and ARM native [88179311417](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29681590469/job/88179311417) passed, and the run finished 25/25. The new installed upgrade/rollback/stale-Socket step passed in all five push native jobs and all five PR native jobs. Classification: **existing Helper reader and Stage 5 readiness timing instability, not an autostart, session isolation, installed upgrade/rollback, Socket recovery, database-byte, or workflow-policy regression**. This verifies the implementation batch but does not complete published-channel rollback, Helper-too-new, final native Helper combinations, the exact-RC record, or Production Helper/Level 2. |
| M6.3 daemon stop postcondition and future Helper matrix RED/GREEN | implementation SHA `3bfbd174a7cf264847d8d3bf0ff6e0e5ad62aff0`, tree `ed06f02d8e3b4cc7293e9467cba2ba8ca449e19f`; exact current Go 1.26.5 and oldstable Go 1.25.12 | RED daemon tests failed to compile without an explicit teardown waiter and readiness predicate. GREEN closes the authenticated client before waiting, publishes no success until the Socket is absent and the exact instance lock is reacquirable, retries only a live Socket/held lock, and rejects uninspectable or non-Socket replacement paths without removal. A real daemon proves `stop` returns with the lock immediately available. The Helper RED accepted every unimplemented future child mode; GREEN uses real OpenSSH child processes to prove current v4 success, v5 minimum-client rejection, protocol-v2 rejection, exact-manifest rejection of a protocol-compatible v5 artifact, and current-v4 recovery after each failure. Focused app/helper packages pass. |
| M6.3 transition-hardening complete local and exact-SHA Hosted gates | implementation SHA `3bfbd174a7cf264847d8d3bf0ff6e0e5ad62aff0`, tree `ed06f02d8e3b4cc7293e9467cba2ba8ca449e19f`; documentation SHA `80b0689b1794ce75607fdcf6580ba713e183a16f`; exact current Go 1.26.5 `make ci`; exact oldstable Go 1.25.12 `make check`; push [29683308347](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29683308347), attempts 1–2; PR [29683309618](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29683309618), attempt 1 | Initial parallel invocations were invalid environment attempts: `AMSFTP_TEST_ROOT` was not the repository's `AMSFTP_TEST_PERSISTENT_ROOT`, so current/oldstable shared fixtures, and interactive `umask 0077` neutralized negative 0755/0644 permission fixtures. With corrected environment and serial execution, current passed the complete CI gate. Oldstable had one existing same-inode rewrite ctime miss; unchanged current and oldstable focused tests each passed 20/20, and the zero-change full oldstable rerun passed. PR passed 25/25 on attempt 1. Push attempt 1 failed only existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow` and Stage 2 PTY delete-while-loading fixtures; their same-SHA PR companions passed. With no code, assertion, timeout, workflow or product-policy change, only the two failed chains were rerun once and push attempt 2 passed 25/25. Classification: **existing Helper stderr visibility and PTY loading-selection timing, not a daemon-stop, future-Helper, ARM64 native, or transition-hardening regression**. Production Helper/Level 2 remain CLOSED. |
| M6.3 real ProFTPD vendor SFTP RED/GREEN and pre-Hosted gates | current working tree before exact candidate SHA; Ubuntu 24.04 auth-integration contract; exact current Go 1.26.5 and oldstable Go 1.25.12 | Five related RED contracts failed on the absent ProFTPD packages/version capture, pre-mutation binding, real provider/preview harness, and provenance workload profile. GREEN installs `proftpd-core` plus `proftpd-mod-crypto`, captures exact `proftpd -v` and sorted package versions, and byte-compares them before creating a reserved user or starting the isolated localhost service. The harness uses system OpenSSH public-key authentication, direct SFTP marker preflight, the production SFTP Provider and Planner/Worker for extension-gated upload/download, then the exact internal-preview binary under a TUI observer. Cleanup is restricted to its validated `/tmp/amsftp-vendor-sftp-*` root, exact server PID and test-owned user/config restoration. Focused docscheck and environment-gated integration suites, complete docscheck, fixed actionlint, shell syntax and diff checks pass. The first current full invocation was an invalid environment attempt because the configured persistent root itself had not been created. The corrected invocation passed ordinary/Provider/integration, docs/NOTICE/tidy/verify and stopped only when lint requested a narrow G304 explanation for reading the exact test-owned download path; the final current `make ci` passed from the beginning through lint 0, full race, four fuzz targets, vulnerability/actionlint gates and all four builds. Exact oldstable `make check` passed from the beginning. Exact-head Hosted remains required; no native vendor support claim is accepted yet, and Production Helper/Level 2 remain CLOSED. |
| M6.3 real ProFTPD first exact-head Hosted diagnostic | implementation SHA `66d741884f7a5a56d8bfa0c3de454897a459507e`, documentation SHA `56ae5b24b486d38cb3072453b5310ef447e14e5d`, tree `67b2f4c5b3cd2f1997634ca014362ba76541985a`; push [29684461175](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29684461175), PR [29684462253](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29684462253) | Both auth jobs captured exact `ProFTPD Version 1.3.8b` with `proftpd-core=1.3.8.b+dfsg-1ubuntu0.1` and `proftpd-mod-crypto=1.3.8.b+dfsg-1ubuntu0.1`, passed byte binding before mutation, direct SFTP marker preflight, and the production Provider/Planner/Worker durable upload/download. Both then exited 92 only because the new raw Expect observer did not set the fixed PTY geometry used by the existing auth/Kerberos TUI harness and retained no screen/stderr diagnostic. This is a deterministic observer defect, not accepted vendor/TUI evidence. The correction keeps the 35-second bound, adds the existing 30×200 PTY contract plus bounded output/server diagnostics, and requires a new exact-head dual-workflow pass; no product code, assertion, server/provider path or support boundary is weakened. |
