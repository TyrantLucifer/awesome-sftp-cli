# Stage 6 Verification Record

- **Status**: In Progress — M6.1 complete; M6.2 historical fixtures captured and locally green, exact-SHA Hosted gate pending
- **Updated**: 2026-07-18
- **Repository root**: `/data00/home/tianchao.thatcher/projects/awsome-sftp-cli`
- **Branch**: `codex/stage6-hardening-release`
- **Delivery PR**: Draft [#6](https://github.com/TyrantLucifer/awsome-sftp-cli/pull/6), title `feat: ship AMSFTP 1.0.0`, base `main`; remains Draft until final gates
- **Sole exact-main baseline**: commit `312bcccbcbd54246bbe5ff9babf4f14560449176`, tree `e0316c286ce11512cb0b92c917fa29b80f9e3305`
- **Baseline Hosted run**: [29579514879](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29579514879) — exact main commit, completed `success`, 24/24 jobs successful
- **Production Helper distribution / production Level 2**: **CLOSED**
- **Final release/tag/Homebrew state**: not created

This ledger records only observed evidence. Planned commands and acceptance ownership live in the [Stage 6 execution plan](../stages/06-hardening-release-plan.md); a planned gate is not evidence.

## Initial safety checkpoint

| Check | Result |
|---|---|
| exact baseline identity | PASS: local `main`, `origin/main`, and remote `refs/heads/main` all resolved to `312bcccbcbd54246bbe5ff9babf4f14560449176`; tree `e0316c286ce11512cb0b92c917fa29b80f9e3305` |
| fixed branch/tag audit | PASS: remote Stage 6 branch and `v1.0.0` tag were absent before branch creation; `codex/stage6-hardening-release` was created once from the sole baseline |
| exact-main Hosted run | PASS: run `29579514879` completed successfully at the exact baseline SHA; 24 of 24 jobs succeeded |
| baseline local CI | PASS: CI-equivalent `make ci` completed unit/contract/integration, docs, tidy/verify, lint, full race, four fuzz smokes, `govulncheck`, `actionlint`, and darwin/linux × arm64/amd64 builds |
| baseline worktree | PASS: index/worktree clean before branch creation and no product files changed before the baseline gate |
| production credentials | ABSENT by design: no production Helper offline key/custody ceremony or final Developer ID/notary evidence is available; no substitute material was generated |

The local baseline command used the installed SDK at `/data00/home/tianchao.thatcher/sdk/go1.25.7/bin`, `umask 0022`, root-owned persistent fixture root `/var/lib/amsftp-tests/1001`, and external build/coverage directories. It establishes that the frozen baseline is green in this environment; it is not the final required Go 1.26.5 or exact Go 1.25.12 release evidence.

## Canonical baseline command

```sh
env PATH=/data00/home/tianchao.thatcher/sdk/go1.25.7/bin:$PATH AMSFTP_TEST_PERSISTENT_ROOT=/var/lib/amsftp-tests/1001 BUILD_DIR=/tmp/amsftp-stage6-baseline/build COVERAGE_DIR=/tmp/amsftp-stage6-baseline/coverage sh -c 'umask 0022; make ci'
```

Result: **PASS** with exit status 0 on the untouched exact-main tree.

## CI failure classification contract

Every failed job is recorded with exact SHA/tree, workflow/run/job, platform image, failing command/test, retained logs/artifacts, same-SHA companion result, rerun result, and reproducibility. Classification rules:

- **Code**: reproducible locally or on another same-SHA leg; changed Stage 6 area is implicated; assertion/security/compatibility contract fails; or available evidence is ambiguous.
- **Environment/known fixture**: the same SHA passes a comparable companion, logs identify a runner/service/timing condition outside changed behavior, and a targeted no-code/no-assertion/no-timeout-change rerun passes.
- **Infrastructure**: setup, service, quota, network, or runner failure occurs before the product/test contract executes and is confirmed by same-SHA evidence.

Only a subsequent complete exact-candidate matrix can become final release evidence. Reruns do not erase the original failure or its classification.

### Initial Draft-PR CI classification

Plan-only commit `59c0d2003e41a6ec798fc696bcffcd4d72526622` produced failed push run [29581551106](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29581551106) and PR run [29581560324](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29581560324) on attempt 1. The failures were outside the documentation-only change:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| push auth-integration | Stage 1 replacement-daemon wait timed out after the OpenSSH auth cases passed | PR auth-integration passed | push attempt 2 passed |
| push oldstable Ubuntu 24.04 | existing remote-command cancel fixture reached start deadline before command bytes | PR oldstable Ubuntu 24.04 passed | push attempt 2 passed |
| push native Ubuntu 22.04 | existing Level 2 in-flight cancel result timing fixture | PR native Ubuntu 22.04 passed | push attempt 2 passed |
| PR oldstable macOS 15 Intel | existing Helper exact-stderr-cap reader observed zero bytes | push oldstable macOS 15 Intel passed | PR attempt 2 passed |

Both failed-only attempt 2 reruns completed `success` without code, assertion, timeout, or workflow changes. Classification: **environment/known timing fixtures, not introduced code**. Attempts 1 remain preserved and neither attempt is final release evidence.

### M6.1 public-interface CI classification

Public-interface commit `51b7cfc2b5c4c3ce9c6989bb482564d1b096f603` produced successful push run [29582457142](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29582457142) and failed PR run [29582459680](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29582459680) on attempt 1. The PR failures were confined to existing macOS timing fixtures:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| PR oldstable macOS 15 | existing Helper exact-stderr-cap reader observed zero bytes; the existing Stage 2 PTY fixture then observed a transient empty selection before delete confirmation | push oldstable macOS 15 job `87891201150` passed | attempt 2 superseded/cancelled by newer PR SHA before execution |
| PR native macOS 15 | existing Helper heartbeat-termination test observed process termination before the client failure became visible | push native macOS 15 job `87891201232` passed | attempt 2 superseded/cancelled by newer PR SHA before execution |

Classification: **environment/known timing fixtures, not introduced code**. The same SHA passed both directly comparable push jobs, Linux native/oldstable legs, quality, integration, reproducibility, and all changed app/config/docs packages; the failing tests do not exercise the new config/help/man/completion behavior. The first failed-only rerun request was cancelled by repository PR concurrency after `01a7b0b` was pushed, so that cancellation was retained as attempt 2 and did not count as evidence. A later no-change attempt 3 reran native macOS 15 job `87897122811` and oldstable macOS 15 job `87897122849`; both passed, followed by successful compare job `87897962800`, without code, assertion, timeout, or workflow changes. Attempts 1–3 remain preserved and none is final release evidence.

Keymap commit `01a7b0b17bf9fc4fe906ed94a82447a7918eb977` then produced successful push run [29582955855](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29582955855) and failed PR run [29582958715](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29582958715) on attempt 1. The sole failure was oldstable macOS 15 Intel job `87892873657`: existing `TestLocalTailDetectsTruncateAndRotateAsHints` completed its 140 ms polling window with no notice or chunk. Directly comparable same-SHA push job `87892849850` passed, as did every other job and all changed packages. Classification before rerun: **environment/known Helper tail-polling timing fixture, not introduced code**. Failed-only same-SHA attempt 2 reran job `87895838476` and completed `success` without code, assertion, timeout, or workflow changes, confirming that classification. Attempt 1 remains preserved; neither attempt is final release evidence.

Cache/transfer commit `55e2d521fb3d90a877e586719d3c123e9ce8374b` produced failed push run [29584976904](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29584976904) on attempt 1 while the same-SHA PR run [29584980364](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29584980364) passed each directly comparable job:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| push quality | existing Stage 3 foreground-PTY fixture observed an empty remote shell cwd marker | PR quality job `87899619189` passed | attempt 2 passed check/lint, then `FuzzNormalizePath` reported `context deadline exceeded` at the 1-second fuzz deadline before reaching the PTY step; attempt 3 job `87902781998` passed |
| push oldstable macOS 15 | existing Helper stderr-hard-cap process fixture received the expected process failure as a closed handshake/read pipe before its overflow classification | PR oldstable macOS 15 job `87899619230` passed | attempt 2 instead hit the existing Helper heartbeat-termination visibility fixture; attempt 3 job `87902782317` passed |
| push native Ubuntu 24.04 | existing weighted-round-robin timing fixture reached its 3-second observation point before the second grant | PR native Ubuntu 24.04 job `87899619214` passed | attempt 2 job `87901591208` passed |
| PR native macOS 15 Intel | existing Helper heartbeat-termination fixture observed process termination before the client failure became visible | push native macOS 15 Intel job `87899607669` passed | attempt 2 job `87901596887` and compare job `87902963047` passed |

The exact same tree also passed local current-toolchain `make ci` and exact-oldstable `make check`. Final classification: **environment/known PTY, process-reader, Helper heartbeat, scheduler, and fuzz-deadline timing fixtures, not introduced code**. No code, assertion, timeout, or workflow change was made in response. Failed-only same-SHA push attempt 2 confirmed the original Ubuntu scheduler failure as timing-only but encountered the two different known timing fixtures recorded above; push attempt 3 then completed `success`. PR attempt 2 also completed `success`, confirming its heartbeat classification. Attempts 1–3 remain preserved and none is final release evidence.

Preview-config commit `f931f76cbd0925acabc108f1b70a416fc82943a6`, tree `fdaa0e29a678023a68be8833aed5accf9a318792`, produced failed push run [29586241010](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29586241010) on attempt 1 while the same-SHA PR run [29586244438](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29586244438) completed `success`. Push macOS 15 Intel job `87903826466` alone failed in the existing `TestLevel0FilenameSearchCancellationRetainsResultsAndReportsCanceled` race fixture after its two-second cancellation observation did not reach the provider. The directly comparable PR macOS Intel job `87903837630` passed, as did push quality, Ubuntu native, oldstable, all preview/config/app coverage and every other job. Failed-only attempt 2 job `87905078050` passed ordinary search but instead hit the existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` timing fixture, observing process termination before client failure visibility. No-change attempt 3 macOS Intel job `87905751697` then passed ordinary tests, race, build and native smoke, and the entire attempt completed `success`. Final classification: **environment/known search-cancellation and Helper-heartbeat timing fixtures, not introduced code**. Preview configuration does not touch either failing path, and no code, assertion, timeout, or workflow change was made in response. Attempts 1–3 remain preserved and none is final release evidence.

Search-config commit `43c336102e8b32e5d2a153bb4f8912d71c887d3b`, tree `4d4361b6d66a5ea4409f445ac15027d9f6253a86`, produced failed push run [29587283734](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29587283734) on attempt 1 while the same-SHA PR run [29587287634](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29587287634) completed `success`. Push Ubuntu 22.04 native job `87907297497` alone failed in the existing `TestTransferSchedulerWeightedRoundRobinNeverStarvesBulk` three-second observation window after recording three rather than four granted bytes and three rather than two waiters. The commit did not change transfer or scheduler code; its search/config/app packages passed in the failed job, and directly comparable PR Ubuntu 22.04 job `87907311590` passed. Failed-only no-change attempt 2 job `87909139504` then passed the scheduler fixture, full race, build and native smoke, and the entire attempt completed `success`. Final classification: **environment/known weighted-round-robin timing fixture, not introduced code**. No code, assertion, timeout, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Retry-config commit `6bc45302c11313f6302160b4574d40d8d664f8a5`, tree `8fba34c6e1cf6f4bcab8911d97961a6e94974d35`, produced successful push run [29588219723](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29588219723) and failed PR run [29588221737](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29588221737) on attempt 1. PR Ubuntu 22.04 native job `87910423007` alone failed when existing `TestOpenSSHBindingProbeRejectsBannerAndStderrOverflow/stderr_overflow` observed the short-lived helper probe succeed before the stderr hard-cap reader surfaced overflow; directly comparable same-SHA push job `87910415665` passed. No-change attempt 2 job `87912261052` passed that original Helper fixture and ordinary tests, then failed under full race when existing `TestSameExecutableIdentityRejectsSameInodeRewrite` observed a same-inode overwrite retain the prior executable identity. The retry commit did not touch Helper or platform identity code, all changed retrypolicy/config/app/transfer packages passed, and the directly comparable push job had already passed both phases. No-change attempt 3 Ubuntu job `87913207665` passed ordinary tests, full race, builds, native filesystem/security/PTY smokes and provenance; compare job `87914056910` and the complete run then finished `success`. Final classification: **environment/known Helper process-reader and filesystem identity timing fixtures, not introduced code**. No code, assertion, timeout, or workflow change was made in response. Attempts 1–3 remain preserved and none is final release evidence.

Integrity/direct-closure commit `e74fa5966573cbf21d7e5b5795591a0c83c8859b`, tree `46335f00568ce65b2aa3af491e032ca916b04acb`, produced successful push run [29589409774](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29589409774) and failed PR run [29589410924](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29589410924) on attempt 1:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| PR oldstable macOS 15 Intel job `87914446885` | existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process termination before client-failure visibility | push oldstable macOS 15 Intel job `87914442988` passed | attempt 2 job `87916471504` passed |
| PR native macOS 15 job `87914446915` | existing Stage 2 PTY delete fixture sent delete while the listing was still loading, then observed `delete requires at least one target` instead of the confirmation | push native macOS 15 job `87914442991` passed | attempt 2 job `87916471509` passed ordinary tests, full race, build and native smokes |

All changed transfer/config/app packages passed in the original failed jobs. Failed-only attempt 2 used the same SHA with zero code, assertion, timeout, or workflow changes; both original fixtures passed, compare job `87916473397` passed, and the complete rerun finished `success`. Final classification: **environment/known Helper-heartbeat and PTY listing/selection timing fixtures, not introduced code**. Both attempts remain preserved and neither is final release evidence.

Diagnostic-config commit `cfff95390209616d25764d059b09d3610f91e2a7`, tree `0fd0b698b059d6a5bf2aadd3485831d78b533c2e`, produced successful push run [29590345136](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29590345136) and failed PR run [29590349058](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29590349058) on attempt 1. PR native macOS 15 Intel job `87917578113` alone failed in existing `TestLevel2InFlightCancelPropagatesAndNeverCommitsOrDeletesSource`: cancellation was reported and the part remained, but the expected terminal result combination was not visible at the fixture's observation point. The directly comparable same-SHA push native macOS 15 Intel job `87917565910` passed its complete ordinary/race/native sequence. The diagnostic/config/app changes did not touch Level 2 cancellation, and their tests passed in the failed job. Failed-only attempt 2 reran native macOS 15 Intel job `87919577526`; the original test, complete race, build and native smokes passed, compare job `87921027698` passed, and the run completed `success`. Final classification: **environment/known Level 2 cancellation-result timing fixture, not introduced code**. No code, assertion, timeout, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Effective-keymap/reset commit `4150814a3e19a81f2254095b426da19051c762a9`, tree `bb3ce60e5c02fc9d55aa0b9dd014167c21b25a7c`, produced successful PR run [29591483906](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29591483906) and failed push run [29591479558](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29591479558) on attempt 1:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| push native macOS 15 Intel job `87921375855` | existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process termination before client-failure visibility under race | PR native macOS 15 Intel job `87921391118` passed ordinary tests, full race, build and native smokes | attempt 2 passed the original fixture but job `87922997291` instead observed zero of the expected exact 65536 stderr bytes in existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap`; attempt 3 job `87924709260` passed |
| push native Ubuntu 24.04 job `87921375990` | existing Stage 3 foreground-PTY fixture observed an empty shell cwd marker | PR native Ubuntu 24.04 job `87921391063` passed | attempt 2 job `87922997471` passed |

All changed keymap/config/app packages passed in the failed jobs. Attempt 2 proved both original failures non-reproducible but encountered the separate known Helper process-reader visibility failure above; attempt 3 then passed complete ordinary/race/native execution and compare job `87925752301`, and the run completed `success`. Final classification: **environment/known Helper heartbeat, Helper stderr-reader, and foreground-PTY timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Attempts 1–3 remain preserved and none is final release evidence.

Configuration-policy/Helper-closure commit `a2a0503f319ec078f415753a3f2e6a2a631d8ca7`, tree `ccbeaabb0d17c3fa59ee08920ddbbfe1833d6edd`, produced successful PR run [29592906123](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29592906123) and failed push run [29592902733](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29592902733) on attempt 1. Push oldstable macOS 15 Intel job `87926142341` alone failed when existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap` observed zero of the expected 65,536 diagnostic bytes. The directly comparable same-SHA PR job `87926155119` passed, as did every other push job and all changed helper/config/app packages. Failed-only no-code attempt 2 job `87927818502` passed the original fixture and the run completed `success`. Final classification: **environment/known Helper stderr-reader visibility timing fixture, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Fixed-quit/keymap-to-reducer commit `0beb36836855ced9dd89f2ebbcfa2587aec48d75`, tree `3215602fd5a58a5ffc7532635d740749415d979b`, produced successful PR run [29615547368](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29615547368) and failed push run [29615545218](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29615545218) on attempt 1. Push native macOS 15 job `87999733032` alone failed during full race: existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process exit before client-failure visibility, and the existing Stage 2 PTY fixture sent delete while the listing/selection was not ready and observed `delete requires at least one target`. Directly comparable same-SHA PR native macOS 15 job `87999739248` passed both ordinary and race phases; the commit changes only fixed Normal-mode `q` keymap validation and an integrated keymap-to-reducer regression, not Helper heartbeat or Stage 2 listing/delete behavior. Failed-only no-change attempt 2 job `88000691806` passed, and both the PR run and rerun completed with all 24 jobs successful. Final classification: **environment/known Helper-heartbeat and PTY listing/selection timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Durable Job CLI commit `bf60e8c02e335063cb1d7f5c272ba8adca131300`, tree `08e0d169cd2eef8cb9bd954e4c046537c0540f44`, produced successful PR run [29616649509](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29616649509) and failed push run [29616646032](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29616646032) on attempt 1:

| Failed leg | Observed failure | Same-SHA companion | No-change rerun |
|---|---|---|---|
| push native Ubuntu 22.04 job `88003082785` | existing `TestStage3LocalShellForegroundPTY` read an empty shell cwd marker during full race | PR native Ubuntu 22.04 job `88003093080` passed ordinary tests, full race, build, and native smokes | attempt 2 job `88004337455` passed |
| push quality job `88003082786` | existing one-second `FuzzNormalizePath` smoke ended with `context deadline exceeded` after the other three fuzz targets passed | PR quality job `88003093079` passed complete check/lint/fuzz/supply-chain/integration | attempt 2 job `88004337387` passed |

The changed app package passed in the failed native race leg, and the quality leg passed check/lint before reaching its unrelated fuzz deadline. Both same-SHA PR companions passed; failed-only attempt 2 then completed all 24 jobs successfully without code, assertion, timeout, fixture, or workflow changes. Final classification: **environment/known foreground-PTY marker and fuzz-deadline timing fixtures, not introduced code**. Attempts 1–2 remain preserved and neither is final release evidence.

Daemon lifecycle CLI commit `60ab5528b66c1b90599f524576ab2d7919a5f536`, tree `c00ff7aa721d278b422f4a6e809662840dd9a339`, produced successful push run [29617804339](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29617804339) and failed PR run [29617806472](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29617806472) on attempt 1. PR native macOS 15 Intel job `88006553742` alone failed during `make test-race` when existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process termination before the client failure became visible. The directly comparable same-tree push native macOS 15 Intel job `88006547086` passed its complete ordinary/race/build/native-smoke sequence, and every changed daemon/app test passed in the failed PR job. Failed-only no-change attempt 2 job `88007628988` passed the original fixture, complete race, build, and native smokes, and the PR run completed `success`. Final classification: **environment/known Helper-heartbeat visibility timing fixture, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Final configuration-mapping commit `3d3dc307816005e1d969f2bd004cd7826ae07826`, tree `cd08a0f2d49dbda99f475ba9ed041c8b11948fe9`, produced successful push run [29618446376](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29618446376) and failed PR run [29618448078](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29618448078) on attempt 1. PR native macOS 15 job `88008434657` alone failed during `make test-race` when the same existing `TestOpenSSHProcessSessionHeartbeatFailureTerminatesProcessWithoutExplicitClose` observed process termination before client-failure visibility. The directly comparable same-SHA push native macOS 15 job `88008429465` passed, every changed config/app/daemon package passed in the failed PR job, and the change does not modify Helper process-session code. Failed-only no-change attempt 2 job `88009601950` passed the original fixture, complete race, build, and native smokes, and the PR run completed `success`. Final classification: **environment/known Helper-heartbeat visibility timing fixture, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Helper-status commit `fbb6f4bdf5806f526b99d5f2dfae9df668842941`, tree `db0c66d28f17e7101e76b441b7b2324fd65eb708`, produced failed push run [29619225477](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29619225477) and PR run [29619227237](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29619227237) on attempt 1. Push Ubuntu 22.04 job `88010724766` alone failed when existing `TestLevel0FilenameSearchCancellationRetainsResultsAndReportsCanceled` did not observe provider cancellation within two seconds; same-SHA PR job `88010730094` passed. PR macOS 15 Intel job `88010730081` failed in the existing Helper heartbeat-termination visibility fixture; same-SHA push job `88010724831` passed. PR macOS 15 job `88010730123` failed when the existing Stage 2 PTY fixture sent delete before its selection was visible; same-SHA push job `88010724808` passed. Failed-only no-change attempt 2 jobs `88011655125`, `88011668360`, and `88011668347` passed their full native sequences, both compare jobs `88012218917` and `88012249701` passed, and both workflows completed `success`. Final classification: **environment/known search-cancellation, Helper-heartbeat, and foreground-PTY timing fixtures, not introduced code**. The changed app tests passed in the failed legs; no code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved and neither is final release evidence.

Historical-source inventory commit `9e2ed6df9fc736d0d86fa5e6f70d3a0ef0146224`, tree `73a8b94974ffc98862f0d28243834acba5d999bc`, produced failed push run [29619828356](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29619828356) and PR run [29619830232](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29619830232) on attempt 1. Push quality job `88012483752` failed when existing `TestRunLocalCommandReportsNonzeroAndCancellation` reached its short context deadline before process startup; same-SHA PR quality completed successfully. PR oldstable Ubuntu 22.04 job `88012489821` failed when existing `TestLevel2InFlightCancelPropagatesAndNeverCommitsOrDeletesSource` returned the correct cancellation and retained part before its result enumeration became visible; same-SHA push oldstable Ubuntu 22.04 job `88012483773` passed. Changed compatibility tests passed in both failed jobs. Failed-only no-change attempt 2 push quality job `88013259504` and PR oldstable job `88013357273` passed, compare jobs `88013787138` and `88013531210` passed, and both workflows completed `success`. Final classification: **environment/known command-start and Level 2 cancel-result timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved; they are the exact M6.1 implementation Hosted evidence, not final release evidence.

M6.1 documentation-closeout commit `137899f047905e3eeac0f064bee1eaa057d21957`, tree `638323d97fe5b88eb165267fbeea8c0f6e9121d0`, produced successful push run [29620380123](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29620380123) and PR run [29620382502](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29620382502) after four preserved attempts. PR attempt 1 native macOS Intel job `88014098824` alone failed when existing `TestOpenSSHProcessSessionAcceptsExactStderrHardCap` observed zero rather than 65,536 diagnostic bytes; the same-SHA push counterpart passed. Failed-only attempt 2 job `88014887257` passed that fixture but reached the observation deadline in existing `TestLocalTailDetectsTruncateAndRotateAsHints` after already observing the truncate notice and new chunk but before the later rotate event. Attempt 3 job `88015178920` passed ordinary tests, full race, build, and native smokes, then artifact upload failed with GitHub infrastructure DNS error `ENOTFOUND`. Failed-only no-change attempt 4 native job `88016419565` and compare job `88017004406` passed, completing the workflow `success`. Final classification: **environment/known Helper reader and tail-polling timing fixtures followed by GitHub artifact-upload infrastructure failure, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. A subsequent required history audit found the malformed SQLite v3 provenance value, so this commit is superseded as M6.1 closeout evidence and is not final release evidence.

Provenance-correction commit `d4d524331b796ab408f292d0412e2d6728afacd0`, tree `2643255fe3cfc07bcd046af42b553c308face3ef`, produced failed push run [29621518452](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29621518452) and PR run [29621520034](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29621520034) on attempt 1. Push oldstable macOS Intel job `88017354931` observed the existing tail fixture's truncate notice and new chunk before its rotate observation deadline; same-SHA PR counterpart `88017359436` passed. Push native macOS Intel job `88017354962` observed process termination before existing Helper heartbeat client-failure visibility; same-SHA PR counterpart `88017359451` passed. PR oldstable Ubuntu 22.04 job `88017359412` observed the short-lived binding probe succeed before the existing stderr-overflow reader surfaced overflow; same-SHA push counterpart `88017355016` passed. The changed compatibility package passed with 100% coverage in all failed legs. Failed-only no-change attempt 2 push jobs `88018137111` and `88018137137`, PR job `88018137206`, and compare jobs `88018592289` and `88018263168` passed; both workflows completed `success`. Final classification: **environment/known Helper tail, heartbeat, and stderr-reader timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved; they are exact M6.1 correction evidence, not final release evidence.

M6.1 closeout commit `cb267a3b4adabf6f43fae51f7ed0a8f432d0aa53`, tree `c8db67def8f711535229416d6f575b47284056ab`, produced failed push run [29622036145](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29622036145) and PR run [29622037354](https://github.com/TyrantLucifer/awsome-sftp-cli/actions/runs/29622037354) on attempt 1. Push oldstable macOS Intel job `88018837330` observed the existing Level 2 cancellation result before its terminal combination became visible; push native Ubuntu 22.04 job `88018837400` reached the existing daemon-restart probe before the control socket appeared; push and PR native macOS Intel jobs `88018837420` and `88018840749` observed process termination before existing Helper heartbeat failure visibility. Same-SHA PR companions `88018840757` and `88018840725` passed the Level 2 and daemon-restart fixtures, while the unchanged parent code had already passed both native macOS legs on the provenance-correction rerun. The commit changed documentation only. Failed-only no-change attempt 2 jobs `88019590581`, `88019590587`, `88019590651`, and `88019590691` all passed; both workflows and their compare jobs completed `success`. Final classification: **environment/known Level 2 cancellation, daemon-startup, and Helper-heartbeat timing fixtures, not introduced code**. No code, assertion, timeout, fixture, or workflow change was made in response. Both attempts remain preserved; they close M6.1 but are not final release evidence.

## Milestone status

| Milestone | Status | Evidence |
|---|---|---|
| M6.1 configuration/keymap/public interfaces | Complete | provenance-corrected commit `d4d524331b796ab408f292d0412e2d6728afacd0`, tree `2643255fe3cfc07bcd046af42b553c308face3ef`; complete application schema/runtime mapping, versioned config/keymap/Job/daemon/Helper-status interfaces, shared help/man/completion, compatibility boundaries, and canonical 11-source historical inventory; current Go 1.26.5 `make ci`, exact Go 1.25.12 `make check`, push `29621518452`, and PR `29621520034` pass |
| M6.2 migration/package/clean machine | In Progress | first RED required all 11 historical inputs captured before owner mutation; config/workspace/SQLite/cache/Helper fixtures are now provenance-pinned, SHA-256 frozen, accepted by current owners, and complete current/oldstable local gates pass; exact-SHA Hosted delivery pending |
| M6.3 security/compatibility/diagnostics | Not Started | no implementation evidence |
| M6.4 RC/1.0 | Not Started | no RC, release artifacts, tag, release, or channel evidence |

## Feature status

VIM-013, VIM-014, WORK-006, REL-001, REL-002, and REL-011 are `In Progress` after the complete versioned application schema, machine-visible precedence/restart/Job-freeze policy, startup-frozen runtime mapping for IPC/listing/cache/transfer/preview/search/retry/integrity/diagnostic/external/keymap sections, owner-derived production-closed Helper/direct-transfer fields, validated context-keymap/reserved-action, fixed-quit unreachable-input rejection, keymap-to-reducer count/Visual/repeat/confirmation regression, versioned effective-keymap export/confirmed atomic reset, bounded durable Job query/control, probe-only daemon status, idempotent start, exact-confirmed authenticated shutdown, read-only strict Helper L0/L1 status with production-gate visibility and exact Endpoint release, explicit snake_case v1 success/error envelopes, redacted machine output, stable exit-code, help/man/completion parity, public compatibility inventory contracts, and 11 SHA-pinned real historical config/workspace/SQLite/cache/Helper sources accepted by current owners. WORK-006 is not `Verified`: migration, backup, failure rollback, and idempotence remain M6.2 work. The other 17 Stage 6-owned rows remain `Planned`: JOB-010, HELP-013, SEC-012, SEC-014, OBS-009, OBS-010, PLAT-003, PLAT-009, REL-003 through REL-010, and REL-012. Shared rows that remain `In Progress` are not advanced by this evidence.

## Exit criteria

All 12 Stage 6 exit criteria remain open. Their milestone ownership and required proof are mapped in the execution plan. No checkbox may be closed from design intent, cross-builds, synthetic substitutes, or credentials that do not exist.

## Protected release boundary

Production Helper distribution and production Level 2 stay **CLOSED**. Opening them requires final release bytes, real protected Developer ID/notary success where applicable, byte-identical accepted notarization input and final tar binary, a production Helper manifest bound only to those final bytes, real offline signature/custody evidence, four-platform manifest-to-tar identity, clean quarantine/Gatekeeper evidence, and all security/compatibility/release gates. Fixture keys, public CI, tabletop ceremonies, or locally generated credentials cannot satisfy this boundary.

## Command ledger

| Candidate | Command/evidence | Result |
|---|---|---|
| exact main | local/origin/remote ref and tree audit | PASS; all identities matched the frozen baseline |
| exact main | Hosted run `29579514879` | PASS; completed success, 24/24 jobs |
| exact main | CI-equivalent baseline `make ci` | PASS; exit 0, no code change required |
| M6.1 config defaults RED | `go test ./internal/config -run='^TestDecode(AppliesDocumentedDefaultsToOmittedFields\|RequiresExplicitSchemaVersion)$' -count=1 -v` | Intended FAIL: minimal explicit-version document produced `ipc.max_frame_bytes must be greater than zero`; missing version remained rejected |
| M6.1 config defaults GREEN | `go test ./internal/config -count=1` | PASS: omitted fields inherit the single documented `Default()` source; explicit schema version remains required; unknown/trailing/invalid explicit values remain rejected |
| M6.1 config integration gate | `go test ./internal/app ./internal/config -count=1`; CI-equivalent `make check` | PASS: application loading regression and complete unit/provider/docs/tidy/verify gate green; config coverage 88.6% |
| M6.1 config CLI/machine-output RED | `go test ./internal/app ./internal/config -run='(TestRunReturnsStableTypedExitCode\|TestWriteRedactedEffectiveConfig)' -count=1` | Intended compile FAIL: no `config` role, typed exit contract, redacted effective writer, or output-version contract existed |
| M6.1 config CLI/machine-output GREEN | `go test ./internal/app ./internal/config -count=1`; focused `-race` on both packages | PASS: `config validate`/`print-effective`, explicit private-file validation, output v1, argv redaction/non-mutation, exit 0–8 snapshot, dispatch and error-channel contracts green |
| M6.1 config CLI complete local gate | CI-equivalent `make check`; `make lint`; `make docs-check`; `git diff --check` | PASS after adding precise `#nosec G302` rationale for the two test-only owner-private 0700 directories; no product permission or lint rule was weakened |
| M6.1 help/man/completion RED | `go test ./internal/app -run='(TestPublicHelpManAndCompletionsShareCommandFacts\|TestRunCompletion\|TestCommittedManPage)' -count=1` | Intended compile FAIL: no shared public CLI facts, man renderer, completion renderer, or completion command existed |
| M6.1 help/man/completion GREEN | focused `go test` and `go test -race ./internal/app -count=1`; `make lint`; `make docs-check` | PASS: ordered facts drive help/man and bash/zsh/fish static completions; committed man parity and forbidden remote/auth operation scans green; lint 0 issues |
| M6.1 keymap registry RED | `go test ./internal/keymap ./internal/tui -run='(TestDefaultSnapshot\|TestNewSupportsContext\|TestNewRejects\|TestTranslateTCellEventWithKeymap)' -count=1` | Intended compile FAIL: no keymap registry, default snapshot, override validation, context lookup, or keymap-aware tcell translation existed |
| M6.1 config keymap RED | `go test ./internal/config -run='TestDecode(AcceptsContextKeymapRemap\|RejectsConflictingOrReservedKeymap)' -count=1` | Intended compile FAIL: schema had no keymap section |
| M6.1 keymap GREEN | `go test ./internal/config ./internal/keymap ./internal/tui ./internal/app -count=1`; `go test -race ./internal/keymap ./internal/tui -count=1`; `make lint`; `make docs-check` | PASS: exact Vim default snapshot, Normal/Visual remap isolation, conflict/unreachable/unknown/count/control/dangerous rejection, schema round-trip, app wiring, default tcell regressions, race, lint 0, and docs green |
| M6.1 keymap complete local gate | CI-equivalent `make check`; `make lint`; `make docs-check`; `git diff --check` | PASS: full unit/provider/integration/docs/tidy/verify gate green; keymap coverage 90.3%, config 87.0%, TUI 69.7%; lint 0 and clean diff check |
| M6.1 fixed-quit/keymap-to-reducer RED | current Go 1.26.5 `go test ./internal/keymap ./internal/tui` on parent `a2a0503f319ec078f415753a3f2e6a2a631d8ca7` plus the new contracts | Intended FAIL: `q` was accepted as a Normal remap target even though the real application event loop consumes it as fixed quit before keymap dispatch; the new tcell-to-reducer count/Visual/repeat/delete-confirmation regression already passed |
| M6.1 fixed-quit/keymap-to-reducer GREEN | current Go 1.26.5 ordinary and race tests for keymap/config/TUI/app | PASS: `q` is rejected as an unreachable fixed Normal-mode quit input; real tcell input preserves count-before-lookup, context-local Visual dispatch, reserved repeat, and directory-delete confirmation without a second mutation path |
| M6.1 keymap-to-reducer complete local gate | current Go 1.26.5 and exact oldstable Go 1.25.12 CI-equivalent `make check`; current `make lint`; `make docs-check`; `git diff --check` | PASS under `GOTOOLCHAIN=local`, `umask 0022`, trusted fixture root, and separate external build/coverage directories; both complete unit/provider/integration/docs/tidy/verify gates green, lint 0 |
| M6.1 compatibility inventory RED | `go test ./internal/compatibility -count=1` | Intended compile FAIL: no registry/snapshot/Markdown renderer and no explicit owning constants for SQLite head, cache manifest, Helper manifest, or Helper envelope existed |
| M6.1 compatibility inventory GREEN | `umask 0022; go test ./internal/compatibility ./internal/app ./internal/cachefs ./internal/helper ./internal/ipc ./internal/state/migration ./internal/workspace -count=1` | PASS: exact nine-boundary snapshot and committed reference parity; config/workspace/SQLite/cache/IPC/Helper values resolve from owning package constants; outbound Helper envelope and cache manifest behavior is unchanged |
| local permission-fixture classification | the same focused command first ran under inherited `umask 0077` | ENVIRONMENT, not code: deliberate `0755` cache fixtures were masked to `0700`, so two wrong-mode negatives could not create their unsafe precondition; both targeted tests and the full focused set passed under the required CI-equivalent `umask 0022` with no code/assertion/timeout change |
| M6.1 compatibility inventory complete local gate | focused race on compatibility/cachefs/helper/IPC; CI-equivalent `make check`; `make lint`; `make docs-check`; `git diff --check` | PASS: race green; full unit/provider/integration/docs/tidy/verify green; compatibility registry 100% statement coverage; lint 0 and clean diff check |
| M6.1 cache/transfer config RED | `go test ./internal/config ./internal/app -run='Test(DefaultCacheAndTransfer\|DecodeAppliesPartialCacheAndTransfer\|CacheAndTransferSettings\|RuntimeCacheLimits\|RuntimeTransferLimits)' -count=1` | Intended compile FAIL: config had no cache/transfer schema and app had no validated runtime mapping |
| M6.1 cache/transfer config GREEN | focused config/app tests | PASS: omitted values freeze existing cache and manager defaults; partial documents preserve defaults; cache/concurrency/queue can only tighten ceilings; bandwidth is bounded; daemon maps validated values into cache manager and transfer scheduler without hot-reloading Job semantics |
| M6.1 config-source diagnostic RED | `go test ./internal/app -run='TestLoadApplicationConfigNamesSourcePath' -count=1` | Intended FAIL: the validation error named the invalid transfer field but omitted the configuration source path |
| M6.1 config-source diagnostic GREEN | focused app tests | PASS: inspect, validate, open, decode, and validation errors retain the exact config path while preserving field-level diagnostics |
| M6.1 cache/transfer complete local gate | CI-equivalent `go test -race ./internal/config ./internal/app -count=1`; `make check`; `make lint`; `make docs-check`; `git diff --check` | PASS under `umask 0022`: focused race green; full unit/provider/integration/docs/tidy/verify green; config coverage 87.5%; lint 0 and clean diff check |
| M6.1 current toolchain gate | exact tree `a9424373d4c0000473fdfedce06f9fd0c50f3dcf`; official Go 1.26.5 linux-amd64 archive SHA-256 `5c2c3b16caefa1d968a94c1daca04a7ca301a496d9b086e17ad77bb81393f053`; `GOTOOLCHAIN=local make ci` | PASS: exact local toolchain identity, complete check/lint/race/fuzz/supply-chain/actionlint and four-target builds green |
| M6.1 exact oldstable toolchain gate | exact tree `a9424373d4c0000473fdfedce06f9fd0c50f3dcf`; official Go 1.25.12 linux-amd64 archive SHA-256 `234828b7a89e0e303d2556310ee549fbcf253d28de937bac3da13d6294262ac1`; `GOTOOLCHAIN=local go version`; `GOTOOLCHAIN=local make check` | PASS: reported exact `go1.25.12 linux/amd64`; full unit/provider/integration/docs/tidy/verify gate green |
| M6.1 preview config RED | `go test ./internal/config ./internal/app -run='Test(DefaultPreviewSettings\|DecodeAppliesPartialPreviewSettings\|PreviewSettings\|RuntimePreviewLimits\|PreviewLocationUsesConfiguredRenderLimits)' -count=1` | Intended compile FAIL: config had no preview schema/runtime mapping and `previewLocation` accepted only hard-coded renderer/image defaults |
| M6.1 preview config GREEN | focused config/app tests; full config/app tests and focused race | PASS: config defaults derive from the owning preview package; every field and dependent combination can only tighten existing ceilings; the client freezes validated renderer/image limits and metadata rendering proves the configured output bound. The first full-package run correctly caught the exact `Default()` test snapshot missing the new public section; the snapshot was completed and the unchanged product implementation then passed full and race gates |
| M6.1 preview complete local gate | official Go 1.26.5 with `GOTOOLCHAIN=local`; CI-equivalent `make check`; `make lint`; `make docs-check`; `git diff --check`; exact Go 1.25.12 `make check`, all under `umask 0022` | PASS on current and exact oldstable: full unit/provider/integration/docs/tidy/verify green; config coverage 88.8%; lint 0 and clean diff check |
| M6.1 search config RED | `go test ./internal/search ./internal/config ./internal/app -run='Test(DefaultSearchBudgets\|DefaultSearchSettings\|DecodeAppliesPartialSearchSettings\|SearchSettings\|RuntimeSearchBudgets\|PendingSearchIdentities)' -count=1` | Intended compile FAIL: search owned no public current-budget defaults, config had no search section/runtime mapping, and filename/content identities accepted only hard-coded budgets |
| M6.1 search config GREEN | focused search/config/app tests; full package tests and focused race | PASS: owning search defaults preserve the exact prior filename/content envelopes; omitted/partial config and every field can only tighten those ceilings; startup maps validated millisecond durations and freezes each configured budget into the corresponding request identity. The first full lint gate correctly rejected unchecked unsigned/signed duration conversions; duration milliseconds were changed to validated signed fields, no suppression was added, and focused tests plus scoped lint returned green |
| M6.1 search complete local gate | official Go 1.26.5 `make check`; `make lint`; `make docs-check`; `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: full unit/provider/integration/docs/tidy/verify green; config coverage 90.2%; lint 0 and clean diff check; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 retry config RED | `go test ./internal/retrypolicy ./internal/config ./internal/app ./internal/transfer -run='Test(DefaultRetrySettings\|DefaultsFreezeCurrentRuntimeBehavior\|DecodeAppliesPartialRetrySettings\|RetrySettingsRemainBoundedAndNonAggressive\|RuntimeRetrySettings\|ManagerRetryAt)' -count=1` | Intended compile FAIL: no shared retry-policy defaults, config section/runtime mapping, or manager-frozen retry delay existed |
| M6.1 retry config GREEN | focused retrypolicy/config/app/transfer tests; full four-package tests and focused race; scoped lint | PASS: shared defaults preserve three existing reconnect delays and one-minute Job retry; explicit empty reconnect array disables attempts; validation cannot add attempts, shorten corresponding defaults, decrease delays, exceed 30 seconds, or set Job retry outside 60 seconds..10 minutes; client and manager freeze runtime values, persisted `retry_at` uses the manager delay, and scoped lint reports 0 issues |
| M6.1 retry complete local gate | official Go 1.26.5 `make check`; `make lint`; `make docs-check`; `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: full unit/provider/integration/docs/tidy/verify green; retrypolicy coverage 100%, config 91.4%, transfer 74.6%; lint 0 and clean diff check; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 integrity/direct-closure config RED | `go test ./internal/transfer ./internal/config ./internal/app -run='Test(ProductionPolicyDefaults\|DefaultIntegrityAndDirectTransfer\|DecodeAppliesPartialIntegrityPolicy\|IntegrityAndDirectTransferSettings\|RuntimeDirectPolicy)' -count=1` | Intended compile FAIL: transfer exposed no owning production policy defaults, config/runtime mapping, or configured copy-intent freeze boundary |
| M6.1 production-closure evidence regression RED | `go test ./internal/transfer -run '^TestLevel2PolicyAndProductionClosureNeverInvokeDirectFixture/ordinary_runtime_is_production_closed$' -count=1` after freezing explicit `IntegrityStrong` | Intended FAIL: the frozen integrity value made the old whole-struct `DirectPolicy.zero()` check report `level2_policy_disabled` instead of the required `production_distribution_closed`, despite all three enablement gates remaining false |
| M6.1 integrity/direct-closure config GREEN | focused and complete transfer/config/app tests; focused race | PASS: owning transfer defaults freeze `strong` and production distribution closed; only `strong`/`require_strong` validate, `baseline`/unknown policies fail closed, and `direct_transfer.enabled=true` is rejected before startup. The client maps the validated policy once and every copy Intent carries it through the existing immutable Planner/route-evidence chain. Zero direct policy now means all three enablement gates are false, independent of the integrity floor, so ordinary runtime retains `production_distribution_closed`; it cannot inject the fixture-only Level 2 backend or satisfy its workspace/data gates |
| M6.1 integrity/direct-closure complete local gate | official Go 1.26.5 `go test -race ./internal/transfer ./internal/config ./internal/app`; `make check`; `make lint`; `make docs-check`; `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: focused race and full unit/provider/integration/docs/tidy/verify green; config coverage 91.6%, transfer 74.6%; lint 0, docs and diff clean; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 diagnostic config RED | official Go 1.26.5 `go test ./internal/diagnostic ./internal/config ./internal/app` with `GOTOOLCHAIN=local` | Intended compile FAIL: diagnostic owner exposed no frozen default config or configurable ring capacity, the versioned schema had no diagnostic section, and app had no validated runtime mapping |
| M6.1 diagnostic config GREEN | official Go 1.26.5 `go test ./internal/diagnostic ./internal/config ./internal/app` with `GOTOOLCHAIN=local` | PASS: defaults derive from the diagnostic owner; partial config overlays correctly; log bytes/backups/ring records can only tighten current ceilings; OpenDaemon honors the ring cap; daemon startup maps the values into persistent and read-only-degraded logging paths |
| M6.1 diagnostic config complete local gate | official Go 1.26.5 focused three-package race, `make check`, `make lint`, `make docs-check`, `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: focused race, full unit/provider/integration/docs/tidy/verify, lint 0, docs and diff green; config coverage 92.0%, diagnostic 75.4%; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 effective-keymap/reset RED | official Go 1.26.5 focused keymap/config/app test command | Intended FAIL: keymap had no versioned effective binding export, config had no validated non-redacted encoder for persistence, app rejected both new subcommands, and shared completion facts omitted them |
| M6.1 effective-keymap/reset GREEN | official Go 1.26.5 focused and complete keymap/config/app tests | PASS: deterministic output v1 enumerates exact Normal then Visual effective bindings with current/default input and override/remap state; reset requires exact `--yes`, preserves rejected-source bytes and every unrelated value, publishes validated 0600 config through synced atomic replacement, leaves no temporary file, and shares help/man/completion facts |
| M6.1 effective-keymap/reset complete local gate | official Go 1.26.5 focused three-package race, `make check`, `make lint`, `make docs-check`, `git diff --check`; exact Go 1.25.12 `go version` and `make check`, all with `GOTOOLCHAIN=local` under `umask 0022` | PASS on current and exact oldstable: full unit/provider/integration/docs/tidy/verify and race green; app coverage 43.6%, keymap 88.9%, config 91.4%; the first scoped lint reported only a new if/else style eligible for tagged switch, the equivalent switch was applied, and lint then reported 0 issues; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 config policy/Helper closure RED | official Go 1.26.5 focused config tests with `GOTOOLCHAIN=local` | Intended compile FAIL: config exposed no typed resolution policy, effective output carried no precedence/reload/freeze facts, the schema had no Helper section, and the Helper owner exposed no production-distribution gate |
| M6.1 config policy/Helper closure GREEN | official Go 1.26.5 focused helper/config/app tests | PASS: exact policy freezes CLI/workspace/user/default order, unsupported system/environment config layers, bounded OpenSSH/external-command environment role, restart-only reload, and plan-time Job semantics; `helper.enabled` defaults to explicit opt-out and is gated by the production-closed owner policy |
| M6.1 config policy local environment classification | current/exact-oldstable `make check` launched from the interactive shell without the CI umask | Both toolchains failed the same cachefs public-mode and instance-lock public-mode negative fixtures because shell `umask 077` converted requested 0755/0644 setup into private modes; changed helper/config/app packages passed. Classification: local environment mismatch, not code. No code, assertion, timeout, or fixture change; the exact commands below were rerun with CI `umask 0022` and trusted persistent root. |
| M6.1 config policy/Helper closure complete local gate | official Go 1.26.5 and exact Go 1.25.12 `make check` with `GOTOOLCHAIN=local`, `umask 0022`, `/var/lib/amsftp-tests/1001`, and external build/coverage roots; focused race, `make lint`, `make docs-check`, `git diff --check` | PASS on current and exact oldstable: complete unit/provider/integration/docs/tidy/verify green, config coverage 91.5%, helper 74.4–74.5%, app 43.8%, lint 0, docs/diff green |
| M6.1 durable Job CLI RED | official Go 1.26.5 focused `go test ./internal/app` after adding dispatch, RPC, output, bound, confirmation, and exit-class contracts | Intended compile FAIL after correcting one test-only route constant name: `runJobCommand` and `JobCLIOutputVersion` did not exist; no public Job role, handler, shared help/man/completion fact, bounded parser, or versioned output existed |
| M6.1 Job pre-connect/error-envelope RED | focused app tests for connector ordering and `Run` stderr | Intended compile FAIL: `runJobWithConnector` and `jobCommandError` did not exist; production would have resolved paths and connected/started the daemon before rejecting invalid or unconfirmed input, and JSON-mode errors would have received the human `amsftp: job handler` prefix |
| M6.1 Job explicit DTO RED | focused list/control/events JSON tests | Intended FAIL: internal `Snapshot`, `Location`, and `EventRecord` Go field names leaked into the public JSON, and event payload remained a quoted storage string rather than an embedded JSON value |
| M6.1 Job connection classification RED | focused app exit-class test | Intended compile FAIL: no connection-specific classifier existed; an ordinary control-socket dial/startup failure would otherwise have fallen through the RPC classifier to internal exit 1 instead of network exit 5, while cancellation still required exit 8 |
| M6.1 durable Job CLI GREEN | complete and focused app ordinary/race tests | PASS: canonical Job IDs, 1..100 page bounds, list/events/pause/resume/cancel high-level RPC routing, pre-connect validation, exact same-ID cancel confirmation, human tables, explicit snake_case v1 success/error DTOs, empty-array stability, embedded event JSON, safe daemon diagnostics, stdout/stderr separation, and stable exit-class mapping are covered; no raw provider mutation route was added |
| M6.1 Job CLI complete local gate | official Go 1.26.5 and exact Go 1.25.12 `make check` with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; current focused race, `make lint`, `make docs-check`, `git diff --check`; built-binary help/completion/invalid-JSON smoke | PASS on current and exact oldstable after the final connection-classification change: full unit/provider/integration/docs/tidy/verify green, app coverage 45.6%, lint 0, docs/diff green, and the binary returned exit 2 with empty stdout and one v1 JSON stderr object. The first lint caught four new ST1005 capitalization findings; only those strings were lowercased and final lint reported 0 issues. The first smoke harness stopped before product assertions because zsh reserves `status`; renaming that shell-local variable produced the passing no-product-change rerun |
| M6.1 daemon lifecycle CLI RED | official Go 1.26.5 `go test ./internal/daemon ./internal/app` after adding handshake-info, shutdown ordering, validation, lifecycle, output, and help/completion contracts | Intended compile FAIL: `Client.Info`, shutdown request/response/server callback, daemon command runtime/parser/output, and public lifecycle facts did not exist |
| M6.1 daemon lifecycle CLI GREEN | official Go 1.26.5 complete daemon/app ordinary and race tests; real isolated control-socket lifecycle in `TestDaemonRoleServesLocalProviderAndStopsCleanly` | PASS: status is probe-only and reports absent/running distinctly; start is idempotent; stop validates exact `--confirm stop` before runtime activity, uses only the verified owner-private IPC connection, receives an acknowledgement before one-shot server cancellation, drains daemon resources, and removes the socket; bare daemon/explicit migration-resume compatibility remains intact; daemon and Job commands share the v1 machine-error renderer |
| M6.1 daemon binary smoke environment classification | built binary with isolated XDG config/state/cache/runtime roots, then `amsftp daemon status --format json` | ENVIRONMENT/SAFE DIAGNOSTIC: runtime validation selected the existing user fallback `/tmp/amsftp-1001`, whose pre-existing socket refused connection; status correctly returned one network-class v1 JSON error instead of deleting, replacing, or misreporting the unhealthy socket. No user runtime artifact was changed. The real lifecycle gate uses explicit owner-private test paths and passed without weakening validation |
| M6.1 daemon runtime-invariant RED | official Go 1.26.5 `go test ./internal/app -run '^TestDaemonCommandRejectsRuntimeWithoutClient$' -count=1` | Intended FAIL with a nil-pointer panic: a malformed injected probe could report `found=true` without a client, and a malformed starter could return `(nil, nil)` |
| M6.1 daemon runtime-invariant GREEN | the same focused test after adding explicit invariant checks | PASS: both malformed runtime results return stable internal exit 1 instead of panicking; production probe/starter behavior is unchanged |
| M6.1 daemon lifecycle complete local gate | official Go 1.26.5 and exact Go 1.25.12 `make check` with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; current focused race, `make lint`, `make docs-check`, `git diff --check`, rerun after the final invariant guard | PASS on current and exact oldstable: complete unit/provider/integration/docs/tidy/verify and focused daemon/app race green; app coverage 46.8%, daemon 68.4%; lint 0 issues; docs and diff clean; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 final configuration mapping RED | official Go 1.26.5 focused app/daemon tests with `GOTOOLCHAIN=local` | Intended compile FAIL after one test-only pane constant correction: public `listing.default_page_size` had no list-call input, and `ServerConfig` had no configured frame maximum; graph/code search confirmed both public fields were otherwise validation/output-only |
| M6.1 final configuration mapping GREEN | the same focused tests; complete config/app/daemon ordinary tests and focused race | PASS: every client directory page uses the startup-frozen validated listing size; daemon reader and writer use the startup-frozen validated frame maximum while retaining the 8 MiB protocol ceiling and legacy constructor default; oversized input is rejected at the framed connection boundary |
| M6.1 final configuration mapping complete local gate | official Go 1.26.5 and exact Go 1.25.12 `make check` with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; current focused config/app/daemon race, `make lint`, `make docs-check`, `git diff --check` | PASS on current and exact oldstable: complete unit/provider/integration/docs/tidy/verify and focused race green; app coverage 47.2%, daemon 68.4–68.5%, config 91.5%; lint 0 issues; docs and diff clean; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 Helper status CLI RED | official Go 1.26.5 focused app tests after adding public dispatch, pre-connect validation, RPC lifecycle, L0/L1 parsing, versioned output, cleanup, and help/man/completion contracts | Intended compile FAIL after one test-only pointer-assignment correction: `runHelperCommand`, `runHelperWithConnector`, and the Helper RPC abstraction did not exist; the only production Helper role accepted restricted `serve`, and public command facts exposed no Helper management surface |
| M6.1 Helper status CLI GREEN | complete app ordinary tests and focused app race | PASS: `helper status <SSH-host>` validates command/host/format before runtime activity, connects through the authenticated local daemon, reads exactly one SSH Provider capability snapshot, strictly validates Endpoint/state/`helper_status v1`/bounded unique constraints/L0-L1 combinations, emits human or explicit snake_case v1 JSON, displays the production-distribution gate, and releases the temporary Endpoint on success, malformed status, or snapshot failure; it performs no install/enable/upgrade/disable/remove mutation and restricted `helper serve` remains absent from completions |
| M6.1 Helper success-publication ordering RED/GREEN | focused release-failure and client-close-failure tests | Intended RED showed the first implementation had already written a JSON success object before reporting cleanup failure; GREEN decodes and retains the result in memory, completes exact Endpoint release and RPC client close first, and only then publishes success, so a cleanup failure leaves success stdout empty and returns one classified error |
| M6.1 Helper status local gate | official Go 1.26.5 full `make ci` and exact Go 1.25.12 CI-equivalent `make check` with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; current focused app race, `make lint`, `make docs-check`, `git diff --check` | PASS on current and exact oldstable after the cleanup-before-success regression: complete unit/provider/integration/docs/tidy/verify, full race, four fuzz smokes, supply-chain/actionlint and four-target builds green; app coverage 48.8%, helper 74.6%, compatibility registry 100%; lint 0 issues, docs/diff clean; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 historical-source inventory RED | official Go 1.26.5 focused compatibility tests after adding the frozen-source contract | Intended compile FAIL: `HistoricalSourceSnapshotText`, `HistoricalSources`, and the capture-status model did not exist, so no complete pre-mutation source set or immutable fixture evidence could be asserted |
| M6.1 historical-source inventory GREEN | `go test ./internal/compatibility -count=1` | PASS: exact 11-source config/workspace/SQLite/cache/Helper inventory with provenance and planned immutable paths; config v1 plus workspace v1/v2 are captured with exact SHA-256 and accepted by current owner readers; the other eight entries are truthfully marked `capture-required-before-M6.2-mutation`, so a planned path cannot be mistaken for fixture evidence |
| M6.1 historical-source inventory local gate | official Go 1.26.5 `make ci`; exact Go 1.25.12 `go version` and `make check`, with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots; `git diff --check` | PASS on current and exact oldstable after replacing the first lint-rejected variable `os.ReadFile` test call with repository-root-confined `fs.ReadFile`: complete unit/provider/integration/docs/tidy/verify, full race, four fuzz smokes, supply-chain/actionlint, four-target builds, lint 0, and diff check green; compatibility coverage 100%; oldstable reported `go1.25.12 linux/amd64` |
| M6.1 exact implementation Hosted gate | commit `9e2ed6df9fc736d0d86fa5e6f70d3a0ef0146224`, tree `73a8b94974ffc98862f0d28243834acba5d999bc`; push `29619828356`; PR `29619830232` | PASS on failed-only attempt 2 after the two attempt-1 timing failures were independently contradicted by same-SHA companions; all changed compatibility/fixture/docs contracts, native/oldstable, auth, quality, builds, reproducibility, compare and provenance jobs green; no code/test/timeout/workflow change |
| M6.1 provenance canonicality RED | `go test ./internal/compatibility -run '^TestHistoricalSourceProvenanceCommitsAreCanonicalObjectIDs$' -count=1 -v` | Intended FAIL: SQLite v3 provenance length was 41 rather than a canonical 40-character object ID |
| M6.1 provenance canonicality GREEN | focused complete compatibility tests; explicit `git cat-file -t` for every distinct provenance value | PASS: Git history identifies `939ba9c5d978b8ea5bf1ae060ff62a0769d0d6c0` as the commit that first added `version3.go`; all inventory provenance values are lowercase 40-hex IDs and all eight distinct values resolve to Git commit objects |
| M6.1 provenance correction local gate | official Go 1.26.5 full `make ci`; exact Go 1.25.12 `go version` and `make check`; `make docs-check`; `git diff --check`, with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots | PASS on the correction worktree: complete unit/provider/integration/docs/tidy/verify, lint, full race, four fuzz smokes, supply-chain/actionlint, four-target builds and exact oldstable check green; oldstable reported `go1.25.12 linux/amd64`; docs and diff clean |
| M6.1 exact provenance-correction Hosted gate | commit `d4d524331b796ab408f292d0412e2d6728afacd0`, tree `2643255fe3cfc07bcd046af42b553c308face3ef`; push `29621518452`; PR `29621520034` | PASS on failed-only attempt 2 after all three attempt-1 Helper timing failures passed directly comparable same-SHA companions; changed compatibility/object-ID contracts, native/oldstable, auth, quality, builds, reproducibility, compare, and provenance jobs green; no code/test/timeout/workflow change |
| M6.1 exact closeout Hosted gate | commit `cb267a3b4adabf6f43fae51f7ed0a8f432d0aa53`, tree `c8db67def8f711535229416d6f575b47284056ab`; push `29622036145`; PR `29622037354` | PASS on failed-only attempt 2 after known Level 2 cancellation, daemon-startup, and Helper-heartbeat timing fixtures passed same-SHA or directly comparable companions; documentation-only delta, all native/oldstable/quality/build/reproducibility/compare/provenance jobs green; no code/test/timeout/workflow change |
| M6.2 complete historical capture RED | official Go 1.26.5 focused `TestEveryHistoricalMigrationInputIsCapturedBeforeM62Mutation` | Intended FAIL for exactly the eight uncaptured SQLite v1/v2/v3, cache entry/materialization v1, and Helper release/state/metadata v1 sources; config v1 and workspace v1/v2 remained captured |
| M6.2 historical writer capture | temporary capture harnesses executed at exact provenance commits `486a63f90be51c0d79a454bef52e9e3302df5250`, `4eb1961b7b3b5495620fb1f6fcb3b88c52a4fba9`, `939ba9c5d978b8ea5bf1ae060ff62a0769d0d6c0`, `8a4ada06836b9ed71c72b40949d6b87d8e1f849a`, and `145b50ae871aa91f8acc0505d2b6b9bd19bae742` | PASS: real historical SQLite migration/store APIs generated v1/v2/v3 databases; real cache publication APIs generated entry/materialization manifests; real Helper manifest/state APIs generated release/state/metadata bytes. All temporary capture harnesses and worktrees were removed after copying the exact outputs |
| M6.2 complete historical capture GREEN | official Go 1.26.5 focused compatibility/cachefs/helper tests with trusted persistent fixture root | PASS: all 11 sources are `captured`, exact SHA-256 matches repository bytes, current migration owner validates SQLite heads 1–3 read-only, current cache owner reads historical entry/materialization manifests with exact blob binding, and current Helper owner parses the release manifest and loads enabled state/metadata |
| M6.2 capture safety-lint correction | first current `make ci`, then focused tests and `make lint` after correction | Initial full gate reached lint after every ordinary test passed and correctly rejected variable-path test helpers plus a `uint64`→`int` slice conversion. Classification: introduced test-code safety defects. GREEN uses repository-confined `DirFS`, destination-confined `os.Root`, and an explicit v1/v2/v3 migration mapping; focused owner tests pass and lint reports 0 issues. No suppression or product relaxation was added |
| M6.2 complete historical capture local gate | official Go 1.26.5 full `make ci`; exact Go 1.25.12 `go version` and `make check`; `make docs-check`; `git diff --check`, with `GOTOOLCHAIN=local`, `umask 0022`, trusted persistent fixtures and separate external build/coverage roots | PASS on current and exact oldstable after the safety-lint correction: complete unit/provider/integration/docs/tidy/verify, full race, four fuzz smokes, supply-chain/actionlint, four-target builds, lint 0, hashes and diff check green; oldstable reported `go1.25.12 linux/amd64` |
