# 1.0 功能矩阵

> - 产品：`AMSFTP`（公开命令 `amsftp`；仓库名 `awesome-mac-sftp`）
> - 基线：已批准产品设计
> - 实现状态：Stage 0–5 已完成；Stage 4 的 26/26 行与 Stage 5 的 22/22 行已验证；Stage 6 有 6 个 `Verified`、17 个 `In Progress`，没有 `Planned` 行
> - 最后更新：2026-07-19

## 1. 使用规则

本矩阵是 1.0 交付范围的逐项事实源。每条能力使用稳定 ID；ID 一经进入矩阵不得复用，能力被取消时保留原行并将状态改为 `Removed`，同时链接对应 ADR。实现不得只在代码或聊天中增加能力而不更新本矩阵。

Stage 0–5 已完成且各阶段拥有完整验收范围的行已按证据标记为 `Verified`；验收仍由后续阶段共同拥有的行保持 `In Progress`。Stage 4 的 26 行和 Stage 5 的 22 行都经过完整本地门禁、独立安全/正确性审查、candidate-tree secret/pollution 扫描、cold-start audit 与 exact-SHA push/PR Hosted 交付门禁。Stage 6 已完成 exact-main 基线与 M6.1；M6.2/M6.3 的 migration、packaging、native、diagnostics、OpenSSH/Kerberos 和 review 独立切片已有 exact evidence。JOB-010、PLAT-009、VIM-013、VIM-014、OBS-010 与 REL-001 为 `Verified`；其余 17 个 Stage 6 行均为 `In Progress`，不再有 `Planned` 行。M6.4 已建立 REL-008 exact-candidate gate record、REL-009 user/operations path 和 REL-010 final-only zero-gap audit，但当前没有 RC record，12 个退出条件仍全部开放。real public channel、项目 LICENSE、protected final bytes、production Helper/Level 2、最终独立审查与发布动作仍开放。计划与实时结果分别见 [Stage 6 execution plan](../stages/06-hardening-release-plan.md) 和 [Stage 6 verification](../verification/stage-06.md)。Stage 1 最终证据见 [Stage 1 verification](../verification/stage-01.md)，Stage 2 证据见 [Stage 2 verification](../verification/stage-02.md)，Stage 3 证据见 [Stage 3 evidence](../verification/stage-03.md)，Stage 4 证据见 [Stage 4 evidence](../verification/stage-04.md)，Stage 5 证据见 [Stage 5 evidence](../verification/stage-05.md)。

状态词义：

- `Planned`：范围和验收标准已确定，尚无实现证据；
- `In Progress`：当前阶段正在实现，尚未满足全部验收标准；
- `Implemented`：实现已存在，阶段验证尚未全部完成；
- `Verified`：自动测试与要求的人工/真实环境验证均有证据；
- `Deferred`：经 ADR 明确移出 1.0，原 ID 保留；
- `Removed`：能力明确取消，原 ID 保留且不得复用。

## 2. 阶段索引

| Stage | 名称 | 交付主题 |
|---|---|---|
| 0 | Foundation & Knowledge | 工程、契约、测试基座和可续接文档 |
| 1 | Read-only Explorer | OpenSSH/SFTP 连接、任意双端点浏览和 Vim 导航 |
| 2 | Durable Transfers | 持久任务、安全复制/移动/删除、冲突与恢复 |
| 3 | Preview, Edit & Cache | 日常预览、Vim 编辑、本机打开、缓存、命令与 shell |
| 4 | Search & Helper | 可选 helper、递归搜索、强哈希和远端增强能力 |
| 5 | Direct & Scale | 服务端快速路径、跨主机直传、超大规模与资源控制 |
| 6 | Hardening & Release | 配置、兼容、安全、迁移、诊断和发布 |

## 3. 基础契约与工程

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| CORE-001 | 单一程序、多运行角色 | 0 | Verified | 同一 Go 程序具有稳定、互斥且可测试的 client、daemon、askpass 与 helper 角色分发边界；四个目标 OS/架构由同一源码构建，具体角色功能由所属后续阶段交付。 | 角色参数/退出码/流契约测试、四目标构建、darwin/arm64 本机 help/version 冒烟及 Hosted 四平台 native/oldstable 全部通过。见 [Stage 0 verification](../verification/stage-00.md#core-evidence)。 |
| CORE-002 | 版本化本地 IPC envelope | 0 | Verified | 冻结带协议版本、请求 ID、类型、载荷和结构化错误的 framed JSON 编解码与握手契约；不兼容版本在进入业务处理前被确定拒绝，实际 TUI↔daemon 连接由 Stage 1 交付。 | 信封、握手、帧、字节路径、取消、严格 UTF-8 和 fuzz 测试通过本地完整门禁与 Hosted quality/native 门禁。见 [Stage 0 verification](../verification/stage-00.md#core-evidence)。 |
| CORE-003 | Endpoint/Provider 统一契约 | 0 | Verified | 冻结传输无关的只读 Provider、可选 MutableProvider、分页/游标和句柄契约，Fake 通过可复用 contract suite；LocalFS、SFTP 和 helper 在各自阶段接入同一套件。 | Provider 验证器、共享 contract suite 与 Fake conformance 通过本地双工具链和 Hosted 门禁。见 [Stage 0 verification](../verification/stage-00.md#core-evidence)。 |
| CORE-004 | 能力协商模型 | 0 | Verified | 冻结 SessionID、单调 generation、complete/unknown 语义和能力丢失错误；Fake 可确定复现 complete/incomplete、撤回与恢复，实际路由/UI 由后续阶段验证。 | 能力值对象、Provider contract、D3 session-local bookkeeping 与 E1 typed operation gating 通过完整门禁。见 [Stage 0 verification](../verification/stage-00.md#core-evidence)。 |
| CORE-005 | 可控 fake provider | 0 | Verified | 测试 Provider 可确定注入延迟、gate、短读/短写、权限错误、断线、磁盘满、陈旧 stat 与非原子 rename 报告，且在并发/race 下可重复。 | Fake 故障脚本 Groups A–E、重复/race/fuzz、双工具链和独立终审全部通过，终审为零 High/Medium/Low findings。见 [Stage 0 verification](../verification/stage-00.md#core-evidence)。 |
| CORE-006 | 配置 schema 与默认值 | 0 | Verified | 配置有版本号、确定默认值、严格类型和可定位错误；未知安全关键字段不被静默忽略。 | 严格 schema、默认值、版本、未知字段与 deterministic clock 测试通过本地及 Hosted 门禁。见 [Stage 0 verification](../verification/stage-00.md#core-evidence)。 |
| CORE-007 | 跨平台持续集成基线 | 0 | Verified | 每次变更至少在 macOS 与 Linux 执行 build、unit、contract 和格式检查，Go race/fuzz 按阶段接入。 | 固定 SHA workflow、双平台 native/oldstable、四目标 build、八份独立缓存 reproducibility producer、compare 与 final provenance aggregation 在 Hosted run 29394698864 全部通过。见 [Stage 0 verification](../verification/stage-00.md#core-evidence)。 |
| CORE-008 | 文档真相链 | 0 | Verified | Vision、Feature Matrix、Stage Spec、Verification、Project State 可双向追溯；新会话按固定读取顺序恢复当前阶段。 | docscheck、持久计划和两轮独立 cold-start audit 均通过；完成证据与 Stage 1 M1.1 第一动作已归档。见 [Stage 0 verification](../verification/stage-00.md#core-evidence)。 |

## 4. 认证与信任链

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| AUTH-001 | 系统 OpenSSH 为唯一 SSH 实现 | 1 | Verified | 远端会话按 ADR-0001 以 validated absolute OpenSSH path 和精确安全 argv 启动 SFTP subsystem；默认 `/usr/bin/ssh`、不查找 PATH，产品不实现第二套 SSH/Kerberos 协议栈。 | Exact argv/path/inode tests plus real sshd and MIT Kerberos runs prove the same system OpenSSH stdio path; no embedded SSH/Kerberos stack or `kinit` path exists. See [Stage 1 verification](../verification/stage-01.md#m13-feature-evidence). |
| AUTH-002 | 有边界地继承 `~/.ssh/config` | 1 | Verified | Host/Include/Match/Identity/host-key/auth/GSSAPI/proxy/agent按system ssh生效；SFTP固定关闭TTY/escape/forward/commands/background/tunnel及新GSS delegation但保留ControlMaster兼容，既有master状态是用户边界。Helper与remote `!`/`gs`另强制fresh transport、GSS delegation和ControlMaster/Path/Persist off。 | Real Host/Include/Match/Identity/host-key/auth/GSSAPI/proxy/agent fixtures, conflicting fixed options, and ControlMaster new/reuse all pass through system OpenSSH in Hosted auth runs. See [Stage 1 verification](../verification/stage-01.md). |
| AUTH-003 | Kerberos/GSSAPI 认证 | 1 | Verified | 有效本机 Kerberos ticket 和 OpenSSH GSSAPI 配置可直接建立 SFTP 会话，产品不要求再次保存密码。 | Isolated MIT realm, GSSAPI-only sshd, valid/missing/expired/external-renewal cases and cache-copy scan passed in Hosted job 87330882913. See [Stage 1 verification](../verification/stage-01.md#m13-feature-evidence). |
| AUTH-004 | OpenSSH 支持的 agent/密钥/交互认证 | 1 | Verified | SSH agent、文件密钥、硬件密钥、密码与多因素认证由已安装 OpenSSH 处理；产品只转交交互提示和结果。 | Real key, login-agent, password and key-passphrase+password MFA passed through the system OpenSSH/askpass path in Hosted job 87330882913; fixed argv does not replace OpenSSH credential providers. See [Stage 1 verification](../verification/stage-01.md#m13-feature-evidence). |
| AUTH-005 | ProxyCommand/ProxyJump 链路 | 1 | Verified | 用户现有代理命令与跳板配置无需复制到产品即可用于浏览和任务连接。 | Real ProxyCommand and ProxyJump SFTP cases passed without application-side config parsing in Hosted job 87330882913. See [Stage 1 verification](../verification/stage-01.md#m13-feature-evidence). |
| AUTH-006 | askpass broker | 1 | Verified | daemon 将 OpenSSH 交互提示发送到当前受信任 TUI，答案仅在内存和该次认证会话中存在；提示来源与目标端点清晰可见。 | Broker owner/race/detach/no-client/timeout/cancel/prompt-limit tests, same-binary askpass/TUI tests, focused race and real password/MFA/cancel fixtures pass. See [Stage 1 verification](../verification/stage-01.md#m13-feature-evidence). |
| AUTH-007 | 无附着客户端时等待认证 | 2 | Verified | 任务需要交互认证且没有 TUI 时进入 `waiting_auth`；重新附着后可继续，不进行无限失败重试。 | Transfer Manager classifies `auth_required` separately as durable `waiting_auth`; Resume returns it to queued after system OpenSSH auth is repaired. Existing real auth/reattach Hosted jobs cover broker detach/renewal. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| AUTH-008 | Host key 语义不降级 | 1 | Verified | StrictHostKeyChecking、known_hosts 和主机密钥变更完全遵循系统 OpenSSH；产品不自动接受未知或变化密钥。 | Known-host cases, explicit first-use confirmation, and changed-key fail-closed with unchanged known_hosts passed in Hosted job 87330882913. See [Stage 1 verification](../verification/stage-01.md#m13-feature-evidence). |
| AUTH-009 | 不持久化认证秘密 | 1 | Verified | SQLite、配置、缓存、日志和诊断包中均不出现密码、私钥内容、askpass 答案、agent 数据或 Kerberos ticket。 | Injected password/MFA/passphrase markers were absent from test-owned config/runtime/log/workspace artifacts; Kerberos cache path/content-copy scans and destruction passed in Hosted job 87330882913. See [Stage 1 verification](../verification/stage-01.md#m13-feature-evidence). |
| AUTH-010 | 认证失败可恢复 | 2 | Verified | 认证失败保留任务与安全检查点，展示端点和 OpenSSH 原因；票据/agent 修复后可手动或策略化重试。 | Auth errors do not discard JobJournal state or part; Jobs exposes waiting reason/recent stable error, and explicit Resume reuses frozen Plan after external credential renewal. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |

## 5. 连接与端点

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| CONN-001 | 本地文件系统端点 | 1 | Verified | 任一栏可浏览本地绝对路径，并使用与远端一致的导航、选择和能力展示模型。 | LocalFS shared contract, pagination/cancellation/symlink/range tests, local/local PTY and four Hosted native legs pass. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-002 | OpenSSH stdio 上的结构化 SFTP | 1 | Verified | daemon 按 ADR-0001 的 validated-absolute-ssh-path 与精确 argv 从子进程 stdin/stdout 创建 SFTP client；binary/host 均不可经 PATH 或选项注入，也不解析人类可读 `sftp` 命令输出。 | Exact path/argv and structured pkg/sftp Provider contracts plus the real two-sshd Hosted run 29401801663 pass; poisoned PATH remains 0-hit. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-003 | CLI 双路径启动 | 1 | Verified | `amsftp <left> <right>` 接受本地路径与 `host:path`，两个初始位置均被解析、连接并独立报告错误。 | Zero/one/two Location parser, stable Endpoint startup, pane-local errors and the Hosted local/local, local/remote and remote/remote PTY matrix pass in run 29467496969. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-004 | 无参数 SSH host 模糊选择 | 1 | Verified | 无参数启动从 OpenSSH 配置可见 Host alias 构建可搜索列表；通配符和不可直接选择项有确定处理。 | Include expansion, positive/negated/wildcard Host discovery, deterministic fuzzy picker/manual-alias tests and picker render snapshots pass. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-005 | 多远端并存 | 1 | Verified | daemon 可同时维护 remote A 与 remote B 会话，连接、错误、认证提示和能力不会串线。 | Two isolated real sshd endpoints browse independently and one-session disconnect isolation passed in Hosted run 29401801663; pane connection epochs prevent async cross-talk. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-006 | 自动断线检测与重连 | 1 | Verified | EOF、网络中断和 server restart 被识别为连接状态变化；只读操作可安全重发，写任务交给恢复状态机。 | Typed interruption, bounded reconnect, stale-generation rejection, real sshd restart, daemon restart and nearest-parent recovery pass in Hosted run 29467496969. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-007 | 会话级能力探测 | 1 | Verified | 建连后记录 SFTP 扩展、路径/rename 能力和 helper 可用性；重连后重新探测，旧能力不盲目复用。 | Session/generation snapshots, replacement/withdrawal/stale-revision tests and the real reconnect transaction prove capabilities are re-established without reusing the old snapshot. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-008 | 规范化 Location | 1 | Verified | Location 始终由 Endpoint ID 与规范化绝对路径组成；不同端点的相同路径不会被视为同一对象。 | Domain, IPC, CLI and workspace Location tests cover roots, local/remote parsing, endpoint identity and raw-byte preservation. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-009 | 连接错误带上下文 | 1 | Verified | 错误至少包含端点显示名、阶段、OpenSSH 退出/标准错误摘要与可行动建议，不只显示“连接失败”。 | Pane errors name the Host alias; safe summaries carry stage, request ID, code, endpoint and retry/effect while bounded daemon logs preserve the cause. Auth, host-key, refused/transport and subsystem classifications plus actionable messages pass. See [Stage 1 verification](../verification/stage-01.md). |
| CONN-010 | 受控会话复用与空闲回收 | 5 | Verified | 同端点操作在安全范围内复用连接；并发和空闲回收受配置限制，不因大任务无限创建 SSH 进程。 | Endpoint lease 复用同一 Provider；动态连接硬上限 32，超限 typed `resource_exhausted`，最后一个 session/Job lease 释放即关闭且额度可重用。Stage 5 exact-SHA push/PR Hosted evidence 已通过。见 [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence)。 |

## 6. 双栏浏览

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| PANE-001 | 两个平等文件栏 | 1 | Verified | 启动后始终存在左右两栏；每栏拥有独立端点、路径、光标、选择、排序和加载状态。 | Reducer/render snapshots, independent sort/hidden/refresh controls, 50k visible-window benchmark and native PTY legs pass. See [Stage 1 verification](../verification/stage-01.md). |
| PANE-002 | 任意端点组合 | 1 | Verified | local/local、local/remote、remote/local、同 remote 双路径和 remote A/remote B 均能浏览。 | Independent pane routing and the real local/local, local/remote, remote/remote and two-sshd Hosted PTY scenarios pass in run 29467496969. See [Stage 1 verification](../verification/stage-01.md). |
| PANE-003 | 栏内独立切换端点 | 1 | Verified | 用户可只改变活动栏的端点，另一栏路径、选择和连接保持不变。 | Endpoint reducer tests plus real one-pane sshd restart and second-pane fresh-marker evidence prove pane-local switching/recovery and committed-state preservation. See [Stage 1 verification](../verification/stage-01.md). |
| PANE-004 | 增量目录加载 | 1 | Verified | 目录项以流式/分页方式进入模型，首屏无需等待完整目录读取，加载可取消。 | 256-entry daemon pages, slow/cancel tests and ADR-0011's packet-bounded cursor prove `Limit=1` returns before a blocked second source batch; full current/oldstable/Hosted gates pass. See [Stage 1 verification](../verification/stage-01.md). |
| PANE-005 | 虚拟化文件列表 | 1 | Verified | renderer 只构造可见行与有界 overscan，不为数万条目录项创建等量 UI 组件；5 万项结构性基准证明渲染与分配规模只随可见窗口增长。 | Visible-window/overscan tests and the 50,000-entry structural benchmark pass locally and under the full CI gate. See [Stage 1 verification](../verification/stage-01.md). |
| PANE-006 | 文件元数据与类型展示 | 1 | Verified | 名称、类型、大小、修改时间、权限和链接信息按端点可用数据展示；未知字段不伪造。 | Shared LocalFS/SFTP Provider contracts and renderer snapshots cover kind, size, second-precision mtime, permissions, link target and absent fields. See [Stage 1 verification](../verification/stage-01.md). |
| PANE-007 | 排序、隐藏文件与刷新 | 1 | Verified | 每栏可独立选择排序、显示隐藏文件和手动刷新；刷新尽量保留规范化路径对应的光标/标记。 | Independent-pane sort/hidden/refresh reducer tests and canonical Location cursor/mark remapping pass. See [Stage 1 verification](../verification/stage-01.md). |
| PANE-008 | 直接路径导航 | 1 | Verified | 用户可输入绝对路径或进入父/子目录；无权限、不存在和非目录路径保留原位置并显示原因。 | Location/path-mode, parent/child/root, failed transaction and nearest-accessible-parent tests pass without changing a pane before success. See [Stage 1 verification](../verification/stage-01.md). |
| PANE-009 | 符号链接可见且递归保守 | 2 | Verified | 链接目标信息可见；递归复制/搜索默认不跟随目录链接，除非用户策略显式允许并有循环保护。 | M2.3 discovery emits links without recursing/copying; M2.4 recursive delete removes only the link and preserves its target. Million-entry/depth and dedicated symlink tests pass on the exact candidate. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| PANE-010 | 部分结果状态 | 1 | Verified | 列表被取消、断线或权限错误截断时明确标为 partial，不把已显示条目误报为完整目录。 | Provider contract plus generation-safe reducer/render tests retain partial pages, reject stale completion and recover on refresh. See [Stage 1 verification](../verification/stage-01.md). |

## 7. 工作区与启动

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| WORK-001 | 保存双栏工作区 | 1 | Verified | 工作区保存两栏端点引用、路径、排序/过滤、布局与缓存策略，保存动作原子化。 | Versioned round-trip, owner-private `0600`, interrupted replacement/corrupt preservation and real two-remote PTY save/reopen pass in Hosted run 29467496969. See [Stage 1 verification](../verification/stage-01.md). |
| WORK-002 | `--workspace` 启动 | 1 | Verified | `amsftp --workspace <name>` 恢复工作区；缺失端点或路径逐栏报错且允许修复。 | CLI exclusivity, stable Endpoint restoration, corrupt/missing fallback and exact real `--workspace` re-entry with both remote markers pass. See [Stage 1 verification](../verification/stage-01.md). |
| WORK-003 | 最近工作区与模糊选择 | 1 | Verified | 无参数启动可选择已保存工作区或 SSH host，最近使用排序可预测且不影响显式搜索。 | Deterministic recent workspace ordering, corrupt summaries, host merge, fuzzy/manual selection and minimum-size picker snapshots pass. See [Stage 1 verification](../verification/stage-01.md). |
| WORK-004 | 工作区不含秘密 | 1 | Verified | 工作区只引用 OpenSSH host alias 和产品设置，不包含密码、私钥、ticket 或认证答案。 | Strict workspace schema/serialization tests and Hosted auth artifact scans prove only Endpoint aliases and UI policy are stored. See [Stage 1 verification](../verification/stage-01.md). |
| WORK-005 | 每工作区缓存模式 | 3 | Verified | 工作区可选默认 LRU、ephemeral 或 pinned offline；恢复时准确应用并显示当前模式。 | Workspace v2 严格 round-trip 三种策略、从 v1 确定迁移；新工作区默认 LRU，`gp` 显式循环策略，状态栏始终显示当前值，恢复后的策略传给 edit/open materialization。`TestDocumentV2AcceptsOnlyFrozenCachePolicies`、`TestCachePolicyDefaultsToLRUAndCyclesExplicitly` 和 `TestEditCoordinatorAcceptsOnlyFrozenCachePolicies` 通过。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| WORK-006 | 工作区 schema 迁移 | 6 | Verified | 旧版本工作区在备份后确定性迁移；不可迁移内容不被覆盖，并给出导出/修复路径。 | 13 项历史 source 均 provenance/SHA-256 固定且由当前 owner reader 验证。workspace v1→v2 发布前把 strict-decoded exact v1 bytes 写入独立 0600 临时 inode、fsync，并通过 no-replace link 固定为 `<name>.schema-v1.backup`；byte-identical retry 幂等复用，冲突备份、发布前源变化和不可迁移输入全部 fail closed 且不覆盖任一证据。真实历史 fixture、失败重试、竞态、v2 非迁移和无损修复说明通过 current race 与 exact oldstable 聚焦门禁；SQLite v1–v4 的 verified backup/restore/rollback 属于 REL-003 的独立边界。见 [compatibility boundaries](compatibility-boundaries.md)、[operations recovery](../operations/runbook.md#workspace-migration-recovery) 与 [Stage 6 evidence](../verification/stage-06.md)。 |

## 8. Vim-first 交互

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| VIM-001 | 默认 Normal 模式 | 1 | Verified | 启动与退出弹层后回到 Normal；当前模式始终可见，普通字母不会意外输入路径。 | Mode reducer, tcell translation and status-line/modal snapshots prove Normal startup and layered cancellation. See [Stage 1 verification](../verification/stage-01.md). |
| VIM-002 | `h/j/k/l` 导航 | 1 | Verified | `j/k` 移动光标，`h` 返回父目录，`l` 打开目录或触发默认文件动作；边界行为稳定。 | Pure reducer/tcell tests cover local and remote-independent Location navigation, bounded numeric counts and safe repeat. See [Stage 1 verification](../verification/stage-01.md). |
| VIM-003 | `Tab` 切换活动栏 | 1 | Verified | `Tab` 只改变活动栏，两个 PaneState 均完整保留；视觉焦点清晰。 | Pane-state independence, tcell translation and focused renderer snapshots pass under full race/CI gates. See [Stage 1 verification](../verification/stage-01.md). |
| VIM-004 | `v`/`V` 连续选择 | 1 | Verified | Visual 选择可扩展、收缩、确认或取消；刷新后按规范化条目身份重映射，无法重映射时明确移除。 | Visual selection reducer tests and canonical Location remapping across refresh pass. See [Stage 1 verification](../verification/stage-01.md). |
| VIM-005 | `Space` 离散标记 | 1 | Verified | Normal 模式下 `Space` 独立增减当前条目标记，与连续 Visual 选择可合并为操作集合且不重复。 | Discrete mark toggling, selection identity and refresh remapping tests pass without producing mutation intents. See [Stage 1 verification](../verification/stage-01.md). |
| VIM-006 | `y` 复制引用 | 2 | Verified | `y` 将选中 Location 引用写入内部剪贴板，不读取文件内容、不立即启动传输。 | Reducer and PTY tests capture single/multi/directory FileRefs through the daemon without a mutation; Planner revalidates before Job creation. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| VIM-007 | `d` 剪切引用 | 2 | Verified | `d` 只标记 cut intent，不删除或重命名任何源；状态栏清晰显示待移动项。 | Lowercase `d` shares capture only; source mutation occurs only after confirmed `p`, destination commit, SHA-256 proof and source revalidation. PTY proves the separation. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| VIM-008 | `p` 创建操作意图 | 2 | Verified | `p` 以剪贴板和目标栏位置创建不可变 OperationIntent；普通复制直接排队，高风险项进入确认。 | Copy queues one Job per frozen item; cut paste opens a bounded move confirmation. Counted paste caps at 1,024 and all Jobs re-enter Planner/conflict checks. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| VIM-009 | `D` 执行真实删除 | 2 | Verified | 只有大写 `D` 进入删除计划与确认；确认内容包含端点、数量、目录递归和可逆性。 | Key/reducer tests distinguish `d`/`D`; `D` captures frozen identities then requires scope and irreversible confirmations. Native PTY proves both prompts and deletion. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| VIM-010 | 计数前缀 | 2 | Verified | 支持如 `5j`、`3k` 和适用操作的计数；计数越界有确定截断且不会泄漏到下一命令。 | Reducer bounds navigation at 1,000,000, freezes counted `y`/`d`/`D` runs, caps counted paste Jobs at 1,024 and clears counts after one command. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| VIM-011 | `.` 重复 | 2 | Verified | 重复最近一次可重复的高层动作，重新绑定当前 Location 并重新执行能力/风险检查，不复用过期确认。 | Copy repeats frozen intent; cut/rename reopens move confirmation; delete restarts both confirmations. Stale identities are revalidated by Planner. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| VIM-012 | 可取消的模式与弹层 | 1 | Verified | `Esc` 按层级退出输入、选择、预览或确认，不中断已经排队的后台任务。 | Auth, filter, path, endpoint, workspace, selection and preview cancellation tests prove layered Esc behavior; real auth cancel returns to TUI control. See [Stage 1 verification](../verification/stage-01.md). |
| VIM-013 | 可配置键位且 Vim 默认不变 | 6 | Verified | 用户可重映射非保留动作并检测冲突；无配置时始终使用文档化 Vim 默认键位。 | 完整默认 action/input/remappable snapshot 已冻结；Normal/Visual 上下文可移动非保留动作，冲突、重复 action、未知 context/action、数字、控制/多 rune、危险/序列保留项均拒绝；运行时固定退出键 `q` 仅在其不可达的 Normal remap 位置拒绝，显式 Visual remap 仍可用。app 已把 validated map 接入 tcell translation，reducer/Job 路径不变。真实 tcell→reducer 组合回归覆盖 count-before-lookup、上下文 Visual 移动、保留 `.` 和目录删除确认。版本化有效键位导出包含两个 context 与 default/override 状态；显式 `reset-keymap --yes` 以 0600 临时文件/fsync/原子替换只清除 overrides 并保留其余配置。最终 cold-start audit 以不存在的配置启动完整有效键位导出，精确验证 74 条 Normal/Visual 默认绑定、Vim `hjkl`/Visual/repeat 及零 override，且不创建配置文件；current race 与 exact oldstable 通过。见 [Stage 6 evidence](../verification/stage-06.md). |
| VIM-014 | 1.0 不实现宏与命名寄存器 | 6 | Verified | 帮助和配置 schema 不宣称完整宏/命名寄存器；相关键不会造成未定义破坏性行为。 | action registry 与 schema 不含 macro/named-register action，未知 `macro_record` 等动作 fail closed；文档明确单一 durable clipboard 与 frozen repeat 边界，保留 `.` 的真实事件回归证明 remap 不会引入第二条动作路径。最终 cold-start audit 证明含 `macro_record` 的 owner-private 配置以 config exit 3 拒绝且原字节不变，并逐一检查 help、man 与 bash/zsh/fish completion 不宣称 macro/named-register；current race 与 exact oldstable 通过。见 [Stage 6 evidence](../verification/stage-06.md). |
| VIM-015 | `r` 重命名 | 2 | Verified | `r` 以当前或单个选中条目创建重命名意图，提交前验证同端点能力、目标冲突与源身份。 | `r` rejects multi/root/same/invalid names, captures one frozen FileRef and creates a durable same-endpoint move. PTY rename and atomic/fallback tests pass. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| VIM-016 | `K`/`J`/`L` 抽屉快捷键 | 3 | Verified | 大写 `K` 打开 Preview、`J` 打开 Jobs、`L` 打开 Log；重复按键按定义收起或聚焦，不与小写导航串线。 | `TestDrawerReducerOpensFocusesSwitchesAndClosesWithoutChangingPane`、lowercase separation、tcell translation 及 normal/narrow snapshots 覆盖 open/switch/refocus/close、Esc 和 pane context；workspace 只持久化结构状态。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| VIM-017 | `e`/`o` 文件动作 | 3 | Verified | `e` 始终进入 Vim-first 编辑流程，`o` 始终进入本机 opener 流程；目录或不支持对象给出明确反馈。 | `TestReducerRoutesEditorAndOpenerOnlyForRegularFiles` 覆盖键位/对象门禁；edit coordinator 将两者接到完整 materialize/lease/handoff 流，native Vim 与 macOS `open` 通过，Linux `xdg-open` harness 已纳入 Hosted native。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| VIM-018 | `/`、`f`、`g/` 搜索键序列 | 4 | Verified | `/` 进入当前目录过滤，`f` 进入递归文件名搜索，`g/` 进入递归内容搜索；组合键超时/取消不触发其他写操作。 | `internal/tui/search_test.go`、`tcell_test.go`：模式、冻结 scope、Esc cancel、generation isolation；native PTY gate 已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| VIM-019 | `!` 与 `gs` 命令键序列 | 3 | Verified | `!` 进入一次性命令，`gs` 暂停并进入当前端点 shell；键序列只在 Normal 模式解析。 | `TestCommandModeFreezesPaneContextOnlyAfterExplicitConfirmation`、`TestGoShellSequenceFreezesActivePaneWithoutPathFallback` 与 tcell translation 覆盖 Normal-mode 进入、确认、local/remote frozen context 和显式 home fallback；native local/remote PTY harness 覆盖真实路由。见 [Stage 3 evidence](../verification/stage-03.md#m34--command-shell-and-platform-closeout)。 |

## 9. 本地 daemon 与 IPC

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| DAEM-001 | 按用户自动启动 daemon | 1 | Verified | 客户端连接不到 daemon 时可安全拉起一个当前用户实例；并发启动最终只有一个有效监听者。 | Ten-process lock convergence, stale socket, five reconnects, idle cancellation, PTY exit/re-entry and four Hosted native legs pass. See [Stage 1 verification](../verification/stage-01.md). |
| DAEM-002 | 权限受限 Unix socket 与路径信任链 | 1 | Verified | config/state/cache/log/runtime应用根为euid-owned真实`0700`，文件/socket不宽于`0600`；ancestor/ssh用integrity-only，owner roots/files用owner-private。仅受测APFS/ext4/XFS（runtime可含受测tmpfs）明确no-ACL/unsupported可回到DAC；真实ACL查询/解析错误、NFSv4/CIFS/未知FS、不安全path/override均fail closed。runtime唯一宽松DAC回退是root-owned sticky`01777 /tmp`；peer非当前uid不可连接且无TCP。 | Path/ACL/lock/socket tests include Darwin kernel deny/direct/inherited allow profiles, Linux kernel access/default ACLs, cross-process lock exclusivity and a root-gated real hostile-other-UID socket peer. All four native security steps passed in exact-head Hosted run 29417470068. See [Stage 1 verification](../verification/stage-01.md). |
| DAEM-003 | TUI 可附着/分离 | 1 | In Progress | TUI 退出只关闭客户端连接；重新启动可恢复 panes/workspace 并订阅现有任务。 | Stage 1 client detach/re-entry and workspace restoration pass locally and in native PTY smokes; subscription to durable Stage 2 tasks is intentionally not yet implemented. See [Stage 1 verification](../verification/stage-01.md). |
| DAEM-004 | TUI 退出后任务继续 | 2 | Verified | 最后一个 TUI 退出后，非交互任务继续运行；需要交互时进入等待状态。 | Manager owns a daemon context, not request context; client-cancellation test completes the Job, and PTY exits/reattaches to the same durable Jobs store. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DAEM-005 | 事件流与重放游标 | 2 | Verified | 客户端按游标接收任务/连接事件；短暂断连后可补齐或用一致快照恢复，不重复执行命令。 | Monotonic Job events are queried by after-sequence with bounded limits; request dedup and snapshot/Jobs reload tests prevent command replay across daemon restarts. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DAEM-006 | 单写者持久状态 | 2 | Verified | SQLite仅在本地APFS/ext4/XFS且跨进程WAL/full-durability probe通过。唯一pristine是final+sidecars全不存在；existing先raw identity，无sidecar才immutable，有合法sidecar先probe/recovery再验证whole-main schema/history。final-absent intent bootstrap、受测SQL lexer、original..target durable attempt、sanitized/hold backup、非ready状态显式恢复、retention/space/fullsync/no-replace及最终清attempt→retention→checkpoint→immutable均fail closed；online WAL为64MiB soft/256MiB stop/264MiB absolute。 | ADR-0008 的 filesystem/identity/bootstrap/probe/migration/backup/retention/WAL/crash 矩阵均通过；Hosted run 29476167115 的两个 Linux native 作业在 ext4 与私有 XFS 上通过完整状态套件和真实 ENOSPC 回滚。single-writer、busy、foreign DB 零写和 Stage 1 fail-closed degradation 同时覆盖。见 [M2.1 feature evidence](../verification/stage-02.md#m21-feature-evidence)。 See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DAEM-007 | daemon 重启恢复 | 2 | Verified | 异常退出后重新启动可恢复未完成任务、缓存租约和连接需求，并先验证外部状态再继续。 | M2.1 state recovery plus M2.2 part, M2.3 directory and M2.4 commit→delete restart fixtures prove conservative pause, external revalidation and explicit resume. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DAEM-008 | helper 不作为 daemon 前置 | 1 | Implemented | 本地 daemon 在所有远端均未安装 helper 时仍提供完整 Level 0 功能。 | Stage 1 Explorer and Stage 2 local/SFTP copy, same-remote and remote A→B relay all run without helper; exact M2.3 Hosted push/PR matrices prove the complete Level 0 baseline. See [Stage 2 verification](../verification/stage-02.md#m23--directory-copy-and-dual-remote-relay). |

## 10. 预览

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| PREV-001 | 基础文本预览 | 1 | Verified | 活动栏文件可在底部 Preview 抽屉中查看有界文本；预览不阻塞目录导航。 | LocalFS/SFTP bounded reads, 64 KiB RPC/model cap, binary detection, split UTF-8, multiline sanitization, stale-chunk rejection and cancellation pass. See [Stage 1 verification](../verification/stage-01.md). |
| PREV-002 | 代码与 JSON 结构化显示 | 3 | Verified | 常见代码按语法高亮，合法 JSON 可格式化且保留原始查看；解析失败回退为纯文本并说明。 | 依赖-free bounded code spans、formatted/raw JSON toggle、256 KiB/depth-64 JSON、512 KiB/10,000-line output 和 invalid/partial/deep fallback 由 `TestRenderCodeReturnsBoundedANSIIndependentSyntaxSpans`、JSON/render tests 与 `TestPreviewDrawerKeysEmitBoundedReadModesAndViewToggles` 覆盖。见 [Stage 3 evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |
| PREV-003 | 元数据预览 | 3 | Verified | 文件、目录、链接和二进制对象均可显示端点、规范化路径、大小、时间、权限、类型和可用哈希。 | `TestRenderObjectMetadataIncludesBoundedSanitizedProviderFacts` 覆盖 Endpoint/path/kind/size/mtime/mode/link/fingerprint/hash；hostile fields 在组合前受限。`TestPreviewLocationRendersObjectMetadataWithoutContentRead` 覆盖 file/directory/symlink，且断言 0 content read/0 materialization。见 [Stage 3 evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |
| PREV-004 | 有界读取与继续加载 | 3 | Verified | 初次只读取配置上限；用户可继续加载，取消后停止远端读；内存不随文件总大小线性增长。 | 每次 Provider read 64 KiB、滑动 retained window 512 KiB；continue/cancel/short response/overflow 与 allocation-to-output tests 通过，100 GiB external fallback 在 0 read/0 materialize 处拒绝。见 [Stage 3 budgets and evidence](../verification/stage-03.md#frozen-persistence-and-resource-contracts)。 |
| PREV-005 | 大文本 head/tail | 3 | Verified | 用户可切换头部、尾部和指定偏移附近内容；远端支持 seek 时不下载前置全部字节。 | `TestPlanHeadTailAndRangeUseExactBoundedOffsets` 对 100 GiB 文件断言 head/tail/range 精确 offset/64 KiB limit；TUI key tests 发出 head/tail/range/continue 模式，Provider range 直接 seek。见 [Stage 3 evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |
| PREV-006 | 终端图片协议探测 | 3 | Verified | 自动探测 Kitty、iTerm2 和 Sixel 能力，仅使用已确认协议；无能力时不输出破坏终端的控制序列。 | Active bounded probes、exact-response proof 和 none/misprobe fail-closed tests 覆盖 Kitty/iTerm2/Sixel；真实 Kitty 0.47.4 `i=31;OK` probe/131-byte output 通过。handoff 后在 tcell 恢复前重新探测。见 [real protocol evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |
| PREV-007 | 图片降级链 | 3 | Verified | 图片依次使用受支持终端协议、配置的外部预览器、元数据提示；失败原因可见。 | Orchestrator 仅在 active proof 下输出 PNG；none/超像素/损坏/错误 media 走 ordered external rule，再安全回退 metadata/builtin，bounded/redacted 原因可见且总会释放 lease。`internal/externalpreviewer/orchestrator_test.go` 覆盖整链。见 [Stage 3 evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |
| PREV-008 | 可配置外部预览器管线 | 3 | Verified | 按 MIME/扩展匹配显式 argv 模板，不经隐式 shell 拼接；远端文件使用缓存租约。 | Strict config 冻结 ordered extension/MIME rules、timeout/max-input/completeness；runner direct-exec revalidated absolute executable + canonical final file arg，lease 在 materialize/run/release 全程可审计，timeout/cancel kill process group。见 [Stage 3 evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |
| PREV-009 | 二进制安全识别 | 3 | Verified | 检测到二进制或解码错误时不向终端倾倒控制字节，改为十六进制摘要/元数据或外部预览。 | NUL/binary 变为 bounded numbered hex；ANSI/control/invalid UTF-8 被转义，图片 payload 只产生 metadata 或经 proof-gated encoder。render fuzz 与 TUI binary tests 验证 output bound/terminal safety。见 [Stage 3 evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |
| PREV-010 | 预览请求可取消且防串片 | 3 | Verified | 快速移动光标时旧请求被取消或结果按请求 ID 丢弃，不会把旧文件内容显示到新选择上。 | 可见 Preview 的 cursor/pane 变化先 cancel 再发新 intent；完整 Location/fingerprint/request/Endpoint/UI generation 任一不匹配均丢弃。`TestPreviewRejectsEveryFullIdentityMismatch`、stale-chunk 与 drawer generation tests 通过。见 [Stage 3 evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |

## 11. 编辑与本机打开

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| EDIT-001 | `e` 启动终端编辑器 | 3 | Verified | TUI 安全暂停并以当前文件启动终端编辑器，编辑器退出后恢复终端状态和栏位上下文。 | 完整 materialize/lease 后显示冻结 executable 并显式确认；idempotent handoff 保存/恢复 termios、pgrp、alternate/cursor/SIGWINCH，再刷新 frozen pane。fake exit矩阵及真实 Vim direct-exec 通过；Neovim 本地缺失时明确 skip。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| EDIT-002 | Vim-first 编辑器解析 | 3 | Verified | 顺序为显式`executable+argv[]`、`VISUAL`、`EDITOR`、`nvim`、`vim`、`vi`；env只做无expansion/operator的受限词法切分，PATH仅发现，启动前解析/显示absolute executable并direct exec，文件为独立absolute末参数。 | `internal/externalprocess` 覆盖 precedence、quote/backslash、预算、operator/substitution/glob/control拒绝、PATH→canonical absolute、replacement/revalidation、scrubbed env 与 final separate file arg；native Vim 使用同一 Plan。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| EDIT-003 | 远端编辑缓存租约 | 3 | Verified | 打开远端文件前获取内容缓存租约并记录端点、路径和基线版本；编辑期间内容不被 LRU 回收。 | 完整 Provider stream 同时 materialize、验证并冻结 SHA-256 baseline；atomic reachability/lease、heartbeat/restart adoption 与 live quota protection 覆盖全生命周期。pinned offline handoff 明示 freshness unknown，远端不可验证时进入 recovery 而不上传。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| EDIT-004 | 编辑后本地变更检测 | 3 | Verified | 编辑器退出后按内容或可靠元数据判断是否变化；未变化不创建上传任务。 | `TestEditWorkflowClassifiesLocalContentRatherThanMtime` 与完整 change matrix 用 SHA-256 区分 no-change/mtime-only/content-change；no-change/remote-only 不能 freeze upload，非零 editor exit 仍观察但不自动上传。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| EDIT-005 | 上传前远端并发检查 | 3 | Verified | 回传前重新 stat/版本检查远端；基线后发生变化则进入显式冲突，不静默覆盖。 | Remote observation matrix 覆盖 unchanged/modified/deleted/replaced/stat-unknown；Provider-stream SHA additionally detects same-size/same-second content rewrite under the same metadata fingerprint。changed-during-diff 与 identity unknown 均 fail closed。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| EDIT-006 | 编辑冲突处置 | 3 | Verified | 冲突允许查看差异、另存为新路径、跳过或显式覆盖；选择被记录到对应任务。 | UI 无默认地提供 bounded diff、save-as、skip/retain、overwrite；四分支绑定同一 persistent edit session/audit reason，cold-start 可重建 exact diff。`internal/edit/conflict_view_test.go` 与 app/TUI conflict tests 通过。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| EDIT-007 | 编辑回传复用任务引擎 | 3 | Verified | 修改后的上传使用 `.part`、验证、原子提交和持久任务状态，不走无恢复的旁路写入。 | Atomic session→Stage 2 Plan/Job binding、completion/failure/restart tests 保持唯一 mutation path；Plan V2 冻结 exact preservation sibling，LocalFS/SFTP/Fake 先 no-replace 移走并有界验证原内容，再 no-replace 发布 part。same-fingerprint rewrite、old-inode later write、restore/publication failure 与 path tamper 测试证明无静默覆盖且保留 Job-reported safety copy。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| EDIT-008 | `o` 使用本机默认应用 | 3 | Verified | macOS固定`/usr/bin/open`、Ubuntu固定`/usr/bin/xdg-open`，或结构化显式opener；direct exec且canonical absolute文件路径为独立arg。远端文件先入租约缓存，下游同UID app是用户/OS边界。 | Exact default-path table、explicit opener freeze/revalidation 和 final canonical arg tests 通过；真实 macOS `/usr/bin/open -R` 通过，Linux native harness 以 isolated desktop handler 证明 `/usr/bin/xdg-open` 收到 exact path。见 [native evidence](../verification/stage-03.md#native-and-hosted-evidence-matrix)。 |
| EDIT-009 | 外部应用修改检测与回传 | 3 | Verified | opener 返回或监测结束后检测缓存内容变化，变更文件走与编辑器相同的远端版本检查和上传流程。 | `TestOpenerRetainsLeaseUntilExplicitChangeCheck` 与 `TestOpenerObservationNeverCreatesSyncBack` 证明 immediate-return opener 保留 heartbeat/grace lease；只有显式 check 进入与 editor 相同 observation/conflict/Job path，绝不因可能修改自动上传。见 [Stage 3 evidence](../verification/stage-03.md#m33--editor-and-default-opener)。 |
| EDIT-010 | 外部进程终端恢复 | 3 | Verified | 编辑器、opener 或 shell 异常退出后恢复 raw mode、光标、备用屏幕和尺寸处理，不需重启 TUI。 | `internal/terminalhandoff` 覆盖 normal/nonzero/signal/start-failure、pgrp、SIGWINCH/idempotent resume；post-spawn foreground failure 先 terminate+reap child 再 resume，2 秒后仍 stalled 则恢复 TUI 但以单一 `cleanupPending` 阻断新 handoff，避免累积 worker/进程。handoff 后 image capability 在 tcell input 前重探测，native PTY 返回双栏。见 [Stage 3 evidence](../verification/stage-03.md#m34--command-shell-and-platform-closeout)。 |

## 12. 缓存与离线内容

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| CACHE-001 | 默认短期 LRU | 3 | Verified | 内容缓存有全局和工作区配额，超过配额时按 LRU 回收未固定且无租约对象。 | Default 2 GiB/4,096 objects/1 GiB-workspace 的 live admission 在 publish/handoff 前检查 bytes、catalog object count、workspace share，并串行化 concurrent spend；可先 exact-evict clean LRU，pinned/dirty/lease/reference/shared protections 重检且 crash 可恢复。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| CACHE-002 | 内容寻址与去重 | 3 | Verified | 相同内容只保存一份 blob，不同 Location 通过元数据引用；引用计数/可达性在崩溃后可重建。 | Verified SHA-256 blob publication 并发收敛为一份；Endpoint+raw path+fingerprint 派生独立 entry，SQLite references/reachability restart round-trip。shared-entry eviction/clear 保留仍引用 blob。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| CACHE-003 | Ephemeral 工作区缓存 | 3 | Verified | 标为 ephemeral 的工作区在安全租约释放后回收其非共享内容；共享 blob 不误删。 | Workspace v2/`gp` 应用 ephemeral；explicit `gc`/safe clear 只选择无保护 ephemeral entry，active lease/pinned/dirty 保留，`TestClearEphemeralSharedEntryPreservesTheSharedBlob` 证明共享内容不误删。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| CACHE-004 | Pinned offline 内容 | 3 | Verified | 用户显式固定的内容在配额策略中受保护，可离线打开；重连后显示新鲜、陈旧或冲突状态。 | Exact Location/workspace 下唯一 verified pinned entry 可在 typed transport interruption 后重启复用，新 materialization 标 `offline`/freshness unknown；unpinned 不回退，多 fingerprint ambiguity fail closed。重连重新读取并发布新 fingerprint，edit UI 在 launch 前明确 unknown。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| CACHE-005 | 缓存租约 | 3 | Verified | preview/edit/open/上传中的 blob 有可续租的 owner；过期 owner 可回收，活跃 owner 不被清理。 | 30 s heartbeat/2 min expiry/15 min opener grace、PID+birth live/mismatch/gone/uncertain、release 与 exact daemon-restart adoption 由 manual clock/native classifier tests 覆盖；Preview/edit/open owners 已接 IPC/materialization。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| CACHE-006 | 远端版本与失效规则 | 3 | Verified | 缓存记录可用的 size/mtime/file ID/hash；每次需要一致性的操作按策略重新验证，不能仅因路径相同当作最新。 | Typed frozen fingerprint 保留 presence bits、size/mtime/file ID/hash；entry ID 绑定完整 Location+fingerprint，same-path replace/mtime-only/content/unknown 由 Preview identity 与 edit remote matrix fail closed。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| CACHE-007 | 崩溃后清理 | 3 | Verified | daemon 重启识别孤儿下载、过期租约和未引用 blob；只清理可证明不可达项并保留恢复所需 part。 | Scan/reconcile 保留 corrupt/unknown/orphan；claimed eviction idempotently resume。过期且 process-dead 的 Preview handoff 可回收，editor/opener/upload 仍保留恢复入口；dirty/live/uncertain/unknown bytes 不删，unsafe rebuild 进入 `ErrCacheNeedsAttention`。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| CACHE-008 | 缓存权限与秘密隔离 | 3 | Verified | 缓存目录和文件仅当前用户可读写；缓存索引不包含认证秘密，诊断导出默认不包含文件内容。 | `cachefs` 对 0700/0600、umask、owner/type/no-follow/hardlink/symlink/hash 全矩阵 fail closed；strict manifests/catalog 拒绝 unknown/secret/body fields，日志/diagnostic 只存 bounded redacted metadata。见 [security evidence](../verification/stage-03.md#security-privacy-and-pollution-evidence)。 |
| CACHE-009 | 配额不足可解释 | 3 | Verified | 无法为预览/编辑分配空间时报告所需、可用、被固定/租约占用容量，并提供清理入口。 | Live admission/`QuotaDiagnostic` 覆盖 bytes、zero-byte object count、workspace share、required/releasable/pinned/dirty/leased/shared/referenced；all-protected 映射 typed `resource_exhausted`，short read 清 staging 且 catalog 不变。`gC` scoped clear 与 XFS ENOSPC gate 保留。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |

## 13. 命令与 shell

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| SHELL-001 | `!` 一次性命令 | 3 | Verified | local用进程cwd+shell`-c`独立arg；remote用fresh exact ssh argv与byte-safe cwd bootstrap，stdout byte0 marker后才经stdin发送≤32KiB单行用户命令。双stream各1MiB持续drain；cancel后remote effect可unknown，结束刷新。 | Exact local/remote argv/Q/marker/32 KiB/dual-ring/cancel tests 通过；TUI single-flight、confirmation transport/no-fallback 文案、Esc cancel、15-minute deadline 和 completion refresh 已接通。native harness 驱动真实 local/temporary-sshd remote `!` + exact cwd，final exact-SHA Hosted 结果记录于 PR #3。见 [Stage 3 evidence](../verification/stage-03.md#m34--command-shell-and-platform-closeout)。 |
| SHELL-002 | `gs` 进入交互 shell | 3 | Verified | local从pane cwd进入shell；remote用fresh`ssh -tt`，home模式无command，current-cwd需probe且失败显式home retry。TUI完整移交/收回TTY pgrp并恢复termios/alternate/cursor/SIGWINCH。 | Exact local/home/current-cwd plans、probe binding 与 explicit fallback tests 通过；terminalhandoff 覆盖 pgrp/termios/alternate/cursor/SIGWINCH，真实 local `/bin/sh` PTY 通过，temporary-sshd remote PTY 已成为 Hosted quality gate。见 [native evidence](../verification/stage-03.md#native-and-hosted-evidence-matrix)。 |
| SHELL-003 | 不内嵌终端模拟器 | 3 | Verified | 命令与 shell 使用当前终端和系统进程；产品不实现嵌入式终端状态机。 | 实现仅有 direct process/OpenSSH plan、bounded one-shot rings 与 TTY ownership controller；interactive bytes 不 capture/log，代码/依赖审查无 terminal emulator/session manager。native PTY 直接复用当前终端。见 [Stage 3 evidence](../verification/stage-03.md#m34--command-shell-and-platform-closeout)。 |
| SHELL-004 | 命令执行边界清晰 | 3 | Verified | Provider/file动作永不调用shell；Helper是唯一app-generated restricted string。只有用户显式进入`!`/`gs`才有shell语义，app生成部分仅受测cwd bootstrap，user command不与path拼接。 | TUI only explicit intent routes reach commandrun；Provider/RPC 没有 shell method。Local cwd 是 `Dir`，remote app text 仅 tested POSIX-Q bootstrap，user command 只在 byte-0 marker 后作为 stdin line；raw/non-UTF8/unsafe path tests fail closed。见 [Stage 3 evidence](../verification/stage-03.md#m34--command-shell-and-platform-closeout)。 |

## 14. 后台任务模型

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| JOB-001 | 持久化 Job | 2 | Verified | 每个写操作在执行前生成稳定 Job ID，并将意图、冻结计划、阶段、进度、决策和检查点事务化写入 SQLite。 | Copy, move, rename and delete all call Manager create APIs that atomically persist V1 Plan/Job/steps/event before scheduler enqueue; no raw mutation RPC exists. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| JOB-002 | 主状态机 | 2 | Verified | 支持 `draft → awaiting_confirmation → queued → running → verifying → completed`，非法跃迁被拒绝。 | `TestStateTransitions` 穷举全部状态对，`TestTransitionIsTransactionalMonotonicAndRejectsIllegalState` 验证事务、事件序号、重复 request 与非法跃迁；current/Go 1.25.12 及 Hosted quality 均通过。见 [M2.1 feature evidence](../verification/stage-02.md#m21-feature-evidence)。 See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| JOB-003 | 等待与终止状态 | 2 | Verified | 支持 `paused`、`waiting_auth`、`waiting_conflict`、`retry_wait`、`failed`、`canceled` 和 `completed_with_source_retained`，每个状态记录原因和允许动作。 | Real transfer errors/actions enter each state; Jobs renders waiting/terminal/recovery/error summaries, and restart tests cover legal resume/cancel paths. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| JOB-004 | 不可变操作计划 | 2 | Verified | 执行开始后源集合、目标、冲突/完整性策略和初始路由被冻结；变更要求新建或显式修订计划并留痕。 | FileRef, descriptors, capability revisions, route, SHA-256, conflict, part and all memory/discovery budgets are persisted and cross-checked on DecodePlan. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| JOB-005 | 普通复制立即排队 | 2 | Verified | 无覆盖、删除或直传风险的复制由 `p` 直接进入 queued，不要求冗余确认。 | Copy PTY presses `y`, Tab, `p` and observes queued/completed without redundant confirmation; racing final still opens a durable conflict. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| JOB-006 | 高风险计划确认 | 2 | Verified | 覆盖、递归删除、不可逆删除、跨端点移动和直传在执行危险阶段前展示源/目标/数量/策略/可逆性。 | Cut paste shows frozen move confirmation; overwrite waits on conflict; delete shows scope plus irreversible confirmation. Direct is not a Stage 2 route and cannot be selected. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| JOB-007 | 队列与有界并发 | 2 | Verified | daemon 在配置的全局/端点并发上限内调度，公平推进多个任务，不能无限创建 goroutine、文件句柄或 SSH 连接。 | Manager uses fixed worker and queue caps (runtime 4/128; tests 1), acquires frozen endpoint leases before enqueue and bounded directory streams; saturation/control tests pass. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| JOB-008 | 暂停、继续与取消 | 2 | Verified | 用户可暂停、继续或取消支持的任务；动作在安全检查点生效并明确当前阶段是否可中断。 | Durable controls, post-close checkpoint refresh, cancel-part retention, event replay and `J` P/U/C reducer/IPC tests pass. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| JOB-009 | 任务级速度与带宽策略 | 5 | Verified | 可配置全局或任务级带宽上限；限速改变吞吐但不破坏校验、暂停和公平调度。 | Plan 冻结 Job rate；global/双 Endpoint/Job 整数 token bucket、4:1 weighted fairness、固定量子、cancel、future-token hot update 均通过；要求精确限速时禁用 `uncontrolled` 快路径。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| JOB-010 | 任务历史与保留 | 6 | Verified | 成功/失败任务按配置保留摘要和验证证据，清理不删除仍被恢复/审计引用的数据。 | SQLite Version 4 冻结单例持久清理 claim；1.0 固定策略保留最新 1,000 个 terminal Jobs 与 30 天内全部 terminal Jobs。每次调用最多 64 个有 WAL 预算的事务、每表每次最多 8 行，进程死亡后续接同一 Job；nonterminal、edit-session、upload reference、active/uncertain lease 均阻止清理，cache 不能在 claim 后新增 upload reference。daemon 启动在接受工作前续接。focused、current Go 1.26.5 `make ci`、exact Go 1.25.12 `make check` 与 corrected exact-SHA push/PR Hosted 门禁全部通过；已拒绝的首个 candidate 不计入验收。见 [Stage 6 evidence](../verification/stage-06.md)。 |

## 15. 复制与传输

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| XFER-001 | 内部 clipboard 保存 Location 引用 | 2 | Verified | `y`/`d` 保存 Endpoint ID、规范化路径、当时元数据与意图；粘贴时重新验证源，不持有完整内容。 | Clipboard holds immutable FileRef Location/kind/fingerprint/capability revision for single/multi/directory; stale source tests reject changed/deleted identity. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-002 | 本地与远端任意方向复制 | 2 | Verified | local→local、local→remote、remote→local、同 remote 和 remote A→remote B 都通过同一 Job 模型完成。 | Local and bidirectional SFTP PTY plus same-remote and independent remote A→B real-sshd directory fixtures passed exact M2.2/M2.3 Hosted runs. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-003 | remote A→B 默认本机中继 | 2 | Verified | 不满足服务端路径时，daemon 使用固定大小缓冲从 A 读向 B 写，默认不把完整文件落本地磁盘。 | Two independent sshd sessions relay through a 7-byte test buffer with no cache/full local file; exact M2.3 Hosted runs passed. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-004 | 有界内存与背压 | 2 | Verified | 读取速度受目标写入背压约束；缓冲总量与并发配置成比例，不随文件大小增长。 | Plans freeze 256 KiB stream plus 64 queue/256 page/128 depth; 100 GiB synthetic stops at 64 KiB checkpoint and million-entry queue stays at 17. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-005 | 目标临时 part | 2 | Verified | 新内容先写目标目录中的唯一 `.part-<job-id>`；未验证完成前最终目标路径不出现新内容。 | Same-directory unique part, ownership/checkpoint identity, final-absent callback, part-open failure and pause/cancel/restart tests prove no partial final. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-006 | 验证后原子提交 | 2 | Verified | 完成大小与策略要求的完整性验证后，使用端点支持的安全 rename 提交；无法保证原子时计划明确标示并采用保守流程。 | Worker rereads part SHA-256, rechecks conflict, conditionally commits and rereads final; LocalFS/SFTP and response-lost real/fake tests pass. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-007 | 断点与检查点 | 2 | Verified | 可恢复端点记录已确认偏移、源版本和 part 身份；恢复前验证三者仍匹配，不匹配则重启或等待决策。 | Durable offset/source/part/SHA state resumes after disconnect/SQLite reopen; close-time fingerprint and abrupt restart revalidation tests pass. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-008 | 完整性策略 | 2 | Verified | 基线至少验证大小和传输边界；有 hash 能力时可选择强哈希；策略和实际验证证据记录到 Job。 | V1 freezes `stream_sha256`; actual checksum is recorded after source-size, part-hash and committed-final-hash proof. No weaker path is advertised. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-009 | 目录递归复制 | 2 | Verified | 递归遍历流式产生子任务，保留相对结构；权限错误和部分失败按条目记录，不将整树预载入内存。 | Million-entry streaming, nested layout, bounded manifest, selective retry and daemon restart tests pass; exact M2.3 Hosted runs are green. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-010 | 目录创建与元数据策略 | 2 | Verified | 目标目录按需创建；权限/时间等元数据只按显式支持与策略保留，不宣称无法保证的所有权语义。 | LocalFS/SFTP workers create roots/descendants on demand and intentionally claim no ownership/time preservation beyond Provider entries. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-011 | 短读短写安全处理 | 2 | Verified | read/write 返回部分字节时正确推进或失败，不跳过数据、不把短写当成功。 | Bounded read/write-all loops and Fake short-I/O effects advance only actual bytes; disconnect/resume digest checks pass. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-012 | 空间预检与运行期磁盘满 | 2 | Verified | 可获取空间时在计划中预检；运行期 ENOSPC 保留可恢复状态且不提交最终文件。 | Stage 2 providers expose no trustworthy free-space capability, so no preflight is guessed; injected ENOSPC/EDQUOT becomes resource-exhausted retry_wait and final stays absent. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-013 | 单文件与批量进度 | 2 | Verified | Job 展示已处理字节/条目、总量已知性、当前文件和阶段；未知总量不伪造百分比。 | JobView derives durable bytes/items/phase/current paths and optional total; renderer prints a ratio only when total is known, otherwise trusted bytes. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-014 | 路由计划可解释 | 2 | Verified | Planner 输出选择的 local、SFTP relay、same-endpoint 或 direct 路径及能力依据，用户可在执行前查看。 | Immutable Plan persists local/SFTP relay and move strategy/capability. Direct is unavailable in Stage 2; Jobs exposes source/final/phase and terminal route outcome. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-015 | 失败条目可重试 | 2 | Verified | 批量任务可只重试失败/跳过条目，已验证成功项不会重复覆盖；重试继承或重新确认相关策略。 | Permission-denied child enters retry_wait; resume content-revalidates its successful sibling and copies only the missing item. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| XFER-016 | 传输结果清单 | 2 | Verified | 任务完成时记录每项源、目标、字节、提交方式、验证级别和异常，不仅提供汇总百分比。 | Bounded 256-item source/destination/status/bytes/error manifest plus complete aggregates/truncation is persisted and rendered through Job summary. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |

## 16. 移动与重命名

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| MOVE-001 | 同端点优先原子 rename | 2 | Verified | 同一端点且能力允许时使用 rename；跨文件系统或服务器拒绝时不误报成功，转为明确的 copy-delete 计划需重新评估风险。 | Explicit frozen `atomic_rename` selects zero-stream Rename; `Atomic=true`, source absence and final fingerprint are mandatory. Otherwise Plan freezes copy-delete. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| MOVE-002 | 跨端点 copy→verify→delete | 2 | Verified | 跨端点移动先完成目标复制、验证和提交，之后才进入源删除阶段。 | File/directory tests assert destination SHA-256/per-item verification and commit precede frozen source revalidation and conditional deletion; PTY confirms cut/paste. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| MOVE-003 | 验证不足保留源 | 2 | Verified | 无法达到移动要求的验证级别时不删除源，任务以 `completed_with_source_retained` 或等待决策结束。 | Directory mismatch/symlink, capability loss and unproved delete retain source and persist `move_reason` in terminal event/Jobs summary. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| MOVE-004 | 源变化阻止删除 | 2 | Verified | 复制后源版本发生变化时，源删除前检测并保留源；不删除与已复制版本不同的对象。 | `TestManagerMoveRetainsSourceWhenItChangesAfterCommit` mutates the source after final commit and proves retained source plus verified destination. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| MOVE-005 | 部分移动结果可解释 | 2 | Verified | 批量移动逐项记录已移动、已复制但保留源、未复制和失败，重试不会重复删除。 | Each selected source becomes its own durable Job; directory manifests are bounded per item, terminal state distinguishes source-retained, and restart postconditions prevent repeat deletion. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |

## 17. 删除与回收

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| DEL-001 | `D` 专用删除入口 | 2 | Verified | Normal/Visual 中只有显式大写 `D` 创建删除意图；`d` 始终是 cut。 | tcell/reducer and native PTY prove lowercase capture-only versus uppercase prepare/confirm/create-delete routes. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DEL-002 | 删除计划确认 | 2 | Verified | 计划列出端点、路径/数量、目录递归、trash 能力和不可逆项；取消不产生写操作。 | Frozen target count/first path and irreversible warning are rendered before two Enter actions; Escape clears pending scope with zero mutation. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DEL-003 | 有能力时优先 trash | 2 | Verified | LocalFS 或 helper 声明可靠 trash 时可移动到回收位置并记录恢复信息；能力不确定时不伪装成可恢复。 | Optional `TrashProvider` is usable only with exact frozen `trash` capability; positive test avoids irreversible flag. Current LocalFS/SFTP honestly advertise none. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DEL-004 | 纯 SFTP 不可逆警示 | 2 | Verified | Level 0 SFTP 删除明确显示不可逆，必须完成确认后才执行。 | No-trash Plan requires `IrreversibleConfirmed`; TUI second modal says irreversible and native PTY proves it gates the Job. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DEL-005 | 递归删除有界执行 | 2 | Verified | 递归删除流式遍历并逐项记录；符号链接默认只删链接本身，取消后给出已删/未删清单。 | Frozen page/depth budgets drive descendant-validated bottom-up removal; symlink target preservation and depth bounds pass. Aggregate result counts are durable. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DEL-006 | 删除审计与后置检查 | 2 | Verified | 每项删除记录执行结果；连接在响应前中断时重新 stat 判断后置状态，不盲目重复。 | Delete Plan has verify/delete/verify-absent steps and terminal event; explicit and move response-lost tests prove not-found before success. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |

## 18. 冲突与覆盖

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| CONF-001 | 目标存在冲突检测 | 2 | Verified | 计划和提交前均检测目标存在/变化；陈旧预检不能授权静默覆盖。 | Planner preflight plus post-verify commit recheck; racing-final tests cover ask/skip/overwrite/auto-rename without stale authorization. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| CONF-002 | 明确冲突策略 | 2 | Verified | 支持跳过、覆盖、重命名/另存为和取消；目录合并与文件覆盖分别呈现。 | Worker/Manager implement ask, skip, overwrite, auto-rename and cancel/pause retention for file and per-item directory Jobs. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| CONF-003 | Job 级 apply-all | 2 | Verified | “对本任务全部应用”只影响当前 Job 和匹配的冲突类型，不写成无意的全局默认。 | Persisted conflict rows bind scope to Job/class; J keys W/X/A set apply-all only on selected Job, with isolation tests. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| CONF-004 | 无客户端时等待冲突 | 2 | Verified | 后台任务遇到未决冲突进入 `waiting_conflict` 并保留安全状态；附着 TUI 后继续。 | Commit-time conflict persists Job/part and waiting state independent of client; reattached J resolves then resumes frozen Plan. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| CONF-005 | 覆盖仍使用临时提交 | 2 | Verified | 覆盖策略不直接截断最终目标；新内容验证成功后再以端点安全流程替换。 | Existing final remains until complete part SHA-256; replacement requires frozen expected destination fingerprint. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| CONF-006 | 决策留痕 | 2 | Verified | 每个冲突记录检测证据、用户/策略决策、作用范围和最终结果，日志默认不包含内容。 | V1 conflict row records frozen source/destination/class/resolution/scope; transactional resolution event and secret scan pass. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |

## 19. 故障恢复

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| RECV-001 | 网络中断恢复 | 2 | Verified | 读写中断进入可恢复状态，重连后验证源/part/目标再从安全偏移继续或重新开始。 | Disconnect fixture persists offset, closes/reopens endpoint, verifies source/part and completes the exact digest. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| RECV-002 | daemon kill -9 恢复 | 2 | Verified | 在 draft、running、verifying、commit 和 delete-source 阶段 kill -9 后，重启不产生损坏最终文件或重复源删除。 | M2.1 process-death transitions plus part, directory and commit→delete manager-stop fixtures cover conservative restart/postcondition reconciliation; native workflows rerun in Hosted. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| RECV-003 | 仅幂等阶段自动重试 | 2 | Verified | read、临时写等可证明幂等阶段可退避重试；rename、提交、删除先检查后置状态。 | Read/write resume uses checkpoint; commit/rename/delete response-lost tests stat postconditions before any re-execution. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| RECV-004 | 退避与重试预算 | 2 | Verified | 可重试错误使用有上限的指数退避与抖动；不可重试错误立即失败，用户可手动重试。 | Stage 2 deliberately has no blind automatic retry loop: retryable faults enter durable retry_wait with a one-minute earliest retry and explicit Resume; non-retryable errors fail immediately. This is the bounded v1 policy. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| RECV-005 | 取消默认保留恢复材料 | 2 | Verified | 取消默认保留匹配的 part 和检查点，并显示占用与清理动作；策略禁止时在取消前说明会清理。 | Cancel control closes/syncs/restats and retains matching part/checkpoint; terminal summary and test assert retained recovery evidence. Explicit orphan cleanup is deferred rather than guessed. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| RECV-006 | part 身份校验 | 2 | Verified | 恢复仅使用属于同一 Job、目标和源版本的 part；孤儿或冲突 part 不被误接续。 | Job-specific canonical part path plus persisted fingerprint/size/source/final bindings are cross-checked; mismatch rejects resume. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| RECV-007 | 磁盘满、权限与短写保持目标安全 | 2 | Verified | ENOSPC、EACCES 和短写使任务失败/等待，但最终目标不出现未验证的新内容，错误包含失败阶段。 | Permission/resource/short-I/O tests prove typed state and absent unchanged final; real XFS ENOSPC also covers the state store transaction boundary. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| RECV-008 | 恢复证据可审计 | 2 | Verified | 恢复记录原失败、重连/重试次数、验证判断、恢复偏移和最终提交；用户可在 Jobs/Log 查看摘要。 | Monotonic events and checkpoint payloads retain phase/offset/outcome; JobView exposes recent stable error and `job_recovered` reason. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |

## 20. 过滤与搜索

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| SRCH-001 | `/` 当前目录即时过滤 | 1 | Verified | 输入时由本地 daemon 对已加载目录项增量过滤，不产生远端命令；清空过滤恢复原列表与合理光标。 | Loaded/incoming page filtering, clear/cursor behavior, filter-mode key translation and zero remote-command routes pass under full tests. See [Stage 1 verification](../verification/stage-01.md). |
| SRCH-002 | `f` 递归文件名搜索 | 4 | Verified | 从活动栏路径流式返回匹配路径、类型和必要元数据；结果可逐条打开或加入选择。 | `internal/search/filename_contract_test.go`、daemon cursor/TUI、temporary-sshd `f` 与 negotiated Helper filename route 已通过；最终完整门禁已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| SRCH-003 | `g/` 递归内容搜索 | 4 | Verified | 从活动栏路径流式返回文件、行号和有界摘要；二进制、编码和超长行有明确处理。 | `internal/search/content_contract_test.go`、content IPC/daemon/TUI、temporary-sshd `g/` 与 negotiated Helper content route 已通过；最终完整门禁已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| SRCH-004 | 搜索结果流式展示 | 4 | Verified | 首批结果无需等待全树结束；结果带进行中、完成、partial 或 canceled 状态，总量未知时不伪造百分比。 | `internal/search/scale_test.go` 首结果流式断言，search drawers 显示 terminal status/reason；完整 resource gate 已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| SRCH-005 | 搜索可取消 | 4 | Verified | 取消停止本地遍历、SFTP 请求或 helper 子进程，并回收资源；已返回结果保留并标为 canceled。 | search cancellation contract、daemon cancel/drain/session-close tests；最终 native/fault cancellation gate 已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| SRCH-006 | 遍历预算 | 4 | Verified | Level 0 搜索受最大深度、条目、字节、时间和并发预算控制；达到预算返回 partial 和具体限制。 | filename/content hard-limit validation、depth/result/file/read/output/time stop reasons；组合/native resource gate 已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| SRCH-007 | 无 helper 的明确降级 | 4 | Verified | `f` 使用有界 SFTP 遍历；`g/` 仅在用户接受受限慢速模式后运行，UI 明示性能/功能差异。 | Level 0 routes only depend on Provider；`g/` modal/drawer 明示 bounded slow SFTP scan，`TestContentSearchGSlashFreezesActivePaneScope` 验证第二次确认后才启动。 See [Stage 4 evidence](../verification/stage-04.md). |
| SRCH-008 | helper 搜索语义一致 | 4 | Verified | helper 可用时优先安全 `rg` 或内置扫描器，但路径范围、排除、链接与结果格式和基线语义一致。 | `internal/helper/local_operations_test.go` 与 `internal/daemon/helper_search_router_test.go` 覆盖内建 scanner、统一 identity/event contract 和 Helper 关闭后的新请求 Level 0 回退；完整 parity/resource gate 已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| SRCH-009 | 链接与权限边界 | 4 | Verified | 默认不跟随目录符号链接；权限错误按路径报告并允许继续，结果标为 partial 而非整体假成功。 | Provider contract fixture 覆盖 symlink no-follow、permission problem 与 partial reason；temporary-sshd matrix 已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| SRCH-010 | 搜索结果回到双栏动作 | 4 | Verified | 从结果可定位所在栏/路径、预览、编辑或加入内部 clipboard；定位失败保留结果并解释原因。 | `reduceSearchKey`/`reduceContentSearchKey` 路由 open/preview/copy 至既有 Intent；deleted-result UX gate 已通过。 See [Stage 4 evidence](../verification/stage-04.md). |

## 21. 可选远端 helper

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| HELP-001 | Level 0 永久可用 | 1 | Implemented | 未安装、禁用或不兼容 helper 时，标准 SFTP 浏览、传输、中继、恢复和有限搜索仍可用。 | Standard SFTP browse/filter/preview plus Stage 2 transfer/relay/restart all pass without helper; exact M2.3 Hosted runs complete the Level 0 acceptance. See [Stage 2 verification](../verification/stage-02.md#m23--directory-copy-and-dual-remote-relay). |
| HELP-002 | 用户明确批准安装 | 4 | Verified | preliminary consent在probe前展示Endpoint/mapping/审计副作用并声明uid/path未知，取消=0 probe；probe/high-water/artifact/ancestor plan后，final consent展示observed numeric uid/target/path/create list/hash/mode/capability，取消=0 app-tree create/content。plan drift须重probe/确认。 | `internal/helper/installer.go` 与 `installer_test.go`：两阶段冻结视图、preliminary 取消0 probe、final取消0 app-tree mutation/content、fresh probe/attrs drift重确认；production install surface仍CLOSED，最终native矩阵已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-003 | OS/架构/目录与 namespace 预检 | 4 | Verified | 默认 unknown/异构 session mapping 禁用 Helper；验证 root-owned `/usr/bin` utilities 后以 absolute fixed command 获取 non-root uid/physical cwd/uname，严格映射 target。cwd/RealPath 只做 compatibility preflight，不证明 node/mount/object；safe ASCII absolute home component≤255，所有 app dir/final/temp absolute path≤1000，全祖先仅 root/uid owner且不可写/symlink。 | `binding.go`、`binding_probe_process.go`、`installer_test.go`：fixed absolute utilities、uid/physical cwd/uname、target、RealPath、safe-home 255/256、path 1000/1001与ancestor attrs；hostile native矩阵已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-004 | 当前策略验签与安全临时安装 | 4 | Verified | current-policy→prelim consent→probe/target/high-water→artifact hash→plan/final consent后才temp/write；首byte前Chmod+双复核、client回读、standard no-replace。Stage4仅non-release testdata key fixture，production trust拒fixture且dist无fixture assets；production manifests到Stage6 final bytes后才可生成。 | `manifest.go`、`policy.go`、`installer.go`、`sftp_remote.go`：current-policy、fixture隔离、expected+1、EXCL、首write前0600、handle/path复核、回读与OpenSSH hardlink no-replace发布；production verifier为空，最终污染扫描已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-005 | `0700` 原子内容寻址安装 | 4 | Verified | helper 以非 root uid 安装到 fresh safe canonical absolute home 下由 typed fields 派生且≤1000 bytes的 immutable path；44-byte CSPRNG temp、应用树/派生目录 exact `0700`，standard SFTP no-replace 发布，旧任务不被半升级破坏。 | `binding.go`、`installer.go`、`sftp_remote.go` tests覆盖typed content-addressed path、44-byte CSPRNG temp、exclusive create、0700目录/最终文件、collision/interruption与no-replace；adapter拒绝create-then-chmod并要求注入raw-MKDIR primitive，production packet实现随distribution保持CLOSED。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-006 | 受限启动、协议与能力握手 | 4 | Verified | validated absolute ssh fresh argv固定GSS delegation/ControlMaster/Path/Persist off，最后恰一restricted string，仅fixed template+fresh safe home+typed fields；byte0 preface、stderr cap、业务framed stdin。remote same-euid cache/shell/server是边界，不声称无secret。 | `process_session.go`、`binding.go`、`binding_probe_process.go` tests覆盖exact argv、fresh CM/GSS off、content-addressed restricted string、shell metachar/relative/arbitrary-safe-path拒绝、byte0 preface、framed stdin、heartbeat/硬deadline自动进程组取消与stderr 65536/65537；M6.4 SHA `60255dc8` 以显式 Wait barrier 合流 client/process/stderr 终态。SHA `573aeefc` 恢复 resident child + Close barrier 后 PR 24/24，push 暴露单次 write 仅 32,768 bytes；SHA `60ff908` 以 `io.CopyN` 精确写满 65,536/65,537，在双工具链各通过 200 轮、完整本地门禁及 Hosted Helper boundary，未改变协议或 timeout。OpenSSH 8.9 hostile shell矩阵已通过。 See [Stage 4 evidence](../verification/stage-04.md) and [Stage 6 evidence](../verification/stage-06.md). |
| HELP-007 | 非常驻、无监听、无提权 | 4 | Verified | helper 仅由 SSH stdio 按需启动，不监听网络、不注册服务、不请求提权，退出后不留后台进程。 | `internal/app/stage1.go` helper role与`process_session.go`仅接收stdio、拒绝euid0、无listener/service API并以进程组结束；最终native进程/端口/服务扫描已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-008 | 快速遍历与内容搜索 | 4 | Verified | helper 提供有界、可取消的 walk 和 content search 流，遵循根路径、链接和排除策略。 | `local_operations.go`、`local_operations_test.go`与daemon route tests覆盖bounded walk/content、no-follow、取消、partial及百万节点synthetic（首结果约0.29ms、peak alloc增量约3.5MiB）；最终native资源记录已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-009 | 强哈希与磁盘空间 | 4 | Verified | helper 可流式计算强哈希并报告文件系统可用空间；错误/不支持以能力结果返回，不伪造数值。 | `local_operations.go` tests覆盖SHA-256已知值、中途变化invalid fingerprint/时点及statfs total/available；quota明确`unknown`而不推断无限，最终超大文件native资源门禁已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-010 | tail/watch | 4 | Verified | helper 支持有界 tail/watch 事件；断线、文件轮转和事件丢失可见，不承诺无证据的零丢失。 | `local_operations_extra.go` tests覆盖append、truncate、rename rotation、duration/event/byte caps；M6.4 SHA `60255dc8` 为 truncate→rotate fixture 注入精确两次 poll，保留原 80 ms duration 并通过 200 轮 race。watch事件固定`lost_possible/coalesced/requires_refresh`，最终断线native矩阵已通过。 See [Stage 4 evidence](../verification/stage-04.md) and [Stage 6 evidence](../verification/stage-06.md). |
| HELP-011 | 同主机复制增强 | 4 | Verified | 同一远端端点可在能力与策略允许时使用 helper 复制，不经本机传完整内容；结果仍走验证与 Job 状态。 | `transfer/same_host.go`与`helper/same_host_backend.go`：Planner冻结Endpoint+artifact+能力+源身份，Job Store admission覆盖prepare→durable create且remove扫描非终态精确引用；Helper只stage精确part，Worker复用verify/conflict/commit/restart。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-012 | 崩溃安全降级 | 4 | Verified | helper 非零退出、超时、输出损坏或版本变化只使当前增强动作失败/降级，不使 SFTP 会话不可用。 | `helper_search_router_test.go`、`process_session_test.go`、`enable_test.go`与same-host cancel tests覆盖关闭/畸形/版本能力丢失后的L0新请求和Provider继续可用；无自动重启循环，最终真实kill/hang矩阵已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| HELP-013 | 安全升级与清理 | 6 | In Progress | 新版本并行安装和握手成功后切换；只删除无活跃任务引用的旧版本，支持显式卸载且不影响 Level 0。 | Helper state v2 已冻结并通过 exact-SHA Hosted lifecycle。公共 Helper 已枚举 status/install/upgrade/disable/remove：install/upgrade/remove 要求 literal shared-session/stable-home consent，disable 禁止该无关 consent，所有命令拒绝任意 manifest/artifact/path 输入；restricted serve 仍是独立私有进程角色且不在补全中。release source 现只先读取有界 manifest/signature；current-policy 与 signed identity 通过后，preliminary consent 拒绝仍保持 0 archive read，fresh probe/high-water 之后才通过 context 惰性读取 exact USTAR binary。compressed/expanded hard limits、立即 unlink 的 0600 streaming file、每次 reopen size/hash 与 bind/Installer policy 复核均已通过双 Go/race 和复审。production trust/composition 未满足时 mutation 命令仍在人类/JSON 模式于 runtime/RPC/SSH/探测/state/write/delete 前稳定 exit 3。真实 production artifacts、daemon/StateStore/SFTP lifecycle 和 Level 0 upgrade/uninstall composition 仍待完成，因此不得标记 `Verified`。见 [Stage 6 evidence](../verification/stage-06.md)。 |
| HELP-014 | helper 错误边界 | 4 | Verified | helper stdout 从 byte0 preface 起采用版本化结构协议，禁止 banner resync；probe/formal stderr 并发 drain、65536-byte cap、超限取消且仅保留有界脱敏摘要。用户路径/搜索词/畸形帧只作为协议数据。 | `protocol.go` fuzz/seeds、`process_session_test.go`、`binding_probe_process_test.go`覆盖banner拒绝、byte0、strict frames、stderr 65536成功/65537失败、redaction与exact command capture；最终无限流/native shell门禁已通过。 See [Stage 4 evidence](../verification/stage-04.md). |

## 22. 服务端快速路径与跨主机直传

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| DIRECT-001 | 同端点 rename 快速路径 | 2 | Verified | 移动在同端点且 rename 语义满足时优先原子 rename，无需传输内容。 | Capability test advertises `atomic_rename`, proves zero OpenRead/OpenWrite calls, conditional Rename, atomic result and both postconditions. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| DIRECT-002 | SFTP copy-data/服务端复制 | 5 | Verified | 端点可靠声明 copy-data 等服务端复制能力时可选用；完成后仍执行策略要求的验证。 | 显式 capability + facet 双门控冻结 exact v1 capability revision/1 TiB hard ceiling；只 stage exact Job-owned part，既有 Worker 独立校验 source/part SHA-256 后 commit。能力/接口单边缺失、未知版本、revision/声明漂移、绑定/checkpoint 篡改、同尺寸损坏、响应丢失收养、执行中取消、part absent 安全降级、unknown part 禁止降级及 restart 复用真实路线均通过 M5.1 本地门禁；Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#m51--route-unification-and-same-endpoint-fast-paths). |
| DIRECT-003 | helper 同主机复制路径 | 4 | Verified | 无 SFTP server-side copy 但 helper 可用时，Planner 可选择同机复制并记录命令能力和验证策略。 | `transfer/same_host_test.go`：durable Plan记录`helper_same_host`、protocol/version/capability/source SHA-256，Job view展示route；Helper不可用时冻结`SFTP relay`，能力消失不在同一请求混合结果。 See [Stage 4 evidence](../verification/stage-04.md). |
| DIRECT-004 | 跨主机直传显式策略 | 5 | Verified | 直传默认受策略门控；用户/工作区可禁用，启用时每个高风险计划显示数据路径和认证前提。 | user/workspace/data/integrity policy 在 Plan 中冻结；显式关闭和 production closure 均以稳定原因选择 relay。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| DIRECT-005 | 直传完整预检 | 5 | Verified | 同时确认源/目标 helper 能力、源到目标网络可达、目标空间、源主机已有非交互认证和 host key 策略；任一失败不启动直传。 | 14 项 ordered preflight 的 fail/unknown、malformed/stale/expired evidence 均在 direct write 前 fail closed；全通过 fixture 和双 sshd native gate 通过。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| DIRECT-006 | 不自动转发或复制认证材料 | 5 | Verified | 产品不为直传自动开启 agent forwarding、Kerberos delegation，不复制私钥/票据/认证答案；只使用源主机已经具备的非交互认证。 | typed control 无 credential/command surface；native gate 强制 strict host key/BatchMode，关闭 Agent/GSS/ControlMaster 并扫描目标根无 credential material。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| DIRECT-007 | 直传计划冻结 | 5 | Verified | 预检结果、端点、认证前提、临时目标和验证策略在 Job 中冻结；执行前过期条件重新预检，不静默放宽。 | route/preflight/request/result/part/final/target alias/expiry 全部 durable 且 tamper-checked；过期证据 fresh revalidate，restart 使用实际持久路线。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| DIRECT-008 | 提交前可降级本机中继 | 5 | Verified | 直传在目标提交前失败且源/目标安全状态可证明时，可记录原因并切换有界本机中继；提交后不盲目改路。 | M5.3 覆盖 expired revalidation、写前 network/auth/permission/space、exact acknowledged part cleanup/reuse、drifted part 禁止清理、mid-part disconnect/resume、corrupt result/proof、commit response loss 与 restart actual-route；所有 fallback 先持久化 planned→actual/reason，未知状态不盲降级。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| DIRECT-009 | 直传完整性等价 | 5 | Verified | 相同完整性策略下，直传与本机中继生成相同目标内容、提交语义和可审计验证记录。 | deterministic-random、sparse-shaped、multi-chunk large direct-relay goldens 的 bytes/SHA-256/final/outcome 一致；共享 conflict/cancel/commit/move/source-retention 和 durable Job/event 契约通过。100GB sparse 资源证据归 M5.4；Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| DIRECT-010 | 降级原因可观察 | 5 | Verified | Jobs/Log 显示未选直传或中途降级的具体条件，不只显示“fallback”。 | Durable JobView、Jobs drawer 与 `job_created` Log 公开 selected route、stable reason、integrity、downgrade boundary 和真实 progress semantics；安全 server-copy→relay 的 runtime Log/Job/TUI 持久显示 planned→actual 与 `server_copy_failed_part_absent`，production-closed Level 2 显示稳定拒绝原因。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 verification](../verification/stage-05.md#stage-5-feature-evidence). |

## 23. 规模、性能与资源边界

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| SCALE-001 | 50,000 项单目录 | 5 | Verified | 目录可增量载入、虚拟化渲染、过滤和导航，内存/渲染成本不要求一次创建全部 UI 节点。 | 50k visible window/overscan 只访问窗口行；固定基准 render 33.7–41.3 µs、约 26.5 KiB/329 alloc，move 603–719 ns/0 alloc。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-002 | 百万文件树 | 5 | Verified | 遍历、复制和搜索以流式队列工作，不将全树路径/元数据同时留在内存；可暂停和取消。 | 百万节点 Provider/Helper search 与 transfer discovery 通过 bounded page/frontier 流式输出；无全树物化。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-003 | 数百 GB 单文件 | 5 | Verified | 传输、hash、预览和恢复按块处理；测试基线含 100 GB 稀疏文件，内存不随文件大小增长。 | ADR-0017 canonical 分解：实际及 synthetic 100 GiB sparse fixture 证明 64-bit size、256 KiB 有界 checkpoint/buffer、durable cancel、part retention 与 final absent；同一生产 Worker/Journal/Scheduler 的 sparse-shaped 多 quantum 测试以新 Worker 实例证明 pause/checksum/restart resume/rate/SHA-256/commit。完整物理 100 GiB LocalFS/SFTP 长跑保留为 Stage 6 release gate；Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-004 | 有界工作队列 | 2 | Verified | 递归生产者受消费者和持久队列背压；队列容量可配置并有安全默认，不因深树耗尽内存。 | Discovery freezes 64 queue items, 256 entries/page and depth 128 by default; the million-entry fixture proves capacity 17/page 31 hard ceilings and exact M2.3 Hosted runs pass. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| SCALE-005 | 文件句柄、goroutine 与进程上限 | 5 | Verified | 并发任务受统一资源预算控制，极端工作负载下打开文件、goroutine 和 SSH/helper 进程保持在配置边界。 | global/Endpoint/Job lease ledger 分层计数 Job/queue/connection/SSH/Helper/FD/goroutine/memory/event/log，配置只可收紧且 release/cancel 不泄漏。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-006 | UI 与取消响应 | 5 | Verified | 目录加载、搜索、hash 和传输在后台运行时，导航/切栏/取消不会等待完整 I/O 操作结束。 | window-only UI、Helper/search cancellation latency、transfer/server/direct cancel 和固定 scheduler quantum 保证有界服务机会。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-007 | 带宽与连接公平 | 5 | Verified | 多任务共享带宽和连接额度时无单任务永久饥饿；高优先级交互读取可获得有界服务机会。 | 4:1 deterministic weighted RR 无 bulk starvation；global/双 Endpoint/Job token bucket、连接 admission/reuse 与 hot update 测试通过。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-008 | 超深路径与目录树 | 5 | Verified | 遍历不依赖递归调用栈；达到端点路径/深度限制时按具体路径失败并继续可安全部分。 | discovery 改为显式 bounded BFS frontier，不使用 Go recursion；depth/frontier overflow 返回 typed stable reason。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-009 | 性能回归基线 | 5 | Verified | 50k 目录、百万树、100 GB 稀疏文件和双远端中继建立可重复基准，变更报告趋势而非隐藏退化。 | canonical `make test-scale`/`make bench-scale` 固定 fixture、GOMAXPROCS/次数/样本，Stage 5 verification 记录硬件、FS、Go 与首份趋势。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-010 | 低磁盘模式 | 5 | Verified | remote relay 不依赖完整本地落盘；缓存/part 空间不足时限制预取并保留任务安全，不拖垮 daemon。 | relay permission/ENOSPC 分类不报假成功并保留可恢复 part；cache quota admission、preview 大文件拒绝和 direct space fallback 均 fail safe。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-011 | 大结果日志有界 | 5 | Verified | 搜索、任务事件和 stderr 采用分页/轮转/截断策略；摘要保留，单个远端不能无限增长本地日志。 | Job event payload 32 KiB、ListEvents 1000/页；2501 条按 1000/1000/501 回放；diagnostic ring/query 和 rotating JSON log 有界。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SCALE-012 | 规模能力降级可见 | 5 | Verified | 端点不支持分页/seek/hash 等能力时显示实际路径和限制，不以不可控内存换取表面一致。 | Level 0 bounded pages、capability removal、production-closed Level 2 和 relay stable reason 保持低能力端点可用且有界。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |

## 24. 安全与隐私

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| SEC-001 | 本地 IPC 最小暴露 | 1 | Verified | 只创建当前用户 `0600` Unix socket，不监听 TCP；请求关联到已验证的本地用户上下文。 | Unix-only `0600` socket, lock/stale-socket and peer checks pass; a root-gated fixture deliberately widens DAC and proves a real other-UID peer is still rejected. Ubuntu 22.04/24.04 and macOS 15 ARM/Intel passed in exact-head Hosted run 29417470068. See [Stage 1 verification](../verification/stage-01.md). |
| SEC-002 | 认证材料零持久化 | 1 | Verified | 密码、私钥、agent 数据、Kerberos ticket 和 askpass 回答不进入数据库、配置、缓存、日志、core-friendly buffer 或诊断包。 | Broker lifecycle tests plus Hosted injected-secret, workspace, diagnostic log and Kerberos cache-path/content scans pass; only one claimed answer reaches askpass stdout. See [Stage 1 verification](../verification/stage-01.md). |
| SEC-003 | OpenSSH 信任策略与 SFTP 会话隔离 | 1 | Verified | host key、代理、认证与加密由系统 OpenSSH 决定；默认绝对 `/usr/bin/ssh`，安全 override逐级验证，永不搜索 PATH。精确 argv 只关闭 TTY/escape/转发/额外命令/后台化/tunnel。 | Resolver ACL/special-bit/symlink/replacement tests, exact argv, conflicting config, poisoned PATH 0-hit, real proxy/auth/GSSAPI and changed-key refusal pass. See [Stage 1 verification](../verification/stage-01.md). |
| SEC-004 | 最小权限 helper | 4 | Verified | helper 明确拒绝 remote euid 0，只以非 root 登录 uid 按需运行，文件为 `0700`，无 sudo、setuid、服务注册或网络监听；remote root/admin/ACL/LSM/server 是声明的环境信任边界。 | `internal/app/helper_test.go`拒绝euid0/非restricted role，installer exact owner/0700 tests，runtime仅stdio且进程组清理；remote root/admin/ACL/LSM/server边界记录于ADR-0016，最终native扫描已通过。 See [Stage 4 evidence](../verification/stage-04.md). |
| SEC-005 | helper 供应链与每次执行 freshness | 4 | Verified | helper canonical raw manifest+detached sig 必须持久保存；安装及每次 enable/exec 都按当前客户端重跑 Ed25519、key/artifact revoke、target/compat/floor/high-water 和远端 attrs/hash。版本/协议不匹配、旧签名重放、客户端升级后撤销/floor 提高或 metadata 缺失均拒绝。 | `state_store.go`、`enable.go` tests覆盖raw bytes、owner-private atomic index、metadata cap、persistent downgrade/republish、current revoke/floor/target/attrs/hash、metadata missing/tamper与disable/remove保留high-water；production trust为空，双key production ceremony仍CLOSED。 See [Stage 4 evidence](../verification/stage-04.md). |
| SEC-006 | 路径规范化与根边界 | 2 | Verified | 所有操作使用 Endpoint+规范化路径；遍历中的 `..`、绝对跳转和链接不会越过用户确认的根范围。 | Provider normalization, rooted LocalFS handles, descendant validation, same-tree rejection and symlink no-follow tests enforce Endpoint/root boundaries. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| SEC-007 | 临时目标与符号链接竞态防护 | 2 | Verified | part 名不可预测且绑定 Job；提交前重新验证目标类型/身份，拒绝把预期文件写到被替换的链接/目录。 | Random Job ID binds same-directory part; conditional open/rename and final-race/symlink tests reject replacement identities. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| SEC-008 | 外部进程与remote command边界 | 3 | Verified | 只有ssh使用严格owner/ACL validated-absolute resolver。editor/opener是用户授权同UID边界，但须结构化配置、PATH发现后absolute direct exec、文件独立arg。Helper是唯一app-generated restricted string；显式`!`/`gs`用ADR-0001 fresh argv/marker/Q/TTY契约，Provider/RPC不可隐式触发。 | External lexer/resolver/direct-exec identity tests、TUI explicit confirmation、remote exact fresh argv/POSIX-Q/marker、TTY handoff 和 Provider/RPC reachability audit 完成；Helper execution/distribution 未加入且 custody gate CLOSED。见 [M3.3/M3.4 evidence](../verification/stage-03.md#stage-3-feature-evidence)。 |
| SEC-009 | 终端输出消毒 | 1 | Verified | 文件名、错误和远端输出中的 ANSI/OSC 控制序列不能执行任意终端动作，必要字符转义后显示。 | Malicious filename, OpenSSH stderr, picker problem, preview line and invalid-UTF-8 sanitization tests/snapshots pass. See [Stage 1 verification](../verification/stage-01.md). |
| SEC-010 | 缓存与状态目录私有 | 3 | Verified | 数据库、缓存、日志、socket 和临时文件使用当前用户私有目录/权限；创建时不受宽松 umask 影响。 | Stage 1 private roots/socket/state/log contracts 扩展到 content-v1 cache：explicit 0700 directories/0600 files、ancestor/ACL/type/owner/no-follow/hardlink checks 和 hostile umask fixtures；APFS/ext4/XFS native state/cache suites 纳入 CI。见 [security/native evidence](../verification/stage-03.md#security-privacy-and-pollution-evidence)。 |
| SEC-011 | 直传不扩张认证权限 | 5 | Verified | 直传只使用源主机已有非交互凭据，不自动 forwarding/delegation/复制密钥；预检结果可审计。 | control evidence 无 secret/command surface；双 sshd native gate 关闭 Agent/GSS/ControlMaster、启用 strict host key/BatchMode，并扫描 target root 无 credential material。Stage 5 exact-SHA push/PR Hosted evidence 已通过。 See [Stage 5 evidence](../verification/stage-05.md#stage-5-feature-evidence). |
| SEC-012 | 日志与诊断默认脱敏 | 6 | In Progress | 用户名/主机/路径按策略可匿名化，环境变量、命令敏感参数、文件内容和认证提示答案不导出。 | SHA `bfc59c8`/`ba334b8` 交付语义注册表、默认省略与 safe-shaped persistent/expanded-archive corpus 0-hit。SHA `fd5ebe4` 通过 dual-Go full local gate 和 PR 24/24；push/PR auth job 均在完整 OpenSSH/Askpass 与 renewed Kerberos/GSSAPI 后真实创建、展开并扫描 support bundle，各有两条 scan-pass marker。production-Helper artifact 与最终 RC pollution audit 仍待，因此保持 `In Progress`。见 [Stage 6 evidence](../verification/stage-06.md)。 |
| SEC-013 | 破坏性操作显式授权 | 2 | Verified | 不可逆删除、覆盖、跨端点移动源删除和直传危险阶段不能由默认焦点误触；确认可取消且作用域明确。 | Cut paste confirms move; D freezes scope then requires irreversible confirmation; overwrite waits conflict; count/dot reconfirm. Direct route is unavailable. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| SEC-014 | 依赖与发布物审查 | 6 | In Progress | 发布前执行依赖许可证/漏洞、二进制来源、构建可重复性和 SBOM/校验清单审查，阻断未解释高危问题。 | 四个真实 release-target build info 固定 target-aware 20-module union（Darwin 20、Linux 18）；`releasepack` 对每个 binary 精确匹配 path/version/canonical h1/replacement，解析 reviewed SPDX expression，并在 SPDX 2.3 中记录 resolved replacement 与 target coverage。SHA `197ff6f` 的 reviewed materials inventory/NOTICE 对全部 20 个 declared/resolved identities 一一覆盖，SHA-256/byte exact 绑定 root licenses、`modernc.org/libc/LICENSE-3RD-PARTY.md` 与 `modernc.org/memory` 两份额外文本；`make notice-check` 和 Hosted packaging 重新解析真实 module graph、重生成并 byte-compare。缺失/额外/重复、identity/license/hash/bytes、symlink/source 与 committed NOTICE 漂移均 fail closed。项目 LICENSE、protected signing 和 attestation 仍待完成，因此保持 `In Progress`。见 [runtime inventory](../release/runtime-dependencies.json)、[license materials](../release/license-materials.json)、[NOTICE](../release/NOTICE)、[public packaging contract](../release/public-packaging.md) 与 [Stage 6 evidence](../verification/stage-06.md)。 |

## 25. 可观测性与诊断

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| OBS-001 | Preview/Jobs/Log 底部抽屉 | 3 | Verified | 三个页签可独立打开/切换/关闭，保留双栏上下文；异常或等待任务有非侵入提示。 | Pure reducer + normal/narrow/minimum snapshots 覆盖 tab/focus/context；Jobs 复用 daemon authority，Log 用 redacted 1,000-record ring/256-page 与 Job/Endpoint filters，workspace 只恢复 tab/focus/rows 不保存 body。见 [Stage 3 evidence](../verification/stage-03.md#m31--drawer-and-cache-foundation)。 |
| OBS-002 | 任务阶段与等待原因 | 2 | Verified | Jobs 显示状态、当前阶段、源/目标、进度和 `waiting_auth`/`waiting_conflict`/retry 等原因及允许动作。 | Bounded J view renders state, phase, source/final, items, bytes/total, wait reason, summary, recent error/recovery and P/U/C/conflict keys. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| OBS-003 | 速度、吞吐与 ETA 诚实展示 | 2 | Verified | 有可靠总量才显示百分比/ETA；瞬时与平均速度有采样窗口，未知值显示 unknown 而非 0 或 100%。 | Stage 2 minimal Jobs renders only durable bytes and an optional known total; it never invents speed, ETA, 0% or 100% from absent samples. Sampled throughput/ETA visualization remains part of the Stage 3 full drawer. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| OBS-004 | 结构化连接与任务事件 | 2 | Verified | 事件至少包含时间、级别、端点、Job、阶段、稳定错误码和脱敏消息，可按 Job/端点过滤。 | V1 job_events are per-Job monotonic/bounded and carry stable phase/outcome/error code; persistent logs allow only registered endpoint/Job/error fields. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| OBS-005 | 路由、能力和降级说明 | 4 | Verified | UI 可查看端点 Level、缺失能力、选定路由、预检结果和降级原因。 | daemon `helper_status` snapshot与TUI status tests展示L0 reason/recovery或L1 exact version+独立capabilities；durable Jobs drawer展示`local`/`sftp_relay`/`helper_same_host`选定route。Level2属于Stage5，不能由Stage4冒充。 See [Stage 4 evidence](../verification/stage-04.md). |
| OBS-006 | partial/degraded 状态不伪装成功 | 1 | In Progress | 列表、搜索、预览或任务只完成部分时显式标识 partial/degraded，并列出失败范围/限制。 | Stage 1 listing、Stage 3 Preview、Stage 4 recursive search 与 Stage 5 bounded directory/Job manifest 都传播 partial/binary/truncated/failed 范围；最终跨 surface 的 release 诊断汇总仍待 Stage 6。 See the Stage 1–5 verification ledgers. |
| OBS-007 | 上下文化错误 | 1 | In Progress | 错误包含操作、端点、路径（可脱敏）、阶段、底层原因和下一步；保留原始 cause 供诊断。 | Connection/browse、Preview/Edit、search/Helper 与 transfer fault/golden matrices 均保留 daemon-local cause，并只公开 safe request/code/endpoint/retry/effect；Stage 6 仍需完成用户可导出的统一诊断包。 See the Stage 1–5 verification ledgers. |
| OBS-008 | 验证与提交证据 | 2 | Verified | 完成任务可查看实际大小/hash 策略、part、提交方式、源删除判定和异常保留源原因。 | Frozen verification plus terminal bytes/items/SHA/outcome/manifest/move_reason are durable; Jobs shows completion/source-retained summary. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| OBS-009 | 脱敏诊断包 | 6 | In Progress | 用户可生成包含版本、平台、配置 schema、能力、近期事件和数据库健康摘要的诊断包；默认排除秘密与文件内容。 | SHA `ae172d9`/`bfc59c8`/`ba334b8` 交付 reviewed layout、public preview/create、0600 publication、semantic fields、expanded corpus scan 与 corrupt-state byte preservation。SHA `fd5ebe4` 的 push/PR auth job 均以同一 public command 在真实 OpenSSH/Askpass/Kerberos material 下完成 0600 publication、archive expansion 与 literal/digest/base64/exact-copy 扫描。production-Helper artifact 与最终 RC audit 仍待，因此保持 `In Progress`。见 [CLI reference](../user/cli.md) 与 [Stage 6 evidence](../verification/stage-06.md)。 |
| OBS-010 | 自检命令 | 6 | Verified | 自检可检查 OpenSSH、运行目录权限、socket、数据库、缓存、helper 兼容和端点基础连通，并逐项给出结果。 | `amsftp doctor` 的 fixed v1 human/JSON、help/man/completion 与 real config/path/socket/daemon/OpenSSH/known-host/stopped/live-DB/cache/Helper/disk/optional-endpoint probes均已冻结；命令不创建/repair/migrate/auth/upload。多故障契约让全部 11 项同时 fail 时仍按序输出稳定 `probe_failed`、精确 remediation 且不泄露 raw cause/endpoint/path。native clean-install 生命周期在 macOS 15 ARM/Intel 与 Ubuntu 22.04/24.04 的运行中 daemon 上执行 installed binary，精确检查 10 项 code/status/severity/detail/remediation；完整 troubleshooting registry 与提交文档 byte-identical。双 Go focused/race、普通 docscheck 和完整本地门禁通过；exact-SHA Hosted 记录见 [Stage 6 evidence](../verification/stage-06.md)。 |

## 26. 平台与终端兼容

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| PLAT-001 | macOS 支持 | 1 | In Progress | 最低 macOS 15，Intel 与 Apple Silicon 均可构建、运行 TUI/daemon、调用系统 OpenSSH、编辑器与 `open`；CI 原生覆盖 `macos-15-intel` 与 `macos-15`。 | macOS 15 Intel/ARM native/oldstable、product builds、PTY、真实 editor 与固定 `/usr/bin/open` 路径已在 Stage 1–5 门禁覆盖；正式安装、签名、公证与 release 支持声明仍待 Stage 6。 |
| PLAT-002 | Linux 功能基线 | 1 | In Progress | 最低 Ubuntu 22.04；Stage 1 在 Ubuntu 22.04/24.04 amd64 原生运行 TUI/daemon、系统 OpenSSH、编辑器与 `xdg-open`，Linux arm64 此时只计构建证据且不得提前声明正式支持；其他 glibc Linux 为 best-effort。 | Ubuntu 22.04/24.04 amd64 native/oldstable、真实 OpenSSH/auth/Kerberos、PTY、editor 与固定 `/usr/bin/xdg-open` 已在 Stage 1–5 门禁覆盖；Linux arm64 仍是 build-only，正式 release 支持待 Stage 6。 |
| PLAT-003 | arm64 与 amd64 发布架构 | 6 | In Progress | macOS/Linux 的 arm64、amd64 均有可启动发布物，行为契约一致。 | 公共打包契约只接受并恰好输出 darwin/linux × amd64/arm64 四目标，固定 archive 名、target metadata、内容与 checksum/SBOM/provenance 绑定；current `make ci` 同批构建四目标，归档内 binary version-smoke 与四目标 native archive-style install/daemon/uninstall 已通过。Homebrew preview SHA `ed7dcd4` 又在 Hosted macOS ARM/Intel 通过 local tap 的 0.9→1.0 生命周期。最终 signed/unsigned protected bytes 与真实公开渠道仍未完成，因此不能标记 `Verified`。见 [public packaging contract](../release/public-packaging.md) 与 [Stage 6 evidence](../verification/stage-06.md)。 |
| PLAT-004 | 终端能力自适应 | 3 | Verified | 基础 TUI 只依赖通用终端能力；颜色、真彩、图片、鼠标等增强经探测启用，错误探测可由配置覆盖。 | 基础 tcell/none 路径不发图片序列；TERM hints 只能选 active probe，Kitty/iTerm2/Sixel 均需 exact bounded response proof，handoff 后 synchronized re-probe 清除 stale capability。真实 Kitty 0.47.4 通过；其他协议为受控 VT fixture。见 [real/native terminal evidence](../verification/stage-03.md#m32--built-in-image-and-external-preview)。 |
| PLAT-005 | 远端 Linux 优先、标准 SFTP 基线 | 1 | In Progress | 远端 Linux 是 helper/增强能力目标；任何兼容标准 SFTP 的非 Linux 服务可使用 Level 0，未支持增强明确降级。 | Real Linux OpenSSH/internal-sftp and protocol-level SFTP contract fixtures pass without helper; a non-OpenSSH/non-Linux server fixture is not yet recorded. See [Stage 1 verification](../verification/stage-01.md). |
| PLAT-006 | Unicode 与原始文件名 | 1 | In Progress | UTF-8 名称正确显示；不可解码字节可无损引用和操作，排序/过滤不改变底层路径字节。 | Domain/IPC/provider paths preserve raw bytes and renderer sanitization covers invalid UTF-8; CJK/emoji/combining renderer cases pass, but real SFTP invalid-byte end-to-end evidence remains open. See [Stage 1 verification](../verification/stage-01.md). |
| PLAT-007 | 文件系统语义差异 | 2 | Verified | 大小写、权限、rename 原子性、链接和时间精度按端点能力处理，不把 macOS/本地语义强加给远端。 | Planner uses Provider capabilities/normalized Location only; UTC fingerprint canonicalization, LocalFS/SFTP contracts, symlink and explicit atomic-rename gating pass cross-platform. See [Stage 2 feature evidence](../verification/stage-02.md#stage-2-feature-evidence). |
| PLAT-008 | 终端尺寸与恢复 | 1 | Verified | 小窗口显示明确最小布局，resize 后重排；正常/异常退出恢复备用屏幕、光标和输入模式。 | Minimum-size renderer snapshots and Ubuntu/macOS Intel/ARM native PTY SIGWINCH, normal quit, re-entry and SIGTERM smokes pass. See [Stage 1 verification](../verification/stage-01.md). |
| PLAT-009 | 系统 OpenSSH 兼容范围 | 6 | Verified | OpenSSH 8.9p1 是最低测试基线而非启动时字符串硬拒绝线；发布文档列出最低验证版本和已测版本，缺少实际必需能力时自检给出明确错误。 | SHA `e7733ec23ec1903dbdc41a926db20fe6defcccc7` 的 push/PR Ubuntu 22.04 native job 均精确记录 `/usr/bin/ssh` 为 `OpenSSH_8.9p1 Ubuntu-3ubuntu0.16`，随后 public doctor、完整认证和 support-bundle scan 全绿；SHA `a7f3ddd8a99df973518140de7eae45e295ae6ea2` 的 push/PR auth job 均精确记录 current `/usr/bin/ssh` 为 `OpenSSH_9.6p1 Ubuntu-3ubuntu13.18`，将该值绑定到 harness 并在任何账户/sshd 变更前重检一致性，随后同一 doctor/auth/support/Kerberos matrix 全绿。生产 runtime 无版本字符串硬拒绝，缺失/无效 executable 或 config/capability expansion 通过 stable doctor failure 报告。见 [environment matrix](environment-compatibility.md) 与 [Stage 6 evidence](../verification/stage-06.md)。 |

## 27. 配置、发布与生命周期

| ID | 能力 | Stage | 状态 | 验收标准 | 当前证据 |
|---|---|---:|---|---|---|
| REL-001 | 完整、可验证配置 | 6 | Verified | 连接引用、缓存、并发、带宽、完整性、预览器、键位与安全策略有版本化配置和生成/校验命令。 | 省略字段从唯一 `Default()` 继承并保持显式版本/未知字段 fail closed；`config validate`/redacted `print-effective` 已实现。完整 schema 覆盖 IPC frame、listing page、cache、Job concurrency/queue/bandwidth、built-in/terminal-image preview、filename/content search、retry、strong/require-strong integrity、daemon diagnostic、external commands、keymap 及 owner-derived closed Helper/Level2 gates；全部在 daemon/client 启动或 Intent/Plan 创建时冻结并有直接 runtime mapping regression。machine output 直接枚举 CLI/workspace/user/default precedence、不支持的 system/environment config layers、restart-only reload 与 Job freeze policy。最终 cold-start audit 从不存在的用户配置执行 validate 与 versioned/redacted effective generation，精确验证 resolution policy 和全部 14 个 schema 顶层域，且不创建配置文件；current race 与 exact oldstable 通过。见 [configuration reference](../user/configuration.md) 与 [Stage 6 evidence](../verification/stage-06.md). |
| REL-002 | Shell 补全与 man page | 6 | In Progress | 支持的主流 shell 获得命令/参数/工作区补全，man/help 与实际 CLI contract 一致。 | `--help`、committed man 与 bash/zsh/fish 静态补全由同一 ordered facts 生成/校验；公共 Helper 不暴露 serve/manifest/artifact/path。doctor 与 support-bundle preview/create/`--consent`/`--output`/`--format` 现已进入同一 contract，focused drift test 通过。最终 release 安装物、public channel 与 complete exact-SHA gate 仍开放。见 [CLI reference](../user/cli.md) 与 [Stage 6 evidence](../verification/stage-06.md). |
| REL-003 | SQLite 与配置迁移 | 6 | In Progress | final-absent v1外，existing pending batch以original..target全集digest和唯一attempt绑定同一online backup；destination发布前删除captured attempt并设置restore hold，catalog/no-replace/fullsync后才迁移。仅ready可自动推进，其他状态显式resume复用；成功按清attempt→retention→quick/FK/TRUNCATE/immutable收口。保留最近两份，space不足阻断；restore保持hold，禁止raw copy/overwrite/newer schema写入。 | 13 个真实历史 source bytes、v1/v2/v3 目录闭包、只读诊断、verified no-replace restore、六个真实进程死亡点、显式 hold release 和 SQLite head 4 forward migration 已通过。SHA `925b22e` 又在冻结 Stage 5 v3→受控 persisted-failure→degraded 默认拒绝→显式 resume v4→Stage 5 read-only rollback→current restart 的真实 owner lifecycle 中复用 verified backup 并保持 pinned-cache identity/bytes；双 Hosted quality 通过。受控失败边界不冒充自然失败 SQL，statement rollback 由 migration runner tests 覆盖。最终 public-channel rollback procedure 仍待完成。见 [compatibility boundaries](compatibility-boundaries.md) 与 [Stage 6 evidence](../verification/stage-06.md)。 |
| REL-004 | macOS/Linux 可安装发布物 | 6 | In Progress | 首发不可变四 tar+checksums+SBOM+attestation；darwin binary 经 Developer ID/runtime/timestamp/notary Accepted 后才生成离线签署 Helper manifest，最终 tar/Accepted ZIP/manifest hash byte-identical；Linux manifest绑定最终 unsigned binary。应用/服务/Formula ID服从 ADR-0009。 | 公共路径已实现一次性 no-overwrite 输出：四个确定性 tar.gz、`checksums.txt`、SPDX 2.3 与 unsigned provenance input；每个 tar 的 canonical root 现包含 `amsftp`、`VERSION.json`、LICENSE/NOTICE/INSTALL/UNINSTALL 及 `share/man/man1/amsftp.1`，mode/owner/time/order固定。生产准入负例拒绝 preview Linux bytes 及 pre-sign、non-Accepted、无 runtime/timestamp/strict verify 或 Accepted-ZIP hash 漂移的 Darwin bytes。确定性 Homebrew renderer 按 platform/architecture 绑定 exact four canonical archive URL/hash；SHA `ed7dcd4` 在 Hosted ARM/Intel 以 loopback assets/local tap 完成完整 preview lifecycle。该 harness 使用 CI-only license placeholder，既不选择项目 LICENSE，也不发布 formula。Helper runtime admission 已实现，但 production key/floor/最终 sidecar 尚未生成或发布；生产 Helper/Level 2 仍 CLOSED。真实项目 LICENSE、final protected bytes、签名/公证、manifest/attestation、quarantine/Gatekeeper、native public channel 及公开 Homebrew install/upgrade/uninstall 仍待完成。见 [public packaging contract](../release/public-packaging.md) 与 [Stage 6 evidence](../verification/stage-06.md)。 |
| REL-005 | 干净安装与原地升级 | 6 | In Progress | 无配置首次运行可进入 host/workspace 选择；旧版升级保留工作区、任务历史和 pinned cache，失败可恢复。 | clean-prefix 与四 native 平台已验证 binary/man/completions、daemon/Job、exact uninstall 与 SQLite retention；preview archive 已通过 fresh-user OpenSSH/TUI/Kerberos 和无参数 host/workspace picker。pinned-cache SHA `925b22e` 从冻结 Stage 5 production owner 发布 exact pinned-offline entry，并在 current 升级、受控 failed-attempt/default refusal/explicit resume、Stage 5 read-only rollback、current restart 后逐次复验同一 entry/blob identity、strong fingerprint 与字节。Homebrew preview SHA `ed7dcd4` 在 Hosted macOS ARM/Intel 完成 local-tap clean install 0.9、upgrade 1.0、version、formula test、forced all-version uninstall 与 untap。真实公开 channel、最终 signed/unsigned archive 和最终发布流程仍待完成。见 [Stage 6 evidence](../verification/stage-06.md)。 |
| REL-006 | OpenSSH/Kerberos/helper 兼容矩阵 | 6 | In Progress | 发布记录已验证的 macOS/Linux、OpenSSH、SFTP server、MIT Kerberos 和 helper 协议组合及已知限制。 | 版本化环境矩阵已记录 macOS 15 ARM/Intel、Ubuntu 22.04/24.04 amd64、精确 OpenSSH `8.9p1 Ubuntu-3ubuntu0.16`/`9.6p1 Ubuntu-3ubuntu13.18`、MIT Kerberos `5 version 1.20.1`、OpenSSH internal-sftp 与 Helper wire v1 的实际证据/降级/未测试边界。SHA `14e5ec4e63c70fb93ed93d15adbe7fdf7cce5d5e` 的 push/PR auth jobs 均捕获并在任何账户/KDC 变更前精确重检 Kerberos 版本，随后完整 GSSAPI matrix 通过。macOS Kerberos、vendor SFTP、Linux arm64 native 与 production Helper distribution 仍未测试，因此尚不能 `Verified`。见 [environment matrix](environment-compatibility.md) 与 [Stage 6 evidence](../verification/stage-06.md)。 |
| REL-007 | 安全与故障模型审查 | 6 | In Progress | 1.0 发布前逐项审查凭据、host key、路径竞态、删除/覆盖、helper、直传、日志脱敏和恢复边界。 | SHA `cc6559e41572aa21a7fc249fe0086fe4df50fe27` 的 [finding ledger](../security/finding-ledger.md#review-coverage) 已用稳定 ID 覆盖八个审查域并绑定现有负向测试；三条 coverage contract、完整双 Go 本地门禁和双工作流 quality/auth 均通过，当前范围无未处置 high finding。production Helper/Level 2、protected final artifacts 与最终独立安全审查仍开放，因此尚不能 `Verified`。 |
| REL-008 | Race、fuzz 与故障注入门禁 | 6 | In Progress | 全量 test、Go race、协议/路径 fuzz、fake provider 故障和真实 sshd/Kerberos 集成均为发布门禁。 | SHA `bad687f67789960bac426bd9414cfbcc5b859b49` 的五条 canonical RED/GREEN contract 建立 [exact-candidate gate record](../release/RC-GATES.md) 与严格 CLI：17 项 gate 逐项绑定同一 commit/tree、精确 evidence kind、completed/success 和 reference；physical 100 GiB、isolated Level 2、soak、两份独立终审及 candidate push/PR/post-merge main 有专门反冒充约束。后续 SHA `60255dc8` 建立 Helper/tail/PTY 同步，SHA `573aeefc` 修复 child lifetime，SHA `60ff908` 保证 stderr exact writes。SHA `2f4ed53` 让四条选中项动作共用屏障，但 Hosted 发现固定 loaded 前缀不适用于远端 capability 行；SHA `98a3315` 的 final-screen observer 同时要求光标目标存在、`READ-ONLY \| loading` 不存在，并通过确定性 local/remote screens、完整本地 PTY current race 20 轮、exact oldstable 10 轮、最终双 Go 本地门禁及 push/PR 各 24/24。当前没有 exact RC 或真实 record，因此仅为 `In Progress`。 |
| REL-009 | 用户与运维文档 | 6 | In Progress | 文档覆盖安装、SSH/Kerberos 前提、默认键位、工作区、传输安全、helper、直传、恢复、自检和故障排查。 | SHA `19e8ab73d5f90a51815634b9b36112f86711f372` 的四条 canonical RED/GREEN contract 已绑定 [documentation map](../README.md#user-and-operations-path)、[first run](../user/getting-started.md)、[operations/recovery](../operations/runbook.md)、[upgrade/rollback](../release/UPGRADE.md) 与 uninstall；exact current `make ci`、exact oldstable `make check`、focused docscheck 及 help/man/completion parity 通过。Hosted push/PR `29656202301`/`29656203801` 的 quality/auth 均通过，残余时序失败有同 SHA 伴随覆盖。exact-RC 独立新用户演练仍开放，故尚不能 `Verified`。 |
| REL-010 | 功能矩阵零无解释缺口 | 6 | In Progress | 1.0 发布时本矩阵每项为 Verified，或以关联 ADR 明确 Deferred/Removed；不得遗留 Planned/In Progress。 | SHA `181f8a8fbf083c5dadf450373981693e82ecdf74` 的四条 canonical RED/GREEN contract 新增显式 final-only `docscheck --release`：精确冻结 23 个 Stage 6 ID，只允许 `Verified` 或有 ADR 链接的 `Deferred`/`Removed`，并要求 Stage 6 的 12 个退出条件逐字、按序、全部勾选。精确双 Go 本地门禁及 push/PR 各 24/24 通过。普通 docscheck 继续通过；当前 release audit 确定报告 21 个非终态行和 12 个未勾选条件，因此尚不能 `Verified`。见 [release gate procedure](../release/RC-GATES.md) 与 [Stage 6 evidence](../verification/stage-06.md)。 |
| REL-011 | 版本与协议兼容策略 | 6 | In Progress | CLI、daemon、数据库、IPC 和 helper 版本在诊断中可见；不兼容组合失败清楚，兼容窗口有测试。 | 公共 CLI 已冻结 0–8 退出码与 v1 contract；effective-config、Job、daemon 和 Helper status success/error 均为 `output_version: 1`。Helper install/upgrade 在 production distribution closed 时新增稳定 configuration exit 3 与相同 v1 machine-error shape，并在 connector/runtime 前返回。compatibility boundaries 继续冻结 config/workspace/SQLite/cache/IPC/Helper 行为；最终 native daemon/Helper 组合矩阵及真实升级/回滚仍待完成。见 [CLI reference](../user/cli.md) 与 [Stage 6 evidence](../verification/stage-06.md). |
| REL-012 | 发布后可回滚 | 6 | In Progress | 二进制回滚不会静默读取不兼容数据库/helper；需要时以只读诊断模式启动并给出恢复步骤。 | current v3→v4 后冻结 Stage 5 daemon 只以 degraded 模式运行，Job API 稳定拒绝且数据库 SHA-256 byte-identical；SHA `925b22e` 进一步证明 pinned-cache exact identity/bytes 在显式恢复、旧版只读回滚与 current restart 后不变。exact trusted-root 与双 Hosted quality 均通过。真实 Helper-too-new、published rollback procedure 与 public channel 仍待完成。见 [Stage 6 evidence](../verification/stage-06.md)。 |

## 28. 覆盖索引

| 产品领域 | 稳定 ID 前缀 | 主要阶段 |
|---|---|---|
| 基础契约与可续接工程 | `CORE` | 0 |
| 认证与 OpenSSH 信任链 | `AUTH` | 1–2 |
| 连接与端点 | `CONN` | 1、5 |
| 双栏浏览 | `PANE` | 1、5 |
| 工作区与启动 | `WORK` | 1、3、6 |
| Vim 键位与模式 | `VIM` | 1–2、6 |
| 本地 daemon 与 IPC | `DAEM` | 1–2 |
| 预览 | `PREV` | 1、3 |
| 编辑与本机打开 | `EDIT` | 3 |
| 缓存与离线 | `CACHE` | 3 |
| 命令与 shell | `SHELL` | 3 |
| 后台任务 | `JOB` | 2、5–6 |
| 复制与传输 | `XFER` | 2 |
| 移动与重命名 | `MOVE` | 2 |
| 删除与回收 | `DEL` | 2 |
| 冲突与覆盖 | `CONF` | 2 |
| 故障恢复 | `RECV` | 2 |
| 过滤与搜索 | `SRCH` | 1、4 |
| 可选远端 helper | `HELP` | 1、4、6 |
| 服务端快速路径与直传 | `DIRECT` | 2、4–5 |
| 规模、性能和资源边界 | `SCALE` | 2、5 |
| 安全与隐私 | `SEC` | 1–6 |
| 可观测性与诊断 | `OBS` | 1–6 |
| 平台与终端兼容 | `PLAT` | 1–3、6 |
| 配置、发布与生命周期 | `REL` | 6 |

## 29. 阶段完成更新协议

每个 Stage 完成时，维护者必须按以下顺序更新本矩阵：

1. 将该阶段实际实现的行从 `Planned`/`In Progress` 更新为 `Implemented`；
2. 把“未实施；预期证据……”替换为实际测试命令、测试名、平台和结果记录链接；
3. 阶段验证通过后改为 `Verified`；若验收失败，保持原状态并在 Verification 与 `PROJECT_STATE.md` 写明阻塞；
4. 对范围变化新增 ADR，并同步 Vision、Stage Spec 和本矩阵；不覆盖或复用旧 ID；
5. 运行 docs-check，确认 ID 唯一、Stage 在 0–6、状态合法、所有非 Planned 行都有实际证据；
6. 最后更新 `PROJECT_STATE.md` 的当前阶段、最后绿色命令和下一步。

这套更新顺序保证即使原始聊天上下文不可用，也能从仓库文档准确恢复最初产品边界、当前实现状态和下一项可执行工作。
