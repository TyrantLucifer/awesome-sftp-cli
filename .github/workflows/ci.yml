name: CI

on:
  pull_request:
  push:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      LEG: quality
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: "go.sum\ntools/go.sum"
      - name: Prepare trusted persistent test root
        run: |
          set -euo pipefail
          if test "${RUNNER_OS}" = Linux; then
            sudo install -d -o root -g root -m 0755 /var/lib/amsftp-tests
            sudo install -d -o "$(id -u)" -g "$(id -g)" -m 0700 "/var/lib/amsftp-tests/$(id -u)"
          fi
      - run: make check
        env:
          BUILD_DIR: ${{ runner.temp }}/quality/build
          COVERAGE_DIR: ${{ runner.temp }}/quality/coverage
      - run: make lint
      - run: make fuzz-smoke
      - run: make supply-chain
      - name: Exercise deterministic public packaging and clean-home lifecycle
        run: |
          set -euo pipefail
          input="${RUNNER_TEMP}/public-package-input"
          first="${RUNNER_TEMP}/public-package-first"
          second="${RUNNER_TEMP}/public-package-second"
          install_root="${RUNNER_TEMP}/public-package-install"
          install_prefix="${install_root}/prefix"
          trusted_root="/var/lib/amsftp-tests/$(id -u)"
          clean_home="${trusted_root}/public-package-home"
          old_source="${RUNNER_TEMP}/stage5-source"
          old_binary="${RUNNER_TEMP}/amsftp-stage5"
          old_cache_harness="${RUNNER_TEMP}/pinned-cache-stage5"
          current_cache_harness="${RUNNER_TEMP}/pinned-cache-current"
          failure_harness="${RUNNER_TEMP}/pinned-cache-failure"
          cache_record="${RUNNER_TEMP}/pinned-cache-evidence.json"
          head="$(git rev-parse HEAD)"
          tree="$(git rev-parse 'HEAD^{tree}')"
          source_epoch="$(git show -s --format=%ct HEAD)"
          modules_json="$(tr -d '\n' <docs/release/runtime-dependencies.json)"
          ldflags="-X github.com/TyrantLucifer/awesome-mac-sftp/internal/buildinfo.version=1.0.0 -X github.com/TyrantLucifer/awesome-mac-sftp/internal/buildinfo.commit=${head}"
          mkdir -p "${input}/bin"
          for target in darwin/amd64 darwin/arm64 linux/amd64 linux/arm64; do
            goos="${target%/*}"
            goarch="${target#*/}"
            CGO_ENABLED=0 GOOS="${goos}" GOARCH="${goarch}" \
              go build -trimpath -ldflags="${ldflags}" -o "${input}/bin/${goos}-${goarch}" ./cmd/amsftp
          done
          printf '%s\n' 'CI-only public packaging contract material; this is not the project license.' >"${input}/LICENSE"
          notice_check="${RUNNER_TEMP}/reviewed-third-party-NOTICE"
          go run ./internal/tools/releasenotice docs/release/runtime-dependencies.json docs/release/license-materials.json >"${notice_check}"
          cmp docs/release/NOTICE "${notice_check}"
          cp docs/release/NOTICE "${input}/NOTICE"
          cp docs/release/INSTALL.md "${input}/INSTALL.md"
          cp docs/release/UNINSTALL.md "${input}/UNINSTALL.md"
          cp docs/man/amsftp.1 "${input}/amsftp.1"
          cat >"${input}/manifest.json" <<EOF
          {
            "schema": "amsftp-public-release-manifest-v1",
            "version": "1.0.0",
            "commit": "${head}",
            "tree": "${tree}",
            "source_date_epoch": ${source_epoch},
            "materials": {"license":"LICENSE","notice":"NOTICE","install":"INSTALL.md","uninstall":"UNINSTALL.md","man":"amsftp.1"},
            "platforms": [
              {"os":"darwin","arch":"amd64","path":"bin/darwin-amd64"},
              {"os":"darwin","arch":"arm64","path":"bin/darwin-arm64"},
              {"os":"linux","arch":"amd64","path":"bin/linux-amd64"},
              {"os":"linux","arch":"arm64","path":"bin/linux-arm64"}
            ],
            "modules": ${modules_json}
          }
          EOF
          go run ./internal/tools/releasepack "${input}/manifest.json" "${first}"
          go run ./internal/tools/releasepack "${input}/manifest.json" "${second}"
          diff -qr "${first}" "${second}"
          test "$(find "${first}" -mindepth 1 -maxdepth 1 -type f | wc -l | tr -d ' ')" = 7
          (cd "${first}" && sha256sum -c checksums.txt)
          mkdir -p "${install_root}" "${clean_home}/config" "${clean_home}/state" "${clean_home}/cache" "${clean_home}/runtime"
          chmod 0700 "${clean_home}" "${clean_home}/config" "${clean_home}/state" "${clean_home}/cache" "${clean_home}/runtime"
          tar -xzf "${first}/amsftp_1.0.0_linux_amd64.tar.gz" -C "${install_root}"
          extracted="${install_root}/amsftp_1.0.0_linux_amd64"
          installed="${install_prefix}/bin/amsftp"
          mkdir -p \
            "${install_prefix}/bin" \
            "${install_prefix}/share/man/man1" \
            "${install_prefix}/share/bash-completion/completions" \
            "${install_prefix}/share/zsh/site-functions" \
            "${install_prefix}/share/fish/vendor_completions.d"
          install -m 0755 "${extracted}/amsftp" "${installed}"
          install -m 0644 "${extracted}/share/man/man1/amsftp.1" "${install_prefix}/share/man/man1/amsftp.1"
          "${installed}" completion bash >"${install_prefix}/share/bash-completion/completions/amsftp"
          "${installed}" completion zsh >"${install_prefix}/share/zsh/site-functions/_amsftp"
          "${installed}" completion fish >"${install_prefix}/share/fish/vendor_completions.d/amsftp.fish"
          grep -F 'complete -F _amsftp amsftp' "${install_prefix}/share/bash-completion/completions/amsftp"
          grep -F '#compdef amsftp' "${install_prefix}/share/zsh/site-functions/_amsftp"
          grep -F 'complete -c amsftp' "${install_prefix}/share/fish/vendor_completions.d/amsftp.fish"
          version_output="$("${installed}" --version)"
          printf '%s\n' "${version_output}" | grep -F "1.0.0 commit=${head} dirty=false"
          tar -xOf "${first}/amsftp_1.0.0_linux_amd64.tar.gz" amsftp_1.0.0_linux_amd64/VERSION.json | grep -F '"application_id":"io.github.tyrantlucifer.amsftp"'
          clean_env=(
            "HOME=${clean_home}"
            "XDG_CONFIG_HOME=${clean_home}/config"
            "XDG_STATE_HOME=${clean_home}/state"
            "XDG_CACHE_HOME=${clean_home}/cache"
            "XDG_RUNTIME_DIR=${clean_home}/runtime"
          )
          mkdir -p "${old_source}"
          git archive 312bcccbcbd54246bbe5ff9babf4f14560449176 | tar -x -C "${old_source}"
          mkdir -p "${old_source}/internal/integration"
          cp -R internal/integration/pinned-cache-lifecycle "${old_source}/internal/integration/"
          (
            cd "${old_source}"
            CGO_ENABLED=0 go build -buildvcs=false -trimpath \
              -ldflags='-X github.com/TyrantLucifer/awesome-mac-sftp/internal/buildinfo.version=stage5 -X github.com/TyrantLucifer/awesome-mac-sftp/internal/buildinfo.commit=312bcccbcbd54246bbe5ff9babf4f14560449176' \
              -o "${old_binary}" ./cmd/amsftp
            CGO_ENABLED=0 go build -buildvcs=false -trimpath \
              -o "${old_cache_harness}" ./internal/integration/pinned-cache-lifecycle
          )
          CGO_ENABLED=0 go build -buildvcs=false -trimpath \
            -o "${current_cache_harness}" ./internal/integration/pinned-cache-lifecycle
          CGO_ENABLED=0 go build -buildvcs=false -trimpath \
            -o "${failure_harness}" ./internal/integration/pinned-cache-failure
          cleanup_daemon() {
            env "${clean_env[@]}" "${installed}" daemon stop --confirm stop --format json >/dev/null 2>&1 || true
          }
          trap cleanup_daemon EXIT
          env "${clean_env[@]}" "${old_binary}" daemon >"${RUNNER_TEMP}/stage5-daemon.log" 2>&1 &
          old_pid=$!
          old_running=0
          for ((attempt = 0; attempt < 200; attempt++)); do
            env "${clean_env[@]}" "${installed}" daemon status --format json >"${RUNNER_TEMP}/stage5-status.json" 2>/dev/null || true
            if grep -qF '"running":true' "${RUNNER_TEMP}/stage5-status.json"; then
              old_running=1
              break
            fi
            sleep 0.05
          done
          test "${old_running}" = 1
          grep -F 'stage5 commit=312bcccbcbd54246bbe5ff9babf4f14560449176' "${RUNNER_TEMP}/stage5-status.json"
          kill -TERM "${old_pid}"
          wait "${old_pid}"
          state_root="${clean_home}/state/amsftp"
          database="${state_root}/amsftp.db"
          cache_root="${clean_home}/cache/amsftp"
          test -f "${database}"
          test -d "${cache_root}"
          "${old_cache_harness}" seed \
            --database "${database}" --cache-root "${cache_root}" --record "${cache_record}"
          "${old_cache_harness}" verify \
            --database "${database}" --cache-root "${cache_root}" --record "${cache_record}"
          "${current_cache_harness}" verify \
            --database "${database}" --cache-root "${cache_root}" --record "${cache_record}"
          "${failure_harness}" prepare \
            --state-root "${state_root}" --database "${database}" | tee "${RUNNER_TEMP}/failed-upgrade.json"
          grep -F '"original_head":3' "${RUNNER_TEMP}/failed-upgrade.json"
          grep -F '"target_head":4' "${RUNNER_TEMP}/failed-upgrade.json"
          grep -F '"status":"failed"' "${RUNNER_TEMP}/failed-upgrade.json"
          "${current_cache_harness}" verify \
            --database "${database}" --cache-root "${cache_root}" --record "${cache_record}"
          failed_database_before="$(sha256sum "${database}")"
          env "${clean_env[@]}" "${installed}" daemon >"${RUNNER_TEMP}/failed-upgrade-daemon.log" 2>&1 &
          failed_pid=$!
          failed_running=0
          for ((attempt = 0; attempt < 200; attempt++)); do
            env "${clean_env[@]}" "${installed}" daemon status --format json >"${RUNNER_TEMP}/failed-upgrade-status.json" 2>/dev/null || true
            if grep -qF '"running":true' "${RUNNER_TEMP}/failed-upgrade-status.json"; then
              failed_running=1
              break
            fi
            sleep 0.05
          done
          test "${failed_running}" = 1
          set +e
          env "${clean_env[@]}" "${installed}" job list --format json >"${RUNNER_TEMP}/failed-upgrade-jobs.out" 2>"${RUNNER_TEMP}/failed-upgrade-jobs.err"
          failed_job_rc=$?
          set -e
          test "${failed_job_rc}" -ne 0
          grep -F 'durable transfer service is unavailable' "${RUNNER_TEMP}/failed-upgrade-jobs.err"
          kill -TERM "${failed_pid}"
          wait "${failed_pid}"
          failed_database_after="$(sha256sum "${database}")"
          test "${failed_database_before}" = "${failed_database_after}"
          "${current_cache_harness}" verify \
            --database "${database}" --cache-root "${cache_root}" --record "${cache_record}"
          env "${clean_env[@]}" "${installed}" daemon --resume-migration >"${RUNNER_TEMP}/resumed-upgrade-daemon.log" 2>&1 &
          resumed_pid=$!
          resumed_running=0
          for ((attempt = 0; attempt < 200; attempt++)); do
            env "${clean_env[@]}" "${installed}" daemon status --format json >"${RUNNER_TEMP}/resumed-upgrade-status.json" 2>/dev/null || true
            if grep -qF '"running":true' "${RUNNER_TEMP}/resumed-upgrade-status.json"; then
              resumed_running=1
              break
            fi
            sleep 0.05
          done
          test "${resumed_running}" = 1
          grep -F "1.0.0 commit=${head}" "${RUNNER_TEMP}/resumed-upgrade-status.json"
          env "${clean_env[@]}" "${installed}" job list --format json | grep -F '"output_version":1'
          kill -TERM "${resumed_pid}"
          wait "${resumed_pid}"
          "${current_cache_harness}" verify \
            --database "${database}" --cache-root "${cache_root}" --record "${cache_record}"
          env "${clean_env[@]}" "${installed}" daemon start --format json | grep -F '"running":true'
          env "${clean_env[@]}" "${installed}" job list --format json | grep -F '"output_version":1'
          env "${clean_env[@]}" "${installed}" daemon status --format json | grep -F '"running":true'
          env "${clean_env[@]}" "${installed}" daemon stop --confirm stop --format json | grep -F '"running":false'
          env "${clean_env[@]}" "${installed}" daemon status --format json | grep -F '"running":false'
          test -f "${database}"
          database_before="$(sha256sum "${database}")"
          env "${clean_env[@]}" "${old_binary}" daemon >"${RUNNER_TEMP}/stage5-rollback.log" 2>&1 &
          old_pid=$!
          old_running=0
          for ((attempt = 0; attempt < 200; attempt++)); do
            env "${clean_env[@]}" "${installed}" daemon status --format json >"${RUNNER_TEMP}/stage5-rollback-status.json" 2>/dev/null || true
            if grep -qF '"running":true' "${RUNNER_TEMP}/stage5-rollback-status.json"; then
              old_running=1
              break
            fi
            sleep 0.05
          done
          test "${old_running}" = 1
          set +e
          env "${clean_env[@]}" "${installed}" job list --format json >"${RUNNER_TEMP}/stage5-jobs.out" 2>"${RUNNER_TEMP}/stage5-jobs.err"
          rollback_job_rc=$?
          set -e
          test "${rollback_job_rc}" -ne 0
          grep -F 'durable transfer service is unavailable' "${RUNNER_TEMP}/stage5-jobs.err"
          kill -TERM "${old_pid}"
          wait "${old_pid}"
          database_after="$(sha256sum "${database}")"
          test "${database_before}" = "${database_after}"
          "${current_cache_harness}" verify \
            --database "${database}" --cache-root "${cache_root}" --record "${cache_record}"
          env "${clean_env[@]}" "${installed}" daemon start --format json | grep -F '"running":true'
          env "${clean_env[@]}" "${installed}" job list --format json | grep -F '"output_version":1'
          env "${clean_env[@]}" "${installed}" daemon stop --confirm stop --format json | grep -F '"running":false'
          "${current_cache_harness}" verify \
            --database "${database}" --cache-root "${cache_root}" --record "${cache_record}"
          trap - EXIT
          rm -f \
            "${installed}" \
            "${install_prefix}/share/man/man1/amsftp.1" \
            "${install_prefix}/share/bash-completion/completions/amsftp" \
            "${install_prefix}/share/zsh/site-functions/_amsftp" \
            "${install_prefix}/share/fish/vendor_completions.d/amsftp.fish"
          rmdir \
            "${install_prefix}/bin" \
            "${install_prefix}/share/man/man1" \
            "${install_prefix}/share/man" \
            "${install_prefix}/share/bash-completion/completions" \
            "${install_prefix}/share/bash-completion" \
            "${install_prefix}/share/zsh/site-functions" \
            "${install_prefix}/share/zsh" \
            "${install_prefix}/share/fish/vendor_completions.d" \
            "${install_prefix}/share/fish" \
            "${install_prefix}/share" \
            "${install_prefix}"
          test ! -e "${installed}"
          test -f "${database}"
          test -d "${extracted}"
      - name: Upload public preview bundle
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: public-preview-bundle-${{ github.sha }}
          path: ${{ runner.temp }}/public-package-first
          if-no-files-found: error
      - name: Run real OpenSSH, editor, opener, command, and PTY integration
        env:
          AMSFTP_REAL_SSHD: "1"
        run: |
          set -euo pipefail
          install_stage3_fixtures=0
          for command in sshd nvim vim xdg-open; do
            if ! command -v "${command}" >/dev/null 2>&1; then
              install_stage3_fixtures=1
            fi
          done
          if test "${install_stage3_fixtures}" = 1; then
            sudo apt-get update
            sudo env DEBIAN_FRONTEND=noninteractive apt-get install -y neovim openssh-server vim xdg-utils
          fi
          sudo mkdir -p /run/sshd
          go test -count=1 -run '^(TestRealOpenSSHSFTPHostAliasAndNonDefaultPort|TestStage2TemporarySSHDPTYUploadDownloadMVP|TestStage3NativeEditorsDirectExecMaterialization|TestStage3NativeDefaultOpenerDirectExec|TestStage3TemporarySSHDRemoteShellForegroundPTY)$' ./internal/integration
      - name: Record provenance
        run: |
          set -euo pipefail
          mkdir -p "${RUNNER_TEMP}/provenance"
          porcelain="${RUNNER_TEMP}/provenance/${LEG}.porcelain"
          record="${RUNNER_TEMP}/provenance/${LEG}.env"
          : "${RUNNER_OS:?RUNNER_OS is required}"
          : "${RUNNER_ARCH:?RUNNER_ARCH is required}"
          : "${ImageOS:?ImageOS is required}"
          : "${ImageVersion:?ImageVersion is required}"
          go_env_goos="$(go env GOOS)"
          go_env_goarch="$(go env GOARCH)"
          test -n "${go_env_goos}"
          test -n "${go_env_goarch}"
          git status --porcelain=v1 -uall >"${porcelain}"
          {
            printf 'schema=amsftp-provenance-v1\n'
            printf 'workflow=%s\n' "${GITHUB_WORKFLOW}"
            printf 'job=%s\n' "${GITHUB_JOB}"
            printf 'leg=%s\n' "${LEG}"
            printf 'head=%s\n' "$(git rev-parse HEAD)"
            printf 'tree=%s\n' "$(git rev-parse 'HEAD^{tree}')"
            printf 'go_version=%s\n' "$(go version)"
            printf 'runner_os=%s\n' "${RUNNER_OS}"
            printf 'runner_arch=%s\n' "${RUNNER_ARCH}"
            printf 'runner_image_os=%s\n' "${ImageOS}"
            printf 'runner_image_version=%s\n' "${ImageVersion}"
            printf 'go_env_goos=%s\n' "${go_env_goos}"
            printf 'go_env_goarch=%s\n' "${go_env_goarch}"
            if test -s "${porcelain}"; then
              printf 'status=dirty\n'
            else
              printf 'status=clean\n'
            fi
          } >"${record}"
          test ! -s "${porcelain}"
      - name: Upload provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provenance-${{ env.LEG }}
          path: ${{ runner.temp }}/provenance/${{ env.LEG }}.*
          if-no-files-found: error

  auth-integration:
    needs: quality
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      LEG: auth-integration
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: "go.sum\ntools/go.sum"
      - name: Install isolated authentication fixtures
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo env DEBIAN_FRONTEND=noninteractive apt-get install -y \
            expect \
            krb5-admin-server \
            krb5-kdc \
            krb5-user \
            netcat-openbsd \
            openssh-server
      - name: Download public preview bundle
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: public-preview-bundle-${{ github.sha }}
          path: ${{ runner.temp }}/auth-integration/bundle
      - name: Verify and extract public preview bundle
        run: |
          set -euo pipefail
          bundle="${RUNNER_TEMP}/auth-integration/bundle"
          test "$(find "${bundle}" -mindepth 1 -maxdepth 1 -type f | wc -l | tr -d '[:space:]')" = 7
          (cd "${bundle}" && sha256sum -c checksums.txt)
          archive="${bundle}/amsftp_1.0.0_linux_amd64.tar.gz"
          install_root="${RUNNER_TEMP}/auth-integration/install"
          test -f "${archive}"
          test ! -e "${install_root}"
          mkdir -p "${install_root}"
          tar -xzf "${archive}" -C "${install_root}"
          installed="${install_root}/amsftp_1.0.0_linux_amd64/amsftp"
          test -x "${installed}"
          "${installed}" --version | grep -F "1.0.0 commit=${GITHUB_SHA} dirty=false"
          tar -xOf "${archive}" amsftp_1.0.0_linux_amd64/VERSION.json | grep -F "\"commit\":\"${GITHUB_SHA}\""
      - name: Run real OpenSSH authentication matrix
        run: |
          set -euo pipefail
          sudo env \
            AMSFTP_AUTH_BINARY="${RUNNER_TEMP}/auth-integration/install/amsftp_1.0.0_linux_amd64/amsftp" \
            AMSFTP_AUTH_ROOT="/tmp/amsftp-auth-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}" \
            bash ./internal/integration/hosted-auth.sh
      - name: Run real MIT Kerberos/GSSAPI matrix
        run: |
          set -euo pipefail
          sudo env \
            AMSFTP_KERBEROS_BINARY="${RUNNER_TEMP}/auth-integration/install/amsftp_1.0.0_linux_amd64/amsftp" \
            AMSFTP_KERBEROS_ROOT="/tmp/amsftp-kerberos-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}" \
            bash ./internal/integration/hosted-kerberos.sh
      - name: Record provenance
        run: |
          set -euo pipefail
          mkdir -p "${RUNNER_TEMP}/provenance"
          porcelain="${RUNNER_TEMP}/provenance/${LEG}.porcelain"
          record="${RUNNER_TEMP}/provenance/${LEG}.env"
          : "${RUNNER_OS:?RUNNER_OS is required}"
          : "${RUNNER_ARCH:?RUNNER_ARCH is required}"
          : "${ImageOS:?ImageOS is required}"
          : "${ImageVersion:?ImageVersion is required}"
          go_env_goos="$(go env GOOS)"
          go_env_goarch="$(go env GOARCH)"
          test -n "${go_env_goos}"
          test -n "${go_env_goarch}"
          git status --porcelain=v1 -uall >"${porcelain}"
          {
            printf 'schema=amsftp-provenance-v1\n'
            printf 'workflow=%s\n' "${GITHUB_WORKFLOW}"
            printf 'job=%s\n' "${GITHUB_JOB}"
            printf 'leg=%s\n' "${LEG}"
            printf 'head=%s\n' "$(git rev-parse HEAD)"
            printf 'tree=%s\n' "$(git rev-parse 'HEAD^{tree}')"
            printf 'go_version=%s\n' "$(go version)"
            printf 'runner_os=%s\n' "${RUNNER_OS}"
            printf 'runner_arch=%s\n' "${RUNNER_ARCH}"
            printf 'runner_image_os=%s\n' "${ImageOS}"
            printf 'runner_image_version=%s\n' "${ImageVersion}"
            printf 'go_env_goos=%s\n' "${go_env_goos}"
            printf 'go_env_goarch=%s\n' "${go_env_goarch}"
            if test -s "${porcelain}"; then
              printf 'status=dirty\n'
            else
              printf 'status=clean\n'
            fi
          } >"${record}"
          test ! -s "${porcelain}"
      - name: Upload provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provenance-${{ env.LEG }}
          path: ${{ runner.temp }}/provenance/${{ env.LEG }}.*
          if-no-files-found: error

  native:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - ubuntu-24.04
          - macos-15
          - macos-15-intel
    env:
      LEG: native-${{ matrix.os }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: "go.sum\ntools/go.sum"
      - name: Prepare trusted persistent test root
        run: |
          set -euo pipefail
          if test "${RUNNER_OS}" = Linux; then
            sudo install -d -o root -g root -m 0755 /var/lib/amsftp-tests
            sudo install -d -o "$(id -u)" -g "$(id -g)" -m 0700 "/var/lib/amsftp-tests/$(id -u)"
          fi
      - run: make fmt-check
      - run: make vet
      - run: make test
        env:
          BUILD_DIR: ${{ runner.temp }}/native/build
          COVERAGE_DIR: ${{ runner.temp }}/native/coverage
      - run: make test-contract
        env:
          BUILD_DIR: ${{ runner.temp }}/native/build
          COVERAGE_DIR: ${{ runner.temp }}/native/coverage
      - run: make test-race
      - run: mkdir -p "${{ runner.temp }}/native/bin"
      - run: go build -trimpath -o "${{ runner.temp }}/native/bin/amsftp" ./cmd/amsftp
      - run: '"${{ runner.temp }}/native/bin/amsftp" --help'
      - run: '"${{ runner.temp }}/native/bin/amsftp" --version'
      - name: Exercise native clean install and uninstall lifecycle
        run: |
          set -euo pipefail
          native_binary="${RUNNER_TEMP}/native/bin/amsftp"
          install_root="${RUNNER_TEMP}/native/install"
          prefix="${install_root}/prefix"
          installed="${prefix}/bin/amsftp"
          if test "${RUNNER_OS}" = Linux; then
            trusted_root="/var/lib/amsftp-tests/$(id -u)"
          else
            trusted_root="${HOME}/.amsftp-native-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          fi
          clean_home="${trusted_root}/clean-home"
          test ! -e "${install_root}"
          test ! -e "${clean_home}"
          mkdir -p \
            "${prefix}/bin" \
            "${prefix}/share/man/man1" \
            "${prefix}/share/bash-completion/completions" \
            "${prefix}/share/zsh/site-functions" \
            "${prefix}/share/fish/vendor_completions.d" \
            "${clean_home}/config" \
            "${clean_home}/state" \
            "${clean_home}/cache" \
            "${clean_home}/runtime"
          chmod 0700 "${clean_home}" "${clean_home}/config" "${clean_home}/state" "${clean_home}/cache" "${clean_home}/runtime"
          install -m 0755 "${native_binary}" "${installed}"
          install -m 0644 docs/man/amsftp.1 "${prefix}/share/man/man1/amsftp.1"
          "${installed}" completion bash >"${prefix}/share/bash-completion/completions/amsftp"
          "${installed}" completion zsh >"${prefix}/share/zsh/site-functions/_amsftp"
          "${installed}" completion fish >"${prefix}/share/fish/vendor_completions.d/amsftp.fish"
          test "$("${native_binary}" --version)" = "$("${installed}" --version)"
          clean_env=(
            "HOME=${clean_home}"
            "XDG_CONFIG_HOME=${clean_home}/config"
            "XDG_STATE_HOME=${clean_home}/state"
            "XDG_CACHE_HOME=${clean_home}/cache"
            "XDG_RUNTIME_DIR=${clean_home}/runtime"
            "TMPDIR=${clean_home}/runtime"
          )
          cleanup_daemon() {
            env "${clean_env[@]}" "${installed}" daemon stop --confirm stop --format json >/dev/null 2>&1 || true
          }
          trap cleanup_daemon EXIT
          env "${clean_env[@]}" "${installed}" daemon start --format json | grep -F '"running":true'
          env "${clean_env[@]}" "${installed}" job list --format json | grep -F '"output_version":1'
          env "${clean_env[@]}" "${installed}" daemon status --format json | grep -F '"running":true'
          env "${clean_env[@]}" "${installed}" daemon stop --confirm stop --format json | grep -F '"running":false'
          env "${clean_env[@]}" "${installed}" daemon status --format json | grep -F '"running":false'
          if test "${RUNNER_OS}" = Linux; then
            database="${clean_home}/state/amsftp/amsftp.db"
          else
            database="${clean_home}/Library/Application Support/io.github.tyrantlucifer.amsftp/state/amsftp.db"
          fi
          test -f "${database}"
          rm -f \
            "${installed}" \
            "${prefix}/share/man/man1/amsftp.1" \
            "${prefix}/share/bash-completion/completions/amsftp" \
            "${prefix}/share/zsh/site-functions/_amsftp" \
            "${prefix}/share/fish/vendor_completions.d/amsftp.fish"
          test ! -e "${installed}"
          test -f "${database}"
          trap - EXIT
      - name: Exercise loopback Homebrew tap lifecycle
        if: runner.os == 'macOS'
        run: |
          set -euo pipefail
          preview_root="${RUNNER_TEMP}/homebrew-preview"
          old_input="${preview_root}/input-0.9.0"
          new_input="${preview_root}/input-1.0.0"
          old_bundle="${preview_root}/bundle-0.9.0"
          new_bundle="${preview_root}/bundle-1.0.0"
          asset_root="${preview_root}/assets"
          tap_remote="${preview_root}/homebrew-preview-tap"
          port_file="${preview_root}/asset-port"
          server_log="${preview_root}/asset-server.log"
          preview_license="BSD-3-Clause"
          printf '%s\n' 'CI-only Homebrew preview license; this is not the project license.'
          head="$(git rev-parse HEAD)"
          tree="$(git rev-parse 'HEAD^{tree}')"
          source_epoch="$(git show -s --format=%ct HEAD)"
          modules_json="$(tr -d '\n' <docs/release/runtime-dependencies.json)"
          mkdir -p "${preview_root}" "${asset_root}"
          build_preview_bundle() {
            version="$1"
            input="$2"
            output="$3"
            ldflags="-X github.com/TyrantLucifer/awesome-mac-sftp/internal/buildinfo.version=${version} -X github.com/TyrantLucifer/awesome-mac-sftp/internal/buildinfo.commit=${head}"
            mkdir -p "${input}/bin"
            for target in darwin/amd64 darwin/arm64 linux/amd64 linux/arm64; do
              goos="${target%/*}"
              goarch="${target#*/}"
              CGO_ENABLED=0 GOOS="${goos}" GOARCH="${goarch}" \
                go build -trimpath -ldflags="${ldflags}" -o "${input}/bin/${goos}-${goarch}" ./cmd/amsftp
            done
            printf '%s\n' 'CI-only public packaging contract material; this is not the project license.' >"${input}/LICENSE"
            cp docs/release/NOTICE "${input}/NOTICE"
            cp docs/release/INSTALL.md "${input}/INSTALL.md"
            cp docs/release/UNINSTALL.md "${input}/UNINSTALL.md"
            cp docs/man/amsftp.1 "${input}/amsftp.1"
            cat >"${input}/manifest.json" <<EOF
          {
            "schema": "amsftp-public-release-manifest-v1",
            "version": "${version}",
            "commit": "${head}",
            "tree": "${tree}",
            "source_date_epoch": ${source_epoch},
            "materials": {"license":"LICENSE","notice":"NOTICE","install":"INSTALL.md","uninstall":"UNINSTALL.md","man":"amsftp.1"},
            "platforms": [
              {"os":"darwin","arch":"amd64","path":"bin/darwin-amd64"},
              {"os":"darwin","arch":"arm64","path":"bin/darwin-arm64"},
              {"os":"linux","arch":"amd64","path":"bin/linux-amd64"},
              {"os":"linux","arch":"arm64","path":"bin/linux-arm64"}
            ],
            "modules": ${modules_json}
          }
          EOF
            go run ./internal/tools/releasepack "${input}/manifest.json" "${output}"
            cp "${output}"/*.tar.gz "${asset_root}/"
          }
          build_preview_bundle 0.9.0 "${old_input}" "${old_bundle}"
          build_preview_bundle 1.0.0 "${new_input}" "${new_bundle}"
          python3 - "${asset_root}" "${port_file}" >"${server_log}" 2>&1 <<'PY' &
          import functools
          import http.server
          import pathlib
          import sys

          root = pathlib.Path(sys.argv[1])
          port_file = pathlib.Path(sys.argv[2])
          handler = functools.partial(http.server.SimpleHTTPRequestHandler, directory=root)
          server = http.server.ThreadingHTTPServer(("127.0.0.1", 0), handler)
          port_file.write_text(str(server.server_port), encoding="ascii")
          server.serve_forever()
          PY
          server_pid=$!
          cleanup_homebrew_preview() {
            brew uninstall amsftp-ci/preview/amsftp >/dev/null 2>&1 || true
            brew untap amsftp-ci/preview >/dev/null 2>&1 || true
            kill "${server_pid}" >/dev/null 2>&1 || true
            wait "${server_pid}" >/dev/null 2>&1 || true
          }
          trap cleanup_homebrew_preview EXIT
          for ((attempt = 0; attempt < 100; attempt++)); do
            test -s "${port_file}" && break
            sleep 0.05
          done
          test -s "${port_file}"
          asset_port="$(cat "${port_file}")"
          asset_origin="http://127.0.0.1:${asset_port}"
          mkdir -p "${tap_remote}/Formula"
          go run ./internal/tools/homebrewpreview 0.9.0 "${preview_license}" "${old_bundle}" "${asset_origin}" >"${tap_remote}/Formula/amsftp.rb"
          git -C "${tap_remote}" init -b main
          git -C "${tap_remote}" config user.name 'AMSFTP CI'
          git -C "${tap_remote}" config user.email 'ci@invalid.example'
          git -C "${tap_remote}" add Formula/amsftp.rb
          git -C "${tap_remote}" commit -m 'amsftp 0.9.0 preview'
          brew tap amsftp-ci/preview "file://${tap_remote}"
          brew trust amsftp-ci/preview
          HOMEBREW_NO_AUTO_UPDATE=1 brew install --build-from-source amsftp-ci/preview/amsftp
          "$(brew --prefix)/bin/amsftp" --version | grep -F "0.9.0 commit=${head} dirty=false"
          go run ./internal/tools/homebrewpreview 1.0.0 "${preview_license}" "${new_bundle}" "${asset_origin}" >"${tap_remote}/Formula/amsftp.rb"
          git -C "${tap_remote}" add Formula/amsftp.rb
          git -C "${tap_remote}" commit -m 'amsftp 1.0.0 preview'
          brew update
          HOMEBREW_NO_AUTO_UPDATE=1 brew upgrade amsftp-ci/preview/amsftp
          "$(brew --prefix)/bin/amsftp" --version | grep -F "1.0.0 commit=${head} dirty=false"
          brew test amsftp-ci/preview/amsftp
          brew uninstall amsftp-ci/preview/amsftp
          test ! -e "$(brew --prefix)/bin/amsftp"
          brew untap amsftp-ci/preview
          kill "${server_pid}"
          wait "${server_pid}"
          trap - EXIT
      - name: Run native ext4 and XFS state durability fixtures
        if: runner.os == 'Linux'
        run: |
          set -euo pipefail
          trusted_root="/var/lib/amsftp-tests/$(id -u)"
          test "$(findmnt -n -o FSTYPE -T "${trusted_root}")" = ext4
          if ! command -v mkfs.xfs >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y xfsprogs
          fi
          xfs_image="${RUNNER_TEMP}/amsftp-state.xfs"
          xfs_root="/mnt/amsftp-state-xfs-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          truncate -s 512M "${xfs_image}"
          sudo mkfs.xfs -f -q "${xfs_image}"
          sudo install -d -o root -g root -m 0700 "${xfs_root}"
          sudo mount -o loop,nosuid,nodev,noexec "${xfs_image}" "${xfs_root}"
          cleanup_xfs() {
            sudo umount "${xfs_root}"
            sudo rmdir "${xfs_root}"
          }
          trap cleanup_xfs EXIT
          sudo chown "$(id -u):$(id -g)" "${xfs_root}"
          chmod 0700 "${xfs_root}"
          test "$(findmnt -n -o FSTYPE -T "${xfs_root}")" = xfs
          AMSFTP_TEST_PERSISTENT_ROOT="${xfs_root}" \
            go test -count=1 ./internal/state/... ./internal/statefs
          AMSFTP_NATIVE_DISK_FULL_ROOT="${xfs_root}" \
            go test -count=1 -run '^TestNativeXFSDiskFullRollsBackWithoutFalseCommit$' ./internal/statefs
      - name: Run native platform kernel security fixtures
        run: |
          set -euo pipefail
          platform_test="${RUNNER_TEMP}/native-platform.test"
          go test -c -o "${platform_test}" ./internal/platform
          "${platform_test}" -test.v -test.run '^(Test(Darwin|Linux)KernelACL.*|TestAcquireInstanceLockIsExclusiveAcrossProcesses)$'
          sudo env \
            AMSFTP_HOSTILE_UID="$(id -u)" \
            AMSFTP_HOSTILE_GID="$(id -g)" \
            "${platform_test}" -test.v -test.run '^TestControlSocketRejectsOtherUIDPeer$'
      - name: Run native PTY resize and recovery smoke
        run: python3 ./internal/integration/hosted-pty.py "${{ runner.temp }}/native/bin/amsftp"
      - name: Record provenance
        run: |
          set -euo pipefail
          mkdir -p "${RUNNER_TEMP}/provenance"
          porcelain="${RUNNER_TEMP}/provenance/${LEG}.porcelain"
          record="${RUNNER_TEMP}/provenance/${LEG}.env"
          : "${RUNNER_OS:?RUNNER_OS is required}"
          : "${RUNNER_ARCH:?RUNNER_ARCH is required}"
          : "${ImageOS:?ImageOS is required}"
          : "${ImageVersion:?ImageVersion is required}"
          go_env_goos="$(go env GOOS)"
          go_env_goarch="$(go env GOARCH)"
          test -n "${go_env_goos}"
          test -n "${go_env_goarch}"
          git status --porcelain=v1 -uall >"${porcelain}"
          {
            printf 'schema=amsftp-provenance-v1\n'
            printf 'workflow=%s\n' "${GITHUB_WORKFLOW}"
            printf 'job=%s\n' "${GITHUB_JOB}"
            printf 'leg=%s\n' "${LEG}"
            printf 'head=%s\n' "$(git rev-parse HEAD)"
            printf 'tree=%s\n' "$(git rev-parse 'HEAD^{tree}')"
            printf 'go_version=%s\n' "$(go version)"
            printf 'runner_os=%s\n' "${RUNNER_OS}"
            printf 'runner_arch=%s\n' "${RUNNER_ARCH}"
            printf 'runner_image_os=%s\n' "${ImageOS}"
            printf 'runner_image_version=%s\n' "${ImageVersion}"
            printf 'go_env_goos=%s\n' "${go_env_goos}"
            printf 'go_env_goarch=%s\n' "${go_env_goarch}"
            if test -s "${porcelain}"; then
              printf 'status=dirty\n'
            else
              printf 'status=clean\n'
            fi
          } >"${record}"
          test ! -s "${porcelain}"
      - name: Upload provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provenance-${{ env.LEG }}
          path: ${{ runner.temp }}/provenance/${{ env.LEG }}.*
          if-no-files-found: error

  oldstable:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - ubuntu-24.04
          - macos-15
          - macos-15-intel
    env:
      GOTOOLCHAIN: local
      LEG: oldstable-${{ matrix.os }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version: 1.25.12
          cache: true
          cache-dependency-path: "go.sum\ntools/go.sum"
      - name: Prepare trusted persistent test root
        run: |
          set -euo pipefail
          if test "${RUNNER_OS}" = Linux; then
            sudo install -d -o root -g root -m 0755 /var/lib/amsftp-tests
            sudo install -d -o "$(id -u)" -g "$(id -g)" -m 0700 "/var/lib/amsftp-tests/$(id -u)"
          fi
      - run: make check
        env:
          BUILD_DIR: ${{ runner.temp }}/oldstable/build
          COVERAGE_DIR: ${{ runner.temp }}/oldstable/coverage
      - name: Record provenance
        run: |
          set -euo pipefail
          mkdir -p "${RUNNER_TEMP}/provenance"
          porcelain="${RUNNER_TEMP}/provenance/${LEG}.porcelain"
          record="${RUNNER_TEMP}/provenance/${LEG}.env"
          : "${RUNNER_OS:?RUNNER_OS is required}"
          : "${RUNNER_ARCH:?RUNNER_ARCH is required}"
          : "${ImageOS:?ImageOS is required}"
          : "${ImageVersion:?ImageVersion is required}"
          go_env_goos="$(go env GOOS)"
          go_env_goarch="$(go env GOARCH)"
          test -n "${go_env_goos}"
          test -n "${go_env_goarch}"
          git status --porcelain=v1 -uall >"${porcelain}"
          {
            printf 'schema=amsftp-provenance-v1\n'
            printf 'workflow=%s\n' "${GITHUB_WORKFLOW}"
            printf 'job=%s\n' "${GITHUB_JOB}"
            printf 'leg=%s\n' "${LEG}"
            printf 'head=%s\n' "$(git rev-parse HEAD)"
            printf 'tree=%s\n' "$(git rev-parse 'HEAD^{tree}')"
            printf 'go_version=%s\n' "$(go version)"
            printf 'runner_os=%s\n' "${RUNNER_OS}"
            printf 'runner_arch=%s\n' "${RUNNER_ARCH}"
            printf 'runner_image_os=%s\n' "${ImageOS}"
            printf 'runner_image_version=%s\n' "${ImageVersion}"
            printf 'go_env_goos=%s\n' "${go_env_goos}"
            printf 'go_env_goarch=%s\n' "${go_env_goarch}"
            if test -s "${porcelain}"; then
              printf 'status=dirty\n'
            else
              printf 'status=clean\n'
            fi
          } >"${record}"
          test ! -s "${porcelain}"
      - name: Upload provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provenance-${{ env.LEG }}
          path: ${{ runner.temp }}/provenance/${{ env.LEG }}.*
          if-no-files-found: error

  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: darwin
            goarch: arm64
            artifact: amsftp-darwin-arm64
          - goos: darwin
            goarch: amd64
            artifact: amsftp-darwin-amd64
          - goos: linux
            goarch: arm64
            artifact: amsftp-linux-arm64
          - goos: linux
            goarch: amd64
            artifact: amsftp-linux-amd64
    env:
      LEG: build-${{ matrix.artifact }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: "go.sum\ntools/go.sum"
      - run: mkdir -p "${{ runner.temp }}/build/${{ matrix.artifact }}"
      - name: Cross-build
        env:
          CGO_ENABLED: 0
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -trimpath -buildvcs=false -o "${{ runner.temp }}/build/${{ matrix.artifact }}/${{ matrix.artifact }}" ./cmd/amsftp
      - name: Record build metadata
        run: |
          set -euo pipefail
          directory="${RUNNER_TEMP}/build/${{ matrix.artifact }}"
          binary="${directory}/${{ matrix.artifact }}"
          sha256sum "${binary}" | awk '{print $1}' >"${directory}/${{ matrix.artifact }}.sha256"
          go version -m "${binary}" >"${directory}/${{ matrix.artifact }}.version-m"
          test -s "${directory}/${{ matrix.artifact }}.sha256"
          test -s "${directory}/${{ matrix.artifact }}.version-m"
      - name: Upload build artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: build-${{ matrix.artifact }}
          path: ${{ runner.temp }}/build/${{ matrix.artifact }}
          if-no-files-found: error
      - name: Record provenance
        run: |
          set -euo pipefail
          directory="${RUNNER_TEMP}/build/${{ matrix.artifact }}"
          binary="${directory}/${{ matrix.artifact }}"
          artifact_sha256="$(sha256sum "${binary}" | awk '{print $1}')"
          saved_sha256="$(cat "${directory}/${{ matrix.artifact }}.sha256")"
          test "${artifact_sha256}" = "${saved_sha256}"
          printf '%s\n' "${artifact_sha256}" | grep -Eq '^[0-9a-f]{64}$'
          mkdir -p "${RUNNER_TEMP}/provenance"
          porcelain="${RUNNER_TEMP}/provenance/${LEG}.porcelain"
          record="${RUNNER_TEMP}/provenance/${LEG}.env"
          : "${RUNNER_OS:?RUNNER_OS is required}"
          : "${RUNNER_ARCH:?RUNNER_ARCH is required}"
          : "${ImageOS:?ImageOS is required}"
          : "${ImageVersion:?ImageVersion is required}"
          go_env_goos="$(GOOS='${{ matrix.goos }}' GOARCH='${{ matrix.goarch }}' go env GOOS)"
          go_env_goarch="$(GOOS='${{ matrix.goos }}' GOARCH='${{ matrix.goarch }}' go env GOARCH)"
          test -n "${go_env_goos}"
          test -n "${go_env_goarch}"
          git status --porcelain=v1 -uall >"${porcelain}"
          {
            printf 'schema=amsftp-provenance-v1\n'
            printf 'workflow=%s\n' "${GITHUB_WORKFLOW}"
            printf 'job=%s\n' "${GITHUB_JOB}"
            printf 'leg=%s\n' "${LEG}"
            printf 'head=%s\n' "$(git rev-parse HEAD)"
            printf 'tree=%s\n' "$(git rev-parse 'HEAD^{tree}')"
            printf 'go_version=%s\n' "$(go version)"
            printf 'runner_os=%s\n' "${RUNNER_OS}"
            printf 'runner_arch=%s\n' "${RUNNER_ARCH}"
            printf 'runner_image_os=%s\n' "${ImageOS}"
            printf 'runner_image_version=%s\n' "${ImageVersion}"
            printf 'go_env_goos=%s\n' "${go_env_goos}"
            printf 'go_env_goarch=%s\n' "${go_env_goarch}"
            printf 'target_goos=%s\n' '${{ matrix.goos }}'
            printf 'target_goarch=%s\n' '${{ matrix.goarch }}'
            printf 'artifact=%s\n' '${{ matrix.artifact }}'
            printf 'artifact_sha256=%s\n' "${artifact_sha256}"
            if test -s "${porcelain}"; then
              printf 'status=dirty\n'
            else
              printf 'status=clean\n'
            fi
          } >"${record}"
          test ! -s "${porcelain}"
      - name: Upload provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provenance-${{ env.LEG }}
          path: ${{ runner.temp }}/provenance/${{ env.LEG }}.*
          if-no-files-found: error

  reproducibility:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: darwin
            goarch: arm64
            artifact: amsftp-darwin-arm64
            replica: a
          - goos: darwin
            goarch: arm64
            artifact: amsftp-darwin-arm64
            replica: b
          - goos: darwin
            goarch: amd64
            artifact: amsftp-darwin-amd64
            replica: a
          - goos: darwin
            goarch: amd64
            artifact: amsftp-darwin-amd64
            replica: b
          - goos: linux
            goarch: arm64
            artifact: amsftp-linux-arm64
            replica: a
          - goos: linux
            goarch: arm64
            artifact: amsftp-linux-arm64
            replica: b
          - goos: linux
            goarch: amd64
            artifact: amsftp-linux-amd64
            replica: a
          - goos: linux
            goarch: amd64
            artifact: amsftp-linux-amd64
            replica: b
    env:
      LEG: repro-${{ matrix.artifact }}-${{ matrix.replica }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: false
      - name: Prepare independent caches
        run: |
          set -euo pipefail
          build_cache="${RUNNER_TEMP}/repro-cache/${{ matrix.artifact }}-${{ matrix.replica }}/build"
          module_cache="${RUNNER_TEMP}/repro-cache/${{ matrix.artifact }}-${{ matrix.replica }}/modules"
          mkdir -p "${build_cache}" "${module_cache}" "${RUNNER_TEMP}/repro/${{ matrix.artifact }}"
          printf 'GOCACHE=%s\n' "${build_cache}" >>"${GITHUB_ENV}"
          printf 'GOMODCACHE=%s\n' "${module_cache}" >>"${GITHUB_ENV}"
      - name: Reproducible build
        env:
          CGO_ENABLED: 0
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -trimpath -buildvcs=false -o "${{ runner.temp }}/repro/${{ matrix.artifact }}/${{ matrix.artifact }}" ./cmd/amsftp
      - name: Record build metadata
        run: |
          set -euo pipefail
          directory="${RUNNER_TEMP}/repro/${{ matrix.artifact }}"
          binary="${directory}/${{ matrix.artifact }}"
          sha256sum "${binary}" | awk '{print $1}' >"${directory}/${{ matrix.artifact }}.sha256"
          go version -m "${binary}" >"${directory}/${{ matrix.artifact }}.version-m"
          test -s "${directory}/${{ matrix.artifact }}.sha256"
          test -s "${directory}/${{ matrix.artifact }}.version-m"
      - name: Upload reproducible artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: repro-${{ matrix.artifact }}-${{ matrix.replica }}
          path: ${{ runner.temp }}/repro/${{ matrix.artifact }}
          if-no-files-found: error
      - name: Record provenance
        run: |
          set -euo pipefail
          directory="${RUNNER_TEMP}/repro/${{ matrix.artifact }}"
          binary="${directory}/${{ matrix.artifact }}"
          artifact_sha256="$(sha256sum "${binary}" | awk '{print $1}')"
          saved_sha256="$(cat "${directory}/${{ matrix.artifact }}.sha256")"
          test "${artifact_sha256}" = "${saved_sha256}"
          printf '%s\n' "${artifact_sha256}" | grep -Eq '^[0-9a-f]{64}$'
          mkdir -p "${RUNNER_TEMP}/provenance"
          porcelain="${RUNNER_TEMP}/provenance/${LEG}.porcelain"
          record="${RUNNER_TEMP}/provenance/${LEG}.env"
          : "${RUNNER_OS:?RUNNER_OS is required}"
          : "${RUNNER_ARCH:?RUNNER_ARCH is required}"
          : "${ImageOS:?ImageOS is required}"
          : "${ImageVersion:?ImageVersion is required}"
          go_env_goos="$(GOOS='${{ matrix.goos }}' GOARCH='${{ matrix.goarch }}' go env GOOS)"
          go_env_goarch="$(GOOS='${{ matrix.goos }}' GOARCH='${{ matrix.goarch }}' go env GOARCH)"
          test -n "${go_env_goos}"
          test -n "${go_env_goarch}"
          git status --porcelain=v1 -uall >"${porcelain}"
          {
            printf 'schema=amsftp-provenance-v1\n'
            printf 'workflow=%s\n' "${GITHUB_WORKFLOW}"
            printf 'job=%s\n' "${GITHUB_JOB}"
            printf 'leg=%s\n' "${LEG}"
            printf 'head=%s\n' "$(git rev-parse HEAD)"
            printf 'tree=%s\n' "$(git rev-parse 'HEAD^{tree}')"
            printf 'go_version=%s\n' "$(go version)"
            printf 'runner_os=%s\n' "${RUNNER_OS}"
            printf 'runner_arch=%s\n' "${RUNNER_ARCH}"
            printf 'runner_image_os=%s\n' "${ImageOS}"
            printf 'runner_image_version=%s\n' "${ImageVersion}"
            printf 'go_env_goos=%s\n' "${go_env_goos}"
            printf 'go_env_goarch=%s\n' "${go_env_goarch}"
            printf 'target_goos=%s\n' '${{ matrix.goos }}'
            printf 'target_goarch=%s\n' '${{ matrix.goarch }}'
            printf 'artifact=%s\n' '${{ matrix.artifact }}'
            printf 'artifact_sha256=%s\n' "${artifact_sha256}"
            if test -s "${porcelain}"; then
              printf 'status=dirty\n'
            else
              printf 'status=clean\n'
            fi
          } >"${record}"
          test ! -s "${porcelain}"
      - name: Upload provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provenance-${{ env.LEG }}
          path: ${{ runner.temp }}/provenance/${{ env.LEG }}.*
          if-no-files-found: error

  reproducibility-compare:
    needs: reproducibility
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      LEG: reproducibility-compare
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: false
      - name: Download reproducible artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: repro-*
          path: ${{ runner.temp }}/repro-downloads
      - name: Compare independent builds
        run: |
          set -euo pipefail
          root="${RUNNER_TEMP}/repro-downloads"
          expected="${RUNNER_TEMP}/expected-repro-artifacts.txt"
          actual="${RUNNER_TEMP}/actual-repro-artifacts.txt"
          comparison="${RUNNER_TEMP}/accepted-sha256.txt"
          : >"${expected}"
          : >"${comparison}"
          for name in \
            amsftp-darwin-arm64 \
            amsftp-darwin-amd64 \
            amsftp-linux-arm64 \
            amsftp-linux-amd64; do
            printf 'repro-%s-a\nrepro-%s-b\n' "${name}" "${name}" >>"${expected}"
            directory_a="${root}/repro-${name}-a"
            directory_b="${root}/repro-${name}-b"
            for directory in "${directory_a}" "${directory_b}"; do
              test "$(find "${directory}" -mindepth 1 -maxdepth 1 -print | wc -l | tr -d '[:space:]')" -eq 3
              test -f "${directory}/${name}"
              test -f "${directory}/${name}.sha256"
              test -s "${directory}/${name}.version-m"
              grep -Eq '^[0-9a-f]{64}$' "${directory}/${name}.sha256"
            done
            saved_a="$(cat "${directory_a}/${name}.sha256")"
            saved_b="$(cat "${directory_b}/${name}.sha256")"
            actual_a="$(sha256sum "${directory_a}/${name}" | awk '{print $1}')"
            actual_b="$(sha256sum "${directory_b}/${name}" | awk '{print $1}')"
            test "${saved_a}" = "${actual_a}"
            test "${saved_b}" = "${actual_b}"
            test "${actual_a}" = "${actual_b}"
            cmp -s "${directory_a}/${name}" "${directory_b}/${name}"
            printf '%s=%s\n' "${name}" "${actual_a}" >>"${comparison}"
          done
          find "${root}" -mindepth 1 -maxdepth 1 -type d -exec basename {} \; | LC_ALL=C sort >"${actual}"
          LC_ALL=C sort -o "${expected}" "${expected}"
          cmp -s "${expected}" "${actual}"
          test "$(wc -l <"${comparison}" | tr -d '[:space:]')" -eq 4
      - name: Upload comparison evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reproducibility-comparison
          path: ${{ runner.temp }}/accepted-sha256.txt
          if-no-files-found: error
      - name: Record provenance
        run: |
          set -euo pipefail
          mkdir -p "${RUNNER_TEMP}/provenance"
          porcelain="${RUNNER_TEMP}/provenance/${LEG}.porcelain"
          record="${RUNNER_TEMP}/provenance/${LEG}.env"
          : "${RUNNER_OS:?RUNNER_OS is required}"
          : "${RUNNER_ARCH:?RUNNER_ARCH is required}"
          : "${ImageOS:?ImageOS is required}"
          : "${ImageVersion:?ImageVersion is required}"
          go_env_goos="$(go env GOOS)"
          go_env_goarch="$(go env GOARCH)"
          test -n "${go_env_goos}"
          test -n "${go_env_goarch}"
          git status --porcelain=v1 -uall >"${porcelain}"
          {
            printf 'schema=amsftp-provenance-v1\n'
            printf 'workflow=%s\n' "${GITHUB_WORKFLOW}"
            printf 'job=%s\n' "${GITHUB_JOB}"
            printf 'leg=%s\n' "${LEG}"
            printf 'head=%s\n' "$(git rev-parse HEAD)"
            printf 'tree=%s\n' "$(git rev-parse 'HEAD^{tree}')"
            printf 'go_version=%s\n' "$(go version)"
            printf 'runner_os=%s\n' "${RUNNER_OS}"
            printf 'runner_arch=%s\n' "${RUNNER_ARCH}"
            printf 'runner_image_os=%s\n' "${ImageOS}"
            printf 'runner_image_version=%s\n' "${ImageVersion}"
            printf 'go_env_goos=%s\n' "${go_env_goos}"
            printf 'go_env_goarch=%s\n' "${go_env_goarch}"
            if test -s "${porcelain}"; then
              printf 'status=dirty\n'
            else
              printf 'status=clean\n'
            fi
          } >"${record}"
          test ! -s "${porcelain}"
      - name: Upload provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provenance-${{ env.LEG }}
          path: ${{ runner.temp }}/provenance/${{ env.LEG }}.*
          if-no-files-found: error

  compare:
    needs:
      - quality
      - auth-integration
      - native
      - oldstable
      - build
      - reproducibility
      - reproducibility-compare
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      LEG: compare
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod
          cache: false
      - name: Download provenance
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: provenance-*
          path: ${{ runner.temp }}/provenance-downloads
          merge-multiple: true
      - name: Download reproducibility comparison
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: reproducibility-comparison
          path: ${{ runner.temp }}/reproducibility-comparison
      - name: Verify prerequisite provenance
        run: |
          set -euo pipefail
          root="${RUNNER_TEMP}/provenance-downloads"
          manifest="${RUNNER_TEMP}/expected-provenance.txt"
          actual_env="${RUNNER_TEMP}/actual-provenance-env.txt"
          actual_porcelain="${RUNNER_TEMP}/actual-provenance-porcelain.txt"
          accepted_root="${RUNNER_TEMP}/reproducibility-comparison"
          accepted="${accepted_root}/accepted-sha256.txt"
          bindings="${RUNNER_TEMP}/expected-artifact-bindings.txt"
          cat >"${manifest}" <<'LEGS'
          quality
          auth-integration
          native-ubuntu-22.04
          native-ubuntu-24.04
          native-macos-15
          native-macos-15-intel
          oldstable-ubuntu-22.04
          oldstable-ubuntu-24.04
          oldstable-macos-15
          oldstable-macos-15-intel
          build-amsftp-darwin-arm64
          build-amsftp-darwin-amd64
          build-amsftp-linux-arm64
          build-amsftp-linux-amd64
          repro-amsftp-darwin-arm64-a
          repro-amsftp-darwin-arm64-b
          repro-amsftp-darwin-amd64-a
          repro-amsftp-darwin-amd64-b
          repro-amsftp-linux-arm64-a
          repro-amsftp-linux-arm64-b
          repro-amsftp-linux-amd64-a
          repro-amsftp-linux-amd64-b
          reproducibility-compare
          LEGS
          test "$(wc -l <"${manifest}" | tr -d '[:space:]')" -eq 23
          find "${root}" -mindepth 1 -maxdepth 1 -type f -name '*.env' -exec basename {} .env \; | LC_ALL=C sort >"${actual_env}"
          find "${root}" -mindepth 1 -maxdepth 1 -type f -name '*.porcelain' -exec basename {} .porcelain \; | LC_ALL=C sort >"${actual_porcelain}"
          LC_ALL=C sort -o "${manifest}" "${manifest}"
          cmp -s "${manifest}" "${actual_env}"
          cmp -s "${manifest}" "${actual_porcelain}"
          test "$(find "${root}" -mindepth 1 -print | wc -l | tr -d '[:space:]')" -eq 46
          cat >"${bindings}" <<'BINDINGS'
          amsftp-darwin-arm64|darwin|arm64|build-amsftp-darwin-arm64|repro-amsftp-darwin-arm64-a|repro-amsftp-darwin-arm64-b
          amsftp-darwin-amd64|darwin|amd64|build-amsftp-darwin-amd64|repro-amsftp-darwin-amd64-a|repro-amsftp-darwin-amd64-b
          amsftp-linux-arm64|linux|arm64|build-amsftp-linux-arm64|repro-amsftp-linux-arm64-a|repro-amsftp-linux-arm64-b
          amsftp-linux-amd64|linux|amd64|build-amsftp-linux-amd64|repro-amsftp-linux-amd64-a|repro-amsftp-linux-amd64-b
          BINDINGS
          test "$(wc -l <"${bindings}" | tr -d '[:space:]')" -eq 4
          test "$(find "${accepted_root}" -mindepth 1 -print | wc -l | tr -d '[:space:]')" -eq 1
          test -f "${accepted}"
          test ! -L "${accepted}"
          test "$(wc -l <"${accepted}" | tr -d '[:space:]')" -eq 4
          expected_head="$(git rev-parse HEAD)"
          expected_tree="$(git rev-parse 'HEAD^{tree}')"
          field_value() {
            key="$1"
            file="$2"
            count="$(grep -c "^${key}=" "${file}" || true)"
            if test "${count}" -ne 1; then
              printf 'provenance field %s occurs %s times in %s\n' "${key}" "${count}" "${file}" >&2
              return 1
            fi
            sed -n "s/^${key}=//p" "${file}"
          }
          while IFS= read -r leg; do
            record="${root}/${leg}.env"
            porcelain="${root}/${leg}.porcelain"
            test -f "${record}"
            test -f "${porcelain}"
            test ! -s "${porcelain}"
            expected_fields=14
            case "${leg}" in
              build-*|repro-*) expected_fields=18 ;;
            esac
            test "$(wc -l <"${record}" | tr -d '[:space:]')" -eq "${expected_fields}"
            test "$(field_value schema "${record}")" = amsftp-provenance-v1
            test -n "$(field_value workflow "${record}")"
            test -n "$(field_value job "${record}")"
            test "$(field_value leg "${record}")" = "${leg}"
            test "$(field_value head "${record}")" = "${expected_head}"
            test "$(field_value tree "${record}")" = "${expected_tree}"
            test -n "$(field_value go_version "${record}")"
            test -n "$(field_value runner_os "${record}")"
            test -n "$(field_value runner_arch "${record}")"
            test -n "$(field_value runner_image_os "${record}")"
            test -n "$(field_value runner_image_version "${record}")"
            test -n "$(field_value go_env_goos "${record}")"
            test -n "$(field_value go_env_goarch "${record}")"
            test "$(field_value status "${record}")" = clean
          done <"${manifest}"
          artifact_hash() {
            leg="$1"
            expected_artifact="$2"
            expected_goos="$3"
            expected_goarch="$4"
            record="${root}/${leg}.env"
            test "$(field_value target_goos "${record}")" = "${expected_goos}" || return 1
            test "$(field_value target_goarch "${record}")" = "${expected_goarch}" || return 1
            test "$(field_value go_env_goos "${record}")" = "${expected_goos}" || return 1
            test "$(field_value go_env_goarch "${record}")" = "${expected_goarch}" || return 1
            test "$(field_value artifact "${record}")" = "${expected_artifact}" || return 1
            hash="$(field_value artifact_sha256 "${record}")" || return 1
            printf '%s\n' "${hash}" | grep -Eq '^[0-9a-f]{64}$' || return 1
            printf '%s\n' "${hash}"
          }
          while IFS='|' read -r artifact target_goos target_goarch build_leg repro_a_leg repro_b_leg; do
            accepted_hash="$(field_value "${artifact}" "${accepted}")"
            printf '%s\n' "${accepted_hash}" | grep -Eq '^[0-9a-f]{64}$'
            repro_a_hash="$(artifact_hash "${repro_a_leg}" "${artifact}" "${target_goos}" "${target_goarch}")"
            repro_b_hash="$(artifact_hash "${repro_b_leg}" "${artifact}" "${target_goos}" "${target_goarch}")"
            test "${repro_a_hash}" = "${accepted_hash}"
            test "${repro_b_hash}" = "${accepted_hash}"
            if test -n "${build_leg}"; then
              build_hash="$(artifact_hash "${build_leg}" "${artifact}" "${target_goos}" "${target_goarch}")"
              test "${build_hash}" = "${accepted_hash}"
            fi
          done <"${bindings}"
      - name: Record comparison provenance
        run: |
          set -euo pipefail
          mkdir -p "${RUNNER_TEMP}/provenance"
          porcelain="${RUNNER_TEMP}/provenance/${LEG}.porcelain"
          record="${RUNNER_TEMP}/provenance/${LEG}.env"
          : "${RUNNER_OS:?RUNNER_OS is required}"
          : "${RUNNER_ARCH:?RUNNER_ARCH is required}"
          : "${ImageOS:?ImageOS is required}"
          : "${ImageVersion:?ImageVersion is required}"
          go_env_goos="$(go env GOOS)"
          go_env_goarch="$(go env GOARCH)"
          test -n "${go_env_goos}"
          test -n "${go_env_goarch}"
          git status --porcelain=v1 -uall >"${porcelain}"
          {
            printf 'schema=amsftp-provenance-v1\n'
            printf 'workflow=%s\n' "${GITHUB_WORKFLOW}"
            printf 'job=%s\n' "${GITHUB_JOB}"
            printf 'leg=%s\n' "${LEG}"
            printf 'head=%s\n' "$(git rev-parse HEAD)"
            printf 'tree=%s\n' "$(git rev-parse 'HEAD^{tree}')"
            printf 'go_version=%s\n' "$(go version)"
            printf 'runner_os=%s\n' "${RUNNER_OS}"
            printf 'runner_arch=%s\n' "${RUNNER_ARCH}"
            printf 'runner_image_os=%s\n' "${ImageOS}"
            printf 'runner_image_version=%s\n' "${ImageVersion}"
            printf 'go_env_goos=%s\n' "${go_env_goos}"
            printf 'go_env_goarch=%s\n' "${go_env_goarch}"
            printf 'workflow_ref=%s\n' "${GITHUB_WORKFLOW_REF}"
            printf 'workflow_sha=%s\n' "${GITHUB_WORKFLOW_SHA}"
            printf 'event=%s\n' "${GITHUB_EVENT_NAME}"
            printf 'run_id=%s\n' "${GITHUB_RUN_ID}"
            printf 'run_attempt=%s\n' "${GITHUB_RUN_ATTEMPT}"
            if test -s "${porcelain}"; then
              printf 'status=dirty\n'
            else
              printf 'status=clean\n'
            fi
          } >"${record}"
          test ! -s "${porcelain}"
      - name: Upload comparison provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provenance-${{ env.LEG }}
          path: ${{ runner.temp }}/provenance/${{ env.LEG }}.*
          if-no-files-found: error
